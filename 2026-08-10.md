# llama.cpp 變更報告 (2026-08-10)

- 追蹤倉庫：[ggml-org/llama.cpp](https://github.com/ggml-org/llama.cpp)（`master` 分支）
- 檢查區間：`7ba604f1c`（2026-08-09）→ `74ce15741`（2026-08-09）
- 本次新增 commit 數：**4**

---

## 分組摘要

### 一、CI 工作流程調整（2 筆）

兩筆皆為 GitHub Actions CI 設定變更，彼此無直接關聯，但同屬 CI 維護性質。

- `61141f148` — **ci: rm `GGML_HIP_ROCWMMA_FATTN`** ([#26760](https://github.com/ggml-org/llama.cpp/pull/26760))
  移除 CUDA/ROCm 相關 CI 設定與 `rocm.Dockerfile` 中的 `GGML_HIP_ROCWMMA_FATTN` 建置旗標，同步調整 `ci/run.sh`，屬建置設定清理。
- `936918514` — **ci: add pr-draft-label** ([#26801](https://github.com/ggml-org/llama.cpp/pull/26801))
  新增 `pr-draft-label.yml` workflow，用於自動為草稿（draft）PR 加上對應標籤。

### 二、CPU 後端修正（1 筆）

- `08659901c` — **ggml-cpu : fix missing Q5_0 dispatch in SpaceMiT backend** ([#26792](https://github.com/ggml-org/llama.cpp/pull/26792))
  修正 SpaceMiT（RISC-V IME）CPU 後端中缺漏的 `Q5_0` 量化格式派發（dispatch）邏輯，補上對應的算子註冊。

### 三、WebUI 修正（1 筆）

- `74ce15741` — **ui: degrade the working directory picker when file search is off** ([#26811](https://github.com/ggml-org/llama.cpp/pull/26811))
  修正工作目錄選擇器（working directory picker）在 `file_glob_search` 工具未啟用或被使用者關閉時，仍會對每次輸入觸發搜尋並拋出錯誤的問題。現改為依工具啟用狀態決定是否啟用 debounce 搜尋（與 mention picker 做法一致），並在搜尋不可用時顯示說明訊息；使用者仍可用 Enter 手動輸入目錄，同時隱藏「瀏覽」按鈕與搜尋範圍註記，避免呼叫到已停用的搜尋工具。

---

## Commit 逐條列表（依時間先後）

| Commit | 說明 | PR |
|---|---|---|
| `61141f148` | ci: rm `GGML_HIP_ROCWMMA_FATTN` | [#26760](https://github.com/ggml-org/llama.cpp/pull/26760) |
| `08659901c` | ggml-cpu : fix missing Q5_0 dispatch in SpaceMiT backend | [#26792](https://github.com/ggml-org/llama.cpp/pull/26792) |
| `936918514` | ci: add pr-draft-label | [#26801](https://github.com/ggml-org/llama.cpp/pull/26801) |
| `74ce15741` | ui: degrade the working directory picker when file search is off | [#26811](https://github.com/ggml-org/llama.cpp/pull/26811) |

---

*本報告由自動化監控流程產生。*
