# llama.cpp 更新報告 (2026-09-21)

- 檢查範圍: `f072b10`..`ce8caa6`（ggml-org/llama.cpp master 分支）
- 新增 commit 數: **7**
- 上次檢查點: `f072b10` (2026-09-20)
- 本次檢查點: `ce8caa6` (2026-09-20)

---

## 分組摘要

### 1. CUDA Flash-Attention 調校與支援擴充
- `3cf0325` **CUDA: enable sparse fa for qwen4** (#28770)
  為 qwen4exp 架構啟用 CUDA 的 sparse flash-attention 路徑，調整
  `fattn-common.cuh`、`fattn-mma-f16.cuh`、`fattn.cu` 與對應模型程式碼，
  並補上 `test-backend-ops` 測試案例。
- `ce8caa6` **CUDA: tune FA for Gemma 4 on Ampere or newer** (#29152)
  針對 Ampere 及更新世代 GPU 調校 Gemma 4 的 flash-attention 效能參數，
  修改範圍與上一項重疊（`fattn-common.cuh`、`fattn-mma-f16.cuh`、
  `fattn.cu`），屬於同一批 FA 相關優化的延續。

### 2. Metal (Apple) 後端 kernel 擴充
- `9a9f939` **metal: add F16 input to the FWHT** (#29094)
  Metal 版 FWHT kernel 原僅支援 F32 輸入，此變更將來源型別改為模板參數，
  使其可直接讀取 F16 來源而不需先轉換。同時將 pipeline 名稱納入來源型別，
  並讓 `supports_op` 針對四種 Hadamard hint 尺寸接受 F16 src1；其餘 F16
  src1 路徑仍走原本的 `ggml_metal_supports_mul_mat_op`。第二次提交修正了
  `supports_op` 與實際 dispatch 判斷條件不一致的問題（原本會在特定情況下
  因找不到 `kernel_mul_mv_f32_f16_4` pipeline 而觸發 nil pipeline 崩潰），
  將完整條件統一收斂到 `ggml_metal_use_fwht`。此為 #27779（CPU 版 F16
  FWHT，已於前次報告收錄）在 Metal 後端的對應實作。
- `a894dae` **metal: support arbitrary hc in dsv4_hc_pre** (#29169)
  修正 `dsv4_hc_pre` kernel 原先以 `constexpr hc = 4` 搭配 `simd_shuffle`
  硬編碼、導致其他 hc 值被 `supports_op` 拒絕而落回 CPU 執行的問題。
  Kimi-K3 架構所需的 hc 值會隨層數增加（對應跨層殘差堆疊中暫存的
  checkpoint 數量），因此本次改為以 function constant（`FC_DSV4_HC`）
  傳入 n_hc，並為每個 n_hc 產生對應 pipeline 變體、在兩個 pre kernel 中
  以迴圈直接讀取，同時新增 hc = 1, 2, 3, 5, 8, 65（含 gated / 非 gated）
  的測試案例。

### 3. 模型正確性修正：Mamba
- `4260903` **fix(mamba): make time-step projection input contiguous** (#28832)
  修正 Mamba 系列模型中 time-step projection 輸入未保持 contiguous 的問題
  （`src/models/mamba-base.cpp`），並在後續 commit 中移除了正規化後多餘的
  contiguous 複製，屬於小範圍正確性修正。

### 4. WebUI 行動裝置版面修正
- `b23efaa` **ui: Fix mobile breakpoint + content overflow issues** (#29108)
  修正聊天介面在窄螢幕/行動裝置下的多項版面溢出問題：
  1) 聊天欄位為 flex item，其預設最小寬度會被最寬的訊息列撐開造成整頁
     橫向捲軸，改設 `min-w-0` 讓內部捲動容器接管；
  2) Markdown 表格原生沒有外層捲動容器，新增 rehype plugin 為每個表格
     包上 `div.table-wrapper`；
  3) 為 Markdown 區塊內的不可斷行內容（inline code、路徑、雜湊值等）加上
     獨立的橫向捲動容器；
  4) 將圖片 hover 效果的 `transition: all` 改為明確指定 `transform` 與
     `box-shadow` 屬性並縮短至 200ms，同時以
     `(hover: hover) and (pointer: fine)` 限制僅在滑鼠裝置上觸發縮放，
     避免觸控點擊誤觸。

### 5. Common / PEG 解析器：UTF-8 邊界處理
- `3d82ef6` **common/peg: handle invalid utf-8 sequences in the AST** (#29161)
  修正 PEG 解析器（`common/peg-parser.cpp`、`common/unicode.cpp` 等）在
  AST 建構過程中處理不合法 UTF-8 位元組序列的方式，改為依照 Unicode
  建議規範回傳「最大子片段」（maximal subpart），並移除原有的 strict
  參數，同時擴充 `test-unicode.cpp` 與新增 `test-chat-peg-parser.cpp`
  測試涵蓋此情境。

---

## 觀察重點

1. 本次檢查區間內新 commit 數量明顯偏少（僅 7 筆），多為延續前一批次
   （#27779 F16 FWHT、qwen4exp/DSV4 相關工作）的收尾與後端對應實作，
   未見大型重構或新架構加入。
2. CUDA 端連續兩個 commit（#28770、#29152）都在調整 `fattn-mma-f16.cuh`
   等相同檔案，建議關注是否有後續 PR 針對 Gemma 4 / qwen4 的 flash-
   attention 路徑再做效能回歸測試。
3. Metal 的 `9a9f939` 顯示先前 F16 FWHT 支援上線初期即出現
   `supports_op` 與 dispatch 判斷不一致造成的 nil pipeline 崩潰，
   後端維護者在同一 PR 內快速修正，反映此類「條件判斷與實際 kernel
   選擇分離」的模式仍是常見的 bug 來源，值得在其他後端新增 op 支援時
   留意是否有相同風險。
