# llama.cpp 更新報告（2026-08-26）

- **來源倉庫**：[ggml-org/llama.cpp](https://github.com/ggml-org/llama.cpp)，分支 `master`
- **檢查區間**：`f280b269`（2026-08-25 上次檢查點）→ `5d5cb4c3`（本次最新）
- **新增 commit 數**：16 筆

> 說明：本次因排程 session 的 GitHub repository 授權範圍僅涵蓋 `KinLo-ASUS/llama.cpp-watch`，改用 `git clone` 直接取得 `ggml-org/llama.cpp` master 分支歷史來比對，而非透過 GitHub MCP 工具讀取該 repo。

---

## Group 1：版本發布與 ggml 子模組同步（3 筆）

- [`93882361`](https://github.com/ggml-org/llama.cpp/commit/93882361f1cdd8d8987215652d9363e9f93edcbe) ggml : bump version to 0.22.0 (ggml/1607)
- [`81191aff`](https://github.com/ggml-org/llama.cpp/commit/81191affa5d5aa99ba81cc454b35682fa90c819e) sync : ggml
- [`c1d0e7a0`](https://github.com/ggml-org/llama.cpp/commit/c1d0e7a004015f23bc0233470b747b596f29b264) llama.cpp : bump version to 0.3.0 (#27696)

一組固定的版本發布流程：先將內嵌 ggml 版本號提升到 0.22.0，接著把 llama.cpp 內嵌的 ggml 子模組原始碼與上游 ggml repo 同步，最後將 llama.cpp 本身版本號提升到 0.3.0，並更新 release 說明產生腳本。三筆合起來標記了一次正式的版本發布節點。

## Group 2：Metal 穩定性修正（2 筆）

- [`3737e413`](https://github.com/ggml-org/llama.cpp/commit/3737e41370da1830a44c663f9929a0f27591ffa6) metal : null-check buffer alloc to fix OOM crash (#25371)
- [`5d5cb4c3`](https://github.com/ggml-org/llama.cpp/commit/5d5cb4c3a4ea8769490d39a275ee49a45184774d) ggml-meta: propagate buffer usage and call init on the new tensors (#27586)

第一筆修正 `ggml_backend_metal_buffer_type_alloc_buffer` 未檢查 buffer 配置失敗（例如記憶體不足）就直接使用回傳值，導致在記憶體受限裝置（如 iOS）上直接硬崩潰的問題，改為記錄錯誤並讓上層可正常處理，而不是 `EXC_BAD_ACCESS`。第二筆讓新建立的 tensor 正確傳遞 buffer usage 旗標並呼叫初始化，避免新增路徑遺漏必要的初始化步驟。兩者都是延續近期 Metal 後端大規模重構後的穩定性收尾工作。

## Group 3：對話樣板、取樣與伺服端行為修正（3 筆）

- [`0cc5b149`](https://github.com/ggml-org/llama.cpp/commit/0cc5b14959ee3a813bd787baaef50a170493547a) chat : scope qwen3-coder workarounds (#27679)
- [`1729ed53`](https://github.com/ggml-org/llama.cpp/commit/1729ed5371cd1ac6f6d6f3226f8803b080042839) server : reject prefilled assistant messages with tool calls (#27626)
- [`eb25b726`](https://github.com/ggml-org/llama.cpp/commit/eb25b7263e1604b4382295563f5a924002d6f87c) grammar : parse `\-` in char classes as literal hyphen (#27591)

第一筆收斂 Qwen3-Coder 相關對話樣板暫時解法（workaround）的作用範圍，避免誤套用到其他模型。第二筆修正 `--prefill-assistant` 搭配含 `tool_calls` 的 assistant 訊息時，continuation 模式會把 tool call 靜默移除、悄悄破壞 prompt 的問題，改為明確拋出錯誤。第三筆修正 GBNF 語法剖析器無法解析字元集合中的 `\-` 轉義（序列化端會輸出 `\-`，但剖析端原本不接受），導致部分工具呼叫（tool-call）文法產生失敗，此問題會直接影響依賴文法約束做結構化輸出的使用情境。三筆都與「模型輸出/工具呼叫的正確性」相關。

## Group 4：CI / DevOps 基礎設施（4 筆）

- [`790b5713`](https://github.com/ggml-org/llama.cpp/commit/790b5713ca94e30f6c604daf79716f26111d20e8) ci : store ccache on HF buckets (test with cuda-ubuntu for now) (#27699)
- [`eab8ee41`](https://github.com/ggml-org/llama.cpp/commit/eab8ee41f889ef7823af517e8098fb8a9b3cf601) ci : update OS used for ROCM to Ubuntu 24.04 (#27681)
- [`0a5ac49b`](https://github.com/ggml-org/llama.cpp/commit/0a5ac49bce4c42893368585edf3ffb41a38f0108) devops: use GGML_NATIVE=OFF for OpenVINO (#27338)
- [`d222767c`](https://github.com/ggml-org/llama.cpp/commit/d222767c7a6516559a3f49e7721b6c6b1acc87b4) kleidiai: Rework KleidiAI Build System/Integration (#26077)

第一筆試點將 ccache 改存放到 Hugging Face bucket（先在 cuda-ubuntu job 測試），且只在 master 分支寫入快取。第二筆把 ROCm CI 使用的作業系統更新為 Ubuntu 24.04，與其他 build target 及 AMD 官方 wheel 支援對齊。第三筆讓 OpenVINO 的 Dockerfile 改用 `GGML_NATIVE=OFF`（與其他 Dockerfile 一致），並啟用 backend dynamic-load 與 CPU all-variants。第四筆重構 KleidiAI（Arm 最佳化函式庫）的建置系統整合方式，並修正 fp16 相關保護與 CMake 快取問題。四筆都屬於建置/CI 基礎設施的維護，不影響執行期行為。

## Group 5：其他後端與功能修正（3 筆）

- [`5ea87dda`](https://github.com/ggml-org/llama.cpp/commit/5ea87ddad22541a37053c7ba92b02ec1923617c6) webgpu : fix handling of infinity values during ARGSORT and TOP_K (#27538)
- [`814d84bc`](https://github.com/ggml-org/llama.cpp/commit/814d84bc9da5f8268ae6705d5a2d93c85e84ac78) sycl : mark tq2_0 as not supported (#27660)
- [`b114b473`](https://github.com/ggml-org/llama.cpp/commit/b114b47397c287f969e8d02053c880e83e8c40c3) rpc: support apple RDMA as an RPC transport (#26421)

第一筆修正 WebGPU 後端 ARGSORT / TOP_K 運算在輸入含無窮值時的錯誤處理。第二筆在 SYCL 後端明確標記 TQ2_0 量化格式為不支援，避免誤用導致未定義行為。第三筆為 RPC 後端新增 Apple RDMA 傳輸層支援，並讓 RPC 相關程式碼變更時額外觸發 Apple 平台的建置驗證。

## Group 6：其他雜項（1 筆）

- [`f1357e49`](https://github.com/ggml-org/llama.cpp/commit/f1357e49980f5462af9783164f3fdec407d90137) ui: ESLint config updates (#27700)

更新 Web UI 的 ESLint 設定與部分程式碼風格規則（如 HTML 元素間距），純程式碼風格調整。

---

## 本次重點摘要

- 這批 16 筆 commit 是接續前次報告（`2026-08-25.md`，涵蓋到 `f280b269`）之後的更新，主軸是**上一輪 Metal 大重構後的穩定性收尾**（OOM 崩潰修正、tensor 初始化修正）以及**一次正式版本發布**（ggml 0.22.0 同步 + llama.cpp 0.3.0）。
- 值得留意的修正：`grammar` 的 `\-` 轉義解析錯誤會直接影響依賴 GBNF 文法做結構化輸出／工具呼叫的使用情境；`server` 的 prefilled-assistant + tool_calls 問題先前是「靜默」破壞 prompt，現在改為明確報錯，使用此組合的使用者需注意行為變化（會變成報錯而不是繼續執行）。
- CI/DevOps 面持續進行零星基礎設施調整（ccache 儲存位置、ROCm CI 版本、OpenVINO 與 KleidiAI 建置設定），不影響一般使用者。
