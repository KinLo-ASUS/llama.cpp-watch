# llama.cpp 更新報告（2026-08-26）

- 檢查範圍：`c060ca974`（2026-08-24 上次檢查點）→ `5d5cb4c3a`（本次最新，master HEAD）
- 新增 commit 數：28
- 來源：https://github.com/ggml-org/llama.cpp （master 分支）

> 說明：本次因排程 session 的 GitHub repository 授權範圍調整，改用 `git clone` 直接取得 master 分支歷史來比對，而非透過 GitHub MCP 工具讀取 `ggml-org/llama.cpp`（該 repo 目前不在此 session 的授權清單中）。

---

## 分組摘要

### 1. Metal 後端效能最佳化與修正

這組是本次更新中改動最大的部分，主要是把 Metal kernel 原始碼拆分成多個檔案以加速平行編譯，並針對 flash-attention 做了逐裝置的效能調校，另外修正了兩個潛在的穩定性問題。

- **`b615f5b`** metal: per-op source split + parallel compile (#26561) — YiChen Lv
  把單一巨大的 Metal kernel 原始碼依運算子拆成 8～20 個檔案，並平行編譯成多個 metallib，大幅縮短建置時間；同時一併合併了多個先前只存在於分支上的 Metal kernel（col2im_1d、CONV_2D_DW、Q2_0、snake activation fuse、FWHT 等）。
- **`f280b26`** metal : per-device tuned (Q, NE) for flash-attn vec (#26570) — YiChen Lv
  為 flash-attention 向量化路徑依裝置型號（含 M1 Pro / M2 Ultra / M5 Max）建立效能調校表，針對不同 (Query, NE) 組合選擇最佳 kernel 參數。
- **`a14dba6`** ggml : shorten virtual device naming in CUDA and Metal (#27608) — Georgi Gerganov
  簡化 CUDA / Metal 虛擬裝置名稱的產生方式，Metal 裝置描述改為在初始化時就建立好。
- **`3737e41`** metal : null-check buffer alloc to fix OOM crash (#25371) — Yuri Khrustalev
  修正 Metal buffer 配置失敗（例如記憶體不足）時未檢查 NULL 就直接使用，導致在記憶體受限裝置（如 iOS）上直接硬崩潰的問題，改為記錄錯誤並讓上層可正常處理。
- **`5d5cb4c`** ggml-meta: propagate buffer usage and call init on the new tensors (#27586) — Max Krasnyansky
  讓新建立的 tensor 正確傳遞 buffer usage 旗標並呼叫初始化，避免新增路徑遺漏必要的初始化步驟。

### 2. WebGPU 後端修正

- **`c1c766d`** webgpu : reorder includes since V may be defined as K when KV_OVERLAP (#27545) — fairydreaming
  修正 shader template 引入順序問題，避免變數名稱衝突。
- **`5ea87dd`** webgpu : fix handling of infinity values during ARGSORT and TOP_K (#27538) — fairydreaming
  修正 ARGSORT / TOP_K 運算在輸入含無窮值時的錯誤處理。
- **`7584430`** tests : disable DOTS3NOTE arch test for WebGPU (#27654) — fairydreaming
  暫時停用 WebGPU 上的 DOTS3NOTE 架構測試（目前尚未支援或不穩定）。

### 3. ggml 核心維護與版本同步

- **`6036c63`** ggml : fix ggml_clamp (#27644) — Georgi Gerganov
  修正 `ggml_clamp` 運算子的實作錯誤，並同步更新 ggml-alloc 的相關邏輯。
- **`9388236`** ggml : bump version to 0.22.0 (ggml/1607) — Georgi Gerganov
  將內嵌的 ggml 版本號提升至 0.22.0。
- **`81191af`** sync : ggml — Georgi Gerganov
  將 llama.cpp 內嵌的 ggml 子模組原始碼與上游 ggml repo 同步。

### 4. llama.cpp 版本發布

- **`c1d0e7a`** llama.cpp : bump version to 0.3.0 (#27696) — Georgi Gerganov
  將 llama.cpp 版本號提升至 0.3.0，並更新發布相關腳本（release 說明產生流程）。

### 5. 模型支援與推論最佳化

- **`bf0a29c`** Deepseek 4: `-sm tensor` (#26490) — Aman Gupta
  為 DeepSeek-V4 加入 tensor 平行切分（split-mode tensor）支援，包含 head 切分粒度調整、模型儲存與 delayed all-reduce 等最佳化。
- **`a130532`** mamba2 : Flatten in/out projections to dispatch GEMM instead of GEMV (#27513) — Prabhsimran Singh
  將 Mamba2 的輸入/輸出投影運算攤平，使其能以 GEMM（矩陣乘矩陣）而非效率較差的 GEMV（矩陣乘向量）方式派發運算，提升吞吐量。

### 6. 轉換、對話樣板與取樣參數修正

- **`71cc86f`** convert: fix GLM regression in index_tensors (#27655) — jacekpoplawski
  修正 GLM 模型轉換時 `index_tensors` 的迴歸錯誤。
- **`b3c3b96`** misc : read repetition_penalty from generation_config.json (#27659) — Tarek Dakhran
  補上讀取 HuggingFace 標準鍵值 `repetition_penalty`（先前只支援 `penalty_repeat`），並對應到內部的 `sampling_penalty_repeat`。
- **`0cc5b14`** chat : scope qwen3-coder workarounds (#27679) — Aldehir Rojas
  收斂 Qwen3-Coder 相關對話樣板暫時解法的作用範圍，避免影響其他模型。
- **`1729ed5`** server : reject prefilled assistant messages with tool calls (#27626) — Kyozzz
  修正 `--prefill-assistant` 搭配含 tool_calls 的 assistant 訊息時，tool call 會被靜默移除的問題；現在改為明確回報錯誤，而不是悄悄破壞 prompt 內容。

### 7. CI / DevOps 基礎設施

- **`985b149`** ci : apply ccache-clear with older/min/dry-run to all ccache jobs (#27602) — Georgi Gerganov
  將 ccache 清理機制（含 dry-run、依時間與數量門檻清除）套用到所有使用 ccache 的 CI job，並修正容器環境安裝 `gh` CLI、macOS BSD date 解析等一連串小問題。
- **`790b571`** ci : store ccache on HF buckets (test with cuda-ubuntu for now) (#27699) — Sigbjørn Skjæret
  試點將 ccache 改存放到 Hugging Face bucket（目前先在 cuda-ubuntu job 上測試），並只在 master 分支上寫入快取。
- **`eab8ee4`** ci : update OS used for ROCM to Ubuntu 24.04 (#27681) — Mario Limonciello
  將 ROCm CI 使用的作業系統更新為 Ubuntu 24.04，與其他 build target 及 AMD 官方 wheel 支援對齊。
- **`0a5ac49`** devops: use GGML_NATIVE=OFF for OpenVINO (#27338) — krzsztf
  OpenVINO Dockerfile 改用 `GGML_NATIVE=OFF`，與其他 Dockerfile 保持一致，並啟用 backend dynamic-load 與 CPU all-variants。
- **`d222767`** kleidiai: Rework KleidiAI Build System/Integration (#26077) — Jonathan Clohessy
  重構 KleidiAI（Arm 最佳化函式庫）的建置系統整合方式，並修正 fp16 相關保護與 CMake 快取問題。

### 8. 其他個別修正

- **`eb25b72`** grammar : parse `\-` in char classes as literal hyphen (#27591) — Saad Ali
  修正 GBNF 語法剖析器無法解析字元集合中的 `\-` 轉義（原本序列化時會輸出 `\-`，但剖析端卻不接受），導致部分工具呼叫（tool-call）文法產生失敗。
- **`160c6b0`** mtmd: video: fix moov atom at the end of file (#27596) — Xuan-Son Nguyen
  修正多模態影片輸入中 moov atom 位於檔案尾端時的解析問題，並處理 Windows 上的 broken pipe（SIGPIPE）狀況。
- **`814d84b`** sycl : mark tq2_0 as not supported (#27660) — Neo Zhang
  在 SYCL 後端明確標記 TQ2_0 量化格式為不支援，避免使用者誤用導致未定義行為。
- **`b114b47`** rpc: support apple RDMA as an RPC transport (#26421) — Ryan C
  RPC 後端新增支援 Apple RDMA 作為傳輸層，並觸發 Apple 相關 build 在 RPC 程式碼變更時執行。
- **`f1357e4`** ui: ESLint config updates (#27700) — Aleksander Grygier
  更新 Web UI 的 ESLint 設定與程式碼風格規則（HTML 元素間距等）。

---

## 完整 commit 清單（依時間順序）

| SHA | 日期 | 作者 | 標題 |
|---|---|---|---|
| bf0a29c | 08-24 | Aman Gupta | Deepseek 4: `-sm tensor` (#26490) |
| a130532 | 08-24 | Prabhsimran Singh | mamba2 : Flatten in/out projections to dispatch GEMM instead of GEMV (#27513) |
| 6036c63 | 08-24 | Georgi Gerganov | ggml : fix ggml_clamp (#27644) |
| 985b149 | 08-24 | Georgi Gerganov | ci : apply ccache-clear with older/min/dry-run to all ccache jobs (#27602) |
| 160c6b0 | 08-24 | Xuan-Son Nguyen | mtmd: video: fix moov atom at the end of file (#27596) |
| c1c766d | 08-24 | fairydreaming | webgpu : reorder includes since V may be defined as K if KV_OVERLAP (#27545) |
| a14dba6 | 08-24 | Georgi Gerganov | ggml : shorten virtual device naming in CUDA and Metal (#27608) |
| 71cc86f | 08-24 | jacekpoplawski | convert: fix GLM regression in index_tensors (#27655) |
| 7584430 | 08-24 | fairydreaming | tests : disable DOTS3NOTE arch test for WebGPU (#27654) |
| b3c3b96 | 08-24 | Tarek Dakhran | misc : read repetition_penalty from generation_config.json (#27659) |
| b615f5b | 08-25 | YiChen Lv | metal: per-op source split + parallel compile (#26561) |
| f280b26 | 08-25 | YiChen Lv | metal : per-device tuned (Q, NE) for flash-attn vec (#26570) |
| 5ea87dd | 08-25 | fairydreaming | webgpu : fix handling of infinity values during ARGSORT and TOP_K (#27538) |
| 814d84b | 08-25 | Neo Zhang | sycl : mark tq2_0 as not supported (#27660) |
| eb25b72 | 08-25 | Saad Ali | grammar : parse `\-` in char classes as literal hyphen (#27591) |
| 9388236 | 08-25 | Georgi Gerganov | ggml : bump version to 0.22.0 (ggml/1607) |
| 81191af | 08-25 | Georgi Gerganov | sync : ggml |
| c1d0e7a | 08-25 | Georgi Gerganov | llama.cpp : bump version to 0.3.0 (#27696) |
| 3737e41 | 08-25 | Yuri Khrustalev | metal : null-check buffer alloc to fix OOM crash (#25371) |
| f1357e4 | 08-25 | Aleksander Grygier | ui: ESLint config updates (#27700) |
| 790b571 | 08-25 | Sigbjørn Skjæret | ci : store ccache on HF buckets (test with cuda-ubuntu for now) (#27699) |
| 0cc5b14 | 08-25 | Aldehir Rojas | chat : scope qwen3-coder workarounds (#27679) |
| 1729ed5 | 08-25 | Kyozzz | server : reject prefilled assistant messages with tool calls (#27626) |
| 0a5ac49 | 08-25 | krzsztf | devops: use GGML_NATIVE=OFF for OpenVINO (#27338) |
| b114b47 | 08-25 | Ryan C | rpc: support apple RDMA as an RPC transport (#26421) |
| eab8ee4 | 08-25 | Mario Limonciello | ci : update OS used for ROCM to Ubuntu 24.04 (#27681) |
| d222767 | 08-25 | Jonathan Clohessy | kleidiai: Rework KleidiAI Build System/Integration (#26077) |
| 5d5cb4c | 08-25 | Max Krasnyansky | ggml-meta: propagate buffer usage and call init on the new tensors (#27586) |
