# llama.cpp 變更追蹤報告 - 2026-09-10

- 檢查範圍：`ggml-org/llama.cpp` master 分支
- 上次檢查點：`f3f1a8f` (2026-09-09)
- 本次最新 commit：`434ddbb` (2026-09-09)
- 新增 commit 數量：17

---

## 分組摘要

### 1. Vulkan 後端效能與功能改進（4 筆）

這組是本次最大的變更集中點，涵蓋 matmul 管線調校、專屬量化格式 shader、以及一次大規模的 shader 精簡重構。

- **`91f6a6c` vulkan: use spec constant for matrix matrix multiplication A-type (#25773)**
  將 mul_mat / mul_mat_id / flash attention 等 shader 中，A 矩陣的資料型別由執行期分支改為 specialization constant 決定，統一以 map 管理各種 shape/type 組合，並將 LUT 量化格式抽離主要 unified shader、另外拆出 coopmat2 的 q4_k/q5_k 專用 shader 以修正 Ampere 上的效能退化。單一檔案 `ggml-vulkan.cpp` 淨減少近百行程式碼（1791 行變動），屬於大型內部重構，預期可降低編譯後 shader 數量並簡化維護。
- **`1945e09` vulkan: add f16 B-type matmul pipelines and warp tile size tuning for Intel coopmat1 (#27471)**
  新增 f16 B 型別的 matmul pipeline，並針對 Intel coopmat1 硬體調整 warp tile size；dense matmul 的 f16B pipeline 開放給所有廠商，MoE 相關則僅限 Intel。
- **`df750f7` vulkan: add dedicated iq4_xs mat-vec shader (#28426)**
  新增專屬的 `mul_mat_vec_iq4_xs` shader 取代原本的通用 fallback 實作，作者回報 RDNA4 上 token generation 速度提升約 6–17%（依模型而異）。
- **`22397c3` vulkan: Convert FILL to distribute workgroups in 2D (#28592)**
  將 FILL 運算的 workgroup 配置改為 2D 分配，避免在 Intel GPU 上執行 Qwen 3.8 flash-next 等大張量時超過 `maxComputeWorkGroupCount` 限制。

### 2. CUDA 後端調整（2 筆）

- **`5a4d0fe` CUDA: replace GGML_FA_ALL_QUANTS with GGML_FA_QUANTS (#28079)**
  以新的 `GGML_FA_QUANTS` 建置選項取代舊有的 `GGML_FA_ALL_QUANTS`，讓開發者能更細緻地控制 flash-attention 要編譯哪些量化組合，並在執行期遇到未編譯的組合時提供警告與 fallback，同時更新 `docs/build.md` 建置文件。
- **`d4abd57` CUDA: size routed MoE MMQ N-tiles from typical expert width on RDNA3 (#28552)**
  依據典型 MoE expert 寬度調整 MMQ 的 N-tile 尺寸選擇邏輯（於 host 端依 `ncols_opt` 挑選 tile size），針對 RDNA3 顯卡的路由式 MoE 矩陣乘法做效能優化；此 PR 為 #24546 的重新提交版本。

### 3. 新興硬體後端（SYCL / Hexagon）（2 筆）

- **`304665f` Add IQ type handling for MoE (#28476)**
  在 SYCL 後端的 `mmvq.cpp` / `vecdotq.hpp` 補上 IQ 系列量化型別在 MoE 情境下的處理邏輯，屬於功能補齊。
- **`6d9c82e` hexagon: rope updates (#28628)**
  Hexagon（Qualcomm HTP）後端對 RoPE 運算的一系列調整：v75 上向量化 theta cache、優化 MROPE/IMROPE theta 選取、收緊 NEOX RoPE 旋轉與尾端對齊複製邏輯，並改為所有情境皆使用 in-place RoPE、移除過時的 scratch pad 用法與計時器，同時補上測試案例，屬於較大幅度的效能與程式碼清理（淨變動超過 900 行）。

### 4. 模型與取樣相關修正（3 筆）

- **`e2d2c0d` model: fix granite3 moe unknown parameter count (#28632)**
  修正 Granite3 MoE 模型參數量計算顯示為 unknown 的問題。
- **`4850c77` llama: use int32_t for llama_sampler_chain_n return type (#28631)**
  將 `llama_sampler_chain_n` 的回傳型別改為 `int32_t`，屬於 API 型別一致性修正（對應長期 issue #4574）。
- **`6de9cdb` mtmd: propagate video ID to bitmap (#28601)**
  多模態（mtmd）處理流程中，將影片 ID 正確傳遞到 bitmap 結構，修正多模態影片輸入的中繼資料遺失問題。

### 5. Jinja 範本引擎修正（1 筆）

- **`b31b71f` jinja: treat a null left operand of `in` as a plain lookup (#28620)**
  修正 chat template 中，當變數預設為 `none` 並用於 `in` 判斷 map 成員時會拋出錯誤的問題；改為視為一般查找並回傳 false，與標準 Jinja 行為一致（`undefined` 情況此前已處理，這次補齊 `none` 的情況）。

### 6. 建置、測試與相依套件維護（5 筆）

- **`30b6a75` tests: use less threads for data initialization (#28325)**
  測試初始化資料時改為依元素數量調整執行緒數（原本固定使用較多執行緒），降低測試時的資源消耗。
- **`9cf3bf2` py: bump numpy to 2.4.6 (#28649)** → 隨後被 **`4b98ab8` py: lower numpy to 2.2.6 (#28654)** revert 並改為鎖定在 2.2.6。兩個 commit 皆於同一天（2026-09-09）發生，屬於快速修正的相依套件版本調整，最終結果是 numpy 需求版本鎖定在 2.2.6。
- **`14a9d09` args: officially deprecate --mmap|mlock|dio (#28334)**
  正式將 `--mmap` / `--mlock` / `--dio` 等命令列參數標記為 deprecated，並清理 README 與 `llama-bench` 中對應的說明與程式碼。
- **`434ddbb` ci: fix sanitizer tests (#28583)**
  CI 維護：移除 `build-riscv.yml` workflow 檔案（80 行）及 `build-sanitize.yml` 中的部分設定，修正 sanitizer 測試相關的 CI 問題。

---

## 小結

本次觀察期間（2026-09-09 單日）共有 17 個新 commit 併入 master，主要聚焦在 **Vulkan 後端的效能調校與大型 shader 重構**（含一次影響 16 個檔案、淨變動近 3700 行的 spec-constant 化重構），其次為 CUDA 建置選項細化與 RDNA3 MoE 效能優化，以及 Hexagon RoPE 的向量化改進。功能面則有多模態影片 ID 傳遞、Granite3 MoE 參數計算、Jinja 範本 null 查找等修正。另外注意到 numpy 版本在同一天內先升版後又被 revert 回 2.2.6，屬於此次觀察到唯一的「先進後退」變更。整體無重大破壞性變更或安全性議題。
