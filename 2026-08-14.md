# llama.cpp 更新報告（2026-08-14）

- 監控倉庫：[ggml-org/llama.cpp](https://github.com/ggml-org/llama.cpp)（`master` 分支）
- 檢查區間：`8e7f22b67e`（2026-08-13，上次記錄）→ `a94d563ed8`（2026-08-13，本次最新）
- 新增 commit 數：**25**

---

## 一、Web UI 重構系列（8 個 commit）

由 Aleksander Grygier 主導的一系列前端（`tools/ui`，Svelte）大型重構，目的是整理常數、型別、Store 架構與命名，屬於純內部整理，不影響對外行為。

| Commit | PR | 摘要 |
| --- | --- | --- |
| `e21152dc` | [#26908](https://github.com/ggml-org/llama.cpp/pull/26908) | ui: 常數重構——將散落的單值常數整併為常數物件/Enum，並清理未使用的常數。 |
| `a6040c92` | [#26909](https://github.com/ggml-org/llama.cpp/pull/26909) | refactor: 清理 UI 型別定義，統一聊天表單、工具呼叫區塊等元件的型別來源。 |
| `094e53db` | [#26910](https://github.com/ggml-org/llama.cpp/pull/26910) | ui: Store 架構改善——移除各 Store（agentic/chat/models/server/mcp/conversations/settings/tools）的包裝匯出，改用 barrel import，並新增 Context Stats store。 |
| `f2efd641` | [#26950](https://github.com/ggml-org/llama.cpp/pull/26950) | ui: 將 `styles/` 目錄移入 `$lib` scope，移除舊有路徑別名。 |
| `d86c7d62` | [#26951](https://github.com/ggml-org/llama.cpp/pull/26951) | ui: 清理 Context——移除聊天設定的廢棄 Context，新增聊天訊息操作專用 Context，消除 prop drilling。 |
| `9c5531e2` | [#24845](https://github.com/ggml-org/llama.cpp/pull/24845) | ui: 修正 `VITE_PUBLIC_SERVER` 環境變數讀取方式。 |
| `fa4ec459` | [#27001](https://github.com/ggml-org/llama.cpp/pull/27001) | refactor: 聊天輸入相關元件/檔案命名調整（如 `ChatFormInput` 系列）。 |
| `bdffafa5` | [#27002](https://github.com/ggml-org/llama.cpp/pull/27002) | ui: 重構 data-attribute 常數與布林字串 Enum，統一使用 CSS class 常數。 |

---

## 二、SYCL 後端優化（5 個 commit）

針對 Intel GPU（SYCL）後端的效能與功能擴充。

| Commit | PR | 摘要 |
| --- | --- | --- |
| `d415e65a` | [#26800](https://github.com/ggml-org/llama.cpp/pull/26800) | sycl: `concat` 算子新增支援 Q4_0、Q4_1、Q5_0、Q5_1、Q8_0 量化格式。 |
| `8efbf65d` | [#26251](https://github.com/ggml-org/llama.cpp/pull/26251) | sycl: 新增 DMMV ESIMD Q3_K kernel，並將 ESIMD 由編譯期開關改為執行期偵測、預設啟用。 |
| `1ee1cd9b` | [#26411](https://github.com/ggml-org/llama.cpp/pull/26411) | sycl: 融合 UNARY（silu/sigmoid/softplus）與 MUL 運算，Arc Pro B70 上測得小幅效能提升（約 0.4–0.6%）。 |
| `154d57af` | [#26372](https://github.com/ggml-org/llama.cpp/pull/26372) | sycl: 移除 GEMM 非 oneDNN 路徑中多餘的 fp32 型別提升，改用自動 fp16 提升。 |
| `a97123e4` | [#26789](https://github.com/ggml-org/llama.cpp/pull/26789) | [SYCL] 支援 Host Pinned Memory 以提升 Host-to-Device 傳輸效能，並修正相關執行緒安全問題。 |

---

## 三、推測解碼（Speculative Decoding）改進（3 個 commit）

| Commit | PR | 摘要 |
| --- | --- | --- |
| `0d0bfcd4` | [#26958](https://github.com/ggml-org/llama.cpp/pull/26958) | spec: 為 dflash 與 dspark 皆啟用後端取樣（backend sampling），並允許 `p_min > 0`。 |
| `f65e568f` | [#26814](https://github.com/ggml-org/llama.cpp/pull/26814) | common: 從草稿模型（draft）GGUF metadata 自動偵測推測解碼類型——修正使用 `-md` 載入本地草稿模型但未指定 `--spec-type` 時，推測解碼不會啟用的問題。 |
| `1d2869c6` | [#27005](https://github.com/ggml-org/llama.cpp/pull/27005) | spec: 自動偵測 MTP 草稿模型類型。 |

---

## 四、Server 端修正（2 個 commit）

| Commit | PR | 摘要 |
| --- | --- | --- |
| `decaf508` | [#26920](https://github.com/ggml-org/llama.cpp/pull/26920) | server: 重構並修正 metrics 統計邏輯——將多數欄位移至 `server_slot_stats`、修正批次（batch）指標計算的正確性問題，並新增測試。 |
| `981184e4` | [#27006](https://github.com/ggml-org/llama.cpp/pull/27006) | server: `index.html` 改為 `no-cache` 提供——避免檔名固定但內容每次建置都變動的 `index.html` 被快取住，改用 ETag 進行 304 驗證。 |

---

## 五、其他後端與雜項修正（6 個 commit）

| Commit | PR | 摘要 |
| --- | --- | --- |
| `1f368f35` | [#26991](https://github.com/ggml-org/llama.cpp/pull/26991) | ggml: 修正 ARM 建置錯誤與未使用變數警告。 |
| `e79e4bf6` | [#26696](https://github.com/ggml-org/llama.cpp/pull/26696) | ggml-hip: 移除 `-funsafe-math-optimizations`，因其連帶啟用的 `-fassociative-math` 會讓浮點數重排，導致 RDNA3.5 上的 greedy argmax 與非推測解碼基準不一致；移除後 HIP 建置符合 IEEE 浮點規範。 |
| `eeae28b6` | [#26947](https://github.com/ggml-org/llama.cpp/pull/26947) | ggml-cpu/ops: 向量化 Flash Attention 中 V-cache 由 F16 轉 F32 的運算，提升 CPU 效能。 |
| `4a84b0ad` | [#26980](https://github.com/ggml-org/llama.cpp/pull/26980) | metal: 新增 TQ2_0（三元、每元素 2 bit）量化格式支援，並優化 `mul_mv` kernel（改用浮點運算、預先計算加總、迴圈提升等）。 |
| `2606220d` | [#26960](https://github.com/ggml-org/llama.cpp/pull/26960) | chat: 修正 LFM2 工具呼叫（tool call）參數名稱前綴解析的歧義問題。 |
| `a94d563e` | [#27026](https://github.com/ggml-org/llama.cpp/pull/27026) | common: 讓 CPU 相關參數設定能一致套用到所有工具（tools）。 |

---

## 六、OpenVINO 後端大型更新（1 個 commit，內含多項變更）

| Commit | PR | 摘要 |
| --- | --- | --- |
| `aee56b3a` | [#26952](https://github.com/ggml-org/llama.cpp/pull/26952) | OpenVINO 後端一次性大型更新，主要內容：<br>• **模型支援**：啟用 Qwen3.5（含 GPT-OSS MoE、MXFP4）、新增/修正多個算子（FILL、SIGMOID、SQR/SQRT、多維 SET_ROWS 等）。<br>• **記憶體優化**：新增 `GGML_OPENVINO_RELEASE_WEIGHTS`（GPU 上釋放已編譯權重的 host 記憶體，1B 模型 RSS 由 ~1555MB 降至 ~710MB）、串流式權重反量化（降低編譯期記憶體峰值，1B 降 1.06GB、8B 降 2.0GB）、以及彙整開關 `GGML_OPENVINO_MEMORY_OPTIMIZE`。<br>• **編譯快取**：新增 `GGML_OPENVINO_COMPILED_MODEL_CACHE_DIR` 前端編譯模型快取，8B 模型編譯時間由 15.3s 降至 6.3s（約 2.4 倍）。<br>• **正確性修正**：修正 NEOX RoPE 在 GPU stateful 模式下的混合秩運算錯誤（Phi-3-mini PPL 由 27120 修正為 6.23，接近 CPU 基準 6.2251）；修正 Qwen3VL 因 `GGML_OP_PAD` 缺失導致的 decode 崩潰，以及 deepstack 殘差流疊加錯誤資料造成的生成錯誤；修正 IMROPE 在 NPU 靜態形狀路徑下的位置編碼 padding 錯誤。<br>• 新增 `test-recurrent-state-rollback` 測試，並修正相關 CPY 回寫邏輯。 |

---

## 備註

- 本次檢查未涉及對 `ggml-org/llama.cpp` 的原始碼修改，僅為變更追蹤與摘要整理。
- 最新已檢查 commit：`a94d563ed801d1da1b8c2432946de07d0231bb3d`（2026-08-13, 「common: apply CPU parameters across tools (#27026)」）。
