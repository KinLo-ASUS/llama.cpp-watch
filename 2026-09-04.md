# llama.cpp 變更報告 — 2026-09-04

- 追蹤範圍：`ggml-org/llama.cpp` master 分支
- 檢查區間：`67a17c1` (2026-09-03 之前的檢查點，不含) → `d230ddd` (本次檢查點)
- 新增 commit 數：18

---

## 分組摘要

### 1. 新模型支援 / 轉換 (Model support & conversion)

- **c61b98b** — model: add NVIDIA Nemotron-3-Puzzle-75B-A9B (NemotronHPuzzle) support (#25444)｜Yaniss Amazouz
  新增對 NVIDIA Nemotron-3-Puzzle-75B-A9B（NemotronHPuzzle）模型架構的支援，包含轉換腳本 (`conversion/nemotron.py`) 與新的 hparams 欄位，並同步調整近 40 個現有模型的 `src/models/*.cpp`（主要是介面簽章的小幅修改，如加 `const` 或參數順序，屬於配合此次架構重構的連動修改）。
- **d646c9d** — convert : skip bias_vl tensor in DeepSeek-V4 DSpark conversion (#28294)｜Georgi Gerganov
  修正 DeepSeek-V4 DSpark 模型轉換時，`bias_vl` 張量不應被寫入 GGUF 的問題。

**分組小結**：本次有一個全新模型架構（Nemotron-3-Puzzle）上線，以及一個既有模型（DeepSeek-V4 DSpark）轉換流程的修正，皆屬模型相容性相關變更。

### 2. 後端效能優化 (Backend performance)

- **4aa6ffb** — sycl: reduce redundant work in Q4_K multi-column MMVQ (#27062)｜Eurekatic
  優化 SYCL 後端 Q4_K 量化格式在多欄位 MMVQ（matrix-vector quantized）運算中的重複計算，並補上對應的 backend-ops 測試。
- **0df974d** — sycl : enhance the api to support peer-to-peer copy (#27550)｜Neo Zhang
  強化 SYCL API 以支援裝置間（peer-to-peer）記憶體複製。
- **c7bda03** — vulkan: fix FA dequant path engagement (#28190)｜Nathan Wilson
  修正 Vulkan 後端 Flash Attention 在 dequant 路徑上未正確啟用的問題。
- **0ba6499** — CUDA: Allow concurrent streams per split for multi-GPU (#28198)｜Tanner Bruhn
  允許多 GPU 情境下，每個 split 使用獨立並行的 CUDA stream，以提升多卡吞吐量。
- **8c1a251** — tune MMVQ to MMQ crossover for SM87 (#28285)｜kbenkhaled
  針對 CUDA SM87（如 Jetson Orin）架構，調整 MMVQ 與 MMQ 運算模式切換的門檻值以提升效能。
- **0df017d** — metal : fix glu dispatch with ne00 = 1 (#28306)｜Georgi Gerganov
  修正 Metal 後端在 GLU 運算子當 `ne00 = 1` 時的 dispatch 錯誤，並補強測試涵蓋率。
- **7bb0fc1** — metal : add sparse FA (#28098)｜Georgi Gerganov
  為 Metal 後端新增稀疏 Flash Attention（sparse FA）實作，牽動 Metal kernel、device 與 ops 層多處程式碼，屬本次規模較大的效能功能新增。
- **95ef7fc** — opencl: quant lm_head / decode GEMV and medium-batch GEMM optimizations (speculative decoding/MTP) (#26477)｜Hongqiang Wang
  針對 OpenCL 後端大幅優化量化 lm_head 的 decode GEMV 與中批量 GEMM 運算，主要提升推測解碼（speculative decoding）與 MTP 場景效能，新增大量 kernel 檔案（近 4800 行新增），為本次變更量最大的單一項目。

**分組小結**：本次有多達 7 筆涉及不同後端（SYCL、Vulkan、CUDA、Metal、OpenCL）的效能優化與修正，顯示各硬體後端持續在做針對性調校，其中 Metal sparse FA 與 OpenCL GEMV/GEMM 優化為較大規模的功能性改動。

### 3. 記憶體 / 模型載入優化

- **5ec4eab** — misc : prevent RAM peaking at model loading stage (#27483)｜Tarek Dakhran
  避免模型載入階段因暫時性配置導致 RAM 用量出現尖峰，改善大模型載入時的記憶體使用狀況。

### 4. mtmd（多模態）程式碼品質改善

- **f45576a** — mtmd : add `const` in various places (#28307)｜Mads Marquart
  在 mtmd（multi-modal）模組多處加入 `const` 修飾詞，提升介面正確性。
- **de8656b** — mtmd: propagate const to preproc class (#28310)｜Xuan-Son Nguyen
  延續上一筆修改，將 `const` 正確性推廣至前處理（preproc）類別。

**分組小結**：兩筆皆屬 mtmd 模組的 const-correctness 重構，屬同一系列的程式碼品質改善，非功能性變更。

### 5. 測試 / CI 基礎設施

- **42f0225** — server : use pytest-xdist for server tests (#28298)｜Daniel Bevenius
  將 server 測試改用 `pytest-xdist` 以支援平行執行，縮短測試時間，並同步調整部分測試案例。
- **d30500b** — snapdragon: ci updates to use new run script (#28293)｜Max Krasnyansky
  更新 Snapdragon 平台 CI 使用新的執行腳本，調整 build/測試流程腳本。

### 6. 文件

- **e107984** — ops: add Hexagon to ops.md and update main README.md (#28263)｜Todor Boinovski
  在 `docs/ops.md` 補上 Hexagon 後端算子支援清單，並更新主 README；新增一份龐大的 `docs/ops/Hexagon.csv` 算子對照表（近 2 萬行）。

### 7. 一般性 Bug 修正

- **c5a5535** — common/json-schema : fix GBNF grammar generation for empty object schemas (#28279)｜Sergey Sklyarov
  修正 JSON Schema 轉 GBNF 語法產生器在處理空物件 schema（`{}`）時的錯誤。
- **d230ddd** — llama: fix whole source code rebuilt on each new commit (#28278)｜Xuan-Son Nguyen
  修正每次 commit 都會導致整個原始碼被重新編譯的問題（CMake 版本標頭產生方式調整），可大幅改善開發者的建置迭代速度。

---

## 完整 Commit 清單

| Commit | 作者 | 說明 |
|---|---|---|
| c61b98b | Yaniss Amazouz | model: add NVIDIA Nemotron-3-Puzzle-75B-A9B (NemotronHPuzzle) support (#25444) |
| 4aa6ffb | Eurekatic | sycl: reduce redundant work in Q4_K multi-column MMVQ (#27062) |
| 5ec4eab | Tarek Dakhran | misc : prevent RAM peaking at model loading stage (#27483) |
| d646c9d | Georgi Gerganov | convert : skip bias_vl tensor in DeepSeek-V4 DSpark conversion (#28294) |
| 0df974d | Neo Zhang | sycl : enhance the api to support peer-to-peer copy (#27550) |
| c7bda03 | Nathan Wilson | vulkan: fix FA dequant path engagement (#28190) |
| 0ba6499 | Tanner Bruhn | CUDA: Allow concurrent streams per split for multi-GPU (#28198) |
| f45576a | Mads Marquart | mtmd : add `const` in various places (#28307) |
| 0df017d | Georgi Gerganov | metal : fix glu dispatch with ne00 = 1 (#28306) |
| 7bb0fc1 | Georgi Gerganov | metal : add sparse FA (#28098) |
| de8656b | Xuan-Son Nguyen | mtmd: propagate const to preproc class (#28310) |
| 42f0225 | Daniel Bevenius | server : use pytest-xdist for server tests (#28298) |
| e107984 | Todor Boinovski | ops: add Hexagon to ops.md and update main README.md (#28263) |
| d30500b | Max Krasnyansky | snapdragon: ci updates to use new run script (#28293) |
| 8c1a251 | kbenkhaled | tune MMVQ to MMQ crossover for SM87 (#28285) |
| 95ef7fc | Hongqiang Wang | opencl: quant lm_head / decode GEMV and medium-batch GEMM optimizations (speculative decoding/MTP) (#26477) |
| c5a5535 | Sergey Sklyarov | common/json-schema : fix GBNF grammar generation for empty object schemas (#28279) |
| d230ddd | Xuan-Son Nguyen | llama: fix whole source code rebuilt on each new commit (#28278) |

---

## 本次觀察重點

- 後端效能優化仍是本次最大宗的變更類別，涵蓋 SYCL、CUDA、Vulkan、Metal、OpenCL 五種後端，其中 OpenCL 的量化 GEMV/GEMM 優化（#26477）與 Metal 的 sparse FA（#28098）為規模較大的功能性新增。
- 新增一個全新模型架構支援（NVIDIA Nemotron-3-Puzzle-75B-A9B）。
- `d230ddd` 修正了長期存在的建置系統問題（每次 commit 都全量重建），對日常開發迭代效率有直接幫助。
