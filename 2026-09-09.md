# llama.cpp 變更報告 — 2026-09-09

- 追蹤範圍：`ggml-org/llama.cpp` master 分支
- 檢查區間：`050dde5`（2026-09-08 之前的檢查點，不含） → `f3f1a8f`（本次檢查點）
- 新增 commit 數：15

---

## 分組摘要

### 1. GPU／CPU 後端：效能調校與正確性修復（Vulkan / Metal / OpenCL / CUDA-HIP / CPU）

- **f014bfe** — Fix Vulkan-Hpp handle usage on 32-bit targets. (#22892)｜miyan（1 file, +9/-1）
- **64e9bce** — vulkan : fuse UNARY(GELU|SIGMOID|SILU|SOFTPLUS) + MUL (#27220)｜Ankit Khandelwal（4 files, +252/-13）
- **88ada91** — metal : fix idle threads in mul_mv_iq3_xxs for ne00 < 1024 (#28086)｜Foad Abo Dahood（3 files, +58/-11）
- **7d701b5** — opencl: properly handle non-contiguous inputs to conv2d (#28503)｜lhez（3 files, +68/-29）
- **d4389a4** — Revert "ggml-cuda : restore prop.integrated on HIP builds (#24233)" (#28604)｜uvos（1 file, -4）
- **9113cc1** — ggml : fix msvc+clang ggml_vld1q_u32 (#28284)｜Sarah Wu（1 file, +1/-1）

本組是本次區間最大宗的變更，全部集中在各硬體後端的效能與正確性修正。**Vulkan** 有兩項變更：一是修正 32-bit 平台上 Vulkan-Hpp 型別安全機制與 `ggml-vulkan` 的相容性問題——`vk::Buffer` 在 32-bit 環境下是 `uint64_t`，Vulkan-Hpp 停用了隱式轉換，導致除錯輸出串流與 `copyBuffer` 呼叫編譯失敗，修法是補上 `operator<<` 並直接傳遞 `vk::Buffer`；二是為 UNARY（GELU/SIGMOID/SILU/SOFTPLUS）與 MUL 兩個算子新增融合支援，這是一個歷經十餘次迭代的完整功能，包含：依 op 分別特化 pipeline、以 specialization constant 選擇「OP 作用在運算元 B」的變體（涵蓋 qwen3next/qwen35moe 的共享專家閘控場景）、將融合條件從嚴格相鄰放寬到「中間僅有零運算的 VIEW/RESHAPE/TRANSPOSE/PERMUTE 節點」以支援 gemma4 的逐層嵌入閘控模式、並用 lookup table 取代四層巢狀 switch 以簡化未來新增 op 的成本；過程中也發現並修正了會導致 gemma3n 輸出損毀的邊界案例，最終在 prompt-processing 上恢復了原本 10–18% 的效能回歸。

**Metal** 修正 `mul_mv_iq3_xxs` kernel 在 `ne00 < 1024`（矩陣行長度較短）時一半 simdgroup 閒置的問題，做法是新增一個 8-row split 的獨立 kernel 變體，並用 function constant 在執行期選擇，寬矩陣則維持原有 4-row kernel 完全不變。**OpenCL** 修正 `conv2d` 對非連續（non-contiguous）輸入張量 stride 處理不正確的問題。**CUDA/HIP** 方面，`uvos` 回退了先前 #24233 針對 HIP 建置恢復 `prop.integrated`（iGPU 判斷旗標）的變更，理由與該旗標過去曾造成輸出損毀（#15034）有關，回退後 HIP 與 CUDA 路徑統一維持 `integrated = false`。**CPU** 後端修正 `ggml_vld1q_u32` 巨集在「MSVC + Clang」組合（即 clang-cl）下誤用 MSVC 專用實作路徑的問題，改為僅在純 MSVC（非 clang）時才套用該分支。

### 2. 核心運算與模型支援（ggml 精度規格 / Kimi-K3 / iGPU 延遲載入）

- **5a6caa0** — ggml : update ggml_prec specification (#26675)｜Georgi Gerganov（10 files, +134/-22）
- **9dcf84e** — model : support Kimi-K3 recurrent-state rollback (#28466)｜Frank Dai（4 files, +147/-67）
- **f3f1a8f** — llama: disable lazy tensor loading by default on iGPUs (#28326)｜Ruben Ortlam（1 file, +12）

`ggml_prec`（運算精度旗標）的規格經多輪修訂重新定義：新增 `GGML_PREC_BF16`、重新設計 API 並支援 `MUL_MAT_ID`（MoE 用的間接矩陣乘法），同時補上 `TAG_GGML_PREC` 搜尋標籤方便日後查找相關程式碼，屬於核心運算圖精度控制機制的整理與擴充。**Kimi-K3** 模型的循環狀態（recurrent-state）回滾支援進一步完善，並大幅擴充 `test-recurrent-state-rollback` 測試涵蓋範圍。**iGPU 延遲載入**修正一項回歸（#28160）：當 `lazy` 模式設為 `AUTO` 且裝置不支援 mmap（常見於部分 iGPU）時，先前會誤判走上延遲載入路徑，現在會在載入前檢查每個裝置的 `mmap_support` 能力旗標，只要有裝置不支援即自動退回 `OFF`，避免載入失敗或效能異常。

### 3. Server 與推理流程：正確性修復

- **5d806aa** — server : apply checkpoint min-step eviction only when the checkpoint list is full (#28302)｜Foad Abo Dahood（1 file, +17/-1）
- **415e909** — spec: single device drafter should create meta backend wrapper (#28390)｜Aman Gupta（5 files, +27/-7）

`server` 修正 context checkpoint（用於長對話續連時避免重新 prefill）的淘汰邏輯：舊邏輯只要與前一個 checkpoint 距離小於 `checkpoint_min_step` 就會被清除，但這個間距規則過去無論 checkpoint 清單是否已滿都會套用，導致短 prompt（長度小於 `checkpoint_min_step`）在 `n_tokens - 4` 處建立的 checkpoint 被立即清掉，只留下最舊的一筆——對 hybrid／recurrent 架構模型而言，等同於每次接續請求都要從更早的位置重新 prefill。修法是把間距淘汰規則改為只在清單已達 `n_ctx_checkpoints` 上限時才套用，並在同一 `n_tokens` 位置新增取代（取代舊筆而非疊加重複）的邏輯。**投機解碼（speculative decoding）**方面，當草稿模型（drafter）被限定在單一裝置上執行時，先前會沿用主模型繼承而來的 `-sm`（split mode）設定，意外建立不必要的 meta backend wrapper；修法是偵測草稿裝置清單只有一個有效裝置時，強制改用 `LLAMA_SPLIT_MODE_LAYER`，並在 `SPLIT_MODE_TENSOR` 被用於單一裝置時加上警告訊息提示此用法不建議。

### 4. Chat 樣板解析器重構

- **895c045** — chat : split specialized parsers into common/parsers (#27764)｜Piotr Wilkin (ilintar)（19 files, +2586/-2463）

將原本集中在 `chat.cpp` 中的 14 個專用聊天樣板解析器（如各家模型的 tool-call 格式解析）拆分至 `common/parsers/` 目錄下各自獨立的檔案，仿照先前 `src/models` 的拆分模式；`chat.cpp` 僅保留樣板偵測邏輯 `common_chat_try_specialized_template()`，行數由 3915 行降至 1513 行。共用輔助函式與各解析器宣告集中於新增的 `common/parsers/parsers.h`，且改用 `sources.cmake` 明確列出來源檔案，取代原本的 `file(GLOB)`（因為 GLOB 不會在新增/移除檔案時觸發 CMake 重新產生建置腳本，增量建置下容易漏編）。作者說明本次為純重構，不含行為變更，並標註由 Claude Opus 5 協助完成。

### 5. 建置／CI 維護

- **ca86fb2** — llama : add missing headers (#28566)｜Pepper Gray（6 files, +6）
- **1744c6b** — ci : add PYTEST_WORKERS=1 to fix server-self-hosted job (#28603)｜Daniel Bevenius（1 file, +8/-8）
- **03fa73c** — ci : disable npm gha cache (#28600)｜Sigbjørn Skjæret（3 files, +12/-8）

修正六個檔案（`gguf.cpp`、`llama-graph.h`、`llama-mmap.cpp`、`llama-vocab.cpp`、`test-sampling.cpp`、`tools/mtmd/deprecation-warning.cpp`）缺少 `<cerrno>` / `<cstdlib>` 標頭引用，在部分編譯環境（未透過其他標頭間接引入這些符號）下會造成編譯失敗。CI 為 `server-self-hosted`（hf-jobs-t4-small:cuda13 runner）新增 `PYTEST_WORKERS=1` 環境變數，修正先前 #28298 導入 pytest-xdist 並行測試後在該 job 上出現的失敗，並同步套用到 `server-metal` 步驟。另外停用 CI 中 UI 建置相關工作流程（`ui-build`、`ui-build-self-hosted`、`ui.yml`）的 npm GitHub Actions 快取機制。

---

## 小結

本次檢查區間新增 15 筆 commit，數量較前一次（22 筆）略少，重心集中在各硬體後端（Vulkan、Metal、OpenCL、CUDA/HIP）的效能與正確性修正，其中 Vulkan 的 UNARY+MUL 融合是一項經多輪迭代、涵蓋多種張量形狀與圖優化邊界案例的完整效能功能。正確性修復方面較值得留意的兩項：一是 server 端 context checkpoint 淘汰邏輯錯誤，導致短 prompt 在 hybrid／recurrent 模型上每次接續都需重新 prefill 一段內容，屬效能回歸而非崩潰或錯誤輸出；二是 iGPU 上 `lazy` 延遲載入模式在 `AUTO` 下的裝置能力誤判回歸已被修正。此外 `chat.cpp` 的樣板解析器拆分是一項大型但聲明為無行為變更的重構。整體未見會影響現有功能正確性的破壞性變更，多為漸進式修正與例行 CI／建置維護。
