# llama.cpp 每日變更報告 - 2026-09-15

- 監控倉庫：`ggml-org/llama.cpp`（分支 `master`）
- 檢查範圍：`ad6c668`（不含）→ `1bc7a5a`（含），共 **23** 個新 commit
- 檢查區間對應時間：2026-09-14 ～ 2026-09-15

## 摘要

本次新增的 commit 大致可分為六大類：

1. **建置系統重大修正（PCH 相關 heap 損毀）**：修正因預編譯標頭（PCH）造成的 heap 記憶體損毀 bug，隨後直接將 PCH 整組移除，只保留 unity build。
2. **CI / 建置流程與發佈（release）調整**：涵蓋自架 CI 觸發條件、fusion 測試基準線、build tag 命名、以及新增 ROCm gfx1103 與 Ubuntu CUDA 的發佈建置。
3. **模型架構修正**：修正 mimo2 的 SWA pattern 載入邏輯，以及多個架構誤用 `get_key_or_arr` 的問題。
4. **新增模型架構支援**：新增 Maple 20B-A1B 三元（ternary）MoE 架構（CPU 版）。
5. **後端效能與正確性優化**：SYCL oneDNN scratchpad 釋放順序修正、SYCL top_k 導入 radix select 大幅加速、HIP MFMA 裝置改用 fp32 累加、qwen4exp 啟用 rms_norm+mul 融合。
6. **版本號更新與雜項工具**：ggml 與 llama.cpp 版本號 bump、新增 API/ABI 相容性檢查腳本、以及一個 WebUI 修正與一個核心函式呼叫順序調整。

---

## 分組詳情

### 一、建置系統重大修正（PCH 相關 heap 損毀）

| Commit | 作者 | 說明 |
| --- | --- | --- |
| `2f53959` | Georgi Gerganov | ggml-cpu：停用 PCH 並修正 `CACHE_LINE_SIZE` 的定義歧義以修復 heap 損毀——PCH 會先引入 `ggml-impl.h`，間接讓 `<new>` 定義了 `__cpp_lib_hardware_interference_size`，使 C++ 核心程式碼算出 `CACHE_LINE_SIZE = 256`，而 C 端的 work-buffer 配置邏輯卻永遠採用 fallback 值 64，兩者不一致導致 rope 的 work buffer 配置不足，造成 heap-buffer-overflow 並在 `ggml_compute_forward_rope_flt` 當機。 |
| `f3a184b` | Daniel Bevenius | cmake：直接移除先前 (#28091) 引入的 PCH（precompiled headers），只保留 unity build——PCH 在開發階段看起來有效，但後續造成多個問題（包含上一項的 heap 損毀），故團隊決定整組移除。 |

### 二、CI / 建置流程與發佈（release）調整

| Commit | 作者 | 說明 |
| --- | --- | --- |
| `15d8f2d` | Georgi Gerganov | ci：移除 `gg_sum` 摘要邏輯，簡化 `ci/run.sh`。 |
| `89fe242` | Georgi Gerganov | ci：修正自架 CI workflow 的路徑過濾規則，補上 `ci/run.sh`，讓修改該腳本時能正確觸發所有相關 job（先前未被納入，導致改了執行腳本卻不會重跑 CI）。 |
| `bbdd9f2` | Georgi Gerganov | tests：新增 fusion baseline 更新方式的 README，並擴大 fusion CI 的觸發條件（納入 `test-llama-archs.cpp` 與 `src/models` 的變更，因為這些會影響 per-fusion 計數器）；另附帶調整 `.pi/` 內部助理設定，要求發文（PR/issue 留言）前需明確授權。 |
| `7cf1c54` | Sigbjørn Skjæret | ci：修正 build tag 命名邏輯，在使用自訂 tag 名稱時直接重用，而非套用「安全化」後的名稱。 |
| `96ffdc4` | uvos | CI：`hip-quality-check` 腳本忽略前一項 HIP fp32 累加變更所新增的暫存器 spill（該 kernel 雖多用了 5 個暫存器，但實測仍比修改前更快）。 |
| `391fac1` | Oliver Simons | ci/release：新增 Ubuntu CUDA（12.8 / 13.3，x64 + arm64）的發佈建置流程，經多輪調整（安裝 git、打包相依函式庫、處理 NCCL 授權疑慮等）才定案，暫不隨附 NCCL。 |
| `b4fa47d` | Apoorv Parle | release：Ubuntu ROCm 發佈建置新增 `gfx1103` 架構支援。 |
| `be2c6d7` | Aaron Teo | tests(s390x)：CI 新增不含 VXE 指令集的 s390x 建置測試，並補上對應的巨集防護。 |

### 三、模型架構修正

| Commit | 作者 | 說明 |
| --- | --- | --- |
| `1aca1f9` | Sigbjørn Skjæret | models：修正 mimo2 架構的 SWA（sliding window attention）pattern 載入邏輯錯誤。 |
| `97e4ca7` | Sigbjørn Skjæret | models：修正 gemma4、gemma4-assistant、step35 三個架構中誤用 `get_key_or_arr` 的問題。 |

### 四、新增模型架構支援

| Commit | 作者 | 說明 |
| --- | --- | --- |
| `3d10bcd` | Alex | llama：新增 Maple 20B-A1B 三元（ternary）MoE 架構的 CPU 支援——24 層、256 個專家（8 個啟用）、SWA-512 與全域注意力以 3:1 交錯、partial rotary factor 0.5，支援 TQ1_0/TQ2_0 三元量化；歷經多次審查回饋修正（改用 `n_ff_exp_arr`/`n_ff_exp()`、修正 swiglu clamp 改由 converter 寫入、修正 LazyBase 函式簽名等）。 |

### 五、後端效能與正確性優化

| Commit | 作者 | 說明 |
| --- | --- | --- |
| `661643e` | Łukasz Ślusarczyk | sycl：修正 oneDNN scratchpad 記憶體池的釋放順序問題（原先的釋放順序會破壞 pool 的正確性）。 |
| `21f6b0d` | cwriter | sycl：為 `TOP_K` 運算導入 radix select 演算法，取代原本 k > 32 時直接 fallback 到 CPU 的限制——透過多輪 radix 選擇搭配裝置端 atomic 累加與依裝置最大 work-group size 動態調整區塊大小，在大 k 值（qwen4exp 的稀疏注意力索引器單次可用到 k=2048）情境下相較舊路徑最高加速近 5～13 倍，端到端在 3x Arc Pro B60 上小幅提升 token 生成速度，且不影響 perplexity 結果。 |
| `bfdc321` | uvos | HIP：`fattn-mma` 在 MFMA（CDNA）裝置上改用 fp32 累加，提升該路徑的數值精度/效能。 |
| `41abbfd` | Aman Gupta | qwen4exp：啟用 `rms_norm` + `mul` 融合（fusion），並改用 `TENSOR_ALLOW_RESHAPE`。 |

### 六、版本號更新與雜項

| Commit | 作者 | 說明 |
| --- | --- | --- |
| `eeea731` | Georgi Gerganov | ggml：版本號 bump 至 0.24.0。 |
| `d9e03f1` | Georgi Gerganov | sync：同步 ggml 子專案的同步紀錄檔。 |
| `b29c606` | Georgi Gerganov | llama.cpp：版本號 bump 至 0.4.1。 |
| `dfe4516` | Christian Kastner | scripts：新增驗證 API/ABI 相容性的腳本 `check-apiabi-compat.sh`。 |
| `093a2f8` | Daniel Bevenius | common：將 `llama_n_rs_seq` 的呼叫時機提前到 `llama_decode` 之前，若條件成立可直接提早回傳，避免不必要的 `llama_decode` 呼叫，同時簡化程式碼（移除多餘的 `res` 賦值與 `goto`）。 |
| `1bc7a5a` | Abhiram | webui：修正每則訊息都重新探測已停用 `/tools` 端點的問題——先前只要工具清單為空，就會被誤判為「尚未取得」而重試，若伺服器未啟用工具（回應 403）會導致每次對話都重試，甚至可能觸發 fail2ban；修正後改以狀態碼判斷是否已停用，僅在停用旗標未設定時才重試，且工具面板開啟時仍會持續探測，讓伺服器重新啟用工具後 UI 能自動恢復。 |

---

## 完整 Commit 清單（依合併時間排序）

1. `093a2f8` common : move llama_n_rs_seq to before llama_decode (#28749)
2. `661643e` sycl : fix oneDNN scratchpad breaking the pool free order (#28704)
3. `15d8f2d` ci : remove gg_sum summary logic (#28857)
4. `89fe242` ci : trigger self-hosted CI on changes to ci/run.sh (#28859)
5. `2f53959` ggml-cpu : disable PCH and fix CACHE_LINE_SIZE ambiguity to fix heap corruption (#28882)
6. `21f6b0d` sycl: rfc: Use radix select for top_k (#28670)
7. `3d10bcd` llama: add Maple 20B-A1B ternary MoE architecture (CPU) (#27000)
8. `be2c6d7` tests(s390x): add non-vxe build to tests (#28776)
9. `1aca1f9` models : fix mimo2 swa pattern load (#28865)
10. `97e4ca7` models : fix incorrect uses of get_key_or_arr (#28868)
11. `bbdd9f2` tests : add fusion baseline README and broaden fusion CI triggers (#28893)
12. `eeea731` ggml : bump version to 0.24.0 (ggml/1627)
13. `d9e03f1` sync : ggml
14. `b29c606` llama.cpp : bump version to 0.4.1 (#28900)
15. `dfe4516` scripts: Add script to verify API/ABI compatibility (#28579)
16. `f3a184b` cmake : remove precompiled headers (#28892)
17. `b4fa47d` release : added gfx1103 to ubuntu rocm build (#28423)
18. `41abbfd` qwen4exp: enable rms_norm + mul fusion (#28896)
19. `391fac1` ci : add ubuntu-cuda builds to release (#28186)
20. `bfdc321` HIP: fattn-mma: use fp32 accumulation on MFMA devices (#28576)
21. `96ffdc4` CI: hip-quality-check: ignore spill added in bfdc32183d57f1e35bacf35c47d6311e2028bbbc (#28909)
22. `7cf1c54` ci : reuse build tag name when used instead of safe one (#28911)
23. `1bc7a5a` webui: stop re-probing disabled /tools endpoint on every message (#28646)

---

- 最新已檢查 commit：`1bc7a5af0d14b1fb72f266abbd1237b394187115`（短碼 `1bc7a5a`）
