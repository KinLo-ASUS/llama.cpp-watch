# llama.cpp 更新報告 — 2026-08-24

- **來源倉庫**：[ggml-org/llama.cpp](https://github.com/ggml-org/llama.cpp)，分支 `master`
- **檢查區間**：`70adb1b4c`（2026-08-23 之前的紀錄）→ `c060ca97`（本次最新）
- **新增 commit 數**：15 筆

---

## Group 1：WebUI — 對話分頁（Chat Tabs）功能（2 筆）

- [`8144f319`](https://github.com/ggml-org/llama.cpp/commit/8144f3192e5a3131cd043f284525e6ceebf82d0f) ui: Chat Conversation Tabbed navigation (#27263)
- [`d05f8956`](https://github.com/ggml-org/llama.cpp/commit/d05f89562d50e16e8c025081aa03bf3e449efd82) fix: Change chat tabs nav shortcuts (#27609)

WebUI 新增瀏覽器風格的「對話分頁」功能：每個已開啟的對話會顯示為一個分頁，支援點擊切換、中鍵/按鈕關閉、新增分頁、水平捲動的分頁列（carousel），並可用鍵盤快速鍵（Shift+Cmd/Ctrl+方向鍵）在分頁間切換。功能預設開啟，可在設定中關閉。此功能同時重構了捲動元件為統一的 `ScrollCarousel`，並修正了深連結（deep link）、路由與分頁寬度等細節，是本次規模最大的一筆改動（跨 43 個檔案）。緊接著的第二筆則修正分頁切換快速鍵與既有富文本輸入框（rich chat input）的按鍵衝突問題。

## Group 2：張量並行（Tensor Parallel）meta tensor split 狀態修正（2 筆）

- [`d3371929`](https://github.com/ggml-org/llama.cpp/commit/d3371929bb1b6982cf73f1e54156d3d426cd80a1) [Tensor parallel] Fix meta tensor split state propagation (#27574)
- [`95b8e33e`](https://github.com/ggml-org/llama.cpp/commit/95b8e33e16bb9a60de780a70930ebf729db6a90a) ci : add test-llama-archs tensor split for Metal (#27598)

修正 `ggml-backend-meta`（用於在載入模型時模擬多裝置張量切分的 meta backend）中，張量分割狀態未正確傳遞的問題，並同步調整 `llama-model.cpp` 的相關邏輯，同時將 `test-llama-archs` 加入 CI。第二筆則將這項張量切分測試延伸到 Metal 後端（模擬 1～4 個 `GGML_METAL_DEVICES`），並讓該 CI job 改為無條件派工，交由各後端自身的判斷邏輯決定是否執行，補齊先前僅覆蓋 CUDA 的測試缺口。

## Group 3：模型支援與修正 — DeepSeek V4 / GLM-4.5-Air（2 筆）

- [`b0539c43`](https://github.com/ggml-org/llama.cpp/commit/b0539c43ed13b16bf0d8a0840646faea65469702) DeepseekV4: fix rollback with multi-seq (#26756)
- [`c060ca97`](https://github.com/ggml-org/llama.cpp/commit/c060ca974c773c7c3d17fd1b66dc9d312bc292c0) model : support MTP in GLM-4.5-Air (#26534)

修正 DeepSeek V4 壓縮式 KV cache（`llama-kv-cache-dsv4.cpp`）在多序列（multi-seq）情境下的 rollback（回捲）邏輯：讓「待處理的 rollback」改為單次使用、僅在完整載入時才清除對應序列的 cache、對壓縮比新增 assert 檢查，並補上專門的 `test-recurrent-state-rollback.cpp` 測試。另一筆則為 `GLM-4.5-Air` 架構新增 MTP（Multi-Token Prediction，多 token 預測／推測解碼用的草稿層）支援，修改轉換腳本 `conversion/glm.py`、GGUF 常數定義，並在 `glm4-moe.cpp` 中新增大量對應的模型建構邏輯（約 200 行）。

## Group 4：CUDA 後端新增 POOL_1D 運算子（1 筆）

- [`29ea9412`](https://github.com/ggml-org/llama.cpp/commit/29ea9412a6b5e343dc6736a5a4349158cf78b30c) cuda : add POOL_1D support (#27573)

為 CUDA 後端補上 `GGML_OP_POOL_1D`（一維池化）運算子的實作，新增獨立的 `pool1d.cu` / `pool1d.cuh` 檔案並註冊至 `ggml-cuda.cu`，補齊此運算子在 CUDA 上原本缺失的支援。

## Group 5：mtmd 圖像前處理演算法修正（1 筆）

- [`56db501e`](https://github.com/ggml-org/llama.cpp/commit/56db501e73cfb10c8fcce61be708f5c3ee749271) mtmd: use pillow-accurate algo, correct resize_algo for all models (#27594)

大幅重寫 `mtmd-image.cpp` 中的影像縮放（resize）邏輯，改用與 Python Pillow 函式庫數值一致的演算法，並修正各視覺模型對應的 `resize_algo` 選擇（先前部分模型可能套用了錯誤的縮放演算法），此舉有助於讓多模態模型的影像前處理結果更貼近官方 Python 實作、降低推論結果的落差，同時精簡了程式碼（淨減少約 160 行）。

## Group 6：測試與 CI 整理（2 筆）

- [`4a08fa29`](https://github.com/ggml-org/llama.cpp/commit/4a08fa29705b8177e332b134306566c2c4b95902) test: move tools/parser to tests (#27548)
- [`8d9af256`](https://github.com/ggml-org/llama.cpp/commit/8d9af256337d1a501250f9bbf4c0859a654bddd6) test : fix multi-GPU server tests (#27614)

將原本獨立的 `tools/parser`（含 `debug-template-parser.cpp`）除役，其邏輯併入既有的 `tests/test-chat-auto-parser.cpp`（大幅擴充內容），屬於測試程式碼的整併與清理。另一筆修正 `test_slot_save.py`、`test_vision_api.py` 等 server 測試在多 GPU 環境下執行失敗的問題。

## Group 7：Server 與 common 雜項改進（2 筆）

- [`ba8e0edd`](https://github.com/ggml-org/llama.cpp/commit/ba8e0eddfb8e4791915e5e1f77a66615e30e7d2e) common : skip device_info loop if it's not going to be printed (#26692)
- [`e8eed452`](https://github.com/ggml-org/llama.cpp/commit/e8eed4525aaca00a78d8f837dce90dd4d4708133) server : add LLAMA_SERVER_SLOTS_N_DIFF (#27600)

修正一個資源浪費的問題：列印裝置資訊（device_info）的迴圈即使在不需要輸出（log 等級低於 TRACE）時，仍會為每個裝置建立 GPU context（在 CUDA 上會產生約 550MB 的 VRAM 配置），修正後會先檢查是否真的需要輸出再決定是否執行該迴圈，讓不打算使用 GPU 資源的使用者可以避免不必要的顯存佔用。另一筆則為 server 新增 `LLAMA_SERVER_SLOTS_N_DIFF` 環境變數，用於調整 server slot 數量相關的行為。

## Group 8：文件與貢獻指引（2 筆）

- [`a278dcef`](https://github.com/ggml-org/llama.cpp/commit/a278dcef04f2ce950d3a17d8aa76d4ef86483e0e) contrib : recommend waiting for CI before merging (#27603)
- [`ccc8fd2b`](https://github.com/ggml-org/llama.cpp/commit/ccc8fd2baa64ab26c7210b02765d3604069ec7ee) readme : update links (#27617)

`CONTRIBUTING.md` 新增建議：合併 PR 前應等待 CI 跑完。README 則更新連結，並將 `ggml-org` maintainers 團隊新加入的三名成員（nikwen、marty1885、Titaniumtown）補進「maintainer PR 清單」連結的作者篩選條件中，使清單與目前 35 人的團隊名單一致。

## Group 9：第三方套件更新（1 筆）

- [`6657ded4`](https://github.com/ggml-org/llama.cpp/commit/6657ded4faa3b8450221119fc6b4d002e35104a2) vendor : update subprocess.h (#27409)

透過專案自帶的 `scripts/sync_vendor.py` 同步腳本，更新 vendored 的第三方單標頭函式庫 `sheredom/subprocess.h` 至最新版本。

---

## 本次重點摘要

- 本次共 15 筆 commit，與前次規模相當，內容涵蓋 WebUI 新功能、模型架構修正/新增、後端運算子補齊與測試/CI 整理，沒有出現重大 breaking change。
- **WebUI 對話分頁功能上線**：新增瀏覽器風格的多分頁對話介面，是本次改動範圍最大的一筆（跨 43 檔案），預設啟用，並在同一批次內快速修正了鍵盤快速鍵的衝突問題，建議關注後續是否有進一步的 UX 回饋修正。
- **張量並行穩定性修正**：`meta tensor split` 狀態傳遞問題修正，並將相關測試（`test-llama-archs`）從 CUDA 擴展到 Metal 後端，強化多裝置切分情境下的測試覆蓋率。
- **模型面**：DeepSeek V4 修正了多序列情境下 KV cache 的 rollback 邏輯（新增專門測試）；GLM-4.5-Air 新增 MTP（多 token 預測）支援，呼應近期推測解碼相關功能的持續擴充趨勢。
- **多模態影像處理精度提升**：`mtmd` 圖像縮放演算法改為與 Pillow 數值一致，並修正各模型 `resize_algo` 選擇錯誤的問題，可能影響先前使用視覺模型時的辨識準確度，建議實際使用視覺功能的使用者留意此更新。
- 其他：CUDA 補上 `POOL_1D` 運算子；server 新增 `LLAMA_SERVER_SLOTS_N_DIFF` 環境變數；修正在未啟用 TRACE log 時仍會建立 GPU context 造成約 550MB VRAM 浪費的問題；測試與 CI 持續整併（`tools/parser` 併入 tests、修正多 GPU 測試）；文件與 vendor 套件維護性更新。
