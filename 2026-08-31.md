# llama.cpp 變更報告 — 2026-08-31

- 檢查範圍：`c589f0e`（2026-08-30 之前）→ `9723942`（2026-08-31）
- 新增 commit 數：23 筆
- 資料來源：`ggml-org/llama.cpp` master 分支

---

## 1. RPC 後端強化與修正

這組 5 個 commit 都與遠端運算後端（RPC backend）有關，涵蓋 Apple RDMA 傳輸層修正、alloc size 查詢邏輯，以及多伺服器場景下的序列化安全性。

- **[#27815](https://github.com/ggml-org/llama.cpp/commit/2bf0415) rpc : fix pre-rdma macOS versions**
  修正在不支援 RDMA 的較舊版 macOS 上，RPC transport 層的編譯／執行相容性問題。
- **[#27908](https://github.com/ggml-org/llama.cpp/commit/742347b) rpc: fix apple rdma error spew on teardown**
  修正 Apple RDMA transport 在關閉連線時洗版式輸出大量錯誤訊息的問題。
- **[#27960](https://github.com/ggml-org/llama.cpp/commit/73f56d1) ggml : add ggml_backend_op_alloc_size_may_expand, use it in RPC**
  部分後端（Metal、SYCL、WebGPU）在特定運算子上需要額外的暫存記憶體。此修正新增 `ggml_backend_op_alloc_size_may_expand()` 工具函式明確列出這些運算子，並在 RPC 後端用它取代硬編碼清單，決定是否需要向遠端伺服器查詢實際配置大小。
- **[#28038](https://github.com/ggml-org/llama.cpp/commit/6d1479c) ggml : fix ggml_backend_buft_get_alloc_size() guard**
  上一個 commit（#27960）的後續修正，修補該 assertion guard 條件本身的錯誤（同時觸及 CUDA 後端）。
- **[#26500](https://github.com/ggml-org/llama.cpp/commit/a7cc83b) rpc: avoid serializing buffers from other servers**
  在多台 RPC 伺服器組成的運算圖中，先前會把不屬於當前 dispatcher 的遠端 buffer 指標一併序列化，可能導致跨伺服器資料錯亂；此修正只保留屬於接收該運算圖的伺服器自身的 buffer 指標，並新增雙伺服器的回歸測試。

## 2. Metal fa-vec 效能調校（M2 / M3 Pro / M3 Ultra）

延續先前針對各代 Apple GPU 的 Flash-Attention（fa-vec）調校表補齊工作，本次為純數據新增，不影響邏輯。

- **[#27963](https://github.com/ggml-org/llama.cpp/commit/b8b743c) metal : Add fa-vec tuning for M3 Pro**（對應 issue #27668）
- **[#27999](https://github.com/ggml-org/llama.cpp/commit/093adb2) metal: add fa-vec tunings for M3 Ultra**
- **[#28017](https://github.com/ggml-org/llama.cpp/commit/bd55e6a) metal : add remaining Q4_1/Q5_0/Q5_1 fa-vec tunings for M2**
  補齊 M2 尚缺的 Q4_1／Q5_0／Q5_1 資料型別調校表。

## 3. Metal 正確性修正：F16 mul_mat 的 nil-pipeline crash

- **[#25648](https://github.com/ggml-org/llama.cpp/commit/d882575) metal : fix null-pipeline crash for F16 src1 mul_mat/mul_mat_id**
  當 `mul_mat`／`mul_mat_id` 遇到缺少對應 F16 kernel 的形狀組合時，先前會因取得 nil pipeline 而直接崩潰。此修正讓 `supports_op` 與實際 dispatch 共用同一套判斷邏輯，改為在不支援的情況下提前「fail closed」拒絕，並在 `encoder_set_pipeline` 對 nil pipeline 加上 abort 防線，將未預期的崩潰轉為可控的錯誤處理。

## 4. AMD GPU（HIP/CUDA）效能調校

- **[#26753](https://github.com/ggml-org/llama.cpp/commit/e422148) hip : optimize Q2_0 dot-product path for gfx1201**
  針對 gfx1201（RDNA4 系列）架構，使用原生 amdgcn perm 指令優化 `vec_dot_q2_0_q8_1` 與 MMQ 的 unpack 流程。
- **[#26284](https://github.com/ggml-org/llama.cpp/commit/0b5be7e) hip: tune rdna 3 mmq config**
  調整 RDNA3 架構的 MMQ（矩陣乘量化）tile 設定參數，屬於效能調校。

## 5. CUDA MoE 效能優化：任意 n_expert_used 皆可走快速路徑

- **[#27978](https://github.com/ggml-org/llama.cpp/commit/f1793c1) CUDA: use the fast mm_ids_helper path for any n_expert_used**
  原本的快速路徑要求 `warp_size % n_expert_used == 0`，僅對「6 補到 8」的狀況有硬編碼例外，其他 expert 數量皆落回逐 token 處理的通用慢路徑。此修正發現 lane group 只需整除 warp、且迴圈本身已用 `iex < n_expert_used` 保護補位的 lane，因此把補位規則推廣到「補到下一個 2 的次方」，使 `n_expert_used = 10` 等情況也能走快速路徑。實測 Qwen3.8-Flash-Next（512 experts，10 used）於 55k context、RTX PRO 6000 上：prompt processing 由 2334 提升至 2600 t/s；token generation 因單 token 無需走此迴圈而不受影響。

## 6. Hexagon（Qualcomm NPU）裝置探索與修正

- **[#27785](https://github.com/ggml-org/llama.cpp/commit/9e54e68) hexagon: support for device discovery and create sessions on demand**
  新增延遲 session 配置與介面清理機制，支援執行期探索可用的 NPU 核心數量，並在初始化時提早拒絕不存在的裝置。
- **[#28033](https://github.com/ggml-org/llama.cpp/commit/9723942) hexagon: fix CPY fence bug**
  修正 Hexagon HTP 後端 CPY（複製）運算子中的同步 fence 錯誤。

## 7. SYCL TOP_K 效能優化

- **[#27847](https://github.com/ggml-org/llama.cpp/commit/370cb12) sycl: split long rows in TOP_K instead of one work-group per row**
  原本 TOP_K kernel 不論行長皆以一個 work-group 處理一整行；此修正讓過長的行拆分給多個 work-group 平行處理，改善大詞表／長序列情境下的效能。

## 8. Lazy tensor loading：CLI 參數改名與行為改善

- **[#27969](https://github.com/ggml-org/llama.cpp/commit/bebc935) common: rename --tensor-read-lazy to --lazy-mode, add -lzm shorthand**
  將 CLI 參數 `--tensor-read-lazy` 改名為 `--lazy-mode`（與內部參數名稱一致），並新增 `-lzm` 簡寫，同步更新各工具的 README。
- **[#27837](https://github.com/ggml-org/llama.cpp/commit/2578138) llama: improve TENSOR_READ_LAZY handling**
  改善 lazy 模式下的張量讀取處理邏輯，包含在啟用 lazy 模式時強制張量留在 CPU 上等調整。

## 9. KV cache / 記憶體修正與優化

- **[#27967](https://github.com/ggml-org/llama.cpp/commit/bdf3955) memory : copy Hadamard matrix to k_rot tensor only if it has buffer assigned**
  修正在未量化 K cache 進行 context shift 時，因 `k_rot` 張量尚未配置 buffer 就寫入 Hadamard 旋轉矩陣而導致的崩潰。
- **[#28011](https://github.com/ggml-org/llama.cpp/commit/62acc89) kv-cells: stop the sequence scan once all sequences are seen**
  `for_each_token_in` 先前對每個使用中的 cell 都會掃描全部 `LLAMA_MAX_SEQ` 個序列，即使該 cell 通常只屬於一個序列；此修正讓掃描在看過該 cell 自身所屬的序列後即提早結束（存取順序與回呼參數不變，行為不變）。這條路徑只被 n-gram 查詢用的 `get_prev_tokens` 呼叫。實測 RTX PRO 6000、Qwen3.8-Flash-Next UD-Q4_K_XL：55k context 下 generation 由 56.3 提升至 74.3 t/s；132k context 下由 33.6 提升至 50.9 t/s（prompt processing 因掃描成本已分攤在 ubatch 中而不受影響，效益隨 context 增長）。

## 10. ggml 核心：graph_optimize 相依性與新運算子 SWIGLU_CLAMP

- **[#27301](https://github.com/ggml-org/llama.cpp/commit/57291f2) ggml: allow passing alloc dependencies in graph_optimize**
  讓 `graph_optimize` 可以接收跨後端（CUDA、Hexagon、Metal、virtgpu、Vulkan）的記憶體配置相依資訊，並新增對應測試。
- **[#27930](https://github.com/ggml-org/llama.cpp/commit/0190529) ggml: add SWIGLU_CLAMP**
  新增結合 SwiGLU 與數值 clamp 的融合啟動函式運算子，並幾乎在所有後端（CPU、CUDA、Metal、Vulkan、SYCL、OpenCL、CANN、Hexagon、OpenVINO、WebGPU、ET）補齊實作與測試，是本次範圍內改動幅度最大的一筆（涉及 40 個檔案、新增約 768 行）。

## 11. 其他

- **[#27919](https://github.com/ggml-org/llama.cpp/commit/dc7aecf) vendor : update cpp-httplib to 0.54.0**
  更新內建的 `cpp-httplib` 函式庫（分兩階段更新到 0.54.0 及 0.54.1）。
- **[#28000](https://github.com/ggml-org/llama.cpp/commit/cc231cb) dflash: pass missing NVFP4 scales to attention operations**
  修正 DFlash2 推測解碼在使用 NVFP4 量化草稿模型時，因 Q、K、V 與輸出投影的 scale 未傳入對應運算子，導致幾乎沒有草稿 token 被接受的問題。

---

## 小結

本次追蹤區間（2026-08-30 至 2026-08-31）共 23 個 commit，涵蓋範圍較廣，重點如下：

1. **RPC 後端**：一連串修正與強化（Apple RDMA 傳輸、alloc size 查詢、跨伺服器序列化安全性），顯示分散式推理場景近期受到較多關注。
2. **正確性修正**：Metal `mul_mat` 的 nil-pipeline crash、KV cache 未配置 buffer 時的崩潰、DFlash2 NVFP4 推測解碼準確率問題，皆屬會影響穩定性或正確性的修補，建議留意升級。
3. **效能優化**：CUDA MoE 快速路徑泛化、KV cache 序列掃描提前終止、SYCL TOP_K 分行平行化、AMD HIP/RDNA 系列調校，多筆附有實測數據，長 context／MoE 場景效益明顯。
4. **新功能**：新增橫跨近乎全部後端的 `SWIGLU_CLAMP` 融合運算子；`graph_optimize` 支援跨後端 alloc 相依性。
5. **其他**：Lazy tensor 載入的 CLI 參數改名與行為改善、Hexagon NPU 裝置探索、Metal fa-vec 調校表持續補齊、`cpp-httplib` 版本更新。
