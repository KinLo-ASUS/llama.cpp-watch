# llama.cpp 變更報告 — 2026-08-02

檢查範圍：`ggml-org/llama.cpp` master 分支，自上次紀錄的 commit `876a4321` 之後至 `11924d4` 為止，共 **7 筆新 commit**。

## 群組總覽

| 群組 | 主題 | Commit 數 |
|---|---|---|
| A | 對話（Chat）推理內容處理改進 | 2 |
| B | 多模態（mtmd）MiniCPM-V 支援 | 1 |
| C | Vulkan 後端效能優化 | 1 |
| D | 相依套件更新 | 1 |
| E | 開發文件與 CI 維護 | 2 |

---

## 群組 A：對話（Chat）推理內容處理改進

針對聊天與推理（reasoning）內容的處理進行修正與擴充，讓「思考」內容能正確保留與運用。

- **`ddd4ec1` chat : enable tool call in thinking for DS4 (#26269)** — 作者：Piotr Wilkin (ilintar)
  修改 `common/chat.cpp` 的解析邏輯，讓 DeepSeek V4（DS4）系列模型在「thinking／思考」區塊內也能正確解析並啟用工具呼叫（tool call），同時更新相關測試與除錯用的 template parser 工具。

- **`c629da5` cli : persist reasoning_content in chat history (#26362)** — 作者：Nico
  修正 `llama-cli` 的問題：先前僅將串流中收到的 `reasoning_content` 用於顯示，卻沒有存入對話歷史訊息中，導致 `--reasoning-preserve` 選項無法在後續對話輪次中重新注入先前的思考內容。此修正將 reasoning 內容一併持久化到 `tools/cli/cli-context.cpp` 的訊息紀錄。

---

## 群組 B：多模態（mtmd）MiniCPM-V 支援

- **`de69995` mtmd: add minicpmv46 downsample (#25993)** — 作者：tc-mb
  為 MiniCPM-V 4.6 模型新增影像降採樣（downsample）支援，將降採樣模式資訊放入 GGUF 檔案中，並重構 `tools/mtmd/models/minicpmv.cpp`、`clip.cpp` 等模組以支援新的 `mtmd_image_preprocessor_llava_uhd` 前處理流程，同時更新 Python 端轉換腳本 `conversion/minicpm.py`。

---

## 群組 C：Vulkan 後端效能優化

- **`a7a6d0d` vulkan: extend topk_moe fusion to support sqrt(softplus) (#26124)** — 作者：Jeff Bolz
  擴充 Vulkan 後端的 `topk_moe`（Top-K MoE）融合運算，新增對 `sqrt(softplus(x))` 激活函式的支援，修改 `ggml-vulkan.cpp` 與對應的 `topk_moe.comp` shader，讓更多 MoE 模型架構可以在 Vulkan 上享有融合運算帶來的效能提升。

---

## 群組 D：相依套件更新

- **`815a2a5` vendor : update BoringSSL to 0.20260730.0 (#26353)** — 作者：Alessandro de Oliveira Faria (A.K.A.CABELO)
  將專案中透過 `vendor/cpp-httplib` 引用的 BoringSSL 版本更新至 `0.20260730.0`，屬於安全性/相依套件例行更新。

---

## 群組 E：開發文件與 CI 維護

- **`89482bd` agents: clarify comment style and jinja knowledge (#26405)** — 作者：Xuan-Son Nguyen
  更新 `AGENTS.md` 開發文件，釐清程式碼註解風格規範與 Jinja 樣板相關知識，並小幅改善 `skills/code-review/SKILL.md` 中的安全性審查（Security review）說明，屬於協助 AI 代理／貢獻者遵循專案慣例的文件性修改。

- **`11924d4` test: fix some CI errors (#26415)** — 作者：Xuan-Son Nguyen
  修正 CI 測試中的錯誤：調整 `tests/test-llama-archs.cpp` 的測試內容，並在 `tools/ui/CMakeLists.txt` 新增建置設定，以解決 CI 流程失敗的問題。

---

## 本次檢查範圍紀錄

- 起始 commit（不含）：`876a4321163249c43ca4e986818fab5ab081f282`
- 結束 commit（含）：`11924d4c17abc27383376a1ac6a24fa3e36c1c0c`
- 新 commit 數量：7
