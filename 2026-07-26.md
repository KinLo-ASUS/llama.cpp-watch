# llama.cpp 變更報告 — 2026-07-26

- 檢查範圍：`ggml-org/llama.cpp` master 分支
- 上次檢查 commit：`555881e` (2026-07-25)
- 本次最新 commit：`7cdd557` (2026-07-26)
- 新增 commit 數量：11

---

## 分組摘要

### 1. Server：MCP 與生成參數相關更新
新增 MCP (Model Context Protocol) stdio 支援，並修補伺服器端 API 回傳資料與工具參數的缺漏。

- **`20455a4`** — server: support MCP stdio (#26062) — by ngxson, pwilkin, ServeurpersoCom
  新增透過 stdio 與外部 MCP 伺服器溝通的完整基礎架構：管線化 (pipe) 通訊、生命週期管理、工具探索與呼叫（透過 `/tools` 端點）。新增 `--mcp-servers-config` / `--mcp-servers-json` 參數，並補上多個測試 fixture 與整合測試，同時修正殭屍程序與傳輸層未正確關閉的問題。
- **`abc3487`** — server: add format arg to datetime tool (#26117) — by ngxson
  為內建 datetime 工具新增 `format` 參數，可指定 strftime 格式字串（預設為 ISO 8601），並改用 `gmtime_s`/`gmtime_r` 確保輸出為 UTC 時間。
- **`2cfc767`** — server: add missing task parameters (adaptive_target, adaptive_decay) in generation_settings (#25830) — by tekinertekin
  修正 `generation_settings` JSON 輸出遺漏 `adaptive_target`、`adaptive_decay` 兩個取樣參數的問題（對應 issue #25803），並補上回歸測試。

### 2. Common：核心邏輯修正與功能擴充
針對 LoRA 載入、preset 解析與推理預算 (reasoning budget) 取樣器的修正與擴充。

- **`8bb9093`** — common: use-after-free when loading LoRA adapter fails (#25611) — by nmouha
  移除 LoRA adapter 載入失敗時錯誤路徑中多餘的 `pimpl->model.reset(model)`，修正潛在的 use-after-free 問題。
- **`c812c54`** — common: skip empty implicit default preset (#25643) — by hogeheer499-commits
  INI 設定解析器會自動建立隱含的 default 區段；當該區段在移除保留關鍵字（如 `version`）後沒有任何模型選項時，先前仍會被加入並暴露在 router 模式中，現在會跳過這種空的隱含 default，同時保留真正的 default/具名 preset 與全域 `[*]` 設定。
- **`910196f`** — common: add support for multiple end sequences in the reasoning budget sampler — by aldehir
  將 trie/Aho-Corasick 邏輯抽出獨立成 `trie.cpp`/`trie.h`，讓 reasoning budget 取樣器可支援多組結束序列（原僅支援單一 tag），並在 budget 耗盡時重放對應的結束序列；相關的 chat template 設定由單一 tag 改為 tag 陣列。

### 3. Hexagon 後端
- **`355303e`** — hexagon: partial im2col support (#26007) — by tboinovski1
  為 Hexagon 後端新增 IM2COL 運算支援（主要針對 patch-embedding 卷積），包含型別與參數驗證、算子對應（`GGML_OP_IM2COL` → `HTP_OP_IM2COL`）以及新增 `im2col-ops.c` 實作檔。

### 4. 建置 / CI
- **`7cdd557`** — ggml-webgpu: Fix WASM compilation with OpenMP (#25943) — by reeselevine
  將 WASM 建置拆成獨立的 GitHub Actions workflow（`build-wasm.yml`），加入新版 emsdk 所需旗標並停用 WASM 建置中的 OpenMP，解決編譯失敗問題。

### 5. 依賴套件更新
- **`720d7fa`** — vendor: update cpp-httplib to 0.51.0 (#26067) — by angt
  將內嵌的 cpp-httplib 由 0.50.1 升級至 0.51.0，同步更新 `vendor/cpp-httplib/httplib.{h,cpp}` 與同步腳本版本號。

### 6. GGML 核心
- **`fb92d8f`** — Update ggml/src/gguf.cpp: 為 gguf_writer_base 解構子加上 virtual — by yymin1022
  將 `gguf_writer_base` 的解構子改為 `virtual ~gguf_writer_base(void) = default;`，避免透過基底類別指標刪除衍生類別物件時，衍生類別解構子未被呼叫的問題。

### 7. 測試
- **`d67c0b4`** — tests: synchronize save-load-state generation (#26056) — by helanfxz
  在 `test-save-load-state.cpp` 的生成流程中加入 `llama_synchronize(ctx)` 呼叫，確保狀態儲存/讀取測試在生成過程中正確同步 context 狀態。

---

## 完整 Commit 列表（新到舊）

| SHA | 標題 | 分類 |
|---|---|---|
| `7cdd557` | ggml-webgpu: Fix WASM compilation with OpenMP (#25943) | 建置/CI |
| `8bb9093` | common : use-after-free when loading LoRA adapter fails (#25611) | Common 修正 |
| `20455a4` | server: support MCP stdio (#26062) | Server |
| `355303e` | hexagon: partial im2col support (#26007) | Hexagon |
| `c812c54` | common : skip empty implicit default preset (#25643) | Common 修正 |
| `abc3487` | server: add format arg to datetime tool (#26117) | Server |
| `2cfc767` | server : add missing task parameters(adaptive_target, adaptive_decay) in generation_settings (#25830) | Server |
| `720d7fa` | vendor : update cpp-httplib to 0.51.0 (#26067) | 依賴更新 |
| `fb92d8f` | Update ggml/src/gguf.cpp : Defined virtual keyword for destructor of gguf_writer_base (#25867) | GGML 核心 |
| `910196f` | common : add support for multiple end sequences in the reasoning budget sampler (#25544) | Common 功能 |
| `d67c0b4` | tests: synchronize save-load-state generation (#26056) | 測試 |
