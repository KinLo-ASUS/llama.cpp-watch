# llama.cpp 變更報告 — 2026-07-31

- 檢查範圍：`3018a11e` → `5f55650a`（ggml-org/llama.cpp，`master` 分支）
- 新增 commit 數：17

## 群組一：CUDA / Vulkan 後端最佳化與新功能

| Commit | 摘要 |
| --- | --- |
| [`9b2a0888`](https://github.com/ggml-org/llama.cpp/commit/9b2a088819cda774bdbf713168ee1eee8498cda5) CUDA: add Q2_0 support (#25707) | 為 CUDA 後端新增 `Q2_0` 量化格式支援，涵蓋 dequantize、getrows、mmq（多種架構的 config：Ampere/CDNA/Pascal/RDNA2~4）、mmvq 等路徑，並補上對應測試。 |
| [`e1a1abb7`](https://github.com/ggml-org/llama.cpp/commit/e1a1abb78746c025f5e9039f590e37ccdb758ae7) ggml-cuda: Allow transpose-free gemmv computation (#26171) | 當權重矩陣形狀為 `1xK` 時，改用免轉置的 `mat_mul_vec_f` 計算路徑，減少不必要的轉置開銷。 |
| [`ea63b4d3`](https://github.com/ggml-org/llama.cpp/commit/ea63b4d32ea1b66bdbe369be7f9443f6c00f8b31) vulkan: Support quantized concat (#25684) | Vulkan 後端新增對量化張量做 `concat` 運算的支援，擴充 Vulkan 可處理的算子覆蓋範圍。 |
| [`958d9c0b`](https://github.com/ggml-org/llama.cpp/commit/958d9c0b61d0025ca8e2fff32d66a1576173fde9) Test support for alternative conv layout (#25617) | 為 `conv2d` 新增 `cwhn` 替代記憶體佈局的測試與支援：CPU 後端要求資料連續儲存、CUDA/Vulkan 補上圖建置期與 `supports_op` 期的佈局檢查，修正對應的 CI 失敗與執行期 assert 問題。 |

**小結**：這一組聚焦於 GPU 後端（CUDA、Vulkan）的算子覆蓋與效能優化，新增 Q2_0 量化、免轉置 GEMV、量化 concat，並強化 conv2d 在不同記憶體佈局下的正確性檢查。

## 群組二：模型圖（graph）程式碼整理

| Commit | 摘要 |
| --- | --- |
| [`64d528be`](https://github.com/ggml-org/llama.cpp/commit/64d528be72d083fcaa76f3224ee88c742c2dd975) mimo2: address MTP review feedback (#26228) | 依據先前 code review 意見，調整 `mimo2` 模型（含 MTP，Multi-Token Prediction）的實作，更新 `gguf` 常數與 `models.h`/`mimo2.cpp` 邏輯。 |
| [`b2f22168`](https://github.com/ggml-org/llama.cpp/commit/b2f221684fcd898e947a121baeda80f345da3e6b) Remove custom cpu op from the M3 graph, express with stock ops (#26297) | 將 `minimax-m3` 模型圖中原本自訂的 CPU 運算子，改用 ggml 內建的標準算子表示，簡化程式碼並移除客製實作。 |

**小結**：兩個模型（mimo2、minimax-m3）的圖建置程式碼皆進行了整理與簡化，屬於既有模型實作的後續維護。

## 群組三：Metal 後端修正

| Commit | 摘要 |
| --- | --- |
| [`d0bfb198`](https://github.com/ggml-org/llama.cpp/commit/d0bfb1981266c271cd0536a8aa7c5e863e7cdf61) metal: fix memory unwire if model is freed without any GPU operations (#26082) | 修正模型在完全沒有執行過 GPU 運算就被釋放時，Metal 記憶體未正確 unwire（解除常駐）而造成洩漏的問題；並新增系統層級的常駐記憶體量測回歸測試。 |

## 群組四：Server / 推論流程

| Commit | 摘要 |
| --- | --- |
| [`b4ca032a`](https://github.com/ggml-org/llama.cpp/commit/b4ca032ae3729516943884786de4ae39fba0bbca) server: support inp embd to generate next token (#26313) | `llama-server` 新增支援：可直接輸入 embedding（`inp embd`）來產生下一個 token，並修正 `server_batch` 的解構邏輯。 |
| [`432d7ffe`](https://github.com/ggml-org/llama.cpp/commit/432d7ffe2c3b4e539f3d0d4ae0a4893090a018d6) llama-context: sync pending async copies before clearing embd_seq (#25676) | 在清除 `embd_seq` 前，先同步等待尚未完成的非同步複製操作，避免資料競爭 / 未同步造成的錯誤結果。 |
| [`5f55650a`](https://github.com/ggml-org/llama.cpp/commit/5f55650a78f92aff4d48d671423e888fac0469ff) mtmd: add lanczos resize method [no release] (#26341) | 多模態模組 `mtmd` 新增 Lanczos 影像縮放演算法，提供比既有方法更高品質的圖片縮放選項。 |

## 群組五：WebUI

| Commit | 摘要 |
| --- | --- |
| [`21a5f5b7`](https://github.com/ggml-org/llama.cpp/commit/21a5f5b7f919834e37fd492158aa24dc2f5e3b26) ui: IndexedDB and Conversations data fixes (#26278) | 一系列 WebUI 前端修正：對話存放區改為單次初始化（single-flight）、系統訊息建立改為交易式（transactional）、編輯/重新生成時正確串連刪除分支訊息、修正批量刪除計數與時間戳記更新邏輯、系統訊息編輯器的聚焦行為調整等，屬於 IndexedDB 資料層與對話管理的穩定性修正集合。 |

## 群組六：測試與 CI 基礎設施

| Commit | 摘要 |
| --- | --- |
| [`a6a77bc4`](https://github.com/ggml-org/llama.cpp/commit/a6a77bc48d05d8e1f5467b91ec0b5bcc8897b86c) [UT] enhance UT to show all real unsupported backends (#25234) | 改善 `test-backend-ops` 單元測試輸出，讓其能列出所有實際不支援該運算的後端，而非只顯示部分結果。 |
| [`32703b42`](https://github.com/ggml-org/llama.cpp/commit/32703b42d6d8a74336bf27613705cbc76e2363ee) ggml: Fix issue with kleidiai ci and stringop overflow warning (#26277) | 修正 `ggml-cpu/repack.cpp` 中因 KleidiAI CI 環境觸發的 `stringop-overflow` 編譯警告。 |
| [`47f686f5`](https://github.com/ggml-org/llama.cpp/commit/47f686f53f2a20fe82d87d79fa5c835714ee8f46) tests: avoid building get-model.cpp many times (#26317) | 移除重複的 `tests/get-model.cpp`/`.h`，改用 `common/common.cpp` 中共用的模型量化類型選擇邏輯，減少重複編譯、加快測試建置速度。 |

## 群組七：ggml 同步與版本

| Commit | 摘要 |
| --- | --- |
| [`6a4c3357`](https://github.com/ggml-org/llama.cpp/commit/6a4c3357c86517f2ae0a853436e130672a67147c) ggml: bump version to 0.18.0 (ggml/1576) | 將內嵌的 `ggml` 版本號提升至 0.18.0。 |
| [`9ebfc3a8`](https://github.com/ggml-org/llama.cpp/commit/9ebfc3a8cf4c1c6983258c4d603274b2b3d3dd65) sync: ggml | 更新 `scripts/sync-ggml.last`，同步上游 `ggml` 專案的最新狀態記錄。 |

## 群組八：文件

| Commit | 摘要 |
| --- | --- |
| [`6b36c230`](https://github.com/ggml-org/llama.cpp/commit/6b36c2305644fd30db7cce3f4840c74574f31ce9) readme: refresh (#26280) | 大幅整理 README：徽章與連結置中、移除「Hot topics」與「Recent API changes」段落、新增開發分支與編譯時間連結、將 XCFramework、Completions、取得與量化模型等段落抽出至 `docs/` 下的獨立文件，並將 Contributing 段落移至文末，整體讓首頁更精簡。 |

---

## 整體觀察

本次追蹤區間（`3018a11e` → `5f55650a`）共 17 個 commit，重點落在：
1. **GPU 後端持續擴充算子覆蓋**（CUDA Q2_0、免轉置 GEMV、Vulkan 量化 concat、conv2d 多佈局支援）；
2. **多項穩定性修正**（Metal 記憶體洩漏、llama-context 非同步複製同步問題、WebUI IndexedDB 資料層一致性）；
3. **維護性工作**（README 精簡重整、測試建置去重、ggml 版本同步）。

無重大破壞性變更（breaking change）或安全性相關修正。
