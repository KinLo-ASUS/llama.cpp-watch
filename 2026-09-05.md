# llama.cpp 變更報告 — 2026-09-05

- 追蹤範圍：`ggml-org/llama.cpp` master 分支
- 檢查區間：`d230ddd` (2026-09-04 之前的檢查點，不含) → `427291b` (本次檢查點)
- 新增 commit 數：23

---

## 分組摘要

### 1. Metal 後端：M3 / M3 Max flash-attention-vec 調校參數補齊

- **8f83678** — metal : add remaining fa-vec tunings for M3 Max (#28373)｜Niklas Wenzel
- **427291b** — metal : add remaining fa-vec tunings for M3 (#28396)｜Isaac

補齊 `ggml-metal-tuning.cpp` 中 Apple M3 / M3 Max 晶片在 flash-attention-vec kernel 缺漏的調校表項目（含 q4_0、q4_1、q5_0、q5_1 等量化格式），讓這些晶片能套用已調校好的執行參數，而非回退到預設值。純調校資料補充，無邏輯變更。

### 2. SYCL 後端：kernel 融合與程式碼整理

- **f9f09f0** — SYCL: Refactor GGML_SYCL_ENABLE_MKL_FA to global var (#26863)｜Ozymandias_EBON
- **6703d78** — sycl: fuse rms_norm+mul+add and add+add residual chains (#27610)｜Frosty40

前者將 `GGML_SYCL_ENABLE_MKL_FA` 由巨集改為全域變數，屬程式碼整理。後者在 `GGML_SYCL_ENABLE_FUSION` 啟用時，新增 RMS_NORM+MUL+ADD 與 ADD+ADD 殘差鏈的融合運算：ADD+ADD 沿用既有 add() 的 binbcast 索引與型別支援矩陣（f32/f16/f16-f32混合/i32/i16/bf16，含 broadcast 與非連續記憶體），不支援的組合則退回兩次獨立 add() 呼叫，並補上對應的 test-backend-ops 測試。屬效能優化。

### 3. OpenCL 後端：Adreno GPU 功能擴充

- **4acf4a4** — opencl: add Adreno xmem SDPA path (#26331)｜yzyyzyhhh
- **1548a24** — opencl: extend the elementwise and data-movement op coverage (#27633)｜Hongqiang Wang

前者為 Adreno GPU 新增 xmem SDPA（scaled dot-product attention）路徑，可透過環境變數 `GGML_OPENCL_XMEM_SDPA` 啟用。後者一次補齊三項能力：(a) 新增 9 種原本會 fallback 到 CPU 的逐元素單元運算（sgn/step/elu/hardswish/hardsigmoid/floor/ceil/round/trunc）；(b) 針對兩側皆連續的張量新增以 float4 為單位、鋪滿整個裝置的 CPY 快速路徑（可用 `GGML_OPENCL_CPY_FLAT=0` 關閉退回舊 kernel），主要解決 mamba2/gated-delta-net 遞迴狀態快取單一長列複製效率低的問題；(c) 將 CONCAT 從僅支援 F32 擴充到所有「易複製」型別（f16/bf16/i8/i16/i32/i64）。皆已在 Adreno 840/850/740 及 A8X/X2-90/X2E 上以 test-backend-ops 驗證。屬功能覆蓋率與效能優化，本次變更量最大的單一項目（1548a24 一筆即新增/修改超過 400 行）。

### 4. 模型支援與修正

- **49c0dc8** — model : add Tencent Hy 4 (hy_v4) preview architecture support (#28127)｜HongHuang
- **163a407** — model, mtmd: fix gemma4 vision handling (#28335)｜Xuan-Son Nguyen
- **9a4843c** — src : add n_expert_used_max function (#28323)｜Daniel Bevenius

新增騰訊 Hy 4（hy_v4，HF 架構 `HYV4ForCausalLM`）預覽版架構支援，包含 HF→GGUF 轉換腳本、GGUF 常數/架構註冊、模型圖與 hparams/vocab 實作及測試（該模型 `n_ff_exp` 為逐層設定，屬新增量最大的單一模型支援項目，逾 1100 行）。`163a407` 修正 gemma4 視覺（vision）處理問題，涉及 KV cache 與 hparams 調整。`9a4843c` 新增 `n_expert_used_max` 函式，解決每層可有不同專家數（如上次報告提到的 NemotronHPuzzle 架構）時，模型載入階段既有 assert／錯誤檢查未正確處理此情況的問題。

**分組小結**：本次新增一個全新模型架構（Tencent Hy 4 預覽版），並修正一個既有多模態模型（gemma4）的視覺處理缺陷，延續上次報告中「MoE 逐層專家數」相關的後續修正。

### 5. 版本管理 / 建置系統

- **86b351f** — ggml : replace compile definitions with version.h.in (#28364)｜Daniel Bevenius
- **5266f24** — llama.cpp : bump version to 0.4.0 (#28386)｜Georgi Gerganov
- **64a155d** — sync : ggml (#28379)｜Georgi Gerganov（含 ggml 版本升至 0.23.0）
- **24f5bf8** — ggml : remove GGML_CUDA_PEER_MAX_BATCH_SIZE (#28177)｜Adrien Gallouët
- **c390d0a** — common : make build info output stream configurable (#28322)｜Adrien Gallouët

`86b351f` 將版本號改用 `version.h.in` 產生的檔案而非編譯期定義（`compile definition`），避免版本號變動時觸發整個 target 全部原始碼重新編譯——延續並修正上次報告 `d230ddd` (#28278) 同一問題的後續調整。`5266f24` 將 llama.cpp 版本號提升至 0.4.0；`64a155d` 為例行性 ggml 上游同步。`24f5bf8` 移除已不再使用的 CUDA peer 傳輸批次大小上限設定。`c390d0a` 讓 `llama_print_build_info` 可指定輸出串流（預設仍為 stderr），使 `llama-app` 的 version 指令輸出改到 stdout。

### 6. 穩定性 / Bug 修正

- **4cbe8b0** — ggml : don't crash when backend search path can't be read (#28271)｜Adrien Gallouët
- **832fd6f** — ggml-cpu(s390x) : fix q5_1 uninitialized v_acc (#28332)｜Aaron Teo

`4cbe8b0` 改用 `std::error_code` 版本的 `fs::current_path()`／`fs::directory_iterator`，避免後端搜尋路徑不可存取（例如 WebDAV 掛載點、CWD 已被刪除）時，因未捕捉的 filesystem_error 例外導致程式終止。`832fd6f` 修正 s390x 架構下 q5_1 量化反量化時 `v_acc` 累加器未初始化的問題。

### 7. Web UI

- **1863ac0** — ui: export conversations from database instead of cached store (#27432)｜Tom Tan
- **85d5703** — ui : fix MCP image attachments not displayed in tool block (#25789) (#28089)｜nachobh

前者將對話匯出功能改為直接讀資料庫而非快取的 store，避免匯出內容與資料庫不一致，並補上單元測試。後者修正 MCP 工具回傳的圖片附件在工具區塊中不顯示的迴歸問題（源自先前 PR #25450），同時修正誤將 `[Attachment saved: ...]` 字串誤判為 JSON 的正規表示式。

### 8. CI / 建置流程

- **8b4b355** — ci : move more jobs to ccache-buckets (#28375)｜Sigbjørn Skjæret
- **0ef4d56** — ci : disable failing openvino tests (#28347)｜Ravi Panchumarthy

前者將更多 CI job（Apple/CPU/CUDA/SYCL/Vulkan/WASM/WebGPU/HIP/server 等）遷移到 ccache-buckets 快取機制，加快建置速度。後者暫時停用目前失敗中的 OpenVINO 測試。

### 9. 其他雜項

- **38521ec** — vendor: update BoringSSL to 0.20260903.0 (#28354)｜Alessandro de Oliveira Faria — 更新內嵌的 BoringSSL 版本。
- **a529af9** — docs : update maintainer PRs link and regenerate AUTHORS (#28365)｜Georgi Gerganov — 更新維護者連結並重新產生 AUTHORS 名單。
- **d509cb1** — Don't use npx inside a package.json script (#28270)｜Evan Huus — 修正 `tools/ui/package.json` 中不當使用 npx 的腳本。

---

## 完整 Commit 清單

| Commit | 作者 | 說明 |
|---|---|---|
| f9f09f0 | Ozymandias_EBON | SYCL: Refactor GGML_SYCL_ENABLE_MKL_FA to global var (#26863) |
| 6703d78 | Frosty40 | sycl: fuse rms_norm+mul+add and add+add residual chains (#27610) |
| 9a4843c | Daniel Bevenius | src : add n_expert_used_max function (#28323) |
| 832fd6f | Aaron Teo | ggml-cpu(s390x) : fix q5_1 uninitialized v_acc (#28332) |
| c390d0a | Adrien Gallouët | common : make build info output stream configurable (#28322) |
| 0ef4d56 | Ravi Panchumarthy | ci : disable failing openvino tests (#28347) |
| 38521ec | Alessandro de Oliveira Faria | vendor: update BoringSSL to 0.20260903.0 (#28354) |
| a529af9 | Georgi Gerganov | docs : update maintainer PRs link and regenerate AUTHORS (#28365) |
| 24f5bf8 | Adrien Gallouët | ggml : remove GGML_CUDA_PEER_MAX_BATCH_SIZE (#28177) |
| 4cbe8b0 | Adrien Gallouët | ggml : don't crash when backend search path can't be read (#28271) |
| d509cb1 | Evan Huus | Don't use npx inside a package.json script (#28270) |
| 86b351f | Daniel Bevenius | ggml : replace compile definitions with version.h.in (#28364) |
| 8f83678 | Niklas Wenzel | metal : add remaining fa-vec tunings for M3 Max (#28373) |
| 163a407 | Xuan-Son Nguyen | model, mtmd: fix gemma4 vision handling (#28335) |
| 64a155d | Georgi Gerganov | sync : ggml (#28379) |
| 5266f24 | Georgi Gerganov | llama.cpp : bump version to 0.4.0 (#28386) |
| 49c0dc8 | HongHuang | model : add Tencent Hy 4 (hy_v4) preview architecture support (#28127) |
| 1863ac0 | Tom Tan | ui: export conversations from database instead of cached store (#27432) |
| 8b4b355 | Sigbjørn Skjæret | ci : move more jobs to ccache-buckets (#28375) |
| 4acf4a4 | yzyyzyhhh | opencl: add Adreno xmem SDPA path (#26331) |
| 1548a24 | Hongqiang Wang | opencl: extend the elementwise and data-movement op coverage (#27633) |
| 85d5703 | nachobh | ui : fix MCP image attachments not displayed in tool block (#25789) (#28089) |
| 427291b | Isaac | metal : add remaining fa-vec tunings for M3 (#28396) |

---

## 本次觀察重點

- 版本號 0.4.0 正式發布（`5266f24`），且版本產生機制（`86b351f`）延續了上次報告中修正「每次 commit 全量重建」問題（#28278）的方向，進一步把版本號從編譯定義改為標頭檔產生。
- Metal 與 OpenCL 兩個後端本次各有兩筆調校/功能擴充，分別針對 Apple M3 系列晶片的 flash-attention 調校表，以及 Adreno GPU 的逐元素運算覆蓋率與 SDPA 路徑，顯示行動裝置與消費級 GPU 後端持續在補齊功能與效能。
- `9a4843c` 是上次報告新增的 NemotronHPuzzle（逐層專家數）模型架構的後續配套修正，`163a407` 則修正既有 gemma4 多模態模型的視覺處理問題，模型相容性維護仍是持續性工作項目。

---

*本報告由自動化排程 session 產生。*
