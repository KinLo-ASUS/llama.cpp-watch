# llama.cpp 更新報告 — 2026-09-18

- 檢查範圍：`4bc272f`（上次檢查點）→ `972d231`（本次 HEAD）
- 新增 commit 數：18
- 分支：`ggml-org/llama.cpp` `master`

## 摘要

本次共有 18 筆新 commit，主要集中在 **Vulkan / SYCL 後端優化與修正**，以及少量 GGUF 載入、模型架構、UI 與 CI 基礎設施更新。其中 `81aeaeb`（GGUF 資料段對齊修正）屬於較重要的正確性修正，值得留意。

---

## 分組摘要

### 1. Vulkan 後端

| commit | 說明 |
|---|---|
| [`35822af`](https://github.com/ggml-org/llama.cpp/commit/35822afe58475e0506cd51e6573903e46d4c67c9) | vulkan: support qwen4exp hc ops (#28988) |
| [`05f2dcf`](https://github.com/ggml-org/llama.cpp/commit/05f2dcfdba3879c55f735efa0f124b1a56f7ed11) | vulkan: fix buffer_reference alignment in im2col shaders (#28996) |
| [`7490357`](https://github.com/ggml-org/llama.cpp/commit/7490357f22fa84fc3fd91d53fb9fc5bab0b6f9d9) | vulkan: skip unneeded MoE work in mul_mm coopmat1 path (#25483) |
| [`f172be7`](https://github.com/ggml-org/llama.cpp/commit/f172be756ae6e12a6aa294ecaeb2a387649ac6f8) | vulkan: split buffers and debug code into separate files, add shared headers (#28732) |

**群組摘要：** 本組為 Vulkan 後端的一連串優化與修正。`35822af` 新增對 qwen4exp 模型的 hc（`dsv4_hc_pre`/`dsv4_hc_post`）運算子支援；`05f2dcf` 修正 `im2col`/`im2col_3d` shader 中 `buffer_reference` 的對齊設定，原本因對齊宣告不足導致在 GPU-AV（GPU assisted validation）下觸發 20 筆驗證錯誤，修正後降為 0（對應 issue #28960）；`7490357` 讓 `mul_mm` 的 coopmat1 路徑在 MoE（Mixture-of-Experts）情境下跳過不必要的運算以提升效能；`f172be7` 是一次大型重構，將原本集中於 `ggml-vulkan.cpp`（約 5871 行變動）的 buffer 管理與 debug 相關程式碼拆分至獨立檔案（`ggml-vulkan-buffers.cpp`、`ggml-vulkan-debug.cpp` 等），並新增共用標頭檔，屬於程式碼組織性重構，不影響功能。

### 2. SYCL 後端

| commit | 說明 |
|---|---|
| [`aa39d7a`](https://github.com/ggml-org/llama.cpp/commit/aa39d7a3e145a88202793a89462d65e94a5fc25f) | [SYCL] Fix function signature for `ggml_backend_sycl_split_buffer_type` (#28981) |
| [`817e5f8`](https://github.com/ggml-org/llama.cpp/commit/817e5f83eb68a2cf5111ef194ae5b37579363ea7) | sycl: ssm_conv: fuse the SiLU epilogue into the ssm_conv kernel (#28929) |
| [`c9a5eee`](https://github.com/ggml-org/llama.cpp/commit/c9a5eeeb34ab8f794ea7510ca52d25da13728a5b) | sycl : fix the B70 mem allocate error when >19.3GB (#28953) |

**群組摘要：** SYCL 後端修正與優化。`aa39d7a` 修正 `ggml_backend_sycl_split_buffer_type` 函式簽名不一致的問題；`817e5f8` 將 SiLU 激活函數的 epilogue 融合進 `ssm_conv`（狀態空間模型卷積）kernel 中以減少額外的 kernel 呼叫，提升效能；`c9a5eee` 修正 Intel B70 顯示卡在配置記憶體超過 19.3GB 時發生的配置錯誤。

### 3. OpenCL 後端

| commit | 說明 |
|---|---|
| [`c57da6f`](https://github.com/ggml-org/llama.cpp/commit/c57da6fd816c5890d3a25abc9120a5a7fc5ff546) | opencl: fix various warnings (#28984) |

**摘要：** 修正 OpenCL 後端程式碼中多個編譯警告，包含 Adreno 與非 Adreno 平台的警告。

### 4. OpenVINO 後端

| commit | 說明 |
|---|---|
| [`ebbb185`](https://github.com/ggml-org/llama.cpp/commit/ebbb185227c31f1652f1445e2623563d2f67fe5a) | openvino : Update OpenVINO to 2026.4; fix clangd, MSVC warnings (#29009) |

**摘要：** 將 OpenVINO 相關依賴升級至 2026.4 版本，同時修正 clangd 與 MSVC 的編譯警告、int-to-pointer 轉型問題，並加強內部連結性（internal linkage）與消除重複的 switch case。

### 5. GGUF / 模型載入（重要正確性修正）

| commit | 說明 |
|---|---|
| [`81aeaeb`](https://github.com/ggml-org/llama.cpp/commit/81aeaeb74b205121d14475b0353e3749884a04b9) | gguf : align the data section relative to the GGUF start, not the file (#28993) |

**摘要：** 修正 `gguf_init_from_file_ptr` 在讀取「嵌入於檔案中某個非零偏移位置」的 GGUF 資料時，錯誤地以檔案位移 0 而非 GGUF 起始位置計算資料段的對齊填充，導致當嵌入偏移不是對齊倍數時，載入不會報錯卻回傳錯誤的 tensor 資料（屬於靜默資料錯誤，風險較高）。此修正同時新增 `llama_adapter_lora_init_from_file_ptr`，並在偵測到嵌入資料段未對齊時停用 mmap 並顯示警告，而非直接在 ggml 內部觸發 assert。

### 6. 模型架構支援

| commit | 說明 |
|---|---|
| [`7076180`](https://github.com/ggml-org/llama.cpp/commit/7076180486dcac0e965e6a77d711b3ad8997acdd) | model : extend Nemotron MTP support (#29018) |
| [`b49650a`](https://github.com/ggml-org/llama.cpp/commit/b49650adb31f2e49a0d76113aeb1792134fd8413) | model : skip gate_up_exps if TENSOR_SKIP is set (#29014) |

**群組摘要：** 兩者皆與 MTP（Multi-Token Prediction）/ MoE tensor 載入相關。`7076180` 擴充 Nemotron 系列模型的 MTP 支援；`b49650a` 讓模型在設定 `TENSOR_SKIP` 時可跳過 `gate_up_exps` 張量的載入，這是 qwen35moe 在 MTP tensor 已融合但未實際載入情境下所需的修正。

### 7. Chat / 解析器

| commit | 說明 |
|---|---|
| [`7f6f0c2`](https://github.com/ggml-org/llama.cpp/commit/7f6f0c2a9dab36fdb1f6e00e2037c030974e0e5c) | chat : add message delimiters to the DeepSeek V3.2/V4 parser (#29008) |

**摘要：** 為 DeepSeek V3.2/V4 的 chat 解析器新增訊息分隔符號支援。

### 8. RPC

| commit | 說明 |
|---|---|
| [`c77ae69`](https://github.com/ggml-org/llama.cpp/commit/c77ae695c93a6092cd7edb36909de5e43710af25) | rpc : skip ACCEL devices (#29020) |

**摘要：** 讓 RPC server 在列舉裝置時跳過 ACCEL 類型裝置。

### 9. WebUI

| commit | 說明 |
|---|---|
| [`4ff829e`](https://github.com/ggml-org/llama.cpp/commit/4ff829ec2e2f526aa6afba529eebbfb3ef1f95ec) | ui: fix removed reasoning menu in single model mode on desktop (#27985) |

**摘要：** 修正桌面版單模型模式下，先前被意外移除的「reasoning（推理）選單」，並調整該選單僅在非路由模式下才掛載於 Add 選單中，避免與 router 模式下模型選擇器的推理子選單重複。

### 10. CI / 文件 / 基礎設施

| commit | 說明 |
|---|---|
| [`79bfc1d`](https://github.com/ggml-org/llama.cpp/commit/79bfc1d43a2e1e790f455522e0b4edbef7e9d22c) | docs: remove JG as CODEOWNER for test-llama-archs (#29003) |
| [`87f9c82`](https://github.com/ggml-org/llama.cpp/commit/87f9c82f2d19b18ec6d1a08ab0f7727dc4ae4928) | ci : add API/ABI check to make-release workflow (#28947) |
| [`972d231`](https://github.com/ggml-org/llama.cpp/commit/972d2313bc0bf0a45f634f77d95c9fb03aeab12c) | ci : add missing evict-old-files (#29041) |

**群組摘要：** CI 與專案治理相關的小幅調整。`79bfc1d` 從 CODEOWNERS 移除 Johannes Gäßler 對 `test-llama-archs` 的擁有者身份；`87f9c82` 為 release workflow 新增 API/ABI 相容性檢查，可在發布前偵測是否有破壞相容性的變更（預設比對最新 release，可設定跳過或指定其他版本比對）；`972d231` 在 release workflow 中補上缺漏的 `evict-old-files` 步驟。

---

## 備註

- 本報告涵蓋期間內未見安全性（security）相關修正。
- `81aeaeb`（GGUF 資料段對齊）為本次唯一涉及資料正確性的修正，建議留意後續是否有回歸報告。
- Vulkan 後端的 `f172be7` 為大型重構（新增/搬移約 5600 行程式碼），若後續 Vulkan 相關 issue 增加，可留意是否與此次重構相關。
