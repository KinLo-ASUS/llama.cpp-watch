# llama.cpp 變更報告 (2026-08-09)

- 追蹤倉庫：[ggml-org/llama.cpp](https://github.com/ggml-org/llama.cpp)（`master` 分支）
- 檢查區間：`3653e6d6`（2026-08-08）→ `7ba604f1c`（2026-08-09）
- 本次新增 commit 數：**5**

---

## 分組摘要

### 一、Server 工具隔離（Tool Isolation）功能（3 筆）

本次最主要的一組變更，為 server 的內建工具（builtin tools）新增以 Docker 為基礎的隔離執行機制，並修正相關的工作目錄回報邏輯，三筆 commit 為連續開發的系列。

- `dd2c7c447` — **server: add initial tool isolation support (via docker)** ([#26507](https://github.com/ggml-org/llama.cpp/pull/26507))
  為 server 新增初步的工具隔離（isolate）支援，可透過 Docker 容器執行內建工具，並區分 `tools_io_sandbox` / `tools_io_docker` 設定與 `x-tool-runtime` 標頭；同時補上文件與新增測試 `test_tools_builtin.py`。
- `18f7ad7fc` — **server, ui: only offer a working directory when a tool reads it** ([#26762](https://github.com/ggml-org/llama.cpp/pull/26762))
  修正先前只要 server 提供任何內建工具，WebUI 就會顯示「工作目錄」控制項的問題（即使該工具根本不會讀取工作目錄，例如僅有 `get_datetime`，或使用者已關閉所有檔案系統工具）。現在工具需明確宣告是否會解析路徑並在工作目錄下執行，WebUI 才會顯示對應的 chip 與啟用 `/cwd` 指令。
- `7ba604f1c` — **server: report the isolate working directory from get_info** ([#26773](https://github.com/ggml-org/llama.cpp/pull/26773))
  修正 `get_info` 在未明確指定 cwd 時，即使已設定工具隔離（isolate）執行環境，仍回報 server 行程本身的工作目錄（一個隔離環境實際上不會用到的主機路徑）。現在若工具在隔離環境中執行，會改為詢問該隔離環境自身的工作目錄；僅當工具實際跑在主機上時才維持回報行程工作目錄。

### 二、CUDA 後端效能與正確性修正（2 筆）

- `69bf64379` — **CUDA: fix thread/block count in quantized cpy kernel launches** ([#26731](https://github.com/ggml-org/llama.cpp/pull/26731))
  修正量化資料複製（quantized cpy）CUDA kernel 啟動時的 thread／block 數量計算錯誤，並新增區塊數不整除的測試案例（uneven block count）。
- `687e77892` — **CUDA: fuse rms_norm + mul + rope (+ view + set_rows)** ([#26767](https://github.com/ggml-org/llama.cpp/pull/26767))
  將 `rms_norm`、`mul`、`rope`（及後續的 `view`、`set_rows`）等多個算子融合（fuse）為單一 CUDA kernel，減少中間結果的記憶體讀寫以提升效能；並在融合前加入記憶體範圍檢查，同時新增對應的 broadcast 測試案例。

---

## Commit 逐條列表（依時間先後）

| Commit | 說明 | PR |
|---|---|---|
| `69bf64379` | CUDA: fix thread/block count in quantized cpy kernel launches | [#26731](https://github.com/ggml-org/llama.cpp/pull/26731) |
| `dd2c7c447` | server: add initial tool isolation support (via docker) | [#26507](https://github.com/ggml-org/llama.cpp/pull/26507) |
| `18f7ad7fc` | server, ui: only offer a working directory when a tool reads it | [#26762](https://github.com/ggml-org/llama.cpp/pull/26762) |
| `687e77892` | CUDA: fuse rms_norm + mul + rope (+ view + set_rows) | [#26767](https://github.com/ggml-org/llama.cpp/pull/26767) |
| `7ba604f1c` | server: report the isolate working directory from get_info | [#26773](https://github.com/ggml-org/llama.cpp/pull/26773) |

---

*本報告由自動化監控流程產生。*
