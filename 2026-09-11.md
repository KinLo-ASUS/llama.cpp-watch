# llama.cpp 每日變更報告 - 2026-09-11

- 檢查範圍：`434ddbb`（2026-09-10 檢查點）→ `d7e8643`（本次最新）
- 新增 commit 數：18
- 本次追蹤的所有 commit 提交日期皆為 2026-09-10（上游當日活動量較大，尚未在前一次檢查時涵蓋）

## Group 1：Vulkan 後端效能與正確性優化（5 commits）

Vulkan 後端本次集中修了好幾個效能與相容性問題，涵蓋 profiler 除錯支援、小尺寸矩陣運算優化、PowerVR 相容性、以及 op fusion 排程。

- **`72797e8`** vulkan : add command-buffer debug labels for GPU profilers (#28101)
  為 Vulkan command buffer 加入除錯標籤，方便使用 GPU profiler（如 Nsight/RGP）分析時能辨識每個 command 對應的操作。
- **`3ff67eb`** vulkan: fall back to shared-memory reduction for dmmv on PowerVR (#28341)
  修正 PowerVR（Imagination）GPU 上的嚴重相容性問題：其專有 Vulkan compiler 對 subgroup-only reduction shader 會回傳 `VK_ERROR_UNKNOWN`，導致所有 k-quants/i-quants/TQ2_0/MXFP4/NVFP4 模型在產生第一個 token 時直接當掉。改為 shared-memory reduction 後可正常運作（在 q2_K~q6_K 上驗證通過），代價是效能較差（實測約為 hybrid 模式的 73%）。
- **`6788edb`** vulkan: small M matrix optimizations for qwen (#28457)
  針對小 M（batch 很小，例如逐字生成）矩陣乘法優化：交換 A/B 矩陣以優化 m=1 情境，並允許 small M 使用 split_k，coopmat2 的 tile 大小選擇改為依 M 而非只依 N 決定。
- **`50182a5`** vulkan: use add_alloc_dep to enable topk_moe fusion for prefill (#28422)
  透過 `add_alloc_dep` 讓 topk_moe（MoE 模型的 top-k 路由）融合（fusion）在 prefill 階段也能啟用，減少 kernel 呼叫次數。
- **`28ff095`** vulkan: use CPU writes in ggml_backend_vk_cpy_tensor_async if the context is idle (#28618)
  當 context 閒置時，`ggml_backend_vk_cpy_tensor_async` 改用 CPU 直接寫入，減少不必要的 GPU 排隊延遲。

## Group 2：ggml-cpu(s390x) 後端功能擴充（2 commits）

IBM s390x（大型主機架構）CPU 後端持續補齊量化格式支援。

- **`f1b6fbf`** ggml-cpu(s390x): add Q1_0 vector intrinsic support (#28606)
  新增 `ggml_vec_dot_q1_0_q8_0` 的 s390x SIMD intrinsic 實作，並更新文件標示 Q1_0 支援狀態。
- **`4ea6d1b`** ggml-cpu(s390x): add repack support for q4_0 (#28667)
  為 s390x 新增 Q4_0 的 repack（權重重排以加速運算）支援。

## Group 3：模型定義與轉換修正（3 commits）

- **`d7e8643`** model: fix all granite family parameter counts (#28643)
  修正 IBM Granite 系列模型的參數量計算錯誤（含 active experts 需加 `A` 前綴的顯示問題），並移除已不會用到的 40-block 特例判斷。
- **`8c322d5`** convert : expand Nemotron H conversion fix (#28689)
  擴充 Nemotron H 模型的轉換修正：改為 override 方式取得 `n_h_l`，並簡化巢狀屬性擷取與 `has_moe_params` 判斷邏輯（含 CISC 的 review 建議）。
- **`d344123`** models: clean up some dead switch branches in old models (#28669)
  清理舊模型程式碼中因先前重構複製貼上、但已不可能觸發的 if/switch 分支；目前唯一仍共用的邏輯只剩 `llama_model_bert::graph::graph`。

## Group 4：測試調整（2 commits）

- **`e5a8d43`** tests : drop SYCL special-casing in test-backend-ops.cpp (#28688)
  移除 `test-backend-ops.cpp` 中針對 SYCL 後端的特殊處理邏輯，簡化測試程式碼（刪除 124 行、新增 15 行）。
- **`c32d1da`** tests : increase tolerance for Add fusion tests (#28691)
  提高 Add fusion 測試的容許誤差範圍，避免因數值精度差異造成假性失敗。

## Group 5：CI 與建置腳本維護（3 commits）

- **`41fc758`** scripts : use sed instead of grep for version parsing [no ci] (#28700)
  修正 `make-release-checks.sh` 在 macOS 上執行失敗的問題（macOS 的 `grep` 不支援 `-P` 選項），改用 `sed` 解析版本號。
- **`18c17b4`** ci : Update WoA CUDA 13.4 release to use 13.4.1 GA redistributables (#28687)
  將 Windows ARM64（WoA）CUDA 13.4 建置從 Developer Preview 版改為使用 13.4.1 GA（正式版）redistributables。
- **`52d4268`** ci : add self-hosted-gpu-cuda and server-sanitize to hf-jobs (#28693)
  在 hf-jobs 中新增 `self-hosted-gpu-cuda` 與 `server-sanitize` 兩項 CI job。

## Group 6：其他後端與功能修正（3 commits，各自獨立）

- **`311d421`** memory : avoid allocating V cache for indexer (it's not used) (#28330)
  避免替 indexer（不使用 V cache 的元件）配置 V cache，減少不必要的記憶體佔用。
- **`fa67698`** spec: fix failed to decode mtmd chunk with DFlash (#28587)
  修正搭配 DFlash 投機解碼（speculative decoding）與視覺模型（vision model）一起使用時，因圖片使用固定 offset 導致 drafter 無法配置新 token、進而解碼失敗的問題；同時將 M-RoPE 跳過邏輯限制在圖片，讓音訊資料可正常通過。
- **`df03399`** opencl: add A8 Q4_0 mm binary kernel support (#28268)
  為 OpenCL 後端新增 A8 Q4_0 矩陣乘法 binary kernel 支援（新增 402 行程式碼，為本次變更中最大宗）。

## 小結

本次檢查涵蓋 18 個新 commit，主要集中在 **Vulkan 後端效能/相容性優化**（5 個，含一項修正嚴重當機問題的 PowerVR 相容性修補）與 **CI/測試/腳本維護**（5 個）。另有 s390x CPU 後端量化格式支援擴充、模型定義修正（Granite 參數量、Nemotron H 轉換）、以及 OpenCL 新 kernel 支援等功能性變更。無重大破壞性變更（breaking change），`3ff67eb`（PowerVR dmmv fallback）修正的是會直接讓程式當機的問題，建議關注。
