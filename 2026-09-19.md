# llama.cpp 每日追蹤報告 — 2026-09-19

- **追蹤範圍**：`ggml-org/llama.cpp` master 分支
- **檢查區間**：`972d231`（2026-09-18 上次檢查點）→ `5c53396`（本次最新）
- **新增 commit 數**：18 筆

---

## 分組摘要

### 1. Vulkan 後端效能優化

| Commit | 說明 |
|---|---|
| [`5c53396`](https://github.com/ggml-org/llama.cpp/commit/5c53396b89b05666c9d57445b7616a35f6198a16) vulkan: raise the hoisted row-id limit for mul_mat_id from 256 to 512 experts (#28501) | `count_experts.comp` 原先以 `BLOCK_SIZE`（256）決定 shared array 大小，超過 256 個 expert 的模型（如 Qwen3.8-Flash-Next，512 experts）會被迫關閉 row-id hoisting，導致每個 workgroup 都要重新掃描整個 ids tensor。此 PR 改用獨立的 `MAX_EXPERTS` 常數，並在後續 review 中把上限一次拉到 1024（對齊 `LLAMA_MAX_EXPERTS`）。Strix Halo 實測 batch 2048 下 expert matmul 由 12.5ms 降至 9.5ms（iq3_s）、14.0ms 降至 7.5ms（iq4_nl），8k tokens 的 prompt processing 提升約 19%。 |
| [`bbd488c`](https://github.com/ggml-org/llama.cpp/commit/bbd488c42abbcf725e7c713bc184eb0ca4a73986) vulkan: add IQ3_S MMQ matmul kernels (#28822) | 新增 IQ3_S 量化格式的 Vulkan MMQ（matmul quantized）kernel，含 shared memory 對齊與 K tile size 修正。 |

**分組摘要**：兩項皆為 Vulkan 後端針對 MoE/量化矩陣乘法的效能與功能補強，讓更多專家數與量化格式能吃到高效路徑。

---

### 2. CI / Workflow 維護

| Commit | 說明 |
|---|---|
| [`bdcbaaf`](https://github.com/ggml-org/llama.cpp/commit/bdcbaaf6e7520b68c8c60ff724c67409970d70e1) ci: bump android-actions/setup-android to 4.0.4 (#29065) | 升級 Android CI action 版本。 |
| [`f03cf3e`](https://github.com/ggml-org/llama.cpp/commit/f03cf3e9b89a004b573080893e31c01931b5e6b3) ci: disable GHA cache for copilot (#29068) | 停用 GitHub Copilot 相關 workflow 的 Actions cache。 |
| [`d663dd3`](https://github.com/ggml-org/llama.cpp/commit/d663dd3f3af4e7aa3ccbcd7f756c6692eb104577) ci: change ubuntu-latest to ubuntu-24.04 (#29079) | 將多個 workflow 的 `ubuntu-latest` 明確改為 `ubuntu-24.04`，避免 runner 版本漂移造成的不穩定。 |

**分組摘要**：純 CI 基礎設施維護，不影響程式邏輯，目的是穩定與更新建置環境。

---

### 3. GGUF / 詞彙表（vocab）修正

| Commit | 說明 |
|---|---|
| [`bb11ebb`](https://github.com/ggml-org/llama.cpp/commit/bb11ebb6820ae3f20fc96002f8fd8173a5d8ceed) gguf-py: fix Q8_1 block size in GGML_QUANT_SIZES (2+2+32) (#29036) | 修正 Python 端 `GGML_QUANT_SIZES` 中 Q8_1 的 block size 定義錯誤。 |
| [`dc85f89`](https://github.com/ggml-org/llama.cpp/commit/dc85f89c7ee2150c408dea82aeebc799b99ad0cc) vocab: add ufakzeka pre-tokenizer (#29033) | 新增 `ufakzeka` pre-tokenizer 支援，並更新模型雜湊對照表。 |

**分組摘要**：GGUF 格式定義與新模型 tokenizer 相容性的小型修正。

---

### 4. 建置系統（CMake）修正

| Commit | 說明 |
|---|---|
| [`8ed1a55`](https://github.com/ggml-org/llama.cpp/commit/8ed1a55efcd7424d2c592f6cbc9f97756db1d74d) cmake: fix build when GGML_CPU=OFF and GGML_CUDA=ON (#29026) | 修正在關閉 CPU 後端、僅啟用 CUDA 時的建置失敗問題，同時修正一支範例 CMakeLists 的換行符。 |

---

### 5. ggml 核心穩定性強化

| Commit | 說明 |
|---|---|
| [`911f6cd`](https://github.com/ggml-org/llama.cpp/commit/911f6cdc8ab8a530b2bee09ee61471a6f3178eeb) ggml: handle graph buffer reservation failure (#26070) | 在 backend graph buffer 配置失敗時新增處理邏輯，避免程式直接崩潰。 |
| [`5b335f4`](https://github.com/ggml-org/llama.cpp/commit/5b335f413e4f73b0809c4fe39af894efbcc6a0d2) ggml: check for allocation failures to prevent crashes (#28149) | 在 `tools/mtmd/clip.cpp` 增加記憶體配置失敗檢查，防止多模態 clip 模組因配置失敗而崩潰。 |

**分組摘要**：兩者都是針對記憶體/資源配置失敗情境補上防呆檢查，提升整體穩定性、避免 crash。

---

### 6. WebGPU 後端修正

| Commit | 說明 |
|---|---|
| [`44be98f`](https://github.com/ggml-org/llama.cpp/commit/44be98f057e9f9902a8ee12630e181c7f8ec2953) ggml-webgpu: fix supports_op condition for GET_ROWS (#28978) | 修正 GET_ROWS 運算子的 `supports_op` 判斷條件，補上 vec4 對齊時的 src stride 檢查，並新增對應測試案例。 |

---

### 7. Model-Saver / SWA 架構擴充

| Commit | 說明 |
|---|---|
| [`542348a`](https://github.com/ggml-org/llama.cpp/commit/542348a35c9ed5a3f3b554cac1191ee3d3df8173) Model-Saver: Write the SWA pattern, 15 more architectures roundtrip (#29042) | 新增 `llama_model_base::load_swa_pattern()`，可將 sliding_window_pattern 讀成單一週期值或每層陣列；同時讓 model-saver 能寫出 SWA pattern（含 nextn 層）及 MLA 的 KV 長度／LoRA rank，使 plamo3、gemma3、cohere2、cohere2moe、olmo2、exaone-moe、afmoe、mimo2、spark2_5、muse-glimmer、mellum、laguna、granite_swa、dots3note、maple 等 15 種架構通過 bit-exact roundtrip 測試。 |

---

### 8. CPU 後端新功能

| Commit | 說明 |
|---|---|
| [`4fea119`](https://github.com/ggml-org/llama.cpp/commit/4fea119de30f6a923992780f6fd5ccb0bee5d47d) ggml-cpu: add F16 input to the FWHT (#27779) | CPU 版 FWHT（Fast Walsh–Hadamard Transform）原僅支援 F32 輸入，此變更將來源型別改為 template 參數，新增 F16 輸入支援，並在 review 後補上型別 assert 與改用既有的批次轉換函式 `ggml_cpu_fp16_to_fp32`。 |

---

### 9. OpenCL 後端（Qualcomm Adreno）新 kernel

| Commit | 說明 |
|---|---|
| [`ec92815`](https://github.com/ggml-org/llama.cpp/commit/ec928150501c2572fec05cb949061672bb424914) opencl: add bin kernel `kernel_gemm_noshuffle_q6_k_f32_32b_trans_ila_a8_bin` (#28678) | 新增 A8 / Q6_K 非 MoE 二進位 kernel，並修正 layout 相容性。 |
| [`60081bb`](https://github.com/ggml-org/llama.cpp/commit/60081bb2b5b3294165a4d67c5cbeebe74c868014) opencl: add support for bin kernel `flash_attn_f32_f16_bin` (#29046) | 新增 flash-attention 的 F32/F16 二進位 kernel 支援，並加上 prefill 情境的守護條件。 |

**分組摘要**：皆為 OpenCL 後端新增預編譯二進位（bin）kernel，擴大 Qualcomm Adreno GPU 上的量化矩陣運算與 flash-attention 覆蓋範圍。

---

### 10. Hexagon 後端（Qualcomm NPU）功能擴充

| Commit | 說明 |
|---|---|
| [`18a04f0`](https://github.com/ggml-org/llama.cpp/commit/18a04f09c24616898792bcfaa17f3550bdc78912) hexagon: HMX flash-attention head_dim padding (support DK=DV=72) (#26539) | 讓 HMX flash-attention 支援非 64 倍數的 head_dim（如 SigLIP 的 72），做法是將 DK/DV 向上補齊到 64 並以零填充尾端。 |
| [`50631b3`](https://github.com/ggml-org/llama.cpp/commit/50631b3d2c569ad8e5c112090cd28570b1268ee0) hexagon: im2col update (#29103) | IM2COL 支援 1D 與 padded 輸入，pure-DDR kernel 補上 is_2D 判斷，並新增 blocked-staging 版本的通用 IM2COL DMA kernel。 |
| [`2b18470`](https://github.com/ggml-org/llama.cpp/commit/2b1847030cef76ef315eaee0b7ae0cdcd4fb15ff) hexagon: add ROLL op support (#29105) | 新增 ROLL 運算子的 Hexagon 後端支援。 |

**分組摘要**：三項皆為 Hexagon（Qualcomm NPU）後端的算子覆蓋範圍擴充，涵蓋 flash-attention 的非標準 head_dim、IM2COL 的多種輸入形式，以及新增 ROLL 運算子。

---

## 整體觀察

本次更新以**跨硬體後端（Vulkan、WebGPU、OpenCL、Hexagon）的效能優化與算子覆蓋擴充**為主，另有**穩定性強化**（配置失敗防呆）與**模型相容性**（SWA pattern roundtrip、新 pre-tokenizer）相關變更。無重大 breaking change，多數變更皆有對應的 test-backend-ops 測試案例佐證。

---

*本報告由自動化排程產生。上次檢查點：`972d231`（2026-09-18）；本次檢查點：`5c53396`（2026-09-18，llama.cpp 專案內部日期）。*
