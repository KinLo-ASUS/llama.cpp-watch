# llama.cpp 每日變更報告 — 2026-09-12

- **監控倉庫**：ggml-org/llama.cpp（master 分支）
- **本次檢查區間**：`d7e8643`（2026-09-11 檢查點）→ `82d6bb2`（本次最新）
- **新增 commit 數**：33

---

## 分組摘要

### 1. Vulkan 後端優化與修復（6 個 commit）
本輪 Vulkan 後端有較多針對正確性與效能的調整，涵蓋除錯工具、相容性修復與矩陣乘法效能優化。

- `72797e8` **vulkan : add command-buffer debug labels for GPU profilers (#28101)**
  為 Vulkan command buffer 加入除錯標籤，方便 GPU profiler（如 Nsight）辨識各個運算階段。
- `3ff67eb` **vulkan: fall back to shared-memory reduction for dmmv on PowerVR (#28341)**
  修復 PowerVR（Imagination）GPU 上因 subgroup-only reduction 導致 `vkCreateComputePipelines` 失敗、生成第一個 token 就當機的問題，改用 shared-memory reduction 作為 fallback。
- `481c65f` **vulkan: fix data race and OOB access in argsort(large) (#28705)**
  修復 `argsort` 內迴圈的資料競爭（data race）以及 `argsort_large` 的越界存取（OOB）問題，這些是 Vulkan validation layer（VVL）與 CI 間歇性失敗的潛在原因。
- `28ff095` **vulkan: use CPU writes in ggml_backend_vk_cpy_tensor_async if the context is idle (#28618)**
  當 context 閒置時，改用 CPU 直接寫入取代非同步 GPU 拷貝，減少不必要的排隊延遲。
- `50182a5` **vulkan: use add_alloc_dep to enable topk_moe fusion for prefill (#28422)**
  透過新增分配依賴（alloc dependency）機制，讓 prefill 階段也能啟用 topk_moe 融合優化。
- `6788edb` **vulkan: small M matrix optimizations for qwen (#28457)**
  針對小 M 維度矩陣乘法（如 Qwen 模型常見情境）優化：交換 A/B 矩陣以優化 m=1 情況、允許小 M 值使用 split_k、依 M 值調整 coopmat2 tile 選擇策略。

### 2. Metal 後端融合（Fusion）重構與效能修復（3 個 commit）
這是本輪份量最大的變更，將 Metal 後端的運算融合機制整併為單一資料表，並修復一個造成 5% token 生成效能回歸的隱藏 bug。

- `a2878d3` **metal : single-source fusion table + fusion debug rework (#28164)**
  將所有 Metal 可融合運算模式（fusion pattern）整合進單一資料表（`ggml-metal-fuse.cpp`），供圖優化器與運算編碼器共用；同時修復融合模式中輸出索引使用相對而非絕對索引的 bug（此 bug 曾靜默停用 norm/MUL 融合，造成約 5% 的 token 生成效能回歸）；並新增 `gated_delta_net` 與 cache copy 的融合、新增融合統計 API 與獨立的迴歸測試（`test-fusion`），委交至 CI 專屬 workflow 執行。
- `aac8102` **metal : fix idle threads in the remaining iq mul_mv kernels for ne00 < 1024 (#28692)**
  將先前已修復的 row-split 技巧（#28086）套用到其餘六種 iq 量化核心（iq1_s/iq1_m/iq2_xxs/iq2_xs/iq2_s/iq3_s），解決 row 長度小於 32 chunk 時 simdgroup 執行緒閒置的問題。
- `5bda51b` **metal : skip the empty half of the mul_mm_id token tile (#28301)**
  `kernel_mul_mm_id` 將 32-token tile 拆為兩個 16-row 半區，當某 MoE 專家未填滿 tile 上半部時直接跳過計算；同時強化 `test-backend-ops` 的邊界測試覆蓋。

### 3. CUDA/HIP Flash Attention 調校（1 個 commit）
- `16378d9` **CUDA/HIP: Flash Attention tuning (gfx1201) (#28102)**
  在 AMD RDNA4（gfx1201）上為 head size 256 啟用 mma Flash Attention，並調整 stream-k 與 whole-tile grid 的選擇邏輯以提升效能。

### 4. OpenCL 後端新增核心支援（1 個 commit）
- `df03399` **opencl: add A8 Q4_0 mm binary kernel support (#28268)**
  為 OpenCL 後端新增 A8 Q4_0 矩陣乘法二進位核心支援。

### 5. s390x CPU 後端向量化支援擴充（2 個 commit）
- `f1b6fbf` **ggml-cpu(s390x): add Q1_0 vector intrinsic support (#28606)**
  為 s390x 架構新增 `ggml_vec_dot_q1_0_q8_0` 向量化實作。
- `4ea6d1b` **ggml-cpu(s390x): add repack support for q4_0 (#28667)**
  為 s390x 架構新增 q4_0 量化格式的 repack 支援。

### 6. ggml 核心：MoE 專家卸載修正（1 個 commit）
- `43f3dda` **ggml: skip 0-sized ids tensor when offloading selected experts (#28739)**
  當選中的專家 ids tensor 大小為 0 時跳過卸載處理，避免無效運算。

### 7. 模型與轉換修正（4 個 commit）
- `5cdd3d1` **model : fix MTP context kv cache allocation for deepseek2, glm4moe, cohere2moe (#28630)**
  修復 DeepSeek2、GLM4MoE、Cohere2MoE 等架構在 MTP（Multi-Token Prediction）情境下 KV cache 配置錯誤的問題，並新增架構過濾的反向判斷邏輯。
- `8c322d5` **convert : expand Nemotron H conversion fix (#28689)**
  擴充 Nemotron H 模型轉換修復，簡化巢狀屬性提取邏輯。
- `311d421` **memory : avoid allocating V cache for indexer (it's not used) (#28330)**
  避免為未使用的 indexer 配置 V cache，減少不必要的記憶體佔用。
- `d344123` **models: clean up some dead switch branches in old models (#28669)**
  清理 BERT / Jina-BERT-v3 / Nomic-BERT(-MoE) 等舊模型程式碼中因過去重構遺留、實際上永遠不會觸發的 switch 分支。

### 8. Server 與推測解碼（Speculative Decoding）修復（3 個 commit）
- `82d6bb2` **server: refactor subproc handling (#28555)**
  重構 server 子行程（subprocess）處理邏輯，涉及 `server-models`/`server-common` 的下載與行程管理重構。
- `b0dcb81` **server: fix speculation after an image (#28715)**
  修復影像輸入後推測解碼（speculative decoding）位置計算錯誤：改傳遞實際 token 位置而非 token 數量給草稿模型（drafter），影響所有草稿模型類型，不僅限於 DFlash。
- `fa67698` **spec: fix failed to decode mtmd chunk with DFlash (#28587)**
  修復搭配視覺模型使用 DFlash 時，因影像固定位移導致草稿記憶體無法配置新 token 而解碼失敗的問題；限制 M-RoPE 跳過邏輯僅套用於影像，允許音訊正常通過。

### 9. 建置系統與 CI 改進（7 個 commit）
- `3bcfeb7` **cmake : add PCH and unity build to improve build times (#28091)**
  引入預編譯標頭（PCH）與 unity build 機制大幅縮短編譯時間（完整建置從約 1503 秒降至約 993 秒）。
- `8ea2902` **cmake : skip PCH for llama-server PCH when using MSVC (#28763)**
  修復上述 PCH 變更在 MSVC 下造成 llama-server 建置失敗的問題。
- `b78a39a` **ci : run test-backend-ops as a dedicated ci/run.sh test (#28740)**
  將 `test-backend-ops` 改為 `ci/run.sh` 中的獨立測試項目（脫離 ctest），並支援平行執行。
- `451b89b` **ci : key cache to sanitizer matrix (#28708)**
  修正 CI 快取鍵值與 sanitizer 矩陣的對應方式。
- `52d4268` **ci : add self-hosted-gpu-cuda and server-sanitize to hf-jobs (#28693)**
  新增 self-hosted GPU CUDA 與 server-sanitize 任務至 hf-jobs 工作流程。
- `18c17b4` **ci : Update WoA CUDA 13.4 release to use 13.4.1 GA redistributables (#28687)**
  將 Windows ARM64 CUDA 13.4 建置從開發預覽版改為正式（GA）發布套件。
- `41fc758` **scripts : use sed instead of grep for version parsing [no ci] (#28700)**
  修復 macOS 上 `make-release-checks.sh` 因 `grep -P` 不支援而失敗的問題，改用 `sed` 解析版本號。

### 10. 測試強化與穩定性（4 個 commit）
- `982937a` **tests: extend test-quantize-fns to test nrc=2 (i8mm) kernels (#16234)**
  擴充量化函式測試以覆蓋 nrc=2（i8mm）核心，並使用獨立且非平凡跨距（stride）的測試資料。
- `8172e65` **tests: tolerate a shared pool abort in test_completion_unified (#28759)**
  在高負載測試環境下，容忍因請求交錯進入共享池導致的 slot 中止情況，避免測試誤報失敗。
- `c32d1da` **tests : increase tolerance for Add fusion tests (#28691)**
  提高 Add 融合測試的容忍度（tolerance）。
- `e5a8d43` **tests : drop SYCL special-casing in test-backend-ops.cpp (#28688)**
  移除 `test-backend-ops.cpp` 中針對 SYCL 的特殊處理邏輯，精簡測試程式碼。

### 11. 其他（1 個 commit）
- `1dfe94e` **common : fix typo in speculative.cpp comment [no ci] (#28750)**
  修正 `speculative.cpp` 中的註解拼字錯誤。

---

## 整體觀察

本輪變更以 **Metal 融合機制重構**（含一個曾造成 5% 效能回歸的隱藏 bug 修復）與 **Vulkan 後端多項相容性/效能修復**為重點，另有建置系統導入 PCH + unity build 顯著縮短編譯時間。推測解碼（speculative decoding）在影像輸入情境下的兩個修復（`b0dcb81`、`fa67698`）值得關注，屬於功能正確性修復。整體以維護性、正確性修復與後端效能優化為主，無重大破壞性變更（breaking change）。

---

*本報告由自動化排程任務產生。*
