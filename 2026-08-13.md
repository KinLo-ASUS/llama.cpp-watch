# llama.cpp 變更報告 (2026-08-13)

- 追蹤倉庫：[ggml-org/llama.cpp](https://github.com/ggml-org/llama.cpp)（`master` 分支）
- 檢查區間：`0b1bad14f`（2026-08-11）→ `8e7f22b67`（2026-08-12）
- 本次新增 commit 數：**18**

---

## 分組摘要

### 一、CMake 建置系統：導入語義化版本控制（2 筆）

- `680a9ae63` — **cmake : introduce semantic versioning** ([#26839](https://github.com/ggml-org/llama.cpp/pull/26839))
  為 llama.cpp 導入語義化版本號（semver），新增 `make-release` CI 工作流程、`test-cmake` 範例專案與 `LLAMA_BUILD_IS_DEV` 選項；nightly build 僅回報 semver（不含 build number），release/package 維護者可依 tag 取得正確版本號，並補上 `docs/release.md` 說明文件。
- `13fd0bb55` — **cmake : add config version support** (ggml/1582)
  為 `find_package(ggml <version> REQUIRED)` 加入版本比對支援，並更新 `examples/test-cmake` 與建置腳本以驗證此功能；曾嘗試為各 ggml backend 模組檔名加入 semver 以支援多版本並存，但該做法已被還原（revert）。

### 二、CI 維護與修復（3 筆）

- `84e908c62` — **ci: fix thread sanitizer + remove ccache** ([#26927](https://github.com/ggml-org/llama.cpp/pull/26927))
  修正 thread sanitizer 測試（改用 native build 讓 CPU 版本可在多核心環境執行），並停用 ccache 以解決相關 CI 問題。
- `9558fa44c` — **ci : disable ubuntu-rocm** ([#26969](https://github.com/ggml-org/llama.cpp/pull/26969))
  暫時停用 ubuntu-rocm 的 CI 檢查項目。
- `7a9ff9597` — **disable rocm cache** ([#26962](https://github.com/ggml-org/llama.cpp/pull/26962))
  停用 ROCm 相關的建置快取（build-cache／build-cuda-windows／release 工作流程）。

### 三、模型載入與相容性穩健性修復（4 筆）

- `d8a8beac2` — **gguf : harden loader against malformed tensor dims and metadata types** ([#25596](https://github.com/ggml-org/llama.cpp/pull/25596))
  強化 GGUF 載入器對惡意／損壞檔案的防護：修正 `general.alignment` 型別錯誤與零維張量（zero-dim tensor）會導致 assert-abort 或 SIGFPE 崩潰的問題，並補上對應的 `test-gguf` 測試案例（164/164 測試通過）。
- `ece98b87f` — **model : disallow integer dflash sliding_window_pattern** ([#26900](https://github.com/ggml-org/llama.cpp/pull/26900))
  修正並禁止 DFlash 模型的 `sliding_window_pattern` 使用整數型別設定，避免解析錯誤。
- `55f453b92` — **wavtokenizer-dec : bound posnet/convnext block_count against n_layer_all** ([#26892](https://github.com/ggml-org/llama.cpp/pull/26892))
  為 WavTokenizer 解碼器的 PosNet／ConvNeXt `block_count` 加入邊界檢查，避免超出 `n_layer_all` 範圍。
- `6eff59326` — **convert : handle per_layer_config in Gemma4 (transformers 5.15)** ([#26882](https://github.com/ggml-org/llama.cpp/pull/26882))
  修正轉換腳本對 Gemma-4（E4B）新版 `per_layer_config` 設定格式的相容性問題，包含 `global_head_dim`、`head_dim`（僅由 full-attention 層讀取）與 `num_global_key_value_heads` 等欄位的回退（fallback）解析邏輯，以同時相容新舊兩種 config 版面配置。

### 四、後端硬體加速改進：KleidiAI／OpenCL／Vulkan（4 筆）

- `132753bf4` — **kleidiai: Add runtime feature detection mechanism for aarch64/kleidiai** ([#26076](https://github.com/ggml-org/llama.cpp/pull/26076))
  為 aarch64 KleidiAI 加入執行期（runtime）CPU 特性偵測機制，並新增 `ggml-feats.h`，將原本位於 cpu-feats 的相關程式碼移出以供更廣泛使用；對保留的 NSMC 數值加入警告記錄。
- `89e0aa6fd` — **opencl: default FA c8 cluster width to 16 on X1E** ([#26433](https://github.com/ggml-org/llama.cpp/pull/26433))
  針對 X1E 平台，將 OpenCL Flash Attention（c8）預設 cluster width 調整為 16。
- `cb27fe9c3` — **opencl: use flat mv q5_k when weight exceeds image1d_buffer_t limit** ([#26880](https://github.com/ggml-org/llama.cpp/pull/26880))
  當權重大小超過 OpenCL `image1d_buffer_t` 上限時，改用 flat matrix-vector 運算路徑處理 Q5_K 量化格式，避免超限錯誤。
- `a7cd2f0e9` — **vulkan: add TQ2_0 (ternary) support** ([#25850](https://github.com/ggml-org/llama.cpp/pull/25850))
  為 Vulkan 後端新增 TQ2_0（三元／ternary）量化格式支援，是 ggml 中首個支援三元格式的 Vulkan 實作，包含 dequant、專用 mul_mat_vec 與透過 dequant_funcs 實作的 matmul；於 OLMo-125m 模型測試中，TQ2_0 與 F16 的 top-12 logprobs 在完全 offload 情況下小數點後 4 位皆一致，速度與 F16 相近（125m 規模下受 overhead 限制），頻寬優勢預期在更大型 BitNet 模型上更明顯；MMQ/整數點積路徑尚未實作。

### 五、Server／UI 多媒體功能（2 筆）

- `5d9e5ac30` — **server : support slot save/restore with media inputs** ([#26640](https://github.com/ggml-org/llama.cpp/pull/26640))
  server 的 slot 狀態儲存／還原功能新增對多模態（圖片等媒體）輸入的支援：將序列化後的圖片區塊（image chunk）一併存入 llama state，並集中處理媒體驗證邏輯。
- `4dd127584` — **ui: add read_media tool (#25877)** ([#25877](https://github.com/ggml-org/llama.cpp/pull/25877))
  新增 `read_media` 內建工具（前身為 `read_image`），讓具視覺能力的模型可分析伺服器端的圖片與音訊檔案：圖片以 base64 編碼傳給 UI 解碼顯示；音訊格式限制為 wav／mp3（大小寫不區分），未知副檔名回傳錯誤而非灌入巨大 data URI 污染 context；並將此工具移至前端專用工具、繼承 `read_file` 邏輯。

### 六、其他（3 筆）

- `8e7f22b67` — **common: add system-level config file** ([#26118](https://github.com/ggml-org/llama.cpp/pull/26118))
  新增系統層級設定檔支援，確立設定優先順序：CLI 旗標 > 環境變數 > 系統／使用者 config（Linux/BSD/Mac 為 `/etc/llama.cpp/config.ini` 低於 `~/.config/llama.cpp/config.ini`；Windows 為 `%PROGRAMDATA%` 低於 `%APPDATA%`），並補上文件說明。
- `af05a42a7` — **sync : ggml**
  例行同步上游 ggml 專案版本紀錄（`scripts/sync-ggml.last`），無功能性變更。
- `a4a4c51f3` — **tests : update speculative params** ([#26925](https://github.com/ggml-org/llama.cpp/pull/26925))
  更新 server 測試中投機取樣（speculative decoding）相關參數設定。

---

## Commit 逐條列表（依時間先後）

| Commit | 說明 | PR |
|---|---|---|
| `cb27fe9c3` | opencl: use flat mv q5_k when weight exceeds image1d_buffer_t limit | [#26880](https://github.com/ggml-org/llama.cpp/pull/26880) |
| `6eff59326` | convert : handle per_layer_config in Gemma4 (transformers 5.15) | [#26882](https://github.com/ggml-org/llama.cpp/pull/26882) |
| `55f453b92` | wavtokenizer-dec : bound posnet/convnext block_count against n_layer_all | [#26892](https://github.com/ggml-org/llama.cpp/pull/26892) |
| `a7cd2f0e9` | vulkan: add TQ2_0 (ternary) support | [#25850](https://github.com/ggml-org/llama.cpp/pull/25850) |
| `a4a4c51f3` | tests : update speculative params | [#26925](https://github.com/ggml-org/llama.cpp/pull/26925) |
| `89e0aa6fd` | opencl: default FA c8 cluster width to 16 on X1E | [#26433](https://github.com/ggml-org/llama.cpp/pull/26433) |
| `4dd127584` | ui: add read_media tool | [#25877](https://github.com/ggml-org/llama.cpp/pull/25877) |
| `5d9e5ac30` | server : support slot save/restore with media inputs | [#26640](https://github.com/ggml-org/llama.cpp/pull/26640) |
| `13fd0bb55` | cmake : add config version support | ggml/1582 |
| `af05a42a7` | sync : ggml | — |
| `ece98b87f` | model : disallow integer dflash sliding_window_pattern | [#26900](https://github.com/ggml-org/llama.cpp/pull/26900) |
| `132753bf4` | kleidiai: Add runtime feature detection mechanism for aarch64/kleidiai | [#26076](https://github.com/ggml-org/llama.cpp/pull/26076) |
| `d8a8beac2` | gguf : harden loader against malformed tensor dims and metadata types | [#25596](https://github.com/ggml-org/llama.cpp/pull/25596) |
| `680a9ae63` | cmake : introduce semantic versioning | [#26839](https://github.com/ggml-org/llama.cpp/pull/26839) |
| `7a9ff9597` | disable rocm cache | [#26962](https://github.com/ggml-org/llama.cpp/pull/26962) |
| `9558fa44c` | ci : disable ubuntu-rocm | [#26969](https://github.com/ggml-org/llama.cpp/pull/26969) |
| `84e908c62` | ci: fix thread sanitizer + remove ccache | [#26927](https://github.com/ggml-org/llama.cpp/pull/26927) |
| `8e7f22b67` | common: add system-level config file | [#26118](https://github.com/ggml-org/llama.cpp/pull/26118) |

---

*本報告由自動化監控流程產生。*
