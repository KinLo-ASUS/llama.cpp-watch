# llama.cpp 更新報告 - 2026-09-23

- 檢查範圍：`58367713a6935c0810103378144008df32e3d5db` (5836771) → `e6ab7c1a41054a888ada952eab4c886444c2f5ad` (e6ab7c1)
- 新增 commit 數：23
- 上次檢查日期：2026-09-22

## 摘要總覽

本次新增的 23 個 commit 主要集中在：**server 穩定性與新功能**、**GPU/加速後端效能優化（OpenCL、Vulkan、Metal、HIP、Hexagon NPU）**、**模型轉換與推測解碼（Speculative Decoding）支援**、**WebUI 體驗改善**，以及少量 **CI/建置設定與 jinja 範本引擎修正**。

---

## Group 1：Server 功能與穩定性

| Commit | 標題 | 摘要 |
|---|---|---|
| `217f81c` | server: Add support for binding to multiple addresses (#28690) | `llama-server` 新增支援同時綁定多個 TCP 位址，可透過重複 `--host`/`--port`（或新的多位址參數）指定多個監聽端點；同時加入位址重疊檢查、拒絕 `--port 0` 搭配多位址的組合，避免不明確的埠號設定。 |
| `9919911` | server: fix router eviction races with the existing queue (#29217) | 修正 model router 的資源競爭問題：(1) 模型透過「快速路徑」載入時未進入佇列，導致其 LOADED 狀態轉換時被 `tick()` 提前驅逐；改為所有模型載入一律經過佇列，讓佇列項目保護該模型直到所有等待者離開。(2) 修正正在停止（stopping）的模型仍可能被誤判為可用而收到新請求並被導向即將銷毀的子行程的問題，改為在同一把鎖下清除 stopping 標記與設定 UNLOADED，避免請求同時看到「非 stopping 也非 unloaded」的中間狀態。 |
| `4098fdc` | server: support input_image in function_call_output (#20663) (#22575) | Chat Completions API 的 `function_call_output` 訊息現在支援 `input_image` 內容，讓工具呼叫回傳的結果可以夾帶圖片，並修正了程式碼風格與重複查找型別的問題。 |
| `bbf99b1` | server: do not pass log file to children (#29212) | 修正 router 模式下父行程的 log file 設定被意外傳遞給子行程模型實例的問題。 |

**說明**：這組更新聚焦於 `llama-server` 在多模型路由（router）情境下的正確性與部署彈性，尤其 `9919911` 修的是實際生產環境中會遇到的競爭條件（race condition），值得關注。

---

## Group 2：GPU / 加速後端效能優化

| Commit | 標題 | 摘要 |
|---|---|---|
| `ec5a12b` | opencl: add A8 Q4_0 non-MoE dp4a binary kernel (#29055) | 新增 OpenCL 平台上針對 Q4_0 量化、非 MoE 架構的 A8 dp4a binary GEMM kernel，提升 Adreno 等裝置的推論效能。 |
| `d5f6649` | opencl: add bin kernel `kernel_gemm_noshuffle_q4_k_q8_1_dp4a_ila_a8_bin` (#29056) | 延續上一項，新增 Q4_K 量化格式對應的 dp4a binary kernel，並重新命名 binary kernel 選擇的輔助函式，統一命名慣例。 |
| `c350a40` | Performance tune for gemma4-26b-a4b flash attention shape (#28450) | 針對 Gemma-4 26B-A4B 模型的 flash attention 形狀做效能調校。 |
| `f95b0d9` | ggml: IQ1_M build prefix sums once per block (#28706) | 重構 IQ1_M 量化格式的 prefix sum 計算，改為每個 block 只計算一次，減少重複運算，簡化程式碼（44 行增、73 行減）。 |
| `0f8a414` | metal: gate mul_mm_id src1 rescale behind ggml_prec (#29029) | Metal 後端的 `mul_mm_id` 對 src1 的 rescale 邏輯改為受 `ggml_prec`（精度模式）控制；同時讓 CUDA、Vulkan、WebGPU 後端在 `MUL_MAT_ID` 的 `supports_op` 中，於 src1 精度為 F32 時正確回報「不支援」，避免跨後端行為不一致造成的錯誤結果。 |
| `4ceb171` | vulkan: add Intel Xe flash attention optimization kernels (2/3, Xe-LPG Plus/Xe2/Xe3) (#24406) | 為 Intel Xe 系列 GPU（Xe-LPG Plus / Xe2 / Xe3）新增 Vulkan flash attention 最佳化 kernel（分三批提交的第 2 批），包含 split-k 路徑優化、A770 Linux 平台的 op 測試修正，以及對稱 `coopMatMulAdd()` 的使用以解決特定 Mesa 驅動版本下的測試失敗。異動量最大（839 行新增）。 |
| `f46bc30` | HIP: optimize IQ2/IQ3 (`__vsub4` `__vcmpne4`) using SWAR (#27962) | AMD HIP 後端針對 IQ2/IQ3 量化格式，改用 SWAR（SIMD Within A Register）位元運算技巧取代 `__vsub4`/`__vcmpne4` 內建函式，提升解量化效能。 |
| `e6ab7c1` | hex-dma: introduce direct-mapped DMA cache that is better suited for HVX FA mask handling (#29282) | 為 Hexagon NPU 後端新增 direct-mapped DMA cache，更適合處理 HVX（Hexagon Vector eXtensions）flash attention mask 的存取模式。 |

**說明**：這是本次數量最多的一組，涵蓋 OpenCL、Vulkan、Metal、HIP、Hexagon 五種後端的 kernel 級優化，反映社群持續在多硬體平台上打磨量化矩陣乘法與 flash attention 的效能與正確性。`0f8a414` 屬於跨後端一致性修正，值得留意是否影響現有使用 F32 精度的 MoE 模型推論路徑。

---

## Group 3：模型轉換與推測解碼（Speculative Decoding）

| Commit | 標題 | 摘要 |
|---|---|---|
| `bfd73a8` | convert: add MiMo-V2.6 support (#29257) | 新增 MiMo-V2.6 模型的轉換支援，將 K3 mxfp4 的 repack 邏輯上移至 base.py 以便重用，並移除 mmproj 轉換中不必要的 decoder。 |
| `828fdf2` | spec: support DFlash for HunyuanOCR (#28890) | 讓 HunyuanOCR 模型可搭配 DFlash 推測解碼草稿模型使用：在 Hunyuan 系列圖模型中補上各層殘差流（residual stream）的張量標記（`t_layer_inp`），修正因缺少該標記導致的 `GGML_ASSERT` 崩潰；同時修正轉換 DFlash 草稿模型搭配 HunYuan 目標模型時，vocab 處理誤用草稿模型自身 hparams（而非目標模型 hparams）所造成的特殊 token id 錯誤。作者實測 HunyuanOCR 1.5 + DFlash 草稿模型下，推測接受率約 0.5，OCR 輸出與非推測模式逐位元相同。 |

**說明**：`828fdf2` 是一組相對完整的除錯過程（4 個修正 commit 合併呈現），顯示 DFlash 推測解碼功能仍在針對不同模型架構逐步補齊相容性。

---

## Group 4：WebUI 體驗改善

| Commit | 標題 | 摘要 |
|---|---|---|
| `8cfc315` | Add close button to UI toasts (#28246) | 為 WebUI 的 toast 通知加上關閉按鈕，避免通知遮擋模型選擇器等其他 UI 元件時需要等待自動消失。 |
| `9b421fa` | ui: Accept WEBM video files (#28622) | WebUI 檔案上傳功能新增支援 WEBM 格式影片檔案。 |

---

## Group 5：Jinja 範本引擎與 Chat 解析修正

| Commit | 標題 | 摘要 |
|---|---|---|
| `7ab4ee7` | chat: Fix Muse Glimmer tool-call first parser error (#29242) | 修正 Muse Glimmer 模型在「tool-call 優先」解析模式下的錯誤，並補上驗證測試。 |
| `348f853` | jinja: use const for statement::execute and ::visit (#29271) | 將 jinja 範本引擎中 `statement::execute` 與 `::visit` 相關介面改為 `const`，屬程式碼品質/正確性強化，不影響行為。 |
| `709fe75` | jinja: fix dangling reference warning in for_statement (#29279) | 修正 `for_statement` 中透過 lambda 回傳指向區域轉型指標的參考，避免在部分編譯器上觸發 `-Werror=dangling-reference`；改為直接重用預先計算好的 `select_expr` 指標。 |

---

## Group 6：CI / 建置設定

| Commit | 標題 | 摘要 |
|---|---|---|
| `c550d2f` | ci: update Level Zero SDK to v1.33.1 and enable the L0/oneDNN CMake flags in the SYCL job (#29230) | 升級 CI 中 SYCL 相關工作使用的 Level Zero SDK 版本，並開啟 L0/oneDNN 的 CMake 編譯選項。 |
| `0ee9435` | ci: publish snapdragon builds in release workflow (#29007) | 先前 Snapdragon（Hexagon NPU）CI 建置只用於餵給 QDC 裝置測試，並未發佈到 releases 頁面；此修正在 release workflow 中同時建置兩種目標並附加為 release 資產，讓一般使用者也能下載到 Hexagon NPU 專用二進位檔。 |
| `a60f9ae` | cmake : allow repeated find_package calls for llama (#29228) | 允許 `find_package(llama)` 被重複呼叫而不出錯，方便將 llama.cpp 作為子模組多次引入的整合情境。 |

---

## 其他

| Commit | 標題 | 摘要 |
|---|---|---|
| `73c941b` | mtmd: add various sanity checks (#29276) | 為多模態（mtmd）模組新增多項健全性檢查（sanity checks），提升異常輸入時的錯誤偵測與防護。 |

---

## 觀察與建議

1. **`9919911`（server router 競爭條件修正）** 屬於正確性修正，若有部署 `llama-server` 多模型路由功能的環境，建議優先更新。
2. **`0f8a414`（跨後端 MUL_MAT_ID 精度一致性）** 涉及 CUDA / Vulkan / Metal / WebGPU 四個後端的行為變更，若專案有針對特定後端做過客製化效能調整，建議留意是否受影響。
3. GPU 後端優化（Group 2）數量多且分散在不同硬體平台，若團隊使用特定後端（如 OpenCL on Adreno、Vulkan on Intel Xe、HIP on AMD），可針對性抽測效能是否有提升。
4. 本次沒有發現明顯的破壞性變更（breaking change）或安全性問題。
