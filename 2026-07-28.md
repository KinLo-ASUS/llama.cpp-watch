# llama.cpp 變更報告 — 2026-07-28

- 檢查範圍：`ggml-org/llama.cpp` master 分支
- 上次檢查 commit：`88b47a75` (2026-07-27)
- 本次最新 commit：`91f8c9c5` (2026-07-27)
- 新增 commit 數量：13

---

## 分組摘要

### 1. 新模型與多模態支援
新增兩個模型架構的支援：中文模型 Nanbeige4.2，以及具語音輸入能力的 MiMo-V2.5（基於 RVQ 的音訊多模態模型）。

- **`b77d6467`** — model: Add support for Nanbeige4.2 (#25994) — by zql
  新增 Nanbeige4.2 模型架構支援，並修正 flake8 lint 錯誤、迴圈邊界檢查，同時移除多餘的 `head_dim` 設定。
- **`1cbfd198`** — mtmd: support MiMo-V2.5 audio input (RVQ-based model) (#26190) — by Xuan-Son Nguyen
  在 `tools/mtmd` 新增對 MiMo-V2.5（RVQ 語音編碼）音訊輸入的支援，包含對應的 GGUF 轉換腳本與 C++ 推理實作。

### 2. 推測解碼（Speculative Decoding）與 MTP 草稿模型
兩項修正皆針對草稿模型 (draft model) 的選取與 GPU 層分配邏輯，讓手動指定的草稿模型設定能正確優先，並讓 MTP 的前層維持在 GPU 上。

- **`ddfc2288`** — common: fix explicit -md precedence over draft sidecar resolution (#26165) — by (Co-authored with Georgi Gerganov)
  修正明確以 `--model-draft`（`-md`）指定的草稿模型檔案，先前會被 `-hfd` 的 sidecar 自動解析邏輯覆蓋且未正確解析為本地檔案的問題；現在手動指定的草稿模型設定優先於自動解析。同時將 sidecar 的解析改為直接以 tag 為錨點比對，並將推測解碼相關的載入紀錄由 trace 等級提升為 info 等級，方便觀察。
- **`0324696b`** — fit : count nextn (MTP) blocks in n_gpu_layers so front layers stay on GPU (#26177) — by Jonas Jankaitis
  修正 `n_gpu_layers` 的自動計算邏輯，將 MTP（Multi-Token Prediction / nextn）區塊納入計算，避免因未計入而導致模型前段層被擠到 CPU 上。

### 3. 後端建置與效能優化
涵蓋 CPU（PowerPC）、SYCL 建置流程與 HIP（AMD ROCm）後端的建置/效能相關調整。

- **`b9102008`** — ggml-cpu: Enable BF16 tiled gemm optimization on PowerPC (#26068) — by shalinib-ibm
  在 PowerPC 平台的 CPU 後端啟用 BF16 分塊矩陣乘法 (tiled GEMM) 優化，提升 BF16 運算效能。
- **`8e8681e0`** — sycl(build): parallelize ocloc invocations (#25903) — by Titaniumtown
  將 SYCL 建置流程中的 `ocloc`（Intel 離線編譯工具）呼叫改為平行執行，縮短編譯時間。
- **`91f8c9c5`** — Disable -ffast-math on HIP (#25495) — by Beinsezii
  停用 HIP（AMD ROCm）後端建置時的 `-ffast-math` 編譯選項，避免快速數學近似造成的數值精度問題。

### 4. ggml 核心：算子後端配置邏輯
- **`dee2a846`** — ggml : adjust logic for offloading ops to weight's backend (#25832) — by Georgi Gerganov
  調整將運算子卸載 (offload) 到權重所在後端的判斷邏輯，並修正 DeepSeek-V4 計算圖 (graph) 相關問題。

### 5. Server + WebUI：串流路由修復
- **`d73c1d6b`** — server + ui: fix stream routes for model names containing a slash (#26137) — by Pascal
  修正當模型名稱包含斜線（例如 `ggml-org/...`）時，對話 ID 在路徑解析中被錯誤切分，導致串流的停止/續傳 (stop/resume) 無法對應到正確 session 的問題；將 conv_id 移至 query string 傳遞。另外修正模型載入期間發出的停止請求未被正確取消而變成孤兒請求的問題，並讓頁面在模型載入中重新整理後能正確恢復串流與顯示載入進度，皆補上對應的回歸測試。

### 6. CLI 工具、公用函式與文件/測試
- **`ad256ded`** — args: add `-lm mlock` where it mlocks but doesnt mmap (#26135) — by Aaron Teo
  新增 `-lm`（`mlock`）參數，用於「僅 mlock 不 mmap」的情境，並調整 `--mlock` 的語義為非 mmap 情況下的 mlock。
- **`0e4a0362`** — common : add common_print_available_devices() (#26170) — by Adrien Gallouët
  新增 `common_print_available_devices()` 公用函式，用於列印目前可用的運算裝置資訊。
- **`419b881c`** — docs: add exception about weight folding (#26168) — by Xuan-Son Nguyen
  於文件中補充關於權重折疊 (weight folding) 的例外情況說明，並附上範例。
- **`7ef790f9`** — tests : remove unnecessary sync in test-save-load-state (#26166) — by Georgi Gerganov
  移除 `test-save-load-state` 測試中不必要的同步 (sync) 呼叫。

---

## 完整 Commit 列表（新到舊）

| SHA | 標題 | 分類 |
|---|---|---|
| `d73c1d6b` | server + ui: fix stream routes for model names containing a slash (#26137) | Server + WebUI |
| `ad256ded` | args: add `-lm mlock` where it mlocks but doesnt mmap (#26135) | CLI 工具 |
| `b9102008` | ggml-cpu: Enable BF16 tiled gemm optimization on PowerPC (#26068) | 後端/建置優化 |
| `419b881c` | docs: add exception about weight folding (#26168) | 文件 |
| `ddfc2288` | common: fix explicit -md precedence over draft sidecar resolution (#26165) | 推測解碼/MTP |
| `7ef790f9` | tests : remove unnecessary sync in test-save-load-state (#26166) | 測試 |
| `dee2a846` | ggml : adjust logic for offloading ops to weight's backend (#25832) | ggml 核心 |
| `8e8681e0` | sycl(build): parallelize ocloc invocations (#25903) | 後端/建置優化 |
| `0324696b` | fit : count nextn (MTP) blocks in n_gpu_layers so front layers stay on GPU (#26177) | 推測解碼/MTP |
| `b77d6467` | model: Add support for Nanbeige4.2 (#25994) | 新模型 |
| `0e4a0362` | common : add common_print_available_devices() (#26170) | 公用函式 |
| `1cbfd198` | mtmd: support MiMo-V2.5 audio input (RVQ-based model) (#26190) | 新模型/多模態 |
| `91f8c9c5` | Disable -ffast-math on HIP (#25495) | 後端/建置優化 |
