# llama.cpp 更新報告合輯 — 2026 年 7 月（依主題分組）

- **來源倉庫**：[ggml-org/llama.cpp](https://github.com/ggml-org/llama.cpp)，分支 `master`
- **追蹤區間**：`8ee54c8`（監控起點，2026-07-16）→ `5f55650a`（07-31 最新）
- **涵蓋 commit 總數**：160 筆（原按日分為 11 篇報告，現已合併並改為跨整月依主題分組，不再區分日期）

---

## 目錄

1. [WebUI 前端](#1-webui-前端)
2. [推測解碼與草稿模型（DFlash / DSv4 / MTP / EAGLE3 / DSpark）](#2-推測解碼與草稿模型dflash--dsv4--mtp--eagle3--dspark)
3. [新模型架構支援與修正](#3-新模型架構支援與修正)
4. [多模態（mtmd）](#4-多模態mtmd)
5. [CUDA 後端](#5-cuda-後端)
6. [Vulkan 後端](#6-vulkan-後端)
7. [OpenCL 後端（含 Adreno）](#7-opencl-後端含-adreno)
8. [Hexagon 後端](#8-hexagon-後端)
9. [SYCL 後端](#9-sycl-後端)
10. [Metal 後端](#10-metal-後端)
11. [WebGPU 後端](#11-webgpu-後端)
12. [其他後端與跨後端基礎設施](#12-其他後端與跨後端基礎設施)
13. [量化與模型轉換](#13-量化與模型轉換)
14. [Server](#14-server)
15. [Common／核心邏輯／取樣器／CLI](#15-common核心邏輯取樣器cli)
16. [GGML 核心與版本同步](#16-ggml-核心與版本同步)
17. [測試](#17-測試)
18. [依賴套件更新（vendor）](#18-依賴套件更新vendor)
19. [文件與貢獻指南](#19-文件與貢獻指南)
20. [本月重點摘要](#20-本月重點摘要)

---

## 1. WebUI 前端（17 筆）

- `2beefef` ui: Sidebar Conversations Bulk Action + Improved Settings logic/UI (#25815) — 側邊欄對話列表新增批次操作功能，並改善設定頁邏輯與 UI。
- `4ee6a9a` ui: fix collapsed user bubble with markdown rendering (#25869) — 修正使用者訊息收合時的 markdown 呈現問題。
- `43b5e63` UI: fix Settings/Display tool call content toggle (#25783) — 修正設定頁「顯示工具呼叫內容」開關失效問題。
- `1521a9a` ui: enable the agentic flow when only the JS sandbox is active (#25865) — 當僅啟用 JS sandbox 時也能啟用 agentic 流程。
- `cf51256` ui: Add a "Default" option for the reasoning selector (#25846) — 新增「Default」推理選項，讓伺服器自行決定是否啟用思考模式，修正過去強制送出 `enable_thinking` 導致部分模型（如 Gemma 4 E4B）行為異常的問題。
- `61328e6` feat(ui): add symbolic math support to JS sandbox via nerdamer (#25948) — 在 JS 沙盒中整合 nerdamer 符號運算引擎（simplify/expand/factor/diff/integrate/solve），並加入 CSP 阻擋沙盒對外連線。
- `54ce507` ui: fix settings priority + refactor settings-registry (#26002) — 修正設定值套用優先順序為「工廠預設 < 管理員 < 使用者」，並重構 `settings-registry.ts`。
- `95a923a` ui: fix duplicate MCP server display names (#26011) — 修正多個 MCP 伺服器回報相同顯示名稱時工具清單只顯示其中一個的問題，改用穩定的伺服器 id 作為鍵值。
- `96013c5` ui: remove message fade-in and smooth-scroll-to-bottom effects (#26083) — 移除長 agentic 迴圈中可能導致內容永遠隱藏的淡入動畫，以及串流時每 100ms 重播造成畫面抖動的平滑捲動效果，改用 `content-visibility: auto`。
- `555881e` ui: reduce per-token repaint cost during streaming (#26053) — 大幅降低串流輸出時每個 token 的重繪成本（實測多項指標從十幾 ms 降到 1ms 內）。
- `ff067f76` ui: fix context gauge card regressions and land at conversation end (#26099) — 修正 Context Gauge 卡片顯示回歸問題，並調整自動捲動邏輯。
- `55b7d6c4` ui: detect conversation import format from file contents (#26121) — 匯入對話紀錄改為依檔案內容自動判斷格式，而非僅依副檔名。
- `d4d057b6` ui: fix system message edit box not expanding to fit content (#26006) — 修正系統訊息編輯框內容較長時無法自動撐開高度的問題。
- `88b47a75` ui: Fix symbolic math tool JS sandbox prompt (#26131) — 修正符號數學工具在 JS Sandbox 中使用的提示詞常數設定。
- `6e2bc65f` ui: rendering performance follow-up (#26097) — 新增多項快取機制（工具呼叫解析、分類、語法高亮），改用 Map 結構取代重複陣列查找，降低串流時的重複運算。
- `21a5f5b7` ui: IndexedDB and Conversations data fixes (#26278) — 對話存放區改為單次初始化、系統訊息建立改為交易式、修正編輯/重新生成時的分支刪除與批量刪除計數邏輯等一系列 IndexedDB 資料層穩定性修正。
- `3018a11e` fix: increase greeting spacing on md screens (#26287) — 修正中等尺寸螢幕上歡迎訊息的間距，純樣式微調。

> 另一筆同時涉及 server 與 ui 的修正（`d73c1d6b`，模型名稱含斜線時串流路由錯誤）歸入下方「14. Server」分組。

---

## 2. 推測解碼與草稿模型（DFlash / DSv4 / MTP / EAGLE3 / DSpark）（14 筆）

- `91d2fc3` llama_dsv4: write only used rows in state (#25325) — DeepSeek V4 狀態快取僅寫入實際使用到的列，減少不必要的寫入。
- `571d0d5` model: rotate injected K/V cache for DFlash (#25823) — 為 DFlash 機制的注入 KV cache 加入輪替邏輯。
- `0dc74e3` DeepseekV4: Add fused hyper-connection ops (#25585) — 新增融合式 hyper-connection 運算子。
- `a55a8c5` convert: fix dflash target tokenizer mismatch during conversion (#25733) — 修正轉換模型時 DFlash 目標 tokenizer 不匹配的問題。
- `5d5306b` tests: initialize all tensors in test_dsv4_hc to avoid NaNs (#25822) — 修正測試中未初始化張量導致 NaN 的問題。
- `635cdd5` common: auto-download dflash- and eagle3- HF sidecars (#25811) — common 模組自動下載 dflash 與 eagle3 相關的 HuggingFace 附屬檔案。
- `60f6a17` common: resolve draft repo to its requested sidecar (#25955) — 修正 `-hfd` 指定草稿模型 repo 時，草稿模型被錯誤解析成主模型而非對應 sidecar 的問題，使 `--spec-type draft-dflash` 搭配 `-hf`/`-hfd` 正常運作。
- `846e991` cuda: add sqrt_softplus in topk-moe for dsv4 (#25896) — 於 topk-moe 運算中新增 `sqrt_softplus` 支援，供 DeepSeek V4 模型使用。
- `6d5a910` common: infer the speculative type from the draft repo sidecars (#25989) — 當 `-hfd` 指定的草稿 repo 內含 mtp-/dflash-/eagle3- sidecar 而未指定 `--spec-type` 時，自動依優先順序推斷正確的推測解碼類型。
- `ddfc2288` common: fix explicit -md precedence over draft sidecar resolution (#26165) — 修正明確以 `-md` 指定的草稿模型設定會被 `-hfd` sidecar 自動解析覆蓋的問題，讓手動設定優先。
- `0324696b` fit: count nextn (MTP) blocks in n_gpu_layers so front layers stay on GPU (#26177) — 修正 `n_gpu_layers` 自動計算未計入 MTP（nextn）區塊，導致模型前段層被擠到 CPU 上的問題。
- `7be2c65d` model: add NextN/MTP speculative decoding support for GLM_DSA (GLM-5.2) (#25980) — 為 GLM-5.2 新增 NextN/MTP 推測解碼支援（`--spec-type draft-mtp`），含獨立的 MTP KV cache 設定與轉換腳本 `--mtp`/`--no-mtp` 選項。
- `84075273` spec: add DSpark speculative decoding (#25173) — 延伸自 DFlash 的新架構 DSpark，加入半自迴歸 Markov 預測頭，可一次前向傳遞預測整個 block，並支援信心分數截斷。
- `f8706784` spec: add eagle3-v3 support for gpt-oss model (#25794) — 為 gpt-oss 模型新增 EAGLE3-v3 草稿模型支援。

---

## 3. 新模型架構支援與修正（11 筆）

- `1f66c3c` Add support for Laguna XS.2 & M.1 (#25165) — 新增 poolside Laguna 系列模型（S-2.1 / XS-2.1 / XS.2）支援，含轉換腳本、GGUF 常數與新的 chat auto-parser/diff-analyzer 模組。
- `f534da2` Fix DeepSeek4 crafted template (#25414) — 修正 DeepSeek4 的 chat template，新增 `drop_reasoning` 旗標支援，並讓 DS3.2 parser 同時適用於 DS4。
- `67b9b0e` llama-arch: fix DeepSeek4 APE tensor op (#25945) — 修正 DeepSeek4 的兩個 APE 張量運算，由錯誤的 `GGML_OP_ADD` 改為正確的 `GGML_OP_GET_ROWS`。
- `e8e6c7a` minor: fix reasoning preserve var for DS4 [no ci] (#25999) — 將 DeepSeek4 相關變數名稱統一為 `drop_thinking`。
- `b1d4c655` model: Add MiniMax-M3 (MSA: MiniMax Sparse Attention) support (#24908) — 新增 MiniMax-M3 架構完整轉換與推理支援，含大幅擴充的 KV cache 邏輯（因應稀疏注意力機制）。
- `7657a6c2` Keep Minimax's indexer tensors at F32 for speed and accuracy (#26144) — 量化時將 MiniMax indexer 相關張量強制保留 F32，避免精度損失過大。
- `88bfee1` Add GLM 5.2 Indexer architecture support (#25407) — 沿用 DeepSeek 3.2 圖建構方式，啟用 `glm_dsa` 架構的 KV cache 與 rotation，並區分 GLM 5/5.1 與 5.2 的 indexer 行為。
- `b77d6467` model: Add support for Nanbeige4.2 (#25994) — 新增 Nanbeige4.2 模型架構支援。
- `e9fa0781` model: Add Laguna-S-2.1 LLM_TYPE (#26233) — 新增 `LLM_TYPE_118B_A8B` 列舉值，依 48 層架構特徵辨識為 Laguna-S-2.1 變體。
- `64d528be` mimo2: address MTP review feedback (#26228) — 依 code review 意見調整 mimo2（含 MTP）模型實作。
- `b2f22168` Remove custom cpu op from the M3 graph, express with stock ops (#26297) — 將 minimax-m3 模型圖中自訂的 CPU 運算子改用 ggml 內建標準算子表示，簡化程式碼。

---

## 4. 多模態（mtmd）（8 筆）

- `b4aa7dd` mtmd: use align_corners for qwen3vl vision position embedding interpolation (#25781) — 修正 Qwen3-VL 視覺位置編碼插值，改用 `align_corners=True`，避免非正方形/較大圖片座標偏移。
- `e0833bf` mtmd: use RAII for setting and resetting non-causal attention (#25723) — 將設定/還原 non-causal attention 的邏輯改為 RAII 管理。
- `af285020` mtmd: add GLM-5.2-Vision (#26126) — 新增 GLM-5.2-Vision 模型支援。
- `3d1c3a89` mtmd: Add Vision Support for Minimax-M3 (#25113) — 為 MiniMax-M3 新增視覺（多模態）支援，含 CLIP 相關模型定義。
- `0d47ea74` mtmd: fix android build (#26150) — 修正 `mtmd-helper.cpp` 在 Android 平台上的編譯問題。
- `1cbfd198` mtmd: support MiMo-V2.5 audio input (RVQ-based model) (#26190) — 新增對 MiMo-V2.5（RVQ 語音編碼）音訊輸入的支援。
- `7e1e28ca` mtmd: add Nemotron 3 Nano Omni support (parakeet) (#22520) — 新增 Parakeet 子取樣與音訊編碼器實作，同步至最新版 whisper.cpp 的 Parakeet 實作。
- `5f55650a` mtmd: add lanczos resize method [no release] (#26341) — 新增 Lanczos 影像縮放演算法，提供比既有方法更高品質的圖片縮放選項。

---

## 5. CUDA 後端（11 筆）

- `79bba02` CUDA: Support CUDA Virtual Devices (#25228) — 新增 CUDA Virtual Devices 支援。
- `3f08ef2` Enable CUDA graphs on volta+turing (#25749) — 在 Volta 與 Turing 架構上啟用 CUDA graphs。
- `305ba51` CUDA: vectorize same-type get_rows with int4 copy (#25929) — 新增以 int4（16 bytes）為單位的向量化拷貝路徑，大幅提升 `get_rows` 效能（Strix Halo 上約 -27% 耗時）。
- `1a064ab` CUDA: Improve NVFP4 W4A4 activation quantization (#25730) — 優化 NVFP4 W4A4 activation 量化流程，含 32-byte 讀取、per-block amax、融合量化 kernel。
- `c5a4a0b` cuda: GET_ROWS quants (#25962) — 補齊 CUDA `GET_ROWS` 對所有量化型別的支援，避免 embedding 查表退回主機端逐 token 複製。
- `1425386` CUDA: fix external compilation of q1_0 MMQ (#25778) — 修正 CUDA MMQ 核心中 `q1_0` 量化格式在外部編譯情境下無法正確編譯的問題。
- `60bccc37` add rdna3.5, and 3 to mmq configs so they can be tuned independently (#26199) — 將 RDNA3/RDNA3.5 拆分出獨立 mmq 設定檔，不再與 RDNA4 共用參數。
- `caa596ab` ggml-cuda: disable MMQ on devices with less than 48 KiB shared memory (#26141) — 修正共享記憶體不足 48 KiB 裝置上因找不到符合大小的 MMQ tile 而崩潰的問題，自動退回 BLAS 路徑。
- `b62b3509` ggml-cuda: add chunked SSD matmul for Mamba-2 prefill acceleration (#22675) — 新增分塊 SSD 矩陣乘法加速 Mamba-2 prefill 階段，並修正資料競爭問題。
- `9b2a0888` CUDA: add Q2_0 support (#25707) — 新增 `Q2_0` 量化格式支援，涵蓋 dequantize、getrows、mmq、mmvq 等路徑。
- `e1a1abb7` ggml-cuda: Allow transpose-free gemmv computation (#26171) — 當權重矩陣形狀為 `1xK` 時改用免轉置計算路徑，減少轉置開銷。

---

## 6. Vulkan 後端（5 筆）

- `788e07d` vulkan: Support Q2_0 (#25430) — Vulkan 後端新增 Q2_0 量化格式支援。
- `b15ca93` vulkan: when using transfer queue for async copies, sync on event_wait (#25229) — 修正非同步複製時的潛在競爭問題。
- `f048010` vulkan: Refactor vk_queue to use per-instance mutexes and unique handles (#23570) — 大規模重構 `vk_queue`，整合硬體端同步擴充功能，提升多執行緒下的 queue 使用效率。
- `81616410` vulkan: add iq4_nl support back to FA (#24585) — 在 flash attention 中重新加入 IQ4_NL 量化格式支援（取代先前的 Q1_0）。
- `ea63b4d3` vulkan: Support quantized concat (#25684) — 新增對量化張量做 `concat` 運算的支援。

---

## 7. OpenCL 後端（含 Adreno）（11 筆）

- `178a6c4` opencl: Support broadcast for Adreno MUL_MAT (#25910) — 為 Adreno 的 MUL_MAT 新增 broadcast 支援。
- `86a9c79` opencl: load and use kernel_gemm_moe_q6_k_f32_ns from bin kernel lib (#25797) — 從預編譯 kernel library 載入並使用 MoE q6_K GEMM kernel。
- `6bdd77f` opencl: read/write MoE dp4a activation tiles to local memory (#25810) — 將 MoE dp4a 運算的 activation tiles 改為讀寫 local memory。
- `86d86ed` opencl: transpose q4_K noshuffle scales for coalesced reads (#25805) — 轉置 q4_K scale 資料以達成合併記憶體存取。
- `e8f19cc` opencl: loads quants as uint for q4_K and q5_K flat mv (#25780) — 將 q4_K/q5_K 量化資料改以 uint 型態讀取。
- `b85833e` opencl: add ABS op (#25115) — 新增 ABS 運算子的 OpenCL 實作。
- `ac2557c` docs: added a note about using OpenCL with Adreno 810 (#25786) — 補充 Adreno 810 使用 OpenCL 的說明文件。
- `56a8386` opencl: fix flash attention mask NULL causal misdetection (#25771) — 修正 mask 為 NULL 時被誤判為 causal 模式的問題。
- `ed7adbf` opencl: add cl_program disk cache (#26050) — 新增編譯後 `cl_program` 二進位檔的磁碟快取機制，加快後續啟動速度。
- `81908487` opencl: skip the Adreno KQ/KQV image kernels for multi-stream batches (#26189) — 修正忽略張量第 4 維導致多序列批次讀到錯誤 K/V 資料的問題（Adreno 740 上 PPL 由 1817.64 修正回 15.61）。
- `42fc2430` opencl: fix fused RMS norm mul view offset (#26085) — 修正融合 RMS norm + mul 運算的 view offset 計算錯誤。

---

## 8. Hexagon 後端（7 筆）

- `76f46ad` hexagon: add CLAMP op (#25934) — 新增 CLAMP 運算子的 Hexagon 實作。
- `b2dd28a` hexagon: L2 cache handling rework (#25762) — 重構 Hexagon 後端的 L2 快取處理邏輯。
- `71102a7` hexagon: check tensor type when reusing descriptors (#25968) — 重用 tensor descriptor 前檢查張量型態，避免誤用快取的 descriptor。
- `0278d83` hexagon: activation ops update (#25974) — 重構 activation 運算，新增 all-in-one geglu microkernel，支援非連續 tensor 的 strided DMA。
- `355303e` hexagon: partial im2col support (#26007) — 新增 IM2COL 運算支援（主要針對 patch-embedding 卷積）。
- `0a50d99` hexagon 管線持續優化 (#26049) — 改進 L2 快取 dirty-range flush、DMA 提前排程、MM/FA 在 HVX/HMX 上的多項細部效能調整。
- `c0bc859` hexagon: fix Windows crash when op_poll is enabled (#26029) — 修正 Windows 平台啟用 `op_poll` 選項時導致崩潰的問題。

---

## 9. SYCL 後端（5 筆）

- `0bd0ec6` sycl: fix row calculation when K_QUANTS_PER_ITERATION is 1 (#25690) — 修正行數計算錯誤。
- `b4d6c7d` ci: fix SYCL package shared library lookup (#25987) — 修正 SYCL 發布包的共享函式庫路徑設定（加入 `$ORIGIN` RPATH）。
- `8e8681e0` sycl(build): parallelize ocloc invocations (#25903) — 將 `ocloc` 編譯工具呼叫改為平行執行，縮短編譯時間。
- `d6b61ac0` sycl: fix use-after-return of the SDPA scale in the oneDNN flash-attention path (#25880) — 修正長 KV 序列下 scale 數值讀到已回收記憶體、造成輸出亂碼與 KV cache 污染的問題。
- `11b068d0` sycl: contiguous fast path + 32-bit index math for unary elementwise ops (#25946) — 為連續記憶體佈局的一元逐元素運算新增快速路徑，改用 32-bit 索引運算加速。

---

## 10. Metal 後端（3 筆）

- `c588c4f` metal: add f16 type support to leaky relu (#25981) — 為 leaky ReLU 補上 f16 型別支援。
- `f95de977` ggml-metal: FWHT kernel for metal backend (#25924) — 新增快速沃爾什-哈達馬變換 kernel，支援 64/128/256/512 等尺寸。
- `d0bfb198` metal: fix memory unwire if model is freed without any GPU operations (#26082) — 修正模型未執行任何 GPU 運算就釋放時記憶體未正確 unwire 造成洩漏的問題。

---

## 11. WebGPU 後端（3 筆）

- `7347430` webgpu: add CONV_2D_DW (depthwise conv2d) kernel (#25847) — 新增 depthwise Conv2D 運算，移植自 Vulkan 版本。
- `7cdd557` ggml-webgpu: Fix WASM compilation with OpenMP (#25943) — 將 WASM 建置拆成獨立 workflow，並停用 WASM 建置中的 OpenMP 以解決編譯失敗。
- `bc71c24c` ggml-webgpu: Fix binding alias issues, fix recurrent-state-rollback test (#25931) — 修正不同架構下 shader 綁定別名問題，並修復相關測試與超過 4GB 緩衝區的對齊問題。

---

## 12. 其他後端與跨後端基礎設施（12 筆）

- `5735e10` ggml-openvino: Add GGML_BACKEND_DL_IMPL invocation for OpenVINO backend (#25795) — 補上 OpenVINO 後端缺少的巨集呼叫，使其與其他後端一致。
- `3ce7da2` ggml: enable PowerPC backend variants on AIX (#25983) — 讓 PowerPC CPU 後端可在 AIX 平台上建置。
- `b9102008` ggml-cpu: Enable BF16 tiled gemm optimization on PowerPC (#26068) — 在 PowerPC 平台啟用 BF16 分塊矩陣乘法優化。
- `fa72aec` Remove HIP rocWMMA FlashAttention path (#26046) — 移除 HIP 後端以 rocWMMA 實作的 FlashAttention 路徑（刪除近 800 行程式碼），簡化維護面。
- `91f8c9c5` Disable -ffast-math on HIP (#25495) — 停用 HIP 後端建置時的 `-ffast-math`，避免數值精度問題。
- `f5b9bd39` RPC: add tensor_memset (#25912) — RPC 後端新增 `tensor_memset` 操作支援。
- `fb0e6b6` kleidiai: warn once when a weight type has no KleidiAI kernel (#25701) — 避免大量重複的日誌輸出。
- `f15bd60` kleidiai: Add SME vs SME2 distinction in kernel dispatch (#25478) — kleidiai kernel 派送邏輯區分 SME 與 SME2。
- `32703b42` ggml: Fix issue with kleidiai ci and stringop overflow warning (#26277) — 修正 KleidiAI CI 環境觸發的編譯警告。
- `a6a77bc4` [UT] enhance UT to show all real unsupported backends (#25234) — 改善 `test-backend-ops` 輸出，列出所有實際不支援該運算的後端。
- `958d9c0b` Test support for alternative conv layout (#25617) — 為 `conv2d` 新增 `cwhn` 替代記憶體佈局的測試與跨後端（CPU/CUDA/Vulkan）支援。
- `11fd0a6` ggml-blas: default hadamard mul_mat to cpu routine (#25710) — hadamard mul_mat 預設改用 CPU 運算路徑。

---

## 13. 量化與模型轉換（5 筆）

- `4937ca8` llama_quant: exclude i32 ffn_gate_tid2eid routing table from quantization (#25787) — 量化時排除 int32 的 routing table，避免錯誤量化。
- `3278e92` conversion: accept BitNetForCausalLM architecture name (#25769) — 轉換工具新增支援 BitNetForCausalLM 架構名稱。
- `d941f6e` conversion: fix non-MoE NomicBert GGUF conversion error (#25996) — 修正非 MoE NomicBert 模型轉換 GGUF 格式時的錯誤。
- `e1af89a6` conversion: fix Qwen2.5-Omni mmproj conversion regression (#26262) — 修正 Qwen2.5-Omni 多模態投影層轉換的迴歸錯誤。
- `66e4bf7` convert: fix handle HunyuanVL XD-RoPE config (#25514) — 修正轉換 HunyuanVL 模型時對 XD-RoPE 設定的處理問題。

---

## 14. Server（15 筆）

- `a8dc0e3` server: allow text-only slot save/restore with mtmd (#25076) — 支援在 mtmd 模式下純文字 slot 的儲存/還原。
- `8ee54c8` server: Ignore empty / non-existing Origin headers (#25756) — 忽略空的或不存在的 Origin header，避免誤判。
- `40b740a` server: properly handle null llama_context (#25868) — 修正 `llama_context` 為 null 時未妥善處理的問題。
- `fd41bf6` server: return 400 instead of 500 on validation error with X-Conversation-Id (#25760) — 修正串流回呼未設定觸發 `std::bad_function_call` 而誤回 500 的問題。
- `20455a4` server: support MCP stdio (#26062) — 新增透過 stdio 與外部 MCP 伺服器溝通的完整基礎架構，新增 `--mcp-servers-config`/`--mcp-servers-json` 參數。
- `abc3487` server: add format arg to datetime tool (#26117) — 為內建 datetime 工具新增 `format` 參數，改用 UTC 時間輸出。
- `2cfc767` server: add missing task parameters (adaptive_target, adaptive_decay) in generation_settings (#25830) — 修正 `generation_settings` JSON 輸出遺漏的取樣參數。
- `d2a81823` common: add subproc.h wrapper, disabled on android/ios (#26102) — 新增跨平台子行程管理封裝，重構 server-mcp/server-models/server-tools 改用此共用元件。
- `27209a5` server：OpenAI 相容 API 支援 "reasoning_effort": "none" (#26045) — 可完全關閉推理輸出。
- `ee3d1b54` server: abstract llama_memory calls to common_memory (#26221) — 將目標/草稿 context 的 KV cache 操作抽象為 `common_memory`，減少重複邏輯。
- `9a3bf2b8` server: add extra trace log for prompt similarity (#26218) — 新增 trace 等級紀錄，輸出 prompt 長度、最長共同前綴與相似度等除錯資訊。
- `992c3253` server: add trace logging for slot similarity checking (#26271) — 為 prompt cache 的 slot 相似度檢查流程新增 trace 等級日誌。
- `b4ca032a` server: support inp embd to generate next token (#26313) — 新增支援直接輸入 embedding 來產生下一個 token。
- `432d7ffe` llama-context: sync pending async copies before clearing embd_seq (#25676) — 清除 `embd_seq` 前先同步等待未完成的非同步複製，避免資料競爭。
- `d73c1d6b` server + ui: fix stream routes for model names containing a slash (#26137) — 修正模型名稱含斜線時對話 ID 被錯誤切分、串流停止/續傳失效的問題，將 conv_id 移至 query string 傳遞。

---

## 15. Common／核心邏輯／取樣器／CLI（13 筆）

- `8bb9093` common: use-after-free when loading LoRA adapter fails (#25611) — 移除 LoRA adapter 載入失敗錯誤路徑中多餘的 reset 呼叫，修正潛在 use-after-free。
- `c812c54` common: skip empty implicit default preset (#25643) — INI 設定解析器跳過沒有任何模型選項的隱含 default 區段。
- `910196f` common: add support for multiple end sequences in the reasoning budget sampler — 將 trie/Aho-Corasick 邏輯抽出獨立模組，讓推理預算取樣器支援多組結束序列。
- `298219f` llama 核心程式庫的多項小型 bug 修正 (#26051) — 涵蓋 grammar、KV cache、recurrent memory、model、quant、sampler、vocab 等模組的雜項修復合集。
- `da5b4486` ggml : set output of view src (#25729) — 修正 `ggml_set_output()` 對 view source 一併標記輸出旗標，並在多種取樣器中統一改用 `ggml_reshape_1d()`。
- `afeebe10` llama: move suppress_tokens handling to common/sampling (#26276) — 將 `suppress_tokens` 處理邏輯從個別模型程式碼移到共用的 `common/sampling.cpp`。
- `0e4a0362` common : add common_print_available_devices() (#26170) — 新增公用函式列印目前可用的運算裝置資訊。
- `ad256ded` args: add `-lm mlock` where it mlocks but doesnt mmap (#26135) — 新增「僅 mlock 不 mmap」參數。
- `e6dd0e2` args: refactor mlock/mmap/directio into load-mode (#20834) — 將 `--mlock`、`--mmap`、`--direct-io` 整合重構為單一「load-mode」參數，本次改動範圍最大（20 個檔案）。
- `6ba5ef24` common/chat: add specialized minimax m3 parser (#26210) — 新增針對 MiniMax M3 格式的專用聊天解析器，支援工具呼叫參數解析。
- `dee2a846` ggml : adjust logic for offloading ops to weight's backend (#25832) — 調整運算子卸載到權重所在後端的判斷邏輯，並修正 DeepSeek-V4 計算圖問題。
- `2e1fd76` TP: fix Phi3, Bert, Plamo2/3, ChatGLM (#25536) — 修正 Tensor Parallel 對這幾個模型架構的支援問題。
- `8f5ab83` Cohere2 聊天模板解析器：強制文字回應遵循 JSON schema (#26018) — 當請求帶有 response schema 時，強制文字回應也遵循其限制。

---

## 16. GGML 核心與版本同步（5 筆）

- `7d56da7` sync: ggml — 同步上游 ggml 子專案變更。
- `3727404` ggml: bump version to 0.17.0 (ggml/1568)
- `fb92d8f` Update ggml/src/gguf.cpp：為 `gguf_writer_base` 解構子加上 `virtual`，避免透過基底類別指標刪除衍生類別物件時解構子未被呼叫的問題。
- `6a4c3357` ggml: bump version to 0.18.0 (ggml/1576)
- `9ebfc3a8` sync: ggml — 更新 `scripts/sync-ggml.last`。

---

## 17. 測試（4 筆）

- `32e789f` tests: actually exercise test-recurrent-state-rollback (#25758) — 修正測試未實際執行到 recurrent state rollback 邏輯的問題。
- `d67c0b4` tests: synchronize save-load-state generation (#26056) — 加入 `llama_synchronize(ctx)` 呼叫，確保狀態儲存/讀取測試正確同步。
- `7ef790f9` tests : remove unnecessary sync in test-save-load-state (#26166) — 移除不必要的同步呼叫。
- `47f686f5` tests: avoid building get-model.cpp many times (#26317) — 移除重複的測試輔助檔，改用共用邏輯，加快測試建置速度。

---

## 18. 依賴套件更新（vendor）（4 筆）

- `86b719b` vendor: update BoringSSL to 0.20260713.0 (#25624)
- `0cea362` vendor: update sheredom/subprocess.h (#26061) — 同步調整呼叫端 `server-models.cpp`。
- `720d7fa` vendor: update cpp-httplib to 0.51.0 (#26067)
- `e2f59ed7` vendor: update BoringSSL to 0.20260728.0 (#26241)

---

## 19. 文件與貢獻指南（7 筆）

- `da296d6` contrib: fix leftovers from the AI usage policy update (#26030) — 修補「AI 使用政策」文件更新時遺留的疏漏。
- `77095ee` 新增 `skills/add-new-model` 與 `code-review` 專案技能文件 (#26042) — 供協作/AI agent 使用，並更新 `AGENTS.md`、`CODEOWNERS`。
- `4310aa4` contrib: allow all AI-generated code in general (#26012) — 由「禁止大量／完全 AI 生成」改為「有條件允許」，要求貢獻者對每一行程式碼負責。
- `ad77bd31` docs: Adapt conda-forge package name (#26229) — 將安裝文件套件名稱統一改為 `llama.cpp`。
- `c6292cfb` contrib : add guideline about the 'merge ready' label (#26178) — 說明可用 `merge ready` 標籤加速合併流程。
- `419b881c` docs: add exception about weight folding (#26168) — 補充權重折疊相關例外情況說明。
- `6b36c230` readme: refresh (#26280) — 大幅整理 README，抽出多段內容至 `docs/` 獨立文件，讓首頁更精簡。

---

## 20. 本月重點摘要

2026 年 7 月共追蹤到 **160 筆** commit（`8ee54c8` → `5f55650a`），主要趨勢：

- **WebUI 為改動最頻繁的區塊**（17 筆）：涵蓋設定優先權修正、MCP 顯示名稱衝突、對話匯入格式偵測，以及多次針對串流輸出重繪效能與長對話捲動行為的優化，是本月體驗面持續投入最多的方向。
- **推測解碼生態系持續擴張**（14 筆）：DeepSeek V4/DFlash 的狀態管理與正確性修正、新增 GLM-5.2 的 NextN/MTP 支援、新增 DSpark（延伸自 DFlash 的半自迴歸架構）與 gpt-oss 的 EAGLE3-v3 支援，草稿模型的自動偵測與優先權邏輯也多次調整。
- **新模型架構密集上線**（11 筆）：MiniMax-M3（含視覺支援）、Laguna 系列（S-2.1/XS.2/M.1）、Nanbeige4.2、GLM 5.2 Indexer、DeepSeek4 修正等，顯示上游模型覆蓋範圍持續快速擴大。
- **多模態（mtmd）穩定擴充**（8 筆）：新增 GLM-5.2-Vision、MiniMax-M3 視覺、MiMo-V2.5 音訊、Nemotron 3 Nano Omni（Parakeet）等模型的多模態支援，並新增 Lanczos 縮放演算法。
- **GPU 後端百花齊放**：CUDA（11 筆，含 Q2_0、免轉置 GEMV、Mamba-2 chunked SSD）、OpenCL（11 筆，多為 Adreno 相關優化與正確性修正）、Vulkan（5 筆）、Hexagon（7 筆）、SYCL（5 筆）等後端均有持續的效能與正確性投入，其中 OpenCL Adreno 多序列批次修正（PPL 由 1817 修正回 15.61）與 SYCL oneDNN flash-attention 的 use-after-return 修正屬於較嚴重的正確性 bug。
- **Server 端**（15 筆）：新增 MCP stdio 支援、多項錯誤處理與觀測性（trace log）改進、embedding 直接輸入生成下一 token 等功能擴充。
- 其餘為 **Common/核心/取樣器重構**（13 筆，如 load-mode 參數整合、suppress_tokens 集中化）、**GGML 版本同步**（0.17.0 → 0.18.0）、**測試/CI 整併**與**文件維護**，均屬持續性的工程債清理與可維護性提升。
- 整體而言，本月沒有重大破壞性 API 變更；風險較高的修正集中在跨序列/多裝置的 GPU 後端正確性問題（OpenCL Adreno、SYCL oneDNN、CUDA MMQ 共享記憶體不足時的崩潰）。
