# llama.cpp 更新報告 — 2026-07-24

- **檢查範圍**：`ggml-org/llama.cpp` master 分支
- **檢查區間**：`4310aa4`（上次檢查）→ `c0bc859`（本次最新）
- **新增 commit 數**：6 筆

## 摘要總覽

本次共 6 筆新 commit，主要集中在三個方向：多個運算後端（Metal / CUDA / Hexagon）的獨立修正、模型轉換與命令列參數的調整、以及貢獻文件的小幅更新。整體屬於零散的維護性修正，沒有新增重大功能。

---

## Group 1：多後端（Metal / CUDA / Hexagon）錯誤修正

三筆各自獨立、分別修正不同運算後端上的 bug，彼此無程式碼相依性，僅在「後端修正」這個主題上相似。

### `c588c4f` — metal : add f16 type support to leaky relu (#25981)
- 作者：Ilia Ilmer
- 修改：`ggml/src/ggml-metal/ggml-metal-device.m`
- 摘要：為 Metal 後端的 leaky ReLU 運算子補上 f16（半精度浮點）型別支援，修正原本只支援 f32 導致的限制。

### `1425386` — CUDA: fix external compilation of q1_0 MMQ (#25778)
- 作者：Johannes Gäßler
- 修改：`ggml/src/ggml-cuda/mmq.cu`、`ggml/src/ggml-cuda/mmq.cuh`
- 摘要：修正 CUDA MMQ（矩陣乘量化）核心中 `q1_0` 量化格式在「外部編譯」（即獨立於主專案編譯單元）情境下無法正確編譯的問題。

### `c0bc859` — hexagon: fix Windows crash when op_poll is enabled (#26029)
- 作者：adgup-qti（Qualcomm）
- 修改：`ggml/src/ggml-hexagon/ggml-hexagon.cpp`、`ggml/src/ggml-hexagon/htp-drv.cpp`
- 摘要：修正 Hexagon 後端在 Windows 平台上啟用 `op_poll` 選項時會導致程式崩潰的問題。

---

## Group 2：模型轉換與模型載入參數

兩筆變更都與「模型如何被讀入」相關：一筆是 GGUF 轉換腳本的修正，另一筆是執行期載入參數（mmap/mlock/directio）的重構，主題相近但改動範圍與規模差異較大。

### `d941f6e` — conversion: fix non-MoE NomicBert GGUF conversion error (#25996)
- 作者：Shahir Bin Zulfiker
- 修改：`conversion/bert.py`
- 摘要：修正將非 MoE（非混合專家）架構的 NomicBert 模型轉換為 GGUF 格式時發生的錯誤。

### `e6dd0e2` — args: refactor mlock/mmap/directio into load-mode (#20834)
- 作者：Aaron Teo（IBM）
- 修改：`common/arg.cpp`、`src/llama.cpp`、`src/llama-model-loader.*`、`tools/llama-bench/llama-bench.cpp` 等共 20 個檔案，含新增/修改測試
- 摘要：將原本各自獨立的 `--mlock`、`--mmap`（`--no-mmap`）、`--direct-io` 等旗標，整合重構為單一的「load-mode」參數（並支援用 `+` 修飾符組合模式），同步更新相關文件（README、llama-gen-docs）與測試，也修正了舊版旗標的相容性支援。此為本次更新中改動範圍最大的一筆。

---

## Group 3：貢獻文件修正

### `da296d6` — contrib: fix leftovers from the AI usage policy update (#26030)
- 作者：Pascal
- 修改：`AGENTS.md`、`CONTRIBUTING.md`
- 摘要：修補前一次「AI 使用政策」文件更新時遺留的疏漏內容，屬純文件層級的修正。

---

## 狀態更新

- 最新已檢查 commit：`c0bc8591e8815c63cb01dd3f051a8b0df02501c9`
- 下次檢查將從此 commit 之後繼續。
