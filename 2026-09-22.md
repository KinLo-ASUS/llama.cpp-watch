# llama.cpp 更新報告 (2026-09-22)

- **檢查區間**：`ce8caa6` (2026-09-21) → `5836771` (2026-09-21)
- **新增 commit 數**：30
- **上次 checkpoint**：`ce8caa6e60a03093351d6016a818720e0d46f0fb`
- **本次最新 checkpoint**：`58367713a6935c0810103378144008df32e3d5db`

本次檢查期間沒有太大時間跨度（同一天內的一批合併），但涵蓋多個後端（Hexagon、CUDA、Metal、SYCL、WebGPU、CPU）的效能優化與修正，以及測試框架、CI 流程的改善。以下依主題分組整理。

---

## 1. Hexagon (Snapdragon NPU) 後端大型重構與新功能

這是本次更新中變動範圍最大的一組，兩個 commit 皆由 Max Krasnyansky 提出，針對 Qualcomm Hexagon/HTP 後端進行大幅度的記憶體管理與運算子優化。

- **`0c3626e`** — hexagon: overhaul of buffer and DMA handling to support 64bit mappings + improvements (#29197)
  重寫 buffer 與 DMA 處理機制，支援 64-bit 記憶體對映與擴充的緩衝區配置，並針對多種運算子（binary ops、SSM_CONV、CPY、ROPE、softmax、GDN、flash-attention 等）改寫 DMA 路徑、修正競態與邊界問題，同時新增輔助的除錯/檢測腳本。屬於大量細碎 commit 壓縮而成的基礎設施重構，為後續 HMX 優化鋪路。

- **`5836771`** — hexagon: new HMX-optimized GATED_DELTA_NET (#29199)
  在上述 DMA 重構基礎上，新增針對 GATED_DELTA_NET 運算子的 HMX（Hexagon Matrix Extension）優化實作，包含 pipeline 化、HVX 向量化 exp 計算、tile 排程優化，並同步改善 flash-attention 的 DDR 讀取效率（token generation 階段減少 20-30% DDR 讀取）。

> **小結**：Hexagon 後端持續投入大量工程資源做底層記憶體/DMA 基礎設施重構，目標是支援更大模型與更高效能的 NPU 卸載，尤其聚焦於 GDN（Gated Delta Net，用於 linear attention 架構如 Qwen3-Next 等）與 flash-attention 的加速。

---

## 2. CUDA 後端 kernel 優化與修正

- **`68d9053`** — cuda : tune MMVQ to MMQ crossover for SM70 (Volta) (#28912)
  針對 Volta (SM70) 架構重新調校 MMVQ 與 MMQ 兩種矩陣乘法 kernel 的切換門檻，提升該架構下的量化矩陣乘法效能。

- **`1884824`** — CUDA: Follow up of #25635, refactoring FA shared smem swizzle (#28536)
  重構 flash-attention 的 shared memory swizzle 邏輯，移除設定中的顯式 swizzle 數值。

- **`b1c2863`** — cuda: fix sm_70 tile compilation error (#29224)
  修正上一項變更（`1884824`）引入的迴歸問題：5 參數版本的 `load_ldmatrix` 只定義了 `tile<16,8>`，導致 Volta 使用的 `tile<8,4>` 編譯失敗；此修正將 tile 形狀泛化為 `<I,J>`。

- **`e6cef81`** — cuda : accelerate conv2d with implicit GEMM (#29135)
  以 implicit GEMM 方式加速 CUDA 的 conv2d 運算。

- **`f4e276a`** — ggml-cuda : convert contiguous tensors four elements at a time (#29155)
  為連續記憶體佈局的張量轉換新增專用 kernel，每執行緒一次處理 4 個元素（原本為逐元素的 strided kernel），在 gfx1151 上量化模型 prefill 階段 pp2048 提升約 1.26%。

> **小結**：CUDA 相關的 4 個 commit 圍繞 flash-attention 與矩陣乘法效能調校，其中 `b1c2863` 是對同批次 `1884824` 的即時修復，顯示變更之間存在依賴關係。另有 conv2d 加速與資料轉換 kernel 優化。

---

## 3. CI / Docker：CUDA 版本升級與 workflow 重構

- **`6ad1af5`** — ci : Upgrade CUDA to 13.4 for Ubuntu CUDA Release Builds (#29202)
- **`ec91ab5`** — docker : bump cuda to 13.4.1 (#29207)
- **`542e920`** — ci : refactor build-self-hosted into backend-specific workflows (#28991)
  將原本單一的 `build-self-hosted` workflow 拆分為各後端獨立的 workflow（CPU、CUDA、KleidiAI、Metal、OpenVINO、Vulkan、WebGPU），並調整觸發條件。

> **小結**：CI 基礎設施持續朝 CUDA 13.4 系列版本升級，同時將自架 runner 的建置流程模組化，方便各後端獨立維護與觸發。

---

## 4. Metal 後端

- **`8aa161b`** — metal : fix deprecation warnings from macOS 27 SDK (#29136)
  修正在 macOS 27 SDK 下編譯出現的棄用警告。

- **`335b21f`** — ggml-metal : simplify fusion pattern op list declaration (#29206)
  簡化 fusion 模式的運算子清單宣告方式，從 `ops_all` 自動推導非空清單。

- **`fb34fc2`** — metal : fix mask bounds in flash attention block pre-pass (#29220)
  修正 flash-attention block pre-pass 階段的 mask 邊界問題。

---

## 5. SYCL 後端

- **`1aa2954`** — sycl : coalesce MKL-FA softmax loads instead of one work-item per row (#28918)
  將 MKL flash-attention 的 softmax 記憶體讀取合併存取，取代原本一行一個 work-item 的方式。

- **`af91114`** — sycl : pinned memory use right device context instead of 0 (#28895)
  修正 pinned memory 配置時錯誤使用裝置 context 0 的問題，改用正確的裝置 context。

- **`bb3c853`** — sycl : support gated DSV4_HC_PRE and optional HC_POST comb matrix (#29132)
  新增對 DSV4 架構中 gated HC_PRE 與可選 HC_POST comb matrix 的支援。

---

## 6. WebGPU 後端

- **`932a68e`** — webgpu : add fused gdn + cpy (#28976)
  在 WebGPU 後端新增 GDN（Gated Delta Net）與 CPY 的融合 kernel，與 Hexagon 後端的 GDN 優化呼應，顯示 GDN 架構效能優化正跨多個後端同步推進。

---

## 7. CPU 後端

- **`8034c1d`** — ggml-cpu: ARM Repack kernels for Q1_0 (#23492)
  新增 ARM NEON 與 I8MM 版本的 Q1_0 量化格式 repack kernel（4x4 與 4x8），提升 ARM 平台上該量化格式的推論效能。

---

## 8. ggml 核心

- **`c21284c`** — ggml : fix dimension and stride truncation in ggml_permute (#29227)
  修正 `ggml_permute` 中維度與 stride 被截斷的問題，屬於核心正確性修正，影響所有後端。

---

## 9. llama-context / 推論核心

- **`9655061`** — llama-context : report graph inputs and input tensors during sched reserve (#26625)
  在 scheduler reserve 階段新增 graph 輸入與輸入張量數量的報告，修正 token generation 圖表的 batch size 標籤錯誤（改用 `n_seqs` 而非硬編碼 1），並為未命名的輸入張量（如 KV cache index、recurrent state copy index）加上名稱，方便除錯與效能分析。

---

## 10. Server

- **`982a332`** — server : do not forward --api-key-file to router-spawned child instances (#28938)
  修正 router 模式下 `--api-key-file` 被錯誤傳遞給子行程的問題：子行程會重新以檔案金鑰驗證，導致使用 `--api-key` 的用戶端在 chat completions 收到 401；同時修正 router 內部呼叫（如 `/v1/streams/lookup`）因缺少驗證標頭而被拒絕的問題。修正了 issue #28820。

---

## 11. 命令列參數與通用程式庫

- **`e0dff58`** — args: add env vars for temperature, top-p, min-p and penalties (#27380)
  新增可透過環境變數（`LLAMA_ARG_*`）設定 `--temp`、`--top-p`、`--min-p`、`--repeat-penalty`、`--presence-penalty`、`--frequency-penalty`，方便以 systemd EnvironmentFile 等方式控制 `llama-server`。

- **`26394b4`** — json: Fixed json enum handling (#28518)
  修正 `common_json` 對 enum 型別的處理，簡化實作方式使 enum 建構子委派給底層型別的建構子。

- **`62668d6`** — convert: enable --fuse-qkv for muse-glimmer (#29203)
  為 `muse-glimmer` 模型架構在轉換腳本中啟用 `--fuse-qkv` 選項（QKV 融合）。

- **`6f41ac5`** / **`ff0dbb9`** — vendor : update cpp-httplib to 0.57.0 / 0.57.1 (#29214, #29239)
  兩次連續更新內建的 `cpp-httplib` vendor 程式庫版本（0.57.0 → 0.57.1）。

---

## 12. 測試框架改善

- **`711f60b`** — tests : remove stale comment (#29140)：移除過期註解。
- **`1d72b05`** — tests/test-backend-ops : allow regex entries in the -o filter (#29204)：`-o` 過濾參數現在支援正規表示式（如 `MUL_MAT.*`），同時保留純運算子名稱的精確比對行為。
- **`161755f`** — test-llama-archs : make tensor data stdev configurable and improve help (#29133)：讓測試資料標準差可設定，改善說明文字，新增 `--arch` regex 支援，並將部分數值比較改為 NMSE（正規化均方誤差）指標。
- **`c641dfa`** — test-save-load-state : compare logits with NMSE and feed expected tokens (#29238)：`test-save-load-state` 改用 NMSE 比較 logits，並餵入預期 token。

> **小結**：多個測試工具同步導入 NMSE 作為數值比較的標準，取代先前較嚴格的位元級比較，反映團隊在浮點誤差容忍度上的統一調整。

---

## 總結

本次涵蓋 30 個 commit，重點落在：
1. **Hexagon 後端**的大規模 DMA/記憶體基礎設施重構與 GDN 的 HMX 優化（工程量最大）；
2. **CUDA** flash-attention swizzle 重構及其編譯修正、conv2d 與轉換 kernel 加速；
3. 多後端（Metal、SYCL、WebGPU）的細部修正與 GDN 融合優化持續擴散；
4. **Server** 修正一個影響 router 模式驗證的重要 bug（#28820）；
5. 測試框架導入 NMSE 數值比較標準；
6. CI/Docker 的 CUDA 13.4 系列升級與 workflow 模組化。

沒有觀察到破壞性 API 變更或需要使用者立即應對的重大相容性問題。
