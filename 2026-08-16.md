# llama.cpp 更新報告 — 2026-08-16

- **來源倉庫**：[ggml-org/llama.cpp](https://github.com/ggml-org/llama.cpp)，分支 `master`
- **檢查區間**：`2bacf9ea5c` (2026-08-15 之前的紀錄) → `ece963f41b` (本次最新)
- **新增 commit 數**：26 筆

---

## Group 1：SYCL（Intel GPU）效能優化

在 Intel Arc GPU 上針對算子融合（operator fusion）做效能調校，主要目標是減少 kernel 啟動次數、提升 decode/prefill 速度。

- [`3d938853`](https://github.com/ggml-org/llama.cpp/commit/3d93885352a0049c8388a0da0698ec1a69e60d90) sycl: fuse the gated-delta-net state writeback cpy (#26643)
  將 gated-delta-net 的狀態寫回複製（state writeback copy）融合進單一 kernel，在 Arc Pro B70 上測得 tg128 提升約 1.1~1.2%，prefill 幾乎持平。
- [`65091386`](https://github.com/ggml-org/llama.cpp/commit/65091386227039bfb81ee3426537656e3b4a3f83) sycl: fuse mul_mat(gate) + mul_mat(up) + GLU for q4_K dense FFN (#26779)
  將 dense FFN 中的 gate/up 矩陣乘法與 GLU 激活函式融合，q4_K 模型單批推理提升 2~3%，批次推理（batched）在 B=8 時提升可達 12.4%。

## Group 2：Vulkan / Intel Xe 相容性與效能

針對 Intel Xe（coopmat1）架構的共享記憶體佈局調整，並附帶修正問題驅動版本的判斷邏輯。

- [`9b0a2ce8`](https://github.com/ggml-org/llama.cpp/commit/9b0a2ce859d3884252705e9ac7c93c98616bb238) vulkan: add SHMEM_STRIDE_PAD/APPLY_SLM_A_RESHAPE for coopmat1 on Intel Xe (#25380)
  為 coopmat1 matmul 新增共享記憶體 padding 與 SLM-A reshape 選項以提升 Intel Xe 效能，過程中也修正了 mxfp4 cacheline padding 造成的越界讀取（OOB read）問題，最終將該優化限制在 Intel Windows 驅動上啟用。
- [`7b38cb71`](https://github.com/ggml-org/llama.cpp/commit/7b38cb71b93d12f6963671795fad890692d60334) Fixed gating logic for problematic Intel driver version
- [`6b4344ec`](https://github.com/ggml-org/llama.cpp/commit/6b4344ecc7e6a493daba9681991852eb06bb3b33) fixed indent
  以上兩筆為 #25380 的後續小修正，處理已知有問題的 Intel 驅動版本的啟用判斷邏輯與程式碼縮排。

## Group 3：Server 執行緒模型重構

重新設計 server 的請求佇列（yield_to_queue）執行緒模型，讓耗時較長的 `llama_decode()` 執行期間仍能回應監控類請求。

- [`77918caf`](https://github.com/ggml-org/llama.cpp/commit/77918caf30e65f830ab0759c89779efdac28c77b) server: allow accessing /metrics and /slots during llama_decode() (#27041)
  讓 `/metrics`、`/slots` 等只讀端點在 decode 進行中也能被存取，避免長時間生成時監控 API 被阻塞。
- [`22b8e310`](https://github.com/ggml-org/llama.cpp/commit/22b8e310b921d568e013e4533002be5a8fe53f17) server: re-design yield_to_queue thread model (#27133)
  將 speculative decoding 的執行邏輯移入 worker 執行緒，並交換 worker 與主執行緒的職責分工，為上述監控端點的可用性提供架構基礎。

## Group 4：ggml 核心與遞迴狀態（SSM）

- [`1692f9e5`](https://github.com/ggml-org/llama.cpp/commit/1692f9e50bb20fd96b963af38a282daf78feea64) ggml: recurrent state rollback for ggml_ssm_scan (#26623)
  為 `ggml_ssm_scan` 新增遞迴狀態回滾（recurrent state rollback）功能，支援 CPU 與 CUDA，並支援 K > 1 的情境，主要服務 Nemotron 等混合式（hybrid）架構模型的推測解碼（speculative decoding）回滾需求。
- [`06ae2326`](https://github.com/ggml-org/llama.cpp/commit/06ae2326baa6731c2ecbe5f1f2911931f1678993) ggml: bump version to 0.20.0 (ggml/1584)
- [`9b053546`](https://github.com/ggml-org/llama.cpp/commit/9b05354ec6fb58b4e665e9a39ebc40285c015638) sync: ggml
  以上兩筆為 ggml 子專案版本號提升與同步，通常跟隨核心功能變更（如上述 SSM rollback）而來。
- [`c6f6a92c`](https://github.com/ggml-org/llama.cpp/commit/c6f6a92c553984a7f59b712d3a1694fed275e6c3) ggml: force single thread on wasi (#25686)
  在 WASI 環境下強制使用單執行緒，避免該平台缺乏完整執行緒支援所導致的問題。

## Group 5：新模型架構支援

- [`ad1de39e`](https://github.com/ggml-org/llama.cpp/commit/ad1de39e0708e3ced9c71bb3c82d93a2c046a73f) model: add Kimi-K3 text model (#26185)
  新增 Kimi-K3（2.8T 參數）文字模型支援，架構為混合式 KDA（線性）+ MLA（全注意力），並帶有跨層殘差注意力、latent MoE、situ activation、MLA output gate 等五項客製設計；同時支援 MXFP4 量化張量的位元相容直接重打包（無需經過 bf16 還原），並新增對應的 chat template 與工具呼叫解析。此為本次更新中變更規模最大的一筆。
- [`16d222fc`](https://github.com/ggml-org/llama.cpp/commit/16d222fc5ead59d20039501a37251c9ed457a454) model: add support for MiniMaxText01ForCausalLM and MiniMaxM1ForCausalLM (#27018)
  新增 MiniMax-Text-01 / MiniMax-M1 模型支援，處理其特有的 embedding 高位 token 零值輸出、遞迴狀態管理（`build_rs()`）、以及 decay tensor 的位置計算等細節。

## Group 6：Chat 樣板 / Jinja 引擎

- [`7e4c0a96`](https://github.com/ggml-org/llama.cpp/commit/7e4c0a96880dae4fc4268ad441f8a6446bd5460a) chat: pass reasoning_effort to template
  將 OpenAI Chat Completions 的 `reasoning_effort` 參數帶入 jinja 樣板渲染流程，讓支援該參數的模型樣板可依模型客製轉換。
- [`9e40df63`](https://github.com/ggml-org/llama.cpp/commit/9e40df63ba151d771d8b247ac4011cf203337e99) jinja: fix quadratic cost in gather_string_parts (#27034)
  修正 jinja 引擎 `gather_string_parts` 函式中的 O(n²) 效能問題，避免長對話樣板渲染時效能急劇下降。

## Group 7：多模態（mtmd）修正

- [`6fed9f6f`](https://github.com/ggml-org/llama.cpp/commit/6fed9f6ff7a603b124cb8c5864fca6ea879f9f99) mtmd, common: various fixes (#27071)
  多模態與共用元件的一系列小修正。
- [`9d57ce45`](https://github.com/ggml-org/llama.cpp/commit/9d57ce456c94d241dde672b2db9cf18879766568) mtmd: fix Granite4 Vision image sequence assembly (#26653)
  修正 Granite4 Vision 影像序列組裝邏輯，包含縮放後影像寬高在 unpad 前的截斷問題，並移除除錯用的暫存程式碼。

## Group 8：模型載入與命令列參數

- [`0177dcc7`](https://github.com/ggml-org/llama.cpp/commit/0177dcc7300bad8914bb838baabce87899812491) common: migrate the deprecated --mmap/--no-mmap to --load-mode (#26934)
  將已棄用的 `--mmap`、`--no-mmap`、`--mlock`、`--direct-io` 參數統一遷移為 `--load-mode`，並更新腳本、範例與文件。
- [`5f754ea0`](https://github.com/ggml-org/llama.cpp/commit/5f754ea0e2fd21e1213db7ebebfd65d938d9d69c) common: support --models-dir loading MTP assistant models (#24431)
  讓 `--models-dir` 也能載入 MTP（多 token 預測）輔助模型，取代原本僅支援 eagle3 的限制。

## Group 9：相依套件更新與資安/穩定性修正

- [`27df9199`](https://github.com/ggml-org/llama.cpp/commit/27df9199d16c393868d20b3ed28b4f72e30de6bd) fix: check gguf array type before reading (#27075)
  讀取 GGUF 陣列前先驗證型別，避免解析不受信任模型檔案時發生型別錯誤或崩潰。
- [`77140d24`](https://github.com/ggml-org/llama.cpp/commit/77140d247ca345a4ed7e8e38802f6d320fb3dc92) vendor: update cpp-httplib to 0.53.1 (#27103)
- [`adb55e51`](https://github.com/ggml-org/llama.cpp/commit/adb55e5148dc93bcdca7212a2d1df3ccc422959a) vendor: update BoringSSL to 0.20260813.0 (#27099)
  更新內建的 HTTP／TLS 相依套件版本，屬例行性資安更新。

## Group 10：測試與文件清理

- [`885c5bbe`](https://github.com/ggml-org/llama.cpp/commit/885c5bbe8e04dc78db25beb911a2715312ad7b54) tests: replace personal home directory paths with generic placeholders (#27043)
  將測試/文件中殘留的開發者個人家目錄路徑替換為通用佔位路徑，避免洩露個人資訊。

## Group 11：WebUI 修正

- [`0d9ceae1`](https://github.com/ggml-org/llama.cpp/commit/0d9ceae1e38291035605613ab41a8f5e693d6fcd) ui: read structuredContent from MCP tool result when content is empty (#26691)
  當 MCP 工具回傳結果的 `content` 為空時，改讀取 `structuredContent`，避免 UI 顯示空白結果。
- [`ece963f4`](https://github.com/ggml-org/llama.cpp/commit/ece963f41b0b02d7a0d61436ae365762c073a4c8) ui: mask API Key field in settings and error splash to stop browser autofill (#26562)
  遮蔽設定頁與錯誤畫面中的 API Key 欄位，並設定 `autocomplete=new-password`，防止瀏覽器自動填入/儲存 API Key 造成資安風險。

## Group 12：其他裝置支援

- [`4c1a0af4`](https://github.com/ggml-org/llama.cpp/commit/4c1a0af40d88c7fbb3b15c85bf2e8016d1d5b64c) llama: allow virtual igpu devices (#26953)
  允許虛擬整合顯示卡（virtual iGPU）裝置被 llama.cpp 辨識與使用。

---

## 本次重點摘要

- 最重磅變更為新增 **Kimi-K3**（2.8T 參數混合線性/MLA 注意力）與 **MiniMax-Text-01 / MiniMax-M1** 模型架構支援。
- Server 端進行了 **執行緒模型重構**，讓 `/metrics`、`/slots` 監控端點在長時間 decode 期間仍可存取。
- **SYCL（Intel GPU）與 Vulkan（Intel Xe）** 均有針對性效能優化與相容性修正。
- 安全性相關：WebUI 的 API Key 欄位遮蔽、GGUF 陣列型別檢查、TLS/HTTP 相依套件版本更新。
- ggml 核心新增 SSM（狀態空間模型）遞迴狀態回滾支援，並隨之同步升版。
