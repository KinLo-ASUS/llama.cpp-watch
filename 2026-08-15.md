# llama.cpp 更新報告（2026-08-15）

- 監控倉庫：[ggml-org/llama.cpp](https://github.com/ggml-org/llama.cpp)（`master` 分支）
- 檢查區間：`a94d563ed8`（2026-08-14，上次記錄）→ `2bacf9ea5c`（2026-08-14，本次最新）
- 新增 commit 數：**15**

---

## 一、新模型支援：MiniMax 系列（1 個 commit）

| Commit | PR | 摘要 |
| --- | --- | --- |
| `16d222fc` | [#27018](https://github.com/ggml-org/llama.cpp/pull/27018) | model：新增 `MiniMaxText01ForCausalLM` 與 `MiniMaxM1ForCausalLM` 架構支援。內容包含新的 recurrent-state（`build_rs()`）圖形建構、以 token 位置計算 decay 張量、以及在轉換階段抑制 embedding 為零值的 token（避免其干擾取樣），並新增對應的 jinja 對話模板與轉換/測試程式碼。屬於本次較大型的新功能提交。 |

---

## 二、多模態（mtmd）修正（2 個 commit）

| Commit | PR | 摘要 |
| --- | --- | --- |
| `9d57ce45` | [#26653](https://github.com/ggml-org/llama.cpp/pull/26653) | mtmd：修正 Granite4 Vision 的影像序列（grid）組裝邏輯，修正縮放後影像高寬在 unpad 前的截斷計算錯誤，並移除除錯用的 `MTMD_DUMP_EMBD` 程式碼。 |
| `6fed9f6f` | [#27071](https://github.com/ggml-org/llama.cpp/pull/27071) | mtmd, common：一系列雜項修正，涵蓋 `clip.cpp`、`mtmd-image.cpp`、`mtmd-helper-common.h` 等檔案，並小幅調整 `dflash`、`minimax-m3` 模型與 `common/arg.cpp` 的引數處理。 |

---

## 三、SYCL 後端效能優化（2 個 commit）

針對 Intel GPU（SYCL）後端的運算融合（fusion）優化。

| Commit | PR | 摘要 |
| --- | --- | --- |
| `3d938853` | [#26643](https://github.com/ggml-org/llama.cpp/pull/26643) | sycl：融合 gated-delta-net 狀態寫回（writeback）的 cpy 運算（移植自 #23940）。Arc Pro B70 上量測 tg128 約提升 1.1–1.2%，pp2048 大致持平。 |
| `65091386` | [#26779](https://github.com/ggml-org/llama.cpp/pull/26779) | sycl：針對 q4_K dense FFN 融合 `mul_mat(gate) + mul_mat(up) + GLU`。Arc Pro B70 上單批（B=1）tg128 提升約 2.8–3.4%，隨批次大小增加效益更明顯（B=8 時提升約 12.4%）。 |

---

## 四、SSM／Recurrent 狀態管理（1 個 commit）

| Commit | PR | 摘要 |
| --- | --- | --- |
| `1692f9e5` | [#26623](https://github.com/ggml-org/llama.cpp/pull/26623) | ggml：為 `ggml_ssm_scan` 新增 recurrent state rollback（RS-rollback）支援，橫跨 CPU、CUDA、Metal、SYCL 及 ET 後端；並支援 `K > 1` 情境，對不支援的後端會自動 fallback 至 CPU。主要用於支援 Nemotron 等具狀態回溯需求的模型。 |

---

## 五、Server 端改進（1 個 commit）

| Commit | PR | 摘要 |
| --- | --- | --- |
| `77918caf` | [#27041](https://github.com/ggml-org/llama.cpp/pull/27041) | server：重構請求佇列（`server-queue`），讓 `/metrics` 與 `/slots` 端點能在 `llama_decode()` 執行期間仍可被存取，並同步處理 mtmd chunk 的情況，避免長時間解碼時監控端點被阻塞。 |

---

## 六、Chat／Jinja 模板相關（2 個 commit）

| Commit | PR | 摘要 |
| --- | --- | --- |
| `7e4c0a96` | （無對應 PR 編號） | chat：將 OpenAI Chat Completions 的 `reasoning_effort` 參數傳遞給 jinja 對話模板（依模型做對應轉換），並讓 Responses API 的 `reasoning.effort` 也能正確轉換為 `reasoning_effort`。 |
| `9e40df63` | [#27034](https://github.com/ggml-org/llama.cpp/pull/27034) | jinja：修正 `gather_string_parts` 中的二次方（quadratic）時間複雜度問題，避免在字串拼接量大時效能急遽下降。 |

---

## 七、雜項修正與維護（4 個 commit）

| Commit | PR | 摘要 |
| --- | --- | --- |
| `2bacf9ea` | [#27013](https://github.com/ggml-org/llama.cpp/pull/27013) | dflash：釐清 `target_layer_ids` 的輸出日誌訊息，使其更易讀。 |
| `c6f6a92c` | [#25686](https://github.com/ggml-org/llama.cpp/pull/25686) | ggml：在 WASI 環境下強制使用單執行緒，避免多執行緒在該環境下的相容性問題。 |
| `885c5bbe` | [#27043](https://github.com/ggml-org/llama.cpp/pull/27043) | tests：將測試與文件中殘留的個人主機目錄路徑替換為通用佔位字串，避免洩漏開發者本機路徑資訊。 |
| `4c1a0af4` | [#26953](https://github.com/ggml-org/llama.cpp/pull/26953) | llama：允許辨識虛擬 iGPU 裝置，避免此類裝置被誤判排除。 |

---

## 八、版本／同步維護（2 個 commit）

| Commit | PR | 摘要 |
| --- | --- | --- |
| `06ae2326` | ggml/1584 | ggml：版本號提升至 0.20.0。 |
| `9b05354e` | （sync） | sync：同步上游 `ggml` 子模組版本紀錄（`scripts/sync-ggml.last`）。 |

---

## 備註

- 本次檢查未涉及對 `ggml-org/llama.cpp` 的原始碼修改，僅為變更追蹤與摘要整理。
- 最新已檢查 commit：`2bacf9ea5cf9e191430d4c786202d5984df7c193`（2026-08-14, 「dflash : clarify output logging of target_layer_ids (#27013)」）。
