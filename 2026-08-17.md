# llama.cpp 更新報告 — 2026-08-17

- **來源倉庫**：[ggml-org/llama.cpp](https://github.com/ggml-org/llama.cpp)，分支 `master`
- **檢查區間**：`ece963f41b`（2026-08-16 之前的紀錄）→ `4df29be4f`（本次最新）
- **新增 commit 數**：4 筆

---

## Group 1：安全性修正 — LoRA 檔案邊界檢查

- [`10bf611e`](https://github.com/ggml-org/llama.cpp/commit/10bf611e533d81f739128304991c5e133c6aebd8) llama : check LoRA tensor data is within file bounds (#27056)
  在讀取 LoRA adapter 的張量資料前，新增檔案邊界檢查（含整數溢位保護），若計算出的偏移量加大小超出檔案實際大小，會拋出例外並提示「檔案已損毀或不完整」，而不是直接讀取超出範圍的記憶體區域。修正了載入不受信任或損毀的 LoRA 檔案時可能造成越界讀取的問題。

## Group 2：Chat 樣板 / Jinja 能力偵測重構

- [`b94041a9`](https://github.com/ggml-org/llama.cpp/commit/b94041a98ec86e29e63b02decd29175d6820b2a1) chat: refactor handling supports_string_content / supports_typed_content (#27130)
  重構 `supports_string_content` 與 `supports_typed_content` 兩項聊天樣板能力的偵測邏輯，新增 `messages_inp_normalizer` 以統一訊息輸入正規化流程，並補上對應的 jinja 測試案例（含 skip 標記），使樣板能力判斷更準確、減少誤判造成的訊息格式錯誤。

## Group 3：模型程式碼清理

- [`3cb7ffb1`](https://github.com/ggml-org/llama.cpp/commit/3cb7ffb1a1f612d5e4a46244ae5a3c77ad934a70) model : remove some ggml_concat (#27176)
  移除 `deepseek32.cpp` 與 `glm-dsa.cpp` 中原本用 `ggml_concat` 拼接張量的作法，改用更直接的實作方式，簡化程式碼並減少不必要的張量複製/串接運算。

## Group 4：CI 工具鏈修正

- [`4df29be4`](https://github.com/ggml-org/llama.cpp/commit/4df29be4f4c3673f428170fda944a5b19f743bb8) ci : fix dry-run reporting in make-release job [no ci] (#27167)
  修正 `make-release` CI 任務在使用 `--dry-run` 模式時，即使某些檢查步驟失敗，仍會被錯誤回報為「全部通過」的問題。

---

## 本次重點摘要

- 本次更新量較小，僅 4 筆 commit，涵蓋範圍分散於安全性、聊天樣板、模型程式碼與 CI 腳本四個獨立主題，彼此無直接關聯。
- 安全性重點：LoRA adapter 載入流程新增檔案邊界檢查，防止讀取損毀或惡意構造的 LoRA 檔案時發生越界讀取。
- Chat 子系統針對樣板能力偵測（string/typed content）進行重構，提升多樣板相容性判斷的準確度。
- 其餘為 DeepSeek-V3.2 / GLM-DSA 模型程式碼清理與 CI dry-run 回報邏輯修正，均屬例行性維護。
