# llama.cpp 變更報告 (2026-08-12)

- 追蹤倉庫：[ggml-org/llama.cpp](https://github.com/ggml-org/llama.cpp)（`master` 分支）
- 檢查區間：`030ebb558`（2026-08-10）→ `0b1bad14f`（2026-08-12）
- 本次新增 commit 數：**22**

---

## 分組摘要

### 一、模型架構與模型支援（4 筆）

- `14e78ddef` — **model : fix SWA not being enabled for EXAONE 4.5** ([#26848](https://github.com/ggml-org/llama.cpp/pull/26848))
  修正 EXAONE 4.5 因帶有 MTP head（block_count=65、nextn=1）導致 `load_arch_hparams` 在讀取 nextn 層數之前就以 `n_layer() == 64` 判斷架構，誤判層數而整段跳過 SWA（sliding window attention）設定的問題；官方 LGAI-EXAONE GGUF 亦受影響，EXAONE 4.0（無 MTP head）不受影響。
- `6e62ba538` — **mtmd: support pocket-tts** ([#26871](https://github.com/ggml-org/llama.cpp/pull/26871))
  新增 Pocket-TTS 語音合成模型支援：將深度可分離轉置卷積（transposed convolution）重新以 GEMM + col2im 實作，CUDA 生成速度提升約 80%、CPU 提升約 50%，並支援各語言包（如 french_24l、english_2026-01）各自的 EOS 尾端補幀數與短文字補白設定。
- `cc078b45b` — **Dflash support for nemotron-3.5** ([#26905](https://github.com/ggml-org/llama.cpp/pull/26905))
  為 Nemotron-3.5 加入 DFlash（推測解碼 draft head）支援，並新增 NVFP4 量化格式支援；轉換階段會略過未訓練的 DFlash 嵌入。
- `5d16e81dd` — **convert : keep quantization scales for nemotron --mtp export** ([#26903](https://github.com/ggml-org/llama.cpp/pull/26903))
  修正 Nemotron `--mtp` 匯出流程中量化 scale 遺失的問題。

### 二、對話樣板與工具呼叫解析修正（2 筆）

- `ba360efe1` — **chat : tighten bare function parsing for Qwen models** ([#26793](https://github.com/ggml-org/llama.cpp/pull/26793))
  收緊 Qwen 系列模型「裸函式呼叫（bare function call）」的解析規則，降低誤判機率。
- `0b1bad14f` — **chat : fix muse-glimmer detection of tool calls after EOM** ([#26879](https://github.com/ggml-org/llama.cpp/pull/26879))
  修正 Muse Glimmer 樣板在同一輪回覆中先以文字回答、再呼叫工具（以 `<|eom|>` 分隔而非 `<|eot|>`）時，解析器誤將工具呼叫語法整段吞入 content、導致工具未被呼叫的問題（tau2-bench 電信情境測試中曾於 114 個任務中 19 個、共 43 輪受影響）；新增對應 jinja 樣板與 4 組解析測試。

### 三、推理核心／後端效能與正確性修正（4 筆）

- `84f712946` — **ggml-webgpu: fix CI errors from #25025 and #25262** ([#26566](https://github.com/ggml-org/llama.cpp/pull/26566))
  修正 WebGPU 後端 CI 錯誤：於 `max_kv_tile == 0` 時停用 subgroup matrices，並為 `cpy` 運算新增 i32 型別支援。
- `153d324bc` — **llama: add default load-mode auto, which avoids mmap on iGPUs** ([#26081](https://github.com/ggml-org/llama.cpp/pull/26081))
  新增預設載入模式 `auto`：除 Metal 以外的整合式 GPU（iGPU）環境下自動避免使用 mmap 載入模型，涵蓋 CUDA/HIP/Vulkan/OpenCL/SYCL/CANN 等各後端與 `llama-bench` 的對應調整。
- `598863317` — **cuda : add warp-per-row wkv7 kernel for single-token decode** ([#26111](https://github.com/ggml-org/llama.cpp/pull/26111))
  新增 warp-per-row 版本的 WKV7 CUDA kernel，加速單 token 解碼情境下的運算。
- `ebb546b7e` — **CUDA: only disable CUDA graphs when mul_mat_id actually needs a stream sync** ([#26802](https://github.com/ggml-org/llama.cpp/pull/26802))
  修正邏輯，僅在 `mul_mat_id` 確實需要 stream 同步時才停用 CUDA graphs，避免不必要地全面停用 CUDA graph 加速。

### 四、測試與 CI 系統調整（6 筆）

- `4801e3c56` — **tests : disable backend sampler hip multi output** ([#26878](https://github.com/ggml-org/llama.cpp/pull/26878))
  將新增的 `multi_output_sampling_chain`／`multi_output_cpu` 測試加入 HIP 平台跳過清單（因 `top_k` 的 backend probs 路徑需要 CUB，HIP 上不可用），並改善 gpu-rocm self-hosted runner 的 log 保存方式（依 GitHub run id 分資料夾，避免被下一輪覆蓋）。
- `b3df57286` — **tests : clean-up server test, use `tests.sh` in ci** ([#26886](https://github.com/ggml-org/llama.cpp/pull/26886))
  移除 `fetch_server_test_models.py`，CI 改用 `tests.sh` 包裝 pytest 執行 server 測試。
- `9afff1b74` — **tests : fix running server tests on windows** ([#26889](https://github.com/ggml-org/llama.cpp/pull/26889))
  修正 Windows 平台上執行 server 測試的問題。
- `704485942` — **ci: hip-quality-check: update vgpr spill ignore list** ([#26859](https://github.com/ggml-org/llama.cpp/pull/26859))
  更新 HIP VGPR spill 檢查的忽略清單，反映近期 mmq 參數重構後符號名稱的變化。
- `70dfba5ae` — **ci : add windows-rocm to check-release** ([#26897](https://github.com/ggml-org/llama.cpp/pull/26897))
  將 windows-rocm 納入 check-release 工作流程檢查範圍。
- `7b13a8404` — **ci : add missing release check** ([#26923](https://github.com/ggml-org/llama.cpp/pull/26923))
  補上發布（release）流程中缺失的檢查項目。

### 五、模型轉換與量化工具（3 筆）

- `1138b851f` — **model-conversion : use save_output_data for causual embeddings** ([#26890](https://github.com/ggml-org/llama.cpp/pull/26890))
  causal 模型 embedding 驗證腳本改用 `save_output_data`，同時輸出 token id 與 prompt 檔案，修復 `causal-verify-embeddings` 目標因缺少 `-prompt.txt`／`-tokens.bin` 而失敗的問題。
- `38406d597` — **imatrix.cpp: Move finite check and only check touched experts** ([#26861](https://github.com/ggml-org/llama.cpp/pull/26861))
  調整 imatrix 計算中有限值（finite）檢查的時機，並改為只檢查實際被觸發（touched）的 MoE 專家，降低不必要的檢查開銷。
- `2468576f2` — **requirements: use stable torch packages on s390x** ([#26864](https://github.com/ggml-org/llama.cpp/pull/26864))
  在 s390x 架構上改用穩定版 torch 套件，並簡化多個 requirements 檔案中的相依設定。

### 六、文法解析器修正（1 筆）

- `48d22e295` — **common/peg : suppress incomplete escape sequences** ([#26780](https://github.com/ggml-org/llama.cpp/pull/26780))
  修正 PEG 文法解析器對不完整跳脫序列（incomplete escape sequence）的處理方式。

### 七、Web UI 與範例調整（2 筆）

- `8d274dd7c` — **ui: fix context gauge for single-model usage** ([#25738](https://github.com/ggml-org/llama.cpp/pull/25738))
  修正單模型模式下 context gauge（上下文使用量表）顯示問題：單模型模式隱藏已載入模型清單，並讓 context gauge 詳細面板的展開狀態能在重新開啟後維持。
- `f785fc9ea` — **spec : update speculative-simple** ([#26904](https://github.com/ggml-org/llama.cpp/pull/26904))
  重構並簡化 `speculative-simple` 範例程式與 `common/speculative.cpp/h`，同時更新對應文件。

---

## Commit 逐條列表（依時間先後）

| Commit | 說明 | PR |
|---|---|---|
| `84f712946` | ggml-webgpu: fix CI errors from #25025 and #25262 | [#26566](https://github.com/ggml-org/llama.cpp/pull/26566) |
| `48d22e295` | common/peg : suppress incomplete escape sequences | [#26780](https://github.com/ggml-org/llama.cpp/pull/26780) |
| `14e78ddef` | model : fix SWA not being enabled for EXAONE 4.5 | [#26848](https://github.com/ggml-org/llama.cpp/pull/26848) |
| `4801e3c56` | tests : disable backend sampler hip multi output | [#26878](https://github.com/ggml-org/llama.cpp/pull/26878) |
| `b3df57286` | tests : clean-up server test, use `tests.sh` in ci | [#26886](https://github.com/ggml-org/llama.cpp/pull/26886) |
| `153d324bc` | llama: add default load-mode auto, which avoids mmap on iGPUs | [#26081](https://github.com/ggml-org/llama.cpp/pull/26081) |
| `9afff1b74` | tests : fix running server tests on windows | [#26889](https://github.com/ggml-org/llama.cpp/pull/26889) |
| `1138b851f` | model-conversion : use save_output_data for causual embeddings | [#26890](https://github.com/ggml-org/llama.cpp/pull/26890) |
| `704485942` | ci: hip-quality-check: update vgpr spill ignore list | [#26859](https://github.com/ggml-org/llama.cpp/pull/26859) |
| `8d274dd7c` | ui: fix context gauge for single-model usage | [#25738](https://github.com/ggml-org/llama.cpp/pull/25738) |
| `6e62ba538` | mtmd: support pocket-tts | [#26871](https://github.com/ggml-org/llama.cpp/pull/26871) |
| `cc078b45b` | Dflash support for nemotron-3.5 | [#26905](https://github.com/ggml-org/llama.cpp/pull/26905) |
| `5d16e81dd` | convert : keep quantization scales for nemotron --mtp export | [#26903](https://github.com/ggml-org/llama.cpp/pull/26903) |
| `2468576f2` | requirements: use stable torch packages on s390x | [#26864](https://github.com/ggml-org/llama.cpp/pull/26864) |
| `38406d597` | imatrix.cpp: Move finite check and only check touched experts | [#26861](https://github.com/ggml-org/llama.cpp/pull/26861) |
| `70dfba5ae` | ci : add windows-rocm to check-release | [#26897](https://github.com/ggml-org/llama.cpp/pull/26897) |
| `ba360efe1` | chat : tighten bare function parsing for Qwen models | [#26793](https://github.com/ggml-org/llama.cpp/pull/26793) |
| `f785fc9ea` | spec : update speculative-simple | [#26904](https://github.com/ggml-org/llama.cpp/pull/26904) |
| `598863317` | cuda : add warp-per-row wkv7 kernel for single-token decode | [#26111](https://github.com/ggml-org/llama.cpp/pull/26111) |
| `ebb546b7e` | CUDA: only disable CUDA graphs when mul_mat_id actually needs a stream sync | [#26802](https://github.com/ggml-org/llama.cpp/pull/26802) |
| `7b13a8404` | ci : add missing release check | [#26923](https://github.com/ggml-org/llama.cpp/pull/26923) |
| `0b1bad14f` | chat : fix muse-glimmer detection of tool calls after EOM | [#26879](https://github.com/ggml-org/llama.cpp/pull/26879) |

---

*本報告由自動化監控流程產生。*
