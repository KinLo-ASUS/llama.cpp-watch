# llama.cpp 每日變更報告 - 2026-07-23

- **檢查範圍**：`ggml-org/llama.cpp` master 分支
- **上次檢查 commit**：`66e4bf7` (2026-07-21)
- **本次最新 commit**：`4310aa4` (2026-07-22)
- **新增 commit 數量**：15

---

## 分組摘要

### 1. DeepSeek4 模型相關修正
新模型 DeepSeek4 (DS4) 上線後的一系列 bug 修正，涵蓋聊天樣板、推理保留變數與張量運算型別。

- **`f534da2`** Fix DeepSeek4 crafted template (#25414) — @pwilkin
  修正 DS4 的 chat template 使其明確遵循參考行為，新增 `drop_reasoning` 旗標支援，並讓 DS3.2 的 parser 同時適用於 DS4，另修正 tool result 排序問題。
- **`67b9b0e`** llama-arch: fix DeepSeek4 APE tensor op (#25945) — @helanfxz
  修正 DS4 的兩個 APE（Attention Position Encoding）張量運算，由錯誤的 `GGML_OP_ADD` 改為正確的 `GGML_OP_GET_ROWS`。
- **`e8e6c7a`** minor: fix reasoning preserve var for DS4 [no ci] (#25999) — @pwilkin
  將 `caps` 函式中的變數名稱由 `drop_reasoning` 統一改為 `drop_thinking`，與其他推理保留邏輯命名一致。

### 2. 新模型架構支援：Laguna
- **`1f66c3c`** Add support for Laguna XS.2 & M.1 (#25165) — @joerowell
  新增對 poolside Laguna 系列模型（S-2.1 / XS-2.1 / XS.2）的支援，包含轉換腳本 (`conversion/laguna.py`)、GGUF 常數與張量對應、新的 chat auto-parser/diff-analyzer 模組，以及對應的架構與詞彙表程式碼。屬於較大型的新架構整合。

### 3. WebUI 前端改進
- **`cf51256`** ui: Add a "Default" option for the reasoning selector (#25846) — @ServeurpersoCom
  新增「Default」推理選項，讓伺服器自行決定是否啟用思考模式（修正過去 WebUI 強制送出 `enable_thinking` 導致部分模型如 Gemma 4 E4B 行為異常的問題），同時整理推理選單的下拉互動與移除死程式碼。
- **`61328e6`** feat(ui): add symbolic math support to JS sandbox via nerdamer (#25948) — @rankaiyx, @ServeurpersoCom
  在 WebUI 的 JS 沙盒中整合 nerdamer 符號運算引擎（simplify/expand/factor/diff/integrate/solve 等），並加入 CSP 阻擋沙盒對外連線、支援使用者切換與快取。

### 4. CUDA 後端改進
- **`1a064ab`** CUDA: Improve NVFP4 W4A4 activation quantization (#25730) — @ORippler
  優化 NVFP4 W4A4 的 activation 量化流程，包含 32-byte 讀取、per-block amax、融合量化 kernel 及相關效能調校，並修正 HIP 平台上的對齊問題。
- **`c5a4a0b`** cuda: GET_ROWS quants (#25962) — @ServeurpersoCom
  補齊 CUDA `GET_ROWS` 對所有量化型別（k-quants、i-quants、mxfp4）的支援，避免 embedding 查表在單裝置圖中退回主機端逐 token 複製，大幅提升相關情境效能。

### 5. 其他後端更新（Hexagon / WebGPU / PowerPC）
- **`0278d83`** hexagon: activation ops update (#25974) — @tboinovski1, @max-krasnyansky
  重構 Hexagon 後端的 activation 運算，新增 all-in-one geglu microkernel、支援非連續 tensor 的 strided DMA，並將 SiLU/GELU 移至 unary-ops。
- **`7347430`** webgpu: add CONV_2D_DW (depthwise conv2d) kernel (#25847) — @m1el
  為 WebGPU 後端新增 depthwise Conv2D 運算，移植自 Vulkan 版本的實作。
- **`3ce7da2`** ggml: enable PowerPC backend variants on AIX (#25983) — @shalinib-ibm
  讓 PowerPC CPU 後端可在 AIX 平台上建置，並修正一處缺少分號的問題。

### 6. CI / 專案治理
- **`b4d6c7d`** ci: fix SYCL package shared library lookup (#25987) — @KyleHagy
  修正 SYCL 發布包的共享函式庫路徑設定（加入 `$ORIGIN` RPATH）。
- **`4310aa4`** contrib: allow all AI-generated code in general (#26012) — @ngxson
  更新專案的 AI 生成程式碼貢獻政策，由「禁止大量／完全 AI 生成」改為「有條件允許」，但要求貢獻者對每一行程式碼負完全責任並能向審查者解釋。

### 7. 推理與多模態功能
- **`6d5a910`** common: infer the speculative type from the draft repo sidecars (#25989) — @ServeurpersoCom
  當使用 `-hfd` 指定的草稿模型 repo 內含 mtp-/dflash-/eagle3- sidecar 而未指定 `--spec-type` 時，自動依優先順序推斷正確的推測解碼類型，避免誤用完整模型當草稿。
- **`e0833bf`** mtmd: use RAII for setting and resetting non-causal attention (#25723) — @nikwen
  將多模態 (mtmd) 中設定/還原 non-causal attention 的邏輯改為 RAII 管理，簡化程式碼並降低忘記還原狀態的風險。

---

## 完整 Commit 列表

| SHA | 標題 | 作者 |
|---|---|---|
| `4310aa4` | contrib: allow all AI-generated code in general (#26012) | ngxson |
| `cf51256` | ui: Add a "Default" option for the reasoning selector (#25846) | ServeurpersoCom |
| `1a064ab` | CUDA: Improve NVFP4 W4A4 activation quantization (#25730) | ORippler |
| `0278d83` | hexagon: activation ops update (#25974) | tboinovski1, max-krasnyansky |
| `e0833bf` | mtmd: use RAII for setting and resetting non-causal attention (#25723) | nikwen |
| `61328e6` | feat(ui): add symbolic math support to JS sandbox via nerdamer (#25948) | rankaiyx, ServeurpersoCom |
| `e8e6c7a` | minor: fix reasoning preserve var for DS4 [no ci] (#25999) | pwilkin |
| `6d5a910` | common: infer the speculative type from the draft repo sidecars (#25989) | ServeurpersoCom |
| `f534da2` | Fix DeepSeek4 crafted template (#25414) | pwilkin |
| `3ce7da2` | ggml: enable PowerPC backend variants on AIX (#25983) | shalinib-ibm |
| `b4d6c7d` | ci: fix SYCL package shared library lookup (#25987) | KyleHagy |
| `7347430` | webgpu: add CONV_2D_DW (depthwise conv2d) kernel (#25847) | m1el |
| `c5a4a0b` | cuda: GET_ROWS quants (#25962) | ServeurpersoCom |
| `67b9b0e` | llama-arch: fix DeepSeek4 APE tensor op (#25945) | helanfxz |
| `1f66c3c` | Add support for Laguna XS.2 & M.1 (#25165) | joerowell |

---

*報告由自動化監控流程產生。*
