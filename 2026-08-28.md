# llama.cpp 變更報告 — 2026-08-28

- 檢查範圍：`11cd988`（2026-08-26 之前）→ `ca3d5a3`（2026-08-28）
- 新增 commit 數：33 筆
- 資料來源：`ggml-org/llama.cpp` master 分支

---

## 1. WebUI（tools/ui）改版

這組 4 個 commit 都是同一位作者針對內建 Web UI 的一系列重構，主要把設定頁、MCP 伺服器管理、工具授權方式改為以對話框（Dialog）為主的介面，並優化聊天輸入區的操作體驗。

- **[#27743](https://github.com/ggml-org/llama.cpp/commit/0379a19) ui: Update Dialog component styling**
  調整 Dialog（對話框）元件的樣式，包含關閉按鈕、內容區與標題列的間距/外觀微調，為後續把設定頁改成對話框做準備。
- **[#27744](https://github.com/ggml-org/llama.cpp/commit/539f245) ui: Move Settings and MCP Servers routes to dialog-based views**
  把原本獨立路由的「設定」與「MCP Servers」頁面改為對話框呈現，移除舊的路由與導覽邏輯，簡化了側邊欄與設定頁的程式碼結構。
- **[#27745](https://github.com/ggml-org/llama.cpp/commit/fe235f4) ui: Replace per-conversation MCP overrides with per-conversation tool policy**
  將「每個對話可覆寫 MCP 設定」的機制，改為更通用的「每個對話的工具（tool）授權政策」，同時提供舊資料的遷移邏輯（migration.service.ts），並移除舊的 MCP override 測試。
- **[#27746](https://github.com/ggml-org/llama.cpp/commit/cae6357) ui: Improve Chat Form Actions UI/UX (models selector, add panel)**
  改善聊天輸入區的模型選擇器與「新增」面板的互動與外觀，包括模型 ID 顯示、Modality 徽章與推理（reasoning）選單的呈現方式。

## 2. CI / 發布流程調整

這組 4 個 commit 都是針對 GitHub Actions CI 與 release 流程的維運性調整，多與 Windows ROCm 與 UI 打包相關。

- **[#27728](https://github.com/ggml-org/llama.cpp/commit/da9b5d6) ci: make cache bucket public**
  將 CUDA 建置用的 ccache bucket 改為公開存取，調整對應的 workflow 設定。
- **[#27706](https://github.com/ggml-org/llama.cpp/commit/4d19b28) ci: Clean up UI builds from releases**
  清理 release workflow 中與 UI 建置相關的重複/過時步驟，簡化 release.yml 與相關 UI build workflow。
- **[#26973](https://github.com/ggml-org/llama.cpp/commit/6fdd0ac) ci: bundle HIP runtime DLLs with Windows ROCm release**
  在 Windows ROCm 版本的發布包中，額外打包必要的 HIP 執行期 DLL，避免使用者缺少執行環境。
- **[#27753](https://github.com/ggml-org/llama.cpp/commit/3217633) ci: build only the ggml-hip backend for windows-rocm release**
  將 Windows ROCm release 建置範圍限縮為只編譯 `ggml-hip` backend，減少不必要的建置時間。

## 3. Hexagon / HTP（Qualcomm NPU）後端

- **[#26501](https://github.com/ggml-org/llama.cpp/commit/192067b) hexagon: support for multi-NPU devices (IQ9, IQ10) and fully asynchronous backend**（大型變更，44 個檔案）
  為 Hexagon backend 新增對多 NPU 裝置（IQ9、IQ10）的支援，並將整個 backend 改為完全非同步執行模式；同時大幅重寫 matmul/get-rows/set-rows 等運算子，新增 `allreduce-ops`，並將原有的 shell/PowerShell 腳本統一改寫為 Python（`scripts/snapdragon/build.py`、`run.py`）。
- **[#27786](https://github.com/ggml-org/llama.cpp/commit/e70802a) ggml-hexagon: add HTP unary ops for ABS and LOG**
  在 HTP（Hexagon Tensor Processor）新增 `ABS` 與 `LOG` 兩個單元運算子的實作。
- **[#27798](https://github.com/ggml-org/llama.cpp/commit/83d855c) hex-unary: fix RMS_NORM_MUL weight-offset bugs for grouped/broadcast norms**
  修正 `RMS_NORM_MUL` 在分組（grouped）或廣播（broadcast）情境下權重偏移計算錯誤的 bug。

## 4. Vulkan 後端

- **[#27216](https://github.com/ggml-org/llama.cpp/commit/bf94216) Implemented vulkan cross_entropy_loss and cross_entropy_loss_back**
  為 Vulkan backend 新增 `cross_entropy_loss` 與其反向傳播 `cross_entropy_loss_back` 運算子（含新的 GLSL compute shader），主要用於訓練/微調場景。
- **[#27726](https://github.com/ggml-org/llama.cpp/commit/5e6a37c) vulkan: warptiles currently assume warp sizes <= 64, clamp to work around larger warps**
  修正 warp size 大於 64 的顯示卡上，warptile 相關假設不成立導致的問題，加入 clamp 處理。
- **[#27453](https://github.com/ggml-org/llama.cpp/commit/cb30059) Feature: Added LIGHTNING_INDEXER support for Deepseek V4 ops on Vulkan Backend**
  為 Vulkan backend 新增 DeepSeek V4 所需的 `LIGHTNING_INDEXER` 運算支援（新增 shader 與 flash-attention 相關型別擴充），為後續支援該系列模型鋪路。

## 5. 其他運算後端（CUDA / OpenCL / Metal / RPC）

- **[#26264](https://github.com/ggml-org/llama.cpp/commit/fc35562) cuda: unblock mmq for MoE on sm_60**
  解除 sm_60（Pascal 架構，如 GTX 1060/1080）在 MoE（Mixture-of-Experts）模型上使用 mmq（quantized matmul）路徑的限制，新增專用的 pascal 設定檔。
- **[#27768](https://github.com/ggml-org/llama.cpp/commit/5854625) opencl: add bin kernels for gemm-moe (q4_0 / mxfp4)**
  為 OpenCL backend 新增 `kernel_gemm_moe_q4_0_q8_1_dp4a_bin` 與 `kernel_gemm_moe_mxfp4_q8_1_dp4a_bin` 兩個 bin-based GEMM kernel，加速 MoE 模型推論。
- **[#27758](https://github.com/ggml-org/llama.cpp/commit/915dc6d) metal: fix memory leaks due to missing autoreleasepools**
  修正 Metal backend 因缺少 `@autoreleasepool` 包裹而造成的記憶體洩漏問題。
- **[#18626](https://github.com/ggml-org/llama.cpp/commit/d0132a6) rpc : implement event and async backend APIs**
  為 RPC backend 實作事件（event）與非同步 API，讓遠端運算後端可以支援非同步排程，屬於較大幅度的介面調整。

## 6. 新模型架構支援 / 轉換腳本

- **[#27742](https://github.com/ggml-org/llama.cpp/commit/6c84c7d) model: add Qwen3.8-Flash-Next (qwen4exp)**（大型變更，28 個檔案，新增約 2900 行）
  新增對 Qwen3.8-Flash-Next（內部代號 qwen4exp）架構的完整支援，包含新的混合式（hybrid）KV cache/記憶體管理（`llama-memory-hybrid-idx`）、新的 tensor mapping 與轉換腳本 `conversion/qwen4exp.py`。
- **[#27804](https://github.com/ggml-org/llama.cpp/commit/ca3d5a3) model: add DSpark support for Nemotron3.5**
  為 Nemotron 3.5 新增 DSpark 推測解碼（speculative decoding）草稿模型支援，延伸自下方 DFlash2 的架構。
- **[#27342/#27816](https://github.com/ggml-org/llama.cpp/commit/b10f9ca) spec: add DFlash2 support (local convolution + candidate selector)**
  新增 DFlash2 推測解碼架構支援（本地卷積 + 候選選擇器），新增 `src/models/dflash.cpp` 及相對應的轉換與 GGUF 常數定義。
- **[#27730](https://github.com/ggml-org/llama.cpp/commit/d7a2074) models: support nanbeige4.2-3B**
  新增對 nanbeige 4.2-3B 模型的架構支援。
- **[#27790](https://github.com/ggml-org/llama.cpp/commit/deae5ee) model: simplify MiniMax-01 graph**
  簡化 MiniMax-01 模型的計算圖建構程式碼，屬於程式碼精簡而非行為變更。
- **[#27729](https://github.com/ggml-org/llama.cpp/commit/dac869b) conversion: fix Nemotron 3.5 Lightning layers**
  修正 Nemotron 3.5 Lightning 系列層在轉換（GGUF 轉換腳本）時的錯誤。
- **[#27356](https://github.com/ggml-org/llama.cpp/commit/bcb6084) convert: fix Nemotron-H LoRA GGUF conversion**
  修正 Nemotron-H 的 LoRA 模型在轉換為 GGUF 格式時的錯誤。

## 7. 推測解碼（Speculative Decoding）相關

- **[#27711](https://github.com/ggml-org/llama.cpp/commit/2bb9bdd) spec: Add benchmark-only synthetic speculative acceptance options**
  新增僅供 benchmark 使用的「合成推測接受率」選項，方便在沒有真實草稿模型的情況下模擬不同接受率對效能的影響，並補上對應單元測試。

（另有第 6 節中的 DFlash2、DSpark 兩個新模型架構，本質上也是為推測解碼新增草稿模型支援。）

## 8. llama core / KV cache / 模型載入 / Server

- **[#27762](https://github.com/ggml-org/llama.cpp/commit/925e117) llama: add token ID tracking to KV cell**
  在 KV cache 的每個 cell 中新增 token ID 追蹤資訊，讓上層可以得知每個 KV cell 對應的原始 token。
- **[#26622](https://github.com/ggml-org/llama.cpp/commit/c5fc7e3) llama: add --n-cpu-ffn option**
  新增 `--n-cpu-ffn` 參數，可指定將多少層的 FFN（前饋網路）子層固定跑在 CPU 上，用於混合 CPU/GPU 記憶體配置調優。
- **[#27794](https://github.com/ggml-org/llama.cpp/commit/fac889f) llama: model_loader: add TENSOR_READ_LAZY**
  為模型載入器新增「延遲讀取（lazy read）」的 tensor 讀取模式，可延後實際讀取權重資料的時機，有助於降低尖峰記憶體使用量。
- **[#27795](https://github.com/ggml-org/llama.cpp/commit/732707d) quantize: cap working memory size to avoid loading big tensors onto RAM**
  在量化（quantize）工具中限制工作記憶體上限，避免超大 tensor 一次性載入導致記憶體耗盡。
- **[#24124](https://github.com/ggml-org/llama.cpp/commit/1844325) server: add ctx-per-slot (--kv-unified-per-slot)**
  為 server 新增 `--kv-unified-per-slot` 選項，可讓每個 slot 各自擁有獨立的 context/KV 配置，而非共用統一的 KV cache。

## 9. 其他

- **[#24318](https://github.com/ggml-org/llama.cpp/commit/f295512) args: add --video-* CLI arguments**
  新增一系列 `--video-*` 命令列參數，用於在多模態（mtmd）CLI 與 server 中設定影片輸入相關參數（如取樣張數等）。
- **[#27800](https://github.com/ggml-org/llama.cpp/commit/1a946ec) pr2wt: use ssh/https remote in worktree depending on base**
  修正開發用腳本 `pr2wt.sh`，讓建立 worktree 時依照 base remote 是 ssh 或 https 選擇對應的協定，屬於開發工具維護性修正。

---

## 小結

本次追蹤區間（2026-08-26 ～ 2026-08-28）共 33 個 commit，重點包括：
1. Web UI 針對設定/MCP 管理進行了較大幅度的重構（改為對話框介面、工具授權政策取代 MCP overrides）。
2. Hexagon（Qualcomm NPU）後端有一次大型更新，加入多 NPU 裝置支援與完全非同步架構。
3. 新增了 Qwen3.8-Flash-Next、DFlash2、DSpark、nanbeige4.2-3B 等多個新模型/推測解碼架構支援。
4. Vulkan / OpenCL / CUDA / Metal / RPC 各後端均有零星的功能新增或 bug 修正。
5. 模型載入與量化流程新增了延遲讀取與記憶體上限保護機制，有助於降低大型模型載入時的記憶體壓力。
