# llama.cpp 更新報告 (2026-09-20)

- 檢查範圍: `5c53396`..`f072b10`（ggml-org/llama.cpp master 分支）
- 新增 commit 數: **29**
- 上次檢查點: `5c53396` (2026-09-19)
- 本次檢查點: `f072b10` (2026-09-19)

> 說明：ggml-org/llama.cpp 於本次檢查區間內的 commit 時間戳記多集中在
> 2026-09-18～09-19，屬於一次較大量的合併批次。

---

## 分組摘要

### 1. Chat 模板 / Tool-calling / Grammar 解析
處理聊天模板與工具呼叫（function calling）解析器的正確性問題，並修正 JSON schema 轉 grammar 的邊界情況。

- `f072b10` **chat: fix gemma4 required tool grammar** (#29115)
  修正 Gemma4 在「必須呼叫工具」情境下產生的 grammar 有誤的問題，並補上對應測試。
- `59657a6` **chat: add dedicated Ling 3.0 (Bailing V3) parser** (#28682)
  新增 Ling 3.0 (Bailing V3) 專用解析器。因 Ling 3.0 Flash 範本會預先開啟
  `<think>` 區塊，模型本身不會輸出開頭標籤，導致工具呼叫在 `</think>`
  前出現時會被整段誤判為 reasoning 內容、造成 agent loop 中斷。新解析器
  改在 `<think>` 結尾或 `<tool_call>` 起始處終止 reasoning，並新增對應
  chat template 與測試。
- `eb1e1f4` **json-schema: accept escaped hyphen in regex patterns** (#29127)
  修正 JSON schema 轉 grammar 時無法接受 regex 中「被跳脫的連字號」的問題。

### 2. Hexagon (Qualcomm NPU) 後端 op 支援擴充
本批次對 Hexagon 後端新增多個運算子支援，是這次改動量最大的分類之一。

- `e613ef2` **hexagon: enable I32 GET_ROWS** (#29116) — 支援 I32 型別的 GET_ROWS。
- `851cb34` **hexagon: add support for GEGLU_QUICK** (#29114) — 新增 GEGLU_QUICK 激活運算。
- `7d4b92b` **hexagon: enable support for TOP_K op** (#29113) — 新增 TOP_K
  運算支援，含單列多執行緒化、VTCM 容量上限調整與大列選取效能優化。
- `2b18470` **hexagon: add ROLL op support** (#29105) — 新增 ROLL 運算支援。
- `50631b3` **hexagon: im2col update** (#29103) — 更新 IM2COL：支援 1D 與
  padded 輸入，純 DDR 核心支援 2D 判斷，並新增 blocked-staging 通用 DMA 核心。
- `18a04f0` **hexagon: HMX flash-attention head_dim padding (support DK=DV=72)** (#26539)
  讓 HMX flash-attention 支援 head_dim 非 64 倍數的情況（例如 SigLIP 的
  head_dim=72），做法是將 DK/DV 向上補齊到 64 並在尾端補零。

### 3. Metal (Apple) 後端優化
- `5b59b83` **metal: add MoE and SSM_CONV fusion optimizations** (#28948) —
  本批次規模最大的單一 PR（累積 30+ 個子 commit）。為 Metal 後端新增多項
  kernel fusion：top-k MoE（SOFT_MAX + ARGSORT + GET_ROWS）、MoE 加權
  reduction、RMS_NORM/NORM + SCALE、SSM_CONV + SiLU 等，並改用 Metal
  function constant 取代 runtime 分支以降低開銷。期間曾嘗試 MUL_MAT +
  UNARY 融合但因在 Qwen3.6-35B-A3B 上使解碼吞吐下降約 8%（併發度降低）而
  被 revert。
- `60b06ab` **metal: fix FA support checks** (#29122) — 修正 flash-attention
  支援檢查邏輯的問題。
- `59fc5a1` **metal: support qwen4exp hc ops** (#29000) — 新增 DSV4 HC op
  變體（hc_pre 帶 sigmoid gate、hc_post 恆等混合）支援，供 qwen4exp 使用。

### 4. CUDA 後端修正
- `b23701f` **cuda: fix CUB argsort corruption caused by in-place keys** (#28389)
  修正 `argsort_f32_i32_cuda_cub` 呼叫 CUB `DeviceRadixSort::SortPairs`
  時輸入/輸出鍵緩衝區相同（in-place）導致排序結果損毀的問題，會在特定情境
  （如 Maxwell/CUDA 12.5/CCCL 2.x 上對 24.8 萬字彙的 top_k）造成間歇性
  索引錯亂並引發後續 get_rows 的越界存取。修正後六個呼叫點皆改用獨立的
  輸出緩衝區。

### 5. OpenCL 後端新增 kernel
- `60081bb` **opencl: add support for bin kernel `flash_attn_f32_f16_bin`** (#29046)
- `ec92815` **opencl: add bin kernel `kernel_gemm_noshuffle_q6_k_f32_32b_trans_ila_a8_bin`** (#28678)
  兩者皆為 OpenCL 後端新增預先編譯的二進位 kernel，分別對應 flash-attention
  與 Q6_K 量化的 A8 GEMV 運算。

### 6. Vulkan 後端
- `bbd488c` **vulkan: add IQ3_S MMQ matmul kernels** (#28822) — 新增 IQ3_S
  量化格式的 MMQ matmul kernel。

### 7. WebGPU 後端
- `44be98f` **ggml-webgpu: fix supports_op condition for GET_ROWS** (#28978)
  修正 GET_ROWS 的 vec4 對齊判斷條件，並補上來源 stride 檢查與測試案例。

### 8. CPU 後端
- `4fea119` **ggml-cpu: add F16 input to the FWHT** (#27779) — CPU 版 FWHT
  原僅支援 F32 輸入，此變更將來源型別改為模板參數，新增 F16 輸入支援
  （目前尚無後端提供 F16 FWHT kernel，測試會隨後端實作一併補上）。

### 9. ggml 核心穩健性
- `5b335f4` **ggml: check for allocation failures to prevent crashes** (#28149)
  在 `clip.cpp` 增加記憶體配置失敗檢查，避免配置失敗時直接崩潰。
- `911f6cd` **ggml: handle graph buffer reservation failure** (#26070)
  處理計算圖 buffer 保留失敗的情況。

### 10. 模型架構 / 詞彙 / GGUF 相關
- `542348a` **Model-Saver: Write the SWA pattern, 15 more architectures roundtrip** (#29042)
  新增 `llama_model_base::load_swa_pattern()`，讓 sliding-window pattern
  可讀取為「單一週期值」或「逐層陣列」；原本多個 loader（如 olmo2、
  gemma3n、exaone4）在轉換器已寫入陣列的情況下仍被忽略、僅套用預設週期。
  Model-Saver 現在會寫出逐層陣列（含 nextn 層）及 MLA 的 SWA 幾何資訊，
  使 plamo3、gemma3、cohere2、cohere2moe、olmo2、exaone-moe、afmoe、mimo2、
  spark2_5、muse-glimmer、mellum、laguna、granite_swa、dots3note、maple
  等 15 個架構通過 bit-exact roundtrip 測試。
- `efa28e9` **test-llama-archs: generate dummy test vocab** (#29084) —
  為架構 roundtrip 測試產生假詞彙表。
- `dc85f89` **vocab: add ufakzeka pre-tokenizer** (#29033) — 新增
  ufakzeka pre-tokenizer 支援。
- `bb11ebb` **gguf-py: fix Q8_1 block size in GGML_QUANT_SIZES (2+2+32)** (#29036)
  修正 Q8_1 量化格式在 `GGML_QUANT_SIZES` 中的區塊大小計算錯誤。

### 11. Server
- `1af554f` **server: improve startup log messages** (#29125) — 改善啟動
  時的日誌訊息，依模型來源（preset/models_dir/cache）標示來源標籤取代
  原本不易理解的 `*` 標記，並在快取模型預設清單中顯示 HF hub 快取路徑。

### 12. Build / CI 維護
- `d663dd3` **ci: change ubuntu-latest to ubuntu-24.04** (#29079) — 多個
  workflow 由 `ubuntu-latest` 改為固定 `ubuntu-24.04`，避免 runner 版本
  漂移造成的不穩定。
- `8ed1a55` **cmake: fix build when GGML_CPU=OFF and GGML_CUDA=ON** (#29026)
  修正在關閉 CPU 後端、僅啟用 CUDA 時的建置失敗問題。
- `f03cf3e` **ci: disable GHA cache for copilot** (#29068) — 停用
  Copilot workflow 的 GitHub Actions 快取。
- `bdcbaaf` **ci: bump android-actions/setup-android to 4.0.4** (#29065)
  升級 Android CI 所用的 setup-android action 版本。

---

## 觀察重點

1. **Metal MoE/SSM_CONV fusion（#28948）** 是本次最大改動，涉及多輪迭代與
   一次效能回歸後的 revert，值得留意後續是否有進一步的效能報告或跟進 PR。
2. **CUDA argsort 損毀修正（#28389）** 屬於正確性修復，會影響使用 CUB
   排序路徑（如大詞彙 top_k）的既有 CUDA 使用者，建議關注後續是否有回溯
   到穩定版本的 cherry-pick。
3. Hexagon 後端持續大量補齊運算子（GET_ROWS、GEGLU_QUICK、TOP_K、ROLL、
   IM2COL、HMX flash-attention），顯示 Qualcomm NPU 後端仍在積極開發中。
4. Model-Saver 一次新增 15 個架構的 roundtrip 支援，對模型轉換/驗證流程
   的覆蓋率有明顯提升。
