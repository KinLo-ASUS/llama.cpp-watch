# llama.cpp 變更報告 — 2026-07-27

- 檢查範圍：`ggml-org/llama.cpp` master 分支
- 上次檢查 commit：`7cdd557` (2026-07-26)
- 本次最新 commit：`88b47a75` (2026-07-27)
- 新增 commit 數量：11

---

## 分組摘要

### 1. WebUI：介面修復與體驗優化
針對聊天介面的多項小型修復，涵蓋 context 使用量提示卡片、對話匯入格式偵測、系統訊息編輯框，以及數學工具的 JS Sandbox 提示詞。

- **`ff067f76`** — ui: fix context gauge card regressions and land at the conversation end (#26099) — by Pascal
  修正 context 用量提示卡片 (Context Gauge) 的顯示回歸問題，並調整自動捲動邏輯，讓卡片能正確停留在對話末端。
- **`55b7d6c4`** — ui: detect the conversation import format from file contents (#26121) — by Pascal
  匯入對話紀錄時改為依檔案內容自動判斷格式（而非僅依副檔名），新增對應的格式偵測列舉與常數，並補上單元測試。
- **`d4d057b6`** — ui: fix system message edit box not expanding to fit content (#26006) — by Piero Evangelista
  修正系統訊息 (system message) 編輯框在內容較長時無法自動撐開高度的問題。
- **`88b47a75`** — ui: Fix symbolic math tool JS sandbox prompt (#26131) — by rankaiyx
  修正符號數學工具在 JS Sandbox 中使用的提示詞常數設定。

### 2. MiniMax-M3：模型核心與量化支援
新增 MiniMax-M3（MSA，MiniMax Sparse Attention）模型的完整轉換與推理支援，並修正量化時的數值精度問題。

- **`b1d4c655`** — model: Add MiniMax-M3 (MSA: MiniMax Sparse Attention) support (#24908) — by timkhronos
  新增 MiniMax-M3 架構支援：轉換腳本 (`conversion/minimax.py`)、GGUF 常數與張量對應、`llama-arch`/`llama-hparams` 定義、對 KV cache 的大幅擴充（因應其稀疏注意力機制），以及新的 `src/models/minimax-m3.cpp` 推理實作（約 560 行）。
- **`7657a6c2`** — Keep Minimax's indexer tensors at F32 for speed and accuracy (#26144) — by Bartowski
  修正量化流程，將 MiniMax 的 indexer 相關張量強制保留為 F32，以兼顧速度與精確度，避免量化後精度損失過大。

### 3. mtmd：多模態（視覺）支援更新
延續近期的多模態擴充，新增 GLM-5.2-Vision 與 MiniMax-M3 的視覺模型支援，並修正 Android 平台建置問題。

- **`af285020`** — mtmd: add GLM-5.2-Vision (#26126) — by Eric Hartford
  在 `tools/mtmd/mtmd.cpp` 與轉換腳本中新增 GLM-5.2-Vision 模型的支援，補上對應的 conversion 模組 (`conversion/kimivl.py`)。
- **`3d1c3a89`** — mtmd: Add Vision Support for Minimax-M3 (#25113) — by timkhronos
  為 MiniMax-M3 新增視覺（多模態）支援：擴充 `conversion/minimax.py`、新增 CLIP 相關模型定義 (`tools/mtmd/models/minimax-m3.cpp`) 及對應的張量對應與常數。
- **`0d47ea74`** — mtmd: fix android build (#26150) — by Xuan-Son Nguyen
  修正 `mtmd-helper.cpp` 在 Android 平台上的編譯問題。

### 4. 後端與基礎設施
OpenCL 後端的融合運算修正，以及新增跨平台子行程 (subprocess) 包裝工具並重構 server 相關程式碼以使用該工具。

- **`42fc2430`** — opencl: fix fused RMS norm mul view offset (#26085) — by yzyyzyhhh
  修正 OpenCL 後端中融合 RMS norm + mul 運算的 view offset 計算錯誤。
- **`d2a81823`** — common: add `subproc.h` wrapper, disabled on android/ios (#26102) — by Xuan-Son Nguyen
  新增跨平台子行程管理封裝 (`common/subproc.{h,cpp}`，Android/iOS 上停用)，並重構 `server-mcp.cpp`、`server-models.cpp`、`server-tools.cpp` 改用此共用元件，簡化原先重複的行程管理程式碼。

---

## 完整 Commit 列表（新到舊）

| SHA | 標題 | 分類 |
|---|---|---|
| `88b47a75` | ui: Fix symbolic math tool JS sandbox prompt (#26131) | WebUI |
| `3d1c3a89` | mtmd: Add Vision Support for Minimax-M3 (#25113) | mtmd 多模態 |
| `0d47ea74` | mtmd: fix android build (#26150) | mtmd 多模態 |
| `d4d057b6` | ui: fix system message edit box not expanding to fit content (#26006) | WebUI |
| `7657a6c2` | Keep Minimax's indexer tensors at F32 for speed and accuracy (#26144) | MiniMax-M3 |
| `55b7d6c4` | ui: detect the conversation import format from file contents (#26121) | WebUI |
| `d2a81823` | common: add `subproc.h` wrapper, disabled on android/ios (#26102) | 後端/基礎設施 |
| `af285020` | mtmd: add GLM-5.2-Vision (#26126) | mtmd 多模態 |
| `b1d4c655` | model: Add MiniMax-M3 (MSA: MiniMax Sparse Attention) support (#24908) | MiniMax-M3 |
| `42fc2430` | opencl: fix fused RMS norm mul view offset (#26085) | 後端/基礎設施 |
| `ff067f76` | ui: fix context gauge card regressions and land at the conversation end (#26099) | WebUI |
