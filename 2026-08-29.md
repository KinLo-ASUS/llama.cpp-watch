# llama.cpp 變更報告 — 2026-08-29

- 檢查範圍：`ca3d5a3`（2026-08-28 之前）→ `d7bd3bf`（2026-08-28）
- 新增 commit 數：15 筆
- 資料來源：`ggml-org/llama.cpp` master 分支

---

## 1. Metal fa-vec 效能調校（Apple GPU）

這組 3 個 commit 都是社群貢獻者依照官方教學（`ggml-metal-tuning` 工具）跑出的針對特定 Apple GPU 型號的 Flash-Attention（fa-vec）調校表，屬於純數據新增，不影響邏輯。

- **[#27875](https://github.com/ggml-org/llama.cpp/commit/f5e85d4) metal : add fa-vec tunings for M4**
  新增 Apple M4（10 GPU 核心）在 F16/Q4_0/Q4_1/Q5_0/Q5_1/Q8_0 資料型別下的 fa-vec 調校參數表。
- **[#27824](https://github.com/ggml-org/llama.cpp/commit/6d6b697) metal : add fa-vec tunings for M4 Pro**
  新增 Apple M4 Pro 的 fa-vec 調校表。
- **[#27863](https://github.com/ggml-org/llama.cpp/commit/8963a9b) metal : add fa-vec tunings for M3 Max, M5 and M5 Pro**
  一次補齊 M3 Max、M5、M5 Pro 三款晶片的 fa-vec 調校表。

## 2. Vulkan 後端修正與最佳化

- **[#27812](https://github.com/ggml-org/llama.cpp/commit/b387ddf) vulkan: fix missing view-alias dependencies in ggml_vk_graph_optimize**（重要正確性修正）
  修正圖優化器 `is_src_of` 未把同一 tensor 的兩個 view 視為互相依賴的問題，導致優化器可能把有 alias 關係的讀寫節點重新排序。實際影響：在 AMD／NVIDIA 的 Vulkan 上，對有 view-aliased 狀態的模型（如 Qwen3.8 的 recurrent state）造成 greedy decoding 輸出錯誤、每次啟動 server 結果不同、推測解碼接受率異常，且完全沒有錯誤訊息（CUDA 不受影響）。後續修正也排除了 NONE/RESHAPE/TRANSPOSE/VIEW/PERMUTE 這類不執行實際運算的節點，避免誤判為依賴。
- **[#26686](https://github.com/ggml-org/llama.cpp/commit/90c26fc) Vulkan: add hoisting support for row IDs and expert count in shaders**
  在 MoE 相關 shader（`count_experts`、`mul_mm`、`mul_mm_cm2`）中加入 row ID 與專家數量的提升（hoisting）最佳化，並用 `subgroupExclusiveAdd`（裝置支援時）加速每個 expert 的 offset 計算，序列版本作為 fallback。

## 3. ggml 正確性修正：conv_transpose_2d 多批次

- **[#26132](https://github.com/ggml-org/llama.cpp/commit/a43c398) ggml : fix conv_transpose_2d for multiple batches**（重要正確性修正）
  修正 CPU 與 Metal 兩個實作中 `conv_transpose_2d` 只計算第一個 batch、其餘 batch 全部輸出為零的錯誤。CPU 端修正了 src1 的 permutation 與主計算迴圈的批次迭代，並依批次數放大 `ggml_graph_plan` 的工作緩衝區；Metal kernel 則讓 grid 涵蓋所有批次並依 grid z 座標解碼批次索引。新增了多批次測試案例。

## 4. SYCL 後端優化

- **[#26689](https://github.com/ggml-org/llama.cpp/commit/d077b4c) sycl: use TILE for quantized KV decode on BMG**
  將量化 KV 的 decode 路徑在 Intel Xe2（Battlemage, BMG）架構上改用 TILE 演算法，其他架構暫時維持原有的 VEC 路徑，待驗證後再擴大。
- **[#27468](https://github.com/ggml-org/llama.cpp/commit/be87620) sycl: bind the f16 KV cache in place for the oneDNN SDPA path**
  修正 oneDNN SDPA 路徑中 f16 KV cache 需要額外搬移/複製的問題，改為原地綁定。commit 訊息附上實測數據：在 Qwen3.8 27B Q4_K_S、live KV 長度 34816 時，原本每個 2048-token prefill chunk 要搬運約 4.56GB 資料，此修正可大幅降低這類搬移流量。

## 5. Qwen3.8-Flash-Next（qwen4exp）與相關運算子調整

- **[#27880](https://github.com/ggml-org/llama.cpp/commit/6fe7498) model: qwen4exp: reduce number of graph splits**
  減少 qwen4exp（Qwen3.8-Flash-Next）計算圖的分割（graph split）數量，屬於效能最佳化。
- **[#27877](https://github.com/ggml-org/llama.cpp/commit/8663224) context : disable non-fused GDN and LID ops**
  停用未融合（non-fused）版本的 GDN（Gated Delta Net）與 LID（Lightning Indexer）運算子，僅保留融合版本，避免在部分後端上跑到效能較差或有問題的路徑。

## 6. 模型載入延遲讀取（Lazy Read）相關

延續前次報告中新增的 `TENSOR_READ_LAZY` 延遲讀取機制，本次新增了對應的工具支援與轉換端修正。

- **[#27881](https://github.com/ggml-org/llama.cpp/commit/50f068f) bench: add --tensor-read-lazy**
  為 `llama-bench` 新增 `--tensor-read-lazy` 參數，可在 benchmark 中測試延遲讀取模式的效能影響，並將相關列舉統一改名為 `LLAMA_LAZY_MODE_*`。
- **[#27869](https://github.com/ggml-org/llama.cpp/commit/b19cbe9) convert: prevent ndarray conversion in LazyChunkedTensor**
  修正 GGUF 轉換腳本中 `LazyChunkedTensor` 會被意外轉換成 numpy ndarray 的問題，避免延遲載入的張量被提前實體化。

## 7. OpenVINO 後端更新

- **[#27843](https://github.com/ggml-org/llama.cpp/commit/511f9c1) OpenVINO: Update OV to 2026.3.1, whisper.cpp support, Qwen3.5 on NPU, and new ops**（大型變更）
  將 OpenVINO 版本升級至 2026.3.1，新增對 whisper.cpp 的支援，並讓 Qwen3.5 可在 NPU 上以靜態 shape 模式執行（修正 s_copy 相關輸入形狀在靜態模式下仍為動態、以及零位元組緩衝區綁定的問題），另外新增了 IM2COL+MatMul 卷積融合與除錯用的環境變數開關。

## 8. Snapdragon / Hexagon 開發工具

- **[#27903](https://github.com/ggml-org/llama.cpp/commit/d7bd3bf) snapdragon: python SDK setup (Windows)**
  將 Windows 上安裝 Hexagon／OpenCL SDK 的腳本由 PowerShell（`setup-build.ps1`）改寫為 Python（`setup-sdk.py`），屬於開發工具維護性調整。

## 9. 測試基礎設施

- **[#27755](https://github.com/ggml-org/llama.cpp/commit/4e97ac8) tests : run test-save-load-state across all architectures**
  將原本只針對單一模型（llama 架構）執行的 `test-save-load-state` 擴充為可對目錄下所有 `.gguf` 模型執行完整的存檔/讀檔測試，並回報每個模型的 PASS/FAIL；同時修正 dummy DSA indexer 的形狀設定，讓 Lightning Indexer 融合運算能在 GPU（而非退回 CPU）上正確執行。目前已知 deepseek4、gemma2、gpt-oss、lfm2、minimax-01 等架構仍會測試失敗，待後續修正。

---

## 小結

本次追蹤區間（2026-08-28 當日）共 15 個 commit，重點包括：
1. **兩個值得注意的正確性修正**：Vulkan 圖優化器誤判 view-alias 依賴，會在 AMD/NVIDIA 上對有 recurrent state 的模型（如 Qwen3.8）造成靜默的錯誤輸出；以及 `conv_transpose_2d` 在 CPU/Metal 上多批次時只計算第一批的 bug。兩者都屬於「無錯誤訊息、結果悄悄錯誤」的高風險類型，值得追蹤是否有後續回歸。
2. Apple Metal 的 fa-vec 調校表新增了 M3 Max、M4、M4 Pro、M5、M5 Pro 等多款晶片的資料。
3. 延續上次的延遲讀取（lazy read）機制，新增了 benchmark 工具與轉換腳本端的配套支援。
4. Qwen3.8-Flash-Next（qwen4exp）與 GDN/Lightning Indexer 相關運算子有小幅效能調整。
5. OpenVINO 後端有一次較大更新（升級版本、支援 whisper.cpp、Qwen3.5 NPU 靜態 shape）。
