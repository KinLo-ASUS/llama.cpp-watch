# llama.cpp 變更報告 — 2026-07-29

- 檢查範圍：`ggml-org/llama.cpp` master 分支
- 上次檢查 commit：`91f8c9c5` (2026-07-27)
- 本次最新 commit：`e9fa0781` (2026-07-28)
- 新增 commit 數量：17

---

## 分組摘要

### 1. 新模型與多模態支援
新增兩個模型架構的支援：文字模型 Laguna-S-2.1，以及具語音理解能力的 Nemotron 3 Nano Omni（Parakeet 音訊編碼架構）。

- **`e9fa0781`** — model: Add Laguna-S-2.1 LLM_TYPE (#26233) — by crusaderky
  在 `src/llama-model.h`、`src/llama-model.cpp` 與 `src/models/laguna.cpp` 中新增 `LLM_TYPE_118B_A8B` 列舉值，並依 48 層的架構特徵辨識為 Laguna-S-2.1 變體。
- **`7e1e28ca`** — mtmd : add Nemotron 3 Nano Omni support (parakeet) (#22520) — by danbev, CISC, ngxson
  在 `tools/mtmd` 新增 Parakeet 子取樣（subsampling）與音訊編碼器實作，將相對位置張量的產生由轉換腳本移至編碼圖內計算，新增 `clip_get_model` API 以存取 clip 模型張量，並將 mel-filter、Hann window 等參數納入模型超參數，同步至最新版 whisper.cpp 的 Parakeet 實作。

### 2. 推測解碼（Speculative Decoding）新草稿架構
兩項新增皆擴充推測解碼可用的草稿模型類型，分別針對 DFlash 系列與 EAGLE3 系列。

- **`84075273`** — spec: add DSpark speculative decoding (#25173) — by wjinxu, ruixiang63, Zaire404, ggerganov
  新增 DSpark 架構，延伸自 DFlash，加入半自迴歸（semi-autoregressive）Markov 預測頭，草稿模型每次前向傳遞可預測整個 block、並以先前 token 偏移 logits，同時支援以信心分數（confidence head）截斷區塊；DFlash 也一併調整為固定草擬完整 `n_max` 區塊以符合 Markov 頭的批次格式。
- **`f8706784`** — spec: add eagle3-v3 support for gpt-oss model (#25794) — by ruixiang63
  為 gpt-oss 模型新增 EAGLE3-v3 草稿模型支援，包含由 EAGLE3 設定讀取顯式輔助層 ID、新增 `ENC_OUTPUT_NORM` 張量類型與正規化設定旗標，並在 `openai-moe.cpp` 補上帶遮罩的 embedding 擷取邏輯。

### 3. 各後端正確性與效能修正
涵蓋 WebGPU、OpenCL（Adreno）、Vulkan、CUDA、SYCL 與 Metal 六個後端的修正與效能優化，多數與多序列/長上下文情境下的正確性有關。

- **`bc71c24c`** — ggml-webgpu: Fix some binding alias issues to support all archs, fix recurrent-state-rollback test (#25931) — by reeselevine
  修正 WebGPU 後端在不同架構下的 shader 綁定別名（binding alias）問題，包含 SSM scan、GLU、二元運算與 flash attention 管線對來源張量重疊（overlap）情況的處理，並修復 `recurrent-state-rollback` 測試失敗與超過 4GB 緩衝區的對齊問題。
- **`81908487`** — opencl: skip the Adreno KQ/KQV image kernels for multi-stream batches (#26189) — by wanghqc
  修正 Adreno GPU 的 KQ/KQV image kernel 因忽略張量第 4 維，導致統一 KV cache 下多序列（multi-stream，如 `llama-perplexity` 預設設定或多 slot `llama-server`）批次時，第一個串流之後的所有串流讀到錯誤 K/V 資料的問題；改為讓 `ne03`/`ne13` > 1 的情況走一般路徑處理。實測 Adreno 740 上 PPL 由 1817.64 大幅修正回 15.61。
- **`81616410`** — vulkan: add iq4_nl support back to FA (#24585) — by jeffbolznv
  在 Vulkan flash attention 中重新加入 IQ4_NL 量化格式支援（取代先前的 Q1_0），並為共享記憶體使用量新增條件初始化，修復 issue #23681。
- **`b62b3509`** — ggml-cuda: add chunked SSD matmul for Mamba-2 prefill acceleration (#22675) — by BLSharda
  新增分塊（chunked）SSD（Selective State Space）矩陣乘法以加速 Mamba-2 的 prefill 階段，將 M 矩陣的產生融合進 pre_matmul kernel、合併 B_weighted 與 C_scaled 運算、改善記憶體合併存取，並修正 `prepare_dt` fallback scan 迴圈中的資料競爭（data race）問題。
- **`d6b61ac0`** — sycl: fix use-after-return of the SDPA scale in the oneDNN flash-attention path (#25880) — by meatposes, arthw
  修正 SYCL oneDNN flash-attention 路徑中，scale 數值以非同步方式從堆疊區域變數複製、但在 KV 序列長度足夠大（Arc Pro B70 上約 ≥26k）時，該複製會在 host 堆疊已釋放後才執行，讀到已回收記憶體造成輸出亂碼與 KV cache 污染的問題；改為以 kernel（`single_task`）直接寫入 scale 值，並新增環境變數 `GGML_SYCL_FA_ONEDNN_MAX_KV` 讓超長序列可改走原生 FA kernel。
- **`f95de977`** — ggml-metal: FWHT kernel for metal backend (#25924) — by nicklafleur, forforever73, ggerganov
  為 Metal 後端新增快速沃爾什-哈達馬變換（Fast Walsh-Hadamard Transform）kernel，支援 64/128/256/512 等尺寸的模板化實作，並補上對應的 backend-ops 測試案例。

### 4. Server／ggml 核心重構
- **`ee3d1b54`** — server: abstract llama_memory calls to common_memory (#26221) — by ngxson
  將 server 中對目標／草稿 context 的 KV cache 操作（`seq_rm`/`seq_cp`/`seq_add`）抽象為 `common_memory` 結構，減少 `server-context.cpp` 中重複的雙 context 呼叫邏輯。
- **`da5b4486`** — ggml : set output of view src (#25729) — by am17an, ggerganov, gaugarg-nv
  修正 `ggml_set_output()`，改為對張量的 view source 一併標記輸出旗標，並在多種取樣器（greedy、分佈、top-k、top-p、min-p、溫度取樣）中改用 `ggml_reshape_1d()` 壓平 logits，統一各後端的張量處理方式。
- **`9a3bf2b8`** — server : add extra trace log for prompt similarity (#26218) — by ggerganov
  在 prompt cache 相關流程中新增 trace 等級的紀錄，輸出 prompt 長度、最長共同前綴（LCP）與相似度等除錯資訊，並將 `load()` 的參數命名由 `ctx_main`/`ctx_drft` 改為更一致的 `ctx_tgt`/`ctx_dft`。

### 5. Chat 範本與 WebUI
- **`6ba5ef24`** — common/chat: add specialized minimax m3 parser (#26210) — by aldehir
  新增針對 MiniMax M3 格式的專用解析器（`common_chat_peg_minimax_m3_mapper`），支援工具呼叫參數解析與 JSON 序列化，並附上對應的聊天範本檔案。
- **`6e2bc65f`** — ui: rendering performance follow-up (#26097) — by allozaur
  為 WebUI 新增多項快取（memoization）機制，包含工具呼叫解析、分類、程式碼語法高亮等結果的快取，改用 Map 結構取代重複陣列查找，並擴充 LaTeX 分隔符比對規則，以降低串流輸出時的重複運算。

### 6. 文件與貢獻指南
- **`ad77bd31`** — docs: Adapt conda-forge package name (#26229) — by jjerphan, dev-tinker
  將安裝文件中 conda/mamba/pixi 安裝指令與 conda-forge feedstock 連結中的套件名稱由 `llama-cpp` 統一改為 `llama.cpp`。
- **`c6292cfb`** — contrib : add guideline about the 'merge ready' label (#26178) — by ggerganov
  在貢獻指南中新增說明：可為 PR 加上 `merge ready` 標籤，表示該 PR 可在不需等待兩位獨立審查者的情況下快速合併。

---

## 完整 Commit 列表（新到舊）

| SHA | 標題 | 分類 |
|---|---|---|
| `e9fa0781` | model: Add Laguna-S-2.1 LLM_TYPE (#26233) | 新模型 |
| `bc71c24c` | ggml-webgpu: Fix some binding alias issues to support all archs, fix recurrent-state-rollback test (#25931) | 後端修正 |
| `81908487` | opencl: skip the Adreno KQ/KQV image kernels for multi-stream batches (#26189) | 後端修正 |
| `7e1e28ca` | mtmd : add Nemotron 3 Nano Omni support (parakeet) (#22520) | 新模型/多模態 |
| `6e2bc65f` | ui: rendering performance follow-up (#26097) | WebUI |
| `ad77bd31` | docs: Adapt conda-forge package name (#26229) | 文件 |
| `ee3d1b54` | server: abstract llama_memory calls to common_memory (#26221) | Server 重構 |
| `da5b4486` | ggml : set output of view src (#25729) | ggml 核心 |
| `81616410` | vulkan: add iq4_nl support back to FA (#24585) | 後端修正 |
| `b62b3509` | ggml-cuda: add chunked SSD matmul for Mamba-2 prefill acceleration (#22675) | 後端效能 |
| `84075273` | spec: add DSpark speculative decoding (#25173) | 推測解碼 |
| `6ba5ef24` | common/chat: add specialized minimax m3 parser (#26210) | Chat 範本 |
| `d6b61ac0` | sycl: fix use-after-return of the SDPA scale in the oneDNN flash-attention path (#25880) | 後端修正 |
| `9a3bf2b8` | server : add extra trace log for prompt similarity (#26218) | Server |
| `f95de977` | ggml-metal: FWHT kernel for metal backend (#25924) | 後端效能 |
| `f8706784` | spec: add eagle3-v3 support for gpt-oss model (#25794) | 推測解碼 |
| `c6292cfb` | contrib : add guideline about the 'merge ready' label (#26178) | 文件 |
