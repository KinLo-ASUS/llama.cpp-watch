# llama.cpp 變更報告 — 2026-09-07

- 追蹤範圍：`ggml-org/llama.cpp` master 分支
- 檢查區間：`74a7c89`（2026-09-06 之前的檢查點，不含） → `465e49b`（本次檢查點）
- 新增 commit 數：10

---

## 分組摘要

### 1. GPU 後端：效能調校與競態/正確性修復（Metal / CUDA / OpenCL）

- **971595d** — metal : add remaining fa-vec tunings for M2 Max (#28458)｜Niklas Wenzel
- **73a43d1** — cuda: fixes races in mmid and mmf (#28475)｜Aman Gupta
- **d03efa5** — opencl: properly choose weights pack for q4_K, q5_K mul_mat (#28402)｜lhez

第一項為純資料調校：在 `ggml-metal-tuning.cpp` 補齊 M2 Max 上 flash-attention 向量化 kernel 尚缺的參數組合（147 行新增），不涉及邏輯變更。第二、三項則是正確性修復：CUDA 的 `mmid`（mul_mat_id，MoE 常用）與 `mmf`（mul_mat fusion）kernel 中存在競態條件，修正涵蓋 `common.cuh`、`fattn-vec.cuh`、`mmf.cuh`、`mmid.cu` 共 4 個檔案；OpenCL 後端則修正 q4_K、q5_K 量化格式在 `mul_mat` 運算中選擇權重打包（weights pack）方式錯誤的問題。三項合計反映近期各硬體後端仍持續在追求效能同時修補平行運算正確性。

### 2. WebUI：建置流程簡化與聊天訊息渲染效能優化

- **c457e3b** — ui : embed assets directly with CMake (#28445)｜Adrien Gallouët
- **0afb805** — ui: Improve Chat Messages rendering performance (#28460)｜Aleksander Grygier

前者簡化前端資源嵌入建置流程：移除建置時期的 C++ 輔助工具與外部 gzip 依賴，改由 CMake 直接嵌入前端資源，簡化跨編譯（cross-compilation）流程，同時保留生成程式碼的可讀性與完整嵌入的 UI 資源（6 個檔案，+281/-408 行）。後者是一系列連續的聊天訊息渲染效能修正（37 個檔案，+1261/-291 行），主要問題與修法包括：
- 對話物件在每次傳送/工具結果/重新命名時整體替換造成身分（identity）改變，觸發全量訊息重抓 → 改為就地更新欄位。
- 開啟對話時重複讀取資料庫兩次 → 合併為一次讀取後共用。
- 手足（sibling）節點的葉節點解析為 O(訊息數²)的重複走訪 → 加入記憶化（memoization）與路徑壓縮。
- 訊息編輯時不必要的手足資訊全量重抓 → 僅在真正產生分支或刪除時才重抓。
- 移除未被讀取但每個串流字元都寫入的 `currentResponse` 反應式狀態。
- 已完成的 agentic 回合區段在串流時被重複重新推導 → 依訊息身分快取已完成回合。
- Markdown 區塊共用語法高亮主題樣式與 remark/rehype pipeline，避免每個區塊重複建置。
- 僅對最後一則助手訊息做版面量測（ResizeObserver 等），避免每則訊息都觸發版面重排。
- 工具呼叫區塊（write_file/edit_file 等）收合時不再解析完整 JSON 內容，只解析標題所需欄位。
- 訊息列改為以 IntersectionObserver 延遲掛載（lazy mount），僅在接近可視範圍時才渲染完整元件樹。
- 以 CSS transform／動畫取代版面屬性轉場，改善長對話載入時的卡頓。
- 並補上對應的效能不變量單元測試，鎖定上述修正行為。

### 3. 模型推論正確性修復

- **9e0e220** — grammar : fix max repetition threshold (#28469)｜Aldehir Rojas
- **5fdfa62** — models : fix GDN normalization from `max` to `rsqrt` (#28068)｜Daniel Han

前者為 GBNF 文法引擎中「最大重複次數」門檻值的單行修正。後者修正 Gated Delta Net（GDN）的 q/k 正規化實作：flash-linear-attention 參考實作的 l2norm 為 `x * rsqrt(sum(x*x) + eps)`（eps 在根號內），但 llama.cpp 各呼叫點沿用的 `ggml_l2_norm` 實際上是 `x / max(sqrt(sum(x*x)), eps)`（即 `torch.nn.functional.normalize` 語意），在實際數值範圍下 clamp 幾乎不會生效，等同於完全沒有 eps 保護，與參考實作行為不一致（HuggingFace transformers 先前在 Qwen3-Next 上也犯過同樣錯誤並已修正，vLLM/SGLang 因直接沿用 FLA 函式庫而未受影響）。修正方式是借用既有 `rms_norm`（本身在根號內含 eps）換算出等價的 rsqrt 正規化，無需新增 ggml 運算子，`ggml_l2_norm` 本身及其原始呼叫者 rwkv7-base 維持不變。影響範圍含 bailingmoe3、kimi-k3、kimi-linear、qwen35、qwen35moe、qwen3next、qwen4exp 等模型。

### 4. 新模型支援與轉換工具功能

- **3ad1ba7** — [Model] Support for Spark2_5ForCausalLM implementation (#27868)｜KnightYao
- **465e49b** — convert : add `--fuse-qkv` flag to fuse Q/K/V into QKV during HF-to-GGUF conversion (#22780)｜PikaPikachu

前者新增對 Spark2.5（`Spark2_5ForCausalLM`）模型架構的支援，涵蓋轉換腳本、GGUF 常數、詞表、推論圖與對應測試（18 個檔案，+471 行）。後者為 HF→GGUF 轉換工具新增 `--fuse-qkv` 選項，可在轉換階段將原本分離的 Q/K/V 權重矩陣融合成單一 QKV 矩陣，藉此減少推論時的矩陣運算次數以提升效能；影響範圍涵蓋 deepseek2、deepseek2ocr、gemma3n、gemma4、jais2、kimi-linear、llada、minimax-m2、olmo2、olmoe、qwen35、qwen35moe、qwen3next、step35 等多個模型架構（20 個檔案，+393/-122 行）。

### 5. CLI／Server：新增 JSONL 格式日誌選項

- **7620399** — common: add --log-jsonl (#28437)｜Xuan-Son Nguyen

為共用的 log 模組新增 `--log-jsonl` 參數，可將日誌輸出改為每行一筆 JSON（JSON Lines）格式，方便機器解析與日誌收集系統串接，並同步更新 CLI／completion／server 的說明文件。

---

## 小結

本次檢查區間新增 10 筆 commit，涵蓋範圍較廣：GPU 後端（Metal/CUDA/OpenCL）持續進行效能調校與平行運算競態修復；WebUI 一口氣完成建置流程簡化與大規模聊天訊息渲染效能優化（連續 15 個 commit 合併，含新增效能回歸測試）；模型正確性方面修正了文法引擎重複次數門檻與 GDN 正規化公式（影響多個採用 Gated Delta Net 的模型）；功能新增則包含 Spark2.5 新模型支援、HF→GGUF 轉換時可選的 QKV 融合最佳化，以及 CLI/Server 端的 JSONL 日誌輸出選項。
