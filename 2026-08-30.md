# llama.cpp 變更報告 — 2026-08-30

- 檢查範圍：`d7bd3bf`（2026-08-28 之前）→ `c589f0e`（2026-08-30）
- 新增 commit 數：8 筆
- 資料來源：`ggml-org/llama.cpp` master 分支

---

## 1. Metal fa-vec 效能調校（Apple GPU）

這組 3 個 commit 都是依照官方調校工具（`ggml-metal-tuning`）跑出的針對特定 Apple GPU 型號的 Flash-Attention（fa-vec）調校參數表，屬於純數據新增，不影響邏輯。

- **[#27932](https://github.com/ggml-org/llama.cpp/commit/5ea1b12) metal : add fa-vec tunings for M1 Max**
  新增 Apple M1 Max 的 fa-vec 調校表。
- **[#27915](https://github.com/ggml-org/llama.cpp/commit/17252c7) metal : add remaining fa-vec tunings for M4 Pro**
  補齊 Apple M4 Pro 剩餘資料型別（此前 #27824 尚未涵蓋的部分）的 fa-vec 調校表。
- **[#27940](https://github.com/ggml-org/llama.cpp/commit/c589f0e) metal : add fa-vec tunings for M2**
  新增 Apple M2 的 fa-vec 調校表。

## 2. Metal 正確性強化

- **[#27951](https://github.com/ggml-org/llama.cpp/commit/3173a56) metal : assert shared memory padding**
  在 Metal device／ops 層加入 assertion，確保 shared memory 的 padding 符合預期，避免因對齊不足而產生難以察覺的計算錯誤，屬於防禦性檢查而非行為變更。

## 3. Vulkan 後端最佳化

- **[#27925](https://github.com/ggml-org/llama.cpp/commit/77f132c) vulkan: Change mul_mat_id to pad K rather than N**
  `mul_mat`（一般矩陣乘）需要對 N 維度做 padding 以避免越界讀取，但 `mul_mat_id`（MoE 用的 indirect 矩陣乘）透過 shared memory lookup table 間接定址 row，天生就不會有 row 越界的問題，真正需要 padding 的其實是 K 維度。此修正把 `mul_mat_id` 的 padding 策略從「補 N」改為「補 K」，讓兩種運算子使用各自正確的 padding 方式，並同步更新 `mul_mm.comp`／`mul_mm_cm2.comp`／`mul_mmq.comp` 的 shader 與後端測試。
- **[#27526](https://github.com/ggml-org/llama.cpp/commit/c9ca51c) vulkan: combine duplicated fastdiv functions, rename the one optimizing small divs**
  整理多個 shader（`conv2d_mm`、`conv3d_mm`、`count_experts`、`generic_unary_head`、`glu_head`、`sum_rows`）中重複的 fastdiv（快速除法）實作，統一收斂到 `utils.glsl`，並將原本針對小除數優化的版本改用更明確的命名，屬於程式碼整理與可維護性改善，不改變運算結果。

## 4. SYCL 後端：`--fit` 記憶體估算改善

- **[#27629](https://github.com/ggml-org/llama.cpp/commit/cc83d7b) sycl: make --fit respect --fit-target better**
  改進 SYCL 後端 `--fit` 演算法對 VRAM 用量的估算：一方面更準確地反映 context 被完整使用時的實際峰值 VRAM 需求（雖然會讓算出的可用 context 比之前更小，但避免了 OOM），另一方面修掉先前過於保守而多保留 VRAM 的計算。commit 訊息附上實測：在 Intel Arc B70、使用 Qwen3.8（Q4_K_XL）搭配 q8_0 KV cache、MTP 與 4k ubatch size、`--fit-target 1` 的情況下，可跑到 262144 context 且完全可用。

## 5. OpenCL 後端：Adreno GPU matmul 路徑調整

- **[#27640](https://github.com/ggml-org/llama.cpp/commit/c841aee) opencl: use a better matmul path on two Adreno GPU generations**（分兩個子修正）
  1. **X2E 上預設開啟 xmem F16×F32 GEMM**：`kernel_mul_mm_f16_f32_l4_lm` 是 Adreno 後端最慢的 matmul 路徑，在 X2-90 上執行 gpt-oss-20b 的 attention 投影時只有調校過的 dense q4_0 GEMM 約四分之一的速度，而 gpt-oss-20b 的 prefill 有 40.8% 的 GPU 時間耗在這個 kernel 上。原本就存在但預設關閉的 xmem 路徑，此修正將其在 X2E 上預設開啟，實測 gpt-oss-20b 在 Adreno X2-90 上 prefill 提升約 25%；Adreno 840 則無明顯影響（decode 不受影響，且對 q8attn 變體無效益）。同時把控制用環境變數從「存在即生效」改為以 `atoi()` 判斷數值。
  2. **A7X 上繞過 tiled f32 GEMM**：Adreno A7X 編譯器產生的 `kernel_mul_mm_f32_f32_l4_lm` 效能只有同晶片 f16／q4_K kernel 的約十分之一（推測是暫存器配置在較舊架構上於 K-loop 溢位所致）。此修正讓 batched f32×f32（ne11 > 8）在 A7X 上改走逐列 f32 kernel，小 batch 仍維持 tiled 路徑；實測 gemma-3n-E4B 在 Adreno 740 上 prefill 提升約 9%，可用 `GGML_OPENCL_A7X_F32_LM_BYPASS=0` 停用。

---

## 小結

本次追蹤區間（2026-08-29 至 2026-08-30）共 8 個 commit，皆屬效能調校與程式碼整理性質，未發現正確性回歸類的重大修正：

1. **Metal**：新增 M1 Max、M2 與 M4 Pro（補完）的 fa-vec 調校表，並加入 shared memory padding 的防禦性檢查。
2. **Vulkan**：修正 `mul_mat_id` 的 padding 維度選擇（K 而非 N），並整理重複的 fastdiv 程式碼。
3. **SYCL**：改善 `--fit` 對 VRAM 用量的估算精確度，讓可用 context 更貼近實際硬體限制、避免 OOM。
4. **OpenCL**：針對兩代 Adreno GPU（X2E、A7X）調整 matmul 路徑選擇，帶來 gpt-oss-20b、gemma-3n-E4B 等模型的 prefill 效能提升（9%～25%）。
