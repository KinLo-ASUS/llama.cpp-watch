# llama.cpp 變更報告 — 2026-09-02

- 檢查範圍：`ggml-org/llama.cpp` master 分支
- 上次檢查點：`458681e` (2026-09-01)
- 本次最新 commit：`b96806d` (2026-09-02)
- 新增 commit 數：23

---

## 一、qwen4exp / MTP 推測解碼相關 (4 commits)

這組變更集中在 `qwen4exp`（Qwen3.8-Flash-Next 系列混合注意力/線性注意力架構）的正確性修正與效能優化，並讓 MTP（Multi-Token Prediction）推測解碼可以搭配 recurrent state rollback 使用。

- **`0eadefe` qwen4exp: support recurrent state rollback (#28123)** (Pascal)
  MTP 推測解碼在拒絕草稿 token 後需要把 target state 往回退。過去 recurrent cache 的 conv 歷史只寫入單一 plane，導致 rollback 還原到錯誤的卷積歷史；此修正讓每個 snapshot slot 各自寫入獨立的一步歷史。實測（Qwen3.8-Flash-Next UD-Q4_K_XL, 單 slot, n-max 3）解碼速度由 fallback 到全量 checkpoint 的 123/83 tok/s 提升到 183/144 tok/s（code/prose）。
- **`09412af` qwen4exp: sum the indexer heads by slices (#28023)** (Pascal)
  將 indexer heads 的加總從「transpose + sum_rows」改為直接對相鄰 head 做加法鏈，省去多餘的資料搬移。RTX PRO 6000 實測 prompt processing 由 2170 t/s 提升到 2366 t/s，生成速度不受影響。
- **`36b1015` qwen4exp: fix seq_cp, block position keying, mtmd input, cuda abort, add tests (#27941)** (Daniel Han)
  一系列後續修正：`-kvu` NaN collapse、indexer cache 的 `ext.x/ext.y` 還原、`seq_set` 改名為 `seq_get_all`、將 `set_input_qsa` 實作下移到 memory 類別、新增 save/load state round-trip 測試以驗證序列狀態正確性，並修正合成測試模型載入問題（`per_layer_token_embd` 列數推導、PLE 對 full-attention 層的空指標保護）。同時在 `llm_arch_supports_sm_tensor` 中正式停用 `-sm tensor` 對 qwen4exp 的支援。
- **`b356fa2` kv-cells: look up the n-gram history in the sequence position index (#28040)** (Pascal)
  `get_prev_tokens()` 原本每個 ubatch 都要重建 (seq, pos) → token 的雜湊表，改為利用 `llama_kv_cells` 既有的 (pos, cell) 有序索引（`std::set`），以對數時間查詢取代線性重建，並移除舊有的 `for_each_token_in()`。實測 71k context 下 tg 由 69.3 提升到 72.7 t/s（+4.9%），pp 速度不變，輸出結果一致。

## 二、Metal (Apple GPU) 後端 (6 commits)

本輪 Metal 後端有多筆針對特定硬體的 FlashAttention-vec 調校表，以及基礎架構改進。

- **`d5d993a` metal: enable Metal 4.0 tensor API on M5+/A19+ (#27461)** (James Francis)
  為 M5+/A19+ 晶片啟用 Metal 4.0 Tensor API，將對應 kernel 拆分到獨立 metallib，並修正建置順序，新增迴歸測試。
- **`b96806d` metal : add metallib build support for xcframework (#28163)** (Jhen-Jie Hong)
  讓 xcframework 建置流程支援 metallib 打包（`build-xcframework.sh`、CMake 調整）。
- **`fe2120b` metal : fix more leaks due to missing autoreleasepools (#27883)** (Niklas Wenzel)
  補齊多處缺少 `@autoreleasepool` 造成的記憶體洩漏。
- **`d08c787` metal : add fa-vec tuning for M2 Max (#28015)** (Georgi Gerganov)
- **`8887a48` metal : add fa-vec tuning for M2 Pro (#28122)** (Lukasz Stolcman)
- **`be789c3` metal : add fa-vec tunings for A18 Pro (MacBook Neo) (#28152)** (Jhen-Jie Hong)
  以上三筆皆是針對特定 Apple 晶片（GPU 核心數）量測並寫入 `fa_vec_tuned_table` 的 FlashAttention-vec 效能調校參數表，屬於持續性硬體調校收集。

## 三、模型架構載入 / 正確性修正 (5 commits)

Sigbjørn Skjæret 這輪集中處理多個模型架構在 hparams 載入順序與陣列維度上的一致性問題。

- **`9d81721` model : load hparams.n_layer_nextn before n_layer() calls (#28159)**
  將 `n_layer_nextn` 的載入提前到 `n_layer()` 呼叫之前，並移除 21 個模型檔案中重複載入的程式碼（bailingmoe2/3、deepseek 系列、glm 系列等）。
- **`d11b3cc` model : load relevant arrays with n_layer_all (#28173)**
  修正部分陣列應以 `n_layer_all` 而非既有層數載入的問題。
- **`73159c3` model : fix gemma4-assistant (#28183)**
  修正 gemma4-assistant 架構的載入錯誤。
- **`f28493c` models : appropriately flag noscan ssm_a tensors (#28121)**
  修正 bailingmoe3、kimi-k3、kimi-linear 的 `ssm_a` tensor 掃描旗標標記。
- **`dfc29b6` context : autoscale n_ctx_train when yarn scaling specified (#28030)**
  指定 YaRN scaling 時自動調整 `n_ctx_train`，避免設定不一致造成上下文長度誤判。

以上五筆多為前次架構相關修改（如 n_layer_nextn 相關重構）所引發的連鎖修正，屬於同一批架構清理工作的後續收尾。

## 四、SYCL 後端 (3 commits，含一組新增 + 回退)

- **`5eec3ad` sycl : support limit max alloc memory within 2GB for host-pinned memory (#27559)** (Neo Zhang)
  為 host-pinned memory 配置加入 2GB 上限限制。
- **`1f3d318` sycl : add Kronecker product FWHT support for sizes 384, 640, 768, 1280 (#28016)** (Jingxin Li)
  新增 Kronecker product 型 FWHT（Fast Walsh-Hadamard Transform）對特定尺寸的支援。
- **`c845263` Revert "sycl : add Kronecker product FWHT support..." (#28184)** (Titaniumtown)
  上述 `1f3d318` 隨即被完整回退（同日內），推測合併後發現問題。

## 五、CUDA 後端 (1 commit)

- **`3466812` cuda: fuse MoE weighted expert reduction (#25952)** (anujj)
  將 MoE combine 尾端「權重相乘 + view + 累加」的子圖融合為單一 CUDA kernel，避免中間結果寫回 global memory，支援 k=2..15 動態專家數；可用 `GGML_CUDA_MOE_WEIGHTED_REDUCTION=0` 停用。未匹配到的圖仍走原本逐運算路徑，`test-backend-ops` 新增對應覆蓋測試。

## 六、其他雜項（文件 / CI / 量化 / 測試）(4 commits)

- **`518b762` kleidiai : Update KleidiAI Documentation (#26078)** (Jonathan Clohessy) — 更新 KleidiAI 相關的 Android/build 文件。
- **`234a6eb` ci: Bump ggml-org/ccache-action to v1.2.24 (#28083)** (Ludovic Henry) — 統一升級所有 CI workflow 中的 ccache-action 版本。
- **`1b89a43` quantize: row-slab stream to avoid thread starvation (#27830)** (Xuan-Son Nguyen) — 重構量化流程中的 row-slab 排程方式，避免執行緒飢餓。
- **`d086dbb` tests : fix log verbosity for test-llama-archs (#28147)** (Georgi Gerganov) — 修正架構測試的 log 輸出等級。

---

## 摘要統計

| 分類 | commit 數 |
|---|---|
| qwen4exp / MTP 推測解碼 | 4 |
| Metal 後端 | 6 |
| 模型架構載入修正 | 5 |
| SYCL 後端 | 3（含 1 回退）|
| CUDA 後端 | 1 |
| 其他（文件/CI/量化/測試）| 4 |
| **總計** | **23** |

本輪重點：qwen4exp 架構的 MTP 推測解碼與 indexer 效能持續優化（皆有實測數據支撐）；Metal 後端持續累積多款 Apple 晶片的 FlashAttention-vec 調校表並修正記憶體洩漏；模型架構載入相關的收尾修正較多，屬於既有重構的連鎖清理；SYCL 側新增的 Kronecker FWHT 支援於同日內被回退，需留意後續是否有替代方案重新提交。
