# llama.cpp 更新報告 - 2026-09-26

- 檢查範圍：`84e76d8a23162eca70490da131945ebec1f09bf4` (84e76d8) → `171e8846b4af9766c354064cb776cb34a50f053f` (171e884)
- 新增 commit 數：19
- 上次檢查日期：2026-09-25

## 摘要總覽

本次新增的 19 個 commit 中，各硬體後端（MUSA、Hexagon、SYCL、HIP、Metal、Vulkan、OpenCL）的正確性修正與功能擴充占了大多數，其中最大的一筆是 **MUSA（Moore Threads）PH1 / MTT S5000** 的多項算子修正與效能調校（單一 PR 內含 9 個子 commit）。效能面則有 **CUDA RMS_NORM + SCALE 融合 kernel**，對 GDN（Gated Delta Net）架構與推測解碼（speculative decoding）場景有實測加速。模型功能面新增了 **`llama_prec_policy` 與 model-driven W4A4** 精度策略路徑，是一個歷時較久、跨多輪審查的較大功能改動。另外還有一項多模態正確性修正值得留意：**LFM2 音訊 mel 前處理器**修正後大幅提升與參考實作的轉錄一致率。整體屬常規開發批次，沒有發現破壞性 API 變更。

---

## Group 1：效能優化 — CUDA RMS_NORM + SCALE 融合

| Commit | 標題 | 摘要 |
|---|---|---|
| `1ab7e5a` | CUDA: fuse RMS_NORM + SCALE into one kernel (#29393) | 將 GDN（Gated Delta Net）q/k 的 l2norm 計算（`ggml_scale(ggml_rms_norm(x, eps/n), 1/sqrt(n))`）融合成單一 CUDA kernel，避免每個 GDN 層多出 2 個 SCALE 節點造成的額外 kernel launch 負擔。新增 `do_scale` flag（與既有 `do_multiply`/`do_add` 同模式），僅在 SCALE 無 bias 且 rms_norm 輸出只有單一消費者時觸發融合。實測在 Qwen3.8-27B 上一般批次有 +0.4%~1.3% 吞吐提升，搭配 draft-mtp 推測解碼時 prefill 提升可達 +4.2%~4.8%（因額外 kernel launch 的host/driver 成本在該場景放大約 10 倍）。Metal（#28948）與 SYCL（#28931）已有相同融合，此為 CUDA 補齊。 |

**說明**：此為純效能優化，數值結果與未融合版本逐位一致（perplexity、draft acceptance 均驗證相符），對使用 CUDA + GDN 架構模型（如 Qwen3.8 系列）且搭配推測解碼的場景有明顯 host overhead 降低效益。

---

## Group 2：硬體後端支援與正確性修正

| Commit | 標題 | 摘要 |
|---|---|---|
| `f9af9be` | musa: fix PH1 (MTT S5000) operator failures and build issues (#29193) | 大型 PR（9 個子 commit），修正 Moore Threads MUSA 後端在 PH1 (MTT S5000) 上的多項問題：① FlashAttention 系列 kernel 誤用 8-byte 拷貝改為與 sm_70+ 一致的 16-byte（prefill 提升約 5.8%）；② 啟用 CUB 演算法路徑（ARGSORT/TOP_K 由大量 not-supported 變為全數支援）；③ 依實際硬體能力啟用 cooperative launch 等裝置初始化項目；④ 移除已由執行期條件涵蓋、不再需要的編譯期防護；⑤ 停用 PH1 上有缺陷的 MMQ 量化 matmul 路徑（原本 260 個 test-backend-ops 案例失敗、27B perplexity 為 nan，改用 MMVQ/cuBLAS 後全數通過、perplexity 恢復正常）；⑥ 修正融合 TOPK_MOE kernel 在 MUSA 上因提早返回的 warp 卡在 barrier 造成的 launch timeout（hang）；⑦ 啟用 GATED_DELTA_NET（原本因編譯限制強制跑 CPU，改用 GPU 後 pp512 由 964 → 2119 t/s）；⑧ 補齊 MUSA 相容標頭以支援 graph capture 選項的編譯；⑨ 將 CI 與文件的預設目標由第一代 MTT S80 改為 PH1 (MTT S5000)。 |
| `ed319fe` | hexagon: use DMA for contiguous dim1 CONCAT (#29404) | Hexagon 後端在 CONCAT 運算子的 dim1 連續情況下改用 DMA 傳輸（含 DMA64 更新），以降低搬移開銷。 |
| `4de0926` | hexagon: add q5_k quant type support (#29123) | 為 Hexagon 後端新增 Q5_K 量化格式支援。 |
| `cd74ef6` | [SYCL] support sparse FA (#28796) | SYCL 後端新增 sparse flash-attention 支援（含合併衝突與程式碼風格修正）。 |
| `d028c69` | HIP: bump HIP_VERSION requried for fp8 to avoid missing __hip_fp8_e4m3 support in 6.2 (#29231) | 調高 HIP fp8 功能所需的最低 HIP_VERSION 版本判斷，避免在 ROCm 6.2 上因缺少 `__hip_fp8_e4m3` 而編譯失敗。 |
| `5a75f14` | metal : split fa kernels into per-dtype libraries (#29329) | 將 Metal flash-attention kernel 依資料型別拆分為各自獨立的 library，屬建置結構調整。 |
| `e351231` | metal: FWHT kernels for block widths above 512 (#29095) | 既有 Metal FWHT（Fast Walsh–Hadamard Transform）kernel 僅支援 64～512 寬度（每 simdgroup 一列）；新增以 threadgroup 為單位（256 threads/列）的 kernel 涵蓋 1024～8192 寬度，支援 F32/F16，並依裝置 threadgroup memory 上限判斷是否回報不支援，避免在記憶體不足的裝置上因 nil pipeline 而中止。M5 Pro 上 test-backend-ops 全數通過。 |
| `e85e15c` | vulkan: fix build issue of legacy glslc without cooperativeMatrix support (#29409) | 修正舊版 GLSLC（不支援 cooperativeMatrix API）編譯失敗的問題：加入 `GGML_VULKAN_COOPMAT_GLSLC_SUPPORT` 巨集檢查以跳過 Intel FA shader 編譯，並過濾掉不支援的 FA 兩階段 kernel。 |
| `a25c986` | opencl: add bin kernel for Q5_K non-MoE / dp4a paths (#29401) | 為 OpenCL 後端新增 Q5_K 量化的 A8 binary kernel（含非 MoE 非 dp4a 與 dp4a 兩個變體），並修正 s 矩陣轉置僅套用於 binary kernel 的問題。 |

**說明**：本次批次以各硬體後端的正確性修正與涵蓋率擴充為主，`f9af9be`（MUSA PH1）規模最大、影響最廣，將原本因缺陷而繞過 GPU 或無法編譯的多個路徑修復並啟用，對 Moore Threads MTT S5000 使用者有實質效能與正確性提升。

---

## Group 3：模型功能與量化

| Commit | 標題 | 摘要 |
|---|---|---|
| `e9f824d` | llama : add `llama_prec_policy` + model-driven W4A4 path (#24364) | 新增 `llama_prec_policy` 精度策略機制與 model-driven 的 W4A4（4-bit 權重 + 4-bit 活化）量化推論路徑，並針對 MXFP4 依來源精度調整 dispatch。此 PR 歷經多輪 rebase 與審查意見調整（含 HIP 上 launch_bounds overload 的 CI 修正）。 |
| `f805c57` | llama : fix tensor split for fused qkv with uneven K/V head sizes (#29294) | 修正在 K/V head 大小不一致的融合 QKV 張量下，tensor split 計算錯誤的問題；同時修正 convert 腳本中 MTP（multi-token prediction）相關的轉換邏輯與旗標支援。 |

**說明**：`e9f824d` 屬跨多輪審查、影響面較大的新功能（新增精度策略與 W4A4 路徑），建議關注後續是否有更多模型或後端接入此機制；`f805c57` 為修正 fused QKV 架構模型（K/V head 大小不對稱）載入時的正確性問題。

---

## Group 4：Tokenizer / GGUF

| Commit | 標題 | 摘要 |
|---|---|---|
| `d81aef1` | gguf-py : TemplateProcessing has final word on add_special_token (#29417) | 修正 tokenizer 設定優先順序：當 `tokenizer.json` 內含 `TemplateProcessing` 設定時，其結果應優先於 tokenizer config 中的 `add_special_token` 設定，移除先前錯誤的覆寫邏輯。 |
| `b248f4a` | gguf-py : ByteLevel processing defaults bos/eos to False (#29422) | 修正 ByteLevel pre-tokenizer 處理時 bos/eos 預設值，改為預設 `False`。 |

**說明**：兩者皆為 GGUF 轉換階段 tokenizer 特殊 token 判斷邏輯的正確性修正，影響轉換出的 GGUF 模型在推論時是否正確加入/省略 BOS/EOS 等特殊 token，建議重新轉換相關模型以套用修正。

---

## Group 5：多模態（mtmd）

| Commit | 標題 | 摘要 |
|---|---|---|
| `fcc8915` | mtmd: fix mel preprocessor in LFM2 audio (#29403) | 修正 LFM2 音訊模型的 mel 前處理器：改用 `log(x + 2^-24)` 取代對數下限截斷、改用對稱 Hann window（對應 `torch.hann_window(periodic=False)`）、將正規化 epsilon 加在標準差而非平方根內。僅 `lfm2a` 前處理器受影響。實測對 200 筆 LibriSpeech（英）與 200 筆 Common Voice（日）語音，與參考實作 `liquid-audio` 逐字轉錄一致率由 EN 191/200、JP F32 187/200、JP F16 187/200，修正後提升為 EN 200/200、JP F32 200/200、JP F16 199/200；mel 相對 L2 誤差由 3~4% 降至約 2e-6。 |

**說明**：屬明確的正確性修正，且有詳細量化驗證數據，若專案使用 LFM2 音訊模型（`lfm2a` 前處理器）進行語音辨識，建議更新以大幅改善轉錄準確度。

---

## Group 6：RPC / 共用工具維護

| Commit | 標題 | 摘要 |
|---|---|---|
| `66963a8` | rpc: include nb in the get_alloc_size cache key and floor the result at ggml_nbytes (#29283) | RPC 後端 `get_alloc_size` 快取的 key 補上 `nb`（stride）欄位，並將回傳結果下限設為 `ggml_nbytes`，避免因快取 key 不完整或計算結果過小造成的配置錯誤。 |
| `27b20ba` | common : extract shared unicode path/string helpers (#29415) | 將共用的 unicode 路徑／字串處理輔助函式抽取整理，屬程式碼結構重構，未改變行為。 |
| `4b1a27f` | common,rpc : simplify fs_create_directory_with_parents() (#29432) | 簡化並修正 `fs_create_directory_with_parents()`：原函式在 Windows 上對部分 unicode 路徑處理有誤；修正後若路徑未帶尾端分隔符，也會正確建立最後一層目錄（符合函式名稱語意）。現有呼叫端皆已附帶尾端分隔符，故此變更不影響既有行為。 |

**說明**：均屬基礎設施維護性修正與重構，`4b1a27f` 修正了 Windows unicode 路徑下的既有 bug。

---

## Group 7：Vendor 維護

| Commit | 標題 | 摘要 |
|---|---|---|
| `171e884` | vendor : update cpp-httplib to 0.58.0 (#29407) | 將內部 vendor 的 `cpp-httplib` 函式庫升級至 0.58.0。 |

---

## 觀察與建議

1. **`f9af9be`（MUSA PH1 修正批次）**：本次規模最大的變更，修復多個原本會導致錯誤結果（MMQ 量化路徑）、編譯失敗或 kernel hang 的問題，並將 GATED_DELTA_NET 由 CPU 移至 GPU（顯著加速）。若專案在 Moore Threads MTT 系列硬體上執行，建議更新並重新驗證。
2. **`e9f824d`（`llama_prec_policy` + W4A4）**：跨多輪審查的新功能，新增精度策略與 W4A4 量化推論路徑，屬本次批次中 API/功能面影響較大的變更，建議關注後續是否有文件說明與更多模型接入。
3. **`fcc8915`（LFM2 音訊 mel 前處理修正）**：有明確量化驗證數據支持的正確性修正，若使用 LFM2 音訊模型建議優先更新。
4. **`1ab7e5a`（CUDA RMS_NORM+SCALE 融合）**：純效能優化且數值結果不變，對 GDN 架構模型 + 推測解碼場景有感提升，可安全套用。
5. **`d81aef1` / `b248f4a`（gguf-py tokenizer 邏輯修正）**：影響 GGUF 轉換時特殊 token 的判斷結果，建議對已轉換模型評估是否需要重新轉換。
6. 本次沒有發現破壞性 API 變更或安全性問題。
