# llama.cpp 變更報告 — 2026-09-17

- 追蹤範圍：`ggml-org/llama.cpp` master 分支
- 檢查區間：`930e2fa` (2026-09-16) → `4bc272f` (2026-09-16)
- 新增 commit 數：19

## 摘要

本次區間內全部 19 筆 commit 皆於 2026-09-16 送出，內容集中在**後端數值正確性修復**與**效能調校**，其中包含一筆**安全性（Security）修復**（RPC server use-after-free，可能導致遠端程式碼執行），需特別留意。此外新增了兩個模型架構支援，以及若干 CI 與工具鏈小改動。

---

## 🔴 安全性修復（優先關注）

### RPC server：釋放 buffer 後未清除快取的計算圖，導致 use-after-free
- `60199339` — rpc : invalidate cached compute graph when a referenced buffer is freed (#24292) — 作者 y198

RPC server 會快取每個裝置最近一次的計算圖，讓 `GRAPH_RECOMPUTE` 可以重複執行而不必重新傳送 tensor 資料。但快取圖中的節點持有指向 backend buffer 的直接指標；若該 buffer 之後透過 `FREE_BUFFER` 被釋放，下一次 `GRAPH_RECOMPUTE` 會透過懸空指標（dangling pointer）重新執行，造成 use-after-free。

**影響**：此漏洞可被未經驗證的遠端 client 觸發。攻擊者可透過後續的 `ALLOC_BUFFER` / `SET_TENSOR` 指令重新塑形被釋放的記憶體區塊，進而讀寫該懸空指標，足以洩漏 libc 位址並劫持 buffer 介面的 vtable（`BUFFER_CLEAR` 所使用），最終達成遠端程式碼執行（RCE）。

**修法**：在 `free_buffer()` 中丟棄所有快取的計算圖；既有的 null 檢查會讓 `graph_recompute()` 拒絕請求，client 端會在下次呼叫時自動回退到 `GRAPH_COMPUTE`。不涉及協定或 API 變更。

> 若有部署 llama.cpp 的 RPC server（`rpc-server`）對外開放，建議儘快更新。

---

## 分組摘要

### 1. GPU 後端數值正確性 / 效能修復

- **`0a8b29a`** — metal: fix NaN in mul_mm_id when activations exceed f16 range (#26223) — Michael de Gans
  Metal 的 `mul_mm_id`（MoE 矩陣乘）路徑會將 `src1` 窄化為 `half`，當模型的 activation 超過 f16 上限（65504）時會產生 inf，進而讓整個累加 tile 變成 NaN。修法是在乘法前依 2 的次方縮放、儲存時還原，數學上完全等價（非近似），對 decode 幾乎零影響，prefill 略增 ~1-4% 耗時。修復後 Mistral Small 4 系列模型在 Metal 上 prefill ≥32 tokens 不再回傳全 NaN 的輸出（疑似修復 issue #25722）。

- **`4bc272f`** — vulkan: work around NV bug with argsort_large.comp (#28975) — Jeff Bolz
  針對 NVIDIA 驅動在 `argsort_large.comp` 的 bug 做 workaround。

- **`d4365d9`** — vulkan: make MUL_MAT_ID BN/2 tail unconditional (#28923) — SG-Amadeus
  將 `MUL_MAT_ID` 的 BN/2 尾端分支改為預設路徑（不再受 `enable_smaller_matrices` 開關限制），BN/4 分支維持原狀。

- **`fccf716`** — HIP: broaden MoE ncols_opt tile heuristic on RDNA3.5 (#28935) — WenqiangJia2026
  將 MoE tile 啟發式判斷條件由 `GGML_CUDA_CC_IS_RDNA3_0` 放寬為 `GGML_CUDA_CC_IS_RDNA3`，涵蓋 RDNA3.5（如 AMD Radeon 8060S）。實測 MoE 模型 token generation 提升約 6-16%。

- **`83078fe`** — CUDA/HIP: improve access patterns in im2col (#28013) — uvos
  改善 CUDA/HIP `im2col` 的記憶體存取模式。

- **`2f3fd02`** — Enable CUDA graph for MTP draft (#28549) — Gaurav Garg
  為 MTP（多 token 預測）draft 路徑啟用 CUDA graph 以提升效能。

- **`f266648`** — spacemit: fix wrong transpose function for int16 data (#25161) — I3eg1nner
  `permute_transpose_impl` 中 `sizeof(int16_t)` 分支誤呼叫了 32-bit 版本的 transpose 函式（複製貼上錯誤），導致 4x4 int16 矩陣轉置 16 個位置中有 14 個結果錯誤。已修正為呼叫正確的 `rvv_transposed_s16_mn_to_nm`。

- **`82324fc`** — hexagon: accept the zeroed rope probe in supports_op (#28995) — Marco Colombo
  llama.cpp 會用全 0 參數的 rope 來探測 weight 放置位置，先前 hexagon 後端會拒絕 `n_dims==0` 或 `freq_base==0`，導致 `rope_freqs` 被錯誤分配到 CPU，使 decode 計算圖在每個 full-attention 層都被拆分（例如 gemma-4-E2B 從 2 次拆分變成 5 次）。

- **`1ec8188`** — hexagon: Support for K-Quants Q4_K and Q6_K (#28994) — Marco Colombo
  為 Hexagon（Qualcomm NPU）後端新增 Q4_K 與 Q6_K 量化格式的 kernel 支援。

### 2. 模型架構支援

- **`7d6f5d0`** — model: add support for HrmTextForCausalLM (DFM Mimir 1B) (#27625) — George
  新增 HRM-Text 架構支援：此架構在同一序列上以 low/high 兩個 transformer stack 交替循環運算。包含轉換腳本（融合 QKV 權重的重新映射）、載入器（block_count 為多重循環的別名 cache slot）、圖建構（sigmoid-gated attention + SwiGLU FFN）。目前僅支援因果注意力（causal-only），KV cache 開銷約為同寬度 32 層模型的 4 倍。作者標註大部分程式碼由 AI（GLM-5.3）產生並經人工驗證，屬實驗性功能。

- **`37b53fd`** — qwen4exp: add hc ops (#28901) — Aman Gupta
  為 qwen4exp 架構新增 hc 相關運算子。

- **`7ceed87`** — models: allow Nemotron-H models to only define layer_norm_epsilon (#28989) — Kartik Gulia
  放寬 Nemotron-H 模型的 hparams 載入限制，允許只定義 `layer_norm_epsilon`。

### 3. Tensor Parallel / Context 管理

- **`b04d4e5`** — Change max context length for auto-fitting with unified KV (#28849) — Gaurav Garg
  調整 unified KV cache 自動配置時的最大 context length 計算方式。

- **`fb27a52`** — TP: fix split state and granularity for fused QKV gemma4, qwen35 (#28965) — David Friehs
  修正 gemma4／qwen35（含 qwen35moe）在啟用 `--fuse-qkv` 時的 tensor-parallel split 狀態計算（改用 `n_head * n_embd_head_k` 而非直接用 `n_embd`），並處理 qwen35 系列 fused full-attention 層的情況。

### 4. Chat / CLI 工具

- **`0bec16e`** — chat: force `\n</think>` on reasoning budget end for qwen3-coder (#28869) — Aldehir Rojas
  qwen3-coder 在推理預算（reasoning budget）用盡時，強制輸出 `\n</think>` 以正確結束思考區塊。

- **`e13469a`** — llama-bench: support --version to print build info (#28971) — asbelin
  `llama-bench` 新增 `--version` 參數以印出建置資訊。

### 5. CI

- **`583926e`** — ci: add self-hosted webgpu to hf-jobs (#28712) — Sigbjørn Skjæret
  在 hf-jobs 中新增 self-hosted WebGPU（及 Vulkan）測試機。
- **`c6824a9`** — ci: switch fast jobs back to github (#28959) — Eve
  將快速檢查作業由 `ubuntu-slim` 換回 GitHub 官方 runner（因為 ubuntu-slim 跑 check-requirements 需接近 15 分鐘，改用更快的環境）。

---

## 建議關注事項

1. **安全性**：`60199339`（RPC use-after-free / 潛在 RCE）— 若有對外提供 `rpc-server` 服務，應優先套用此修復。
2. **正確性**：`0a8b29a`（Metal MUL_MAT_ID NaN）與 `f266648`（spacemit int16 轉置錯誤）皆修復了會導致模型輸出完全錯誤的 bug，若使用對應後端建議更新。
3. 其餘為效能優化與新模型/新量化格式支援，屬正常迭代。
