# llama.cpp 每日變更報告 - 2026-09-14

- 監控倉庫：`ggml-org/llama.cpp`（分支 `master`）
- 檢查範圍：`790cf51`（不含）→ `ad6c668`（含），共 **14** 個新 commit
- 檢查區間對應時間：2026-09-12 ～ 2026-09-14

## 摘要

本次新增的 commit 大致可分為四大類：

1. **GPU 後端修正**（OpenCL / Vulkan / SYCL / CUDA）：修補記憶體佈局、驅動 bug workaround、記憶體查詢錯誤，以及缺少硬體支援時的 fallback 邏輯。
2. **CI / 建置流程調整**：調整 test-backend-ops 的平行度與逾時、清理殘留指令、將部分檢查搬到通用 runner。
3. **測試修正**：修正測試名稱 typo、縮小測試規模、排除不支援的架構測試案例。
4. **核心正確性與效能修正**：修正 nemotron-h 模型在特定 metadata 下的除以零當機問題，以及 grammar 堆疊處理的效能優化。

另有 1 個與專案內部 AI 助理設定（`.pi/`）相關的雜項調整。

---

## 分組詳情

### 一、GPU 後端修正

| Commit | 作者 | 說明 |
| --- | --- | --- |
| `56b9eb2` | Hongqiang Wang | opencl：將原本僅套用於 q6_K 的 noshuffle row-alignment 規則，擴大套用到 q4_K、q5_K、q8_0，避免這些量化格式在 OpenCL 上因記憶體佈局假設錯誤而出錯。 |
| `f1e44dc` | Jeff Bolz | vulkan：Nvidia 驅動存在「同一 VkDevice 上兩個 queue 同時 submit 會破壞內部同步」的 bug，暫時以 mutex 包住 queuesubmit 來規避（等驅動修好前的暫解）。 |
| `b6b003d` | Neo Zhang | sycl：修正記憶體查詢（get mem）錯誤——處理不支援 zes API 的情況、調整 log 等級、修正偵測 Level Zero SDK/開發套件失敗時的建置錯誤，並清理未使用的標頭檔。 |
| `ad6c668` | thelittlefireman | ggml-cuda：在不支援 BF16 硬體加速的裝置（Nvidia < Ampere、AMD 非 RDNA3/CDNA）上，自動 fallback 使用 F32，避免在缺乏硬體支援的裝置上出錯或效能異常；邏輯同時套用到 NVIDIA 與 AMD 裝置。 |

### 二、CI / 建置流程調整

| Commit | 作者 | 說明 |
| --- | --- | --- |
| `002a12a` | Georgi Gerganov | ci：將 test-backend-ops 的平行工作數上限設為 `min(nproc, 2)`（單核 runner 用 `-j 1`，多核最多 `-j 2`），並為高效能與預設兩條測試路徑各加上 3600 秒逾時，避免測試卡死拖垮整條 CI；註記未來可再縮短逾時到 1800 秒。 |
| `37b3a9e` | Sigbjørn Skjæret | ci：移除 `server-sanitize.yml` 中殘留的多餘指令。 |
| `c95f8e4` | Georgi Gerganov | ci：把 EditorConfig 與 Code Style 檢查的 workflow 從 `[self-hosted, fast]` runner 改為使用通用的 `ubuntu-slim` runner。 |

### 三、測試修正

| Commit | 作者 | 說明 |
| --- | --- | --- |
| `4a89937` | Georgi Gerganov | tests：縮小 test-backend-ops 中 Flash Attention (FA) 測試的資料規模，可能是為了配合前述的逾時限制、加速 CI。 |
| `243a308` | Michael Taylor | tests：修正 `test-quant-type-selection` 中 Nvidia Nemotron 3 Nano 30B A3B 模型名稱的 typo（原先錯誤命名導致測試案例被跳過而非確實失敗，等同該測試名存實亡），同時重新命名對應的 snapshot 檔案。 |
| `5f436dd` | fairydreaming | tests：將 HY_V4 架構從 `test-llama-archs` 的 WebGPU 測試中排除（該架構目前尚不受 WebGPU 後端支援）。 |

### 四、核心正確性 / 效能修正

| Commit | 作者 | 說明 |
| --- | --- | --- |
| `e49d2c2` | Yaniss Amazouz | models：修正 nemotron-h 模型 NextN/MTP 尾端迴圈計算 expert FFN size 時（`n_ff/n_expert_used`）可能除以零的問題——當 checkpoint 的 metadata 在非 MoE 層上兩個相關欄位皆為 0 時，會導致 SIGFPE 當機且無任何錯誤訊息；修正後會改為回報「metadata 格式錯誤」的明確錯誤。 |
| `7a16a6c` | Clint Herron | grammar：優化 grammar 堆疊處理邏輯——(1) 將原本連續的 find + insert 兩次 O(log n) 查詢，合併為一次 insert 嘗試/查詢；(2) 調整先搬移（move，便宜）再視情況複製（copy，昂貴）到 new_stacks 的順序，避免對大量不會成為最終輸出狀態的中間狀態做不必要的昂貴複製，屬效能優化。 |

### 五、其他

| Commit | 作者 | 說明 |
| --- | --- | --- |
| `bc52a12` | Georgi Gerganov | pi：內部 AI 助理（`.pi/`）設定調整，改為優先使用 `PI_MODEL_NAME` 環境變數來揭露模型名稱。 |
| `6978052` | Bernard Ladenthin | ggml-cpu(s390x)：為僅限 VXE 指令集使用的 repack 輔助函式加上防護（guard），避免在不支援 VXE 的 s390x 平台上誤用。 |

---

## 完整 Commit 清單（依合併時間排序）

1. `56b9eb2` opencl: apply the noshuffle row-alignment rule to q4_K, q5_K and q8_0, not just q6_K (#28575)
2. `f1e44dc` vulkan: workaround NV queuesubmit driver bug (#28830)
3. `002a12a` ci : cap test-backend-ops parallel jobs at 2 and add a 3600s timeout (#28833)
4. `37b3a9e` ci : remove leftover command (#28839)
5. `4a89937` tests : reduce FA test sizes (#28842)
6. `bc52a12` pi : prefer PI_MODEL_NAME env var for model disclosure (#28853)
7. `c95f8e4` ci : run editorconfig and code-style checks on ubuntu-slim (#28854)
8. `b6b003d` sycl : Fix get mem error (#28227)
9. `243a308` tests : fix typo in test-quant-type-selection for nemotron 3 nano (#28835)
10. `6978052` ggml-cpu(s390x): guard VXE-only repack helpers (#28775)
11. `e49d2c2` models : guard the expert FFN size fallback in nemotron-h against a zero divisor (#28779)
12. `5f436dd` tests : exclude HY_V4 from WebGPU test-llama-archs tests (#28855)
13. `7a16a6c` grammar : coalesce find + insert into a single insert and adjust move/copy mechanics (#26885)
14. `ad6c668` ggml-cuda: fallback to F32 on device without BF16 hardware acceleration (#28846)

---

- 最新已檢查 commit：`ad6c66839af3c5646fba8c6c2e2087a1e4e38948`（短碼 `ad6c668`）
