# llama.cpp 更新報告合輯 — 2026 年 8 月（依主題分組）

- **來源倉庫**：[ggml-org/llama.cpp](https://github.com/ggml-org/llama.cpp)，分支 `master`
- **追蹤區間**：`5f55650a`（監控起點，2026-07-31）→ `9723942`（08-31 最新）
- **涵蓋 commit 總數**：約 535 筆（原按日分為 28 篇報告，現已合併並改為跨整月依主題分組，不再區分日期）

---

## 目錄

1. [WebUI 前端](#1-webui-前端)
2. [推測解碼與草稿模型（DFlash/DFlash2/DSpark/MTP）](#2-推測解碼與草稿模型dflashdflash2dsparkmtp)
3. [新模型架構支援與修正](#3-新模型架構支援與修正)
4. [多模態（mtmd）](#4-多模態mtmd)
5. [Server](#5-server)
6. [CUDA 後端](#6-cuda-後端)
7. [Vulkan 後端](#7-vulkan-後端)
8. [OpenCL 後端（含 Adreno）](#8-opencl-後端含-adreno)
9. [Metal 後端](#9-metal-後端)
10. [SYCL 後端](#10-sycl-後端)
11. [Hexagon（Qualcomm NPU）後端](#11-hexagonqualcomm-npu後端)
12. [WebGPU 後端](#12-webgpu-後端)
13. [RPC／分散式後端](#13-rpc分散式後端)
14. [其他後端與基礎設施（KleidiAI／OpenVINO／ZenDNN）](#14-其他後端與基礎設施kleidiaiopenvinozendnn)
15. [ggml 核心：版本同步與新運算子](#15-ggml-核心版本同步與新運算子)
16. [量化與模型轉換](#16-量化與模型轉換)
17. [Common／核心邏輯／取樣器／CLI／Chat](#17-common核心邏輯取樣器clichat)
18. [測試](#18-測試)
19. [CI／Release 基礎設施](#19-cirelease-基礎設施)
20. [依賴套件更新（vendor）](#20-依賴套件更新vendor)
21. [文件與貢獻指南](#21-文件與貢獻指南)
22. [本月重點摘要](#22-本月重點摘要)

---

## 1. WebUI 前端

### 1.1 內建工具（Agentic Tools）擴充
- `f2b52a87` server(tools): 新增 `x-tool-cwd` header，讓工具呼叫可指定工作目錄
- `2f56fc34` ui: Web UI 新增可切換 Agent 工作目錄的功能
- `18f7ad7fc` server, ui: 僅在工具真的會讀取工作目錄時才顯示「工作目錄」控制項
- `74ce15741` ui: 在 `file_glob_search` 未啟用時降級工作目錄選擇器行為
- `23634783` ui: 聊天輸入框新增以 `@` 提及本機檔案系統檔案
- `6de1b634` ui: 新增聊天斜線指令（slash commands）
- `fc6545d3` ui: 聊天輸入框改為 `contenteditable` 元素
- `4dd127584` ui: 新增 `read_media`（前身 `read_image`）內建工具，支援圖片/音訊分析
- `7077abbe` ui: 新增瀏覽器端 `get_info` 工具
- `666f8898` ui: 將 `get_datetime` 工具邏輯移至前端執行
- `0021a77d` ui: 重新命名內建工具分類為 Server／Browser
- `fe235f4` ui: 以「每個對話的工具授權政策」取代「每個對話 MCP 覆寫」機制

### 1.2 對話框化重構（Settings／MCP／Dialog）
- `0379a19` ui: 更新 Dialog 元件樣式（響應式佈局＋sticky header）
- `539f245` ui: 將 Settings 與 MCP Servers 頁面改為對話框呈現
- `cae6357` ui: 改善聊天輸入區模型選擇器與新增面板 UI/UX

### 1.3 Store／Services／型別重構
- `e21152dc` ui: 常數重構（整併為常數物件/Enum）
- `a6040c92` ui: 清理型別定義，統一多元件型別來源
- `094e53db` ui: Store 架構改善，改用 barrel import
- `f2efd641` ui: `styles/` 移入 `$lib` scope
- `d86c7d62` ui: 清理 Context，新增聊天訊息操作專用 Context
- `fa4ec459` ui: 聊天輸入相關元件命名調整
- `bdffafa5` ui: 重構 data-attribute 常數與布林字串 Enum
- `fdf4c646` ui: Stores 整併重構
- `3dc7285b` ui: Services 整併重構
- `521a64cd` ui: Stores 拆分重構
- `5b6ddc96` ui: Settings 導覽結構清理

### 1.4 對話管理與其他功能
- `8144f319` ui: 新增瀏覽器風格「對話分頁（Chat Tabs）」導覽，跨 43 檔案，是本月 UI 改動幅度最大的一筆
- `d05f8956` fix: 修正對話分頁快速鍵與聊天輸入框的按鍵衝突
- `82bb4850` ui: 介面改為從 router 模型列表讀取各模型模態（modality）資訊
- `f4f7758c` webui: 支援透過 `?model=`／`?load=true` 網址參數直接載入模型
- `9c5531e2` ui: 修正 `VITE_PUBLIC_SERVER` 環境變數讀取方式
- `3e3a7a41` ui: 聊天設定中「顯示生成統計資訊」預設改為開啟
- `0d9ceae1` ui: MCP 工具結果 `content` 為空時改讀取 `structuredContent`
- `92d1bb0c9` ui: 新增 ESLint 規則並整理排版/import 排序
- `4dee52f82` ui: 聊天輸入表單細節修正
- `77acca43` ui: 已持久化設定改為在 API key 偵測前先讀取
- `f1357e49` ui: ESLint 設定與程式碼風格更新

### 1.5 安全性相關
- `e031d956` ui: 更新有已知漏洞的前端相依套件並清理 Storybook 設定
- `34e9ee57` ui: npm 設定加入 `min-release-age`，降低供應鏈攻擊風險
- `ece963f4` ui: 遮蔽設定頁與錯誤畫面的 API Key 欄位，避免瀏覽器自動填入

---

## 2. 推測解碼與草稿模型（DFlash/DFlash2/DSpark/MTP）

- `596a5795` DeepseekV4 MTP + DSpark 支援
- `3581ba0c` convert：新增獨立 DSpark GGUF 選項與 `--no-nextn`
- `bb4e0e1b` common：DSpark 側車（sidecar）解析規則統一
- `fffbcbdb` metal：實作 DeepSeek V4 hyper-connections 運算核心
- `2b63e061` llama：DeepSeek V3.2 加入 MTP 支援
- `4ed2b13f` model：Qwen3-Next 加入 MTP 支援
- `57c09213` model：GLM-4.7-Flash 加入 MTP 支援
- `1464c62d` metal：實作 DSv4 Lightning Indexer 運算核心
- `563dec81` llama：indexer cache 僅在「full」indexer 層配置
- `1269cb1f` model：載入模型時允許張量 reshape（供 DeepSeek4 使用）
- `1c3c9674` models：修正 dflash `wo_a` 張量載入 reshape 錯誤
- `0ef6e55e` chat：新增 DeepSeek V4 Flash 0731 對話模板
- `9a688e51` fit：修正 MTP 層的記憶體配置錯誤
- `7a20b417f` model：Nemotron 模型加入 MTP 支援
- `62bf73d25` model：Muse Glimmer 支援，整合 DFlash drafter
- `cc078b45b` Nemotron-3.5 加入 DFlash 支援＋NVFP4 量化
- `5d16e81dd` convert：保留 Nemotron `--mtp` 匯出的量化 scale
- `0d0bfcd4` spec：dflash 與 dspark 皆啟用後端取樣、允許 `p_min > 0`
- `f65e568f` common：從草稿模型 GGUF metadata 自動偵測推測解碼類型
- `1d2869c6` spec：自動偵測 MTP 草稿模型類型
- `2bacf9ea` dflash：釐清 `target_layer_ids` 日誌訊息
- `9cd719af` model：支援 DSpark 的 speculators 格式 checkpoint
- `07822bdd` model：LFM2 系列支援 DSpark
- `2c6b141e` common：修正 draft-MTP 搭配 embeddings 使用時的問題
- `2fb989b9` fit：draft context 大小納入 `n_streams` 考量，隨 target context 成長
- `2115b73d` model：bailingmoe3 支援 DSpark 變體
- `bf0a29cc` DeepSeek V4 新增 `-sm tensor`（張量並行）支援
- `ca3d5a3` model：Nemotron3.5 新增 DSpark 支援
- `b10f9ca` spec：新增 DFlash2（本地卷積＋候選選擇器）架構支援
- `2bb9bdd` spec：新增僅供 benchmark 的合成推測接受率選項
- `6fe7498` model：qwen4exp 減少計算圖分割數量
- `8663224` context：停用未融合版 GDN／LID 運算子
- `cc231cb` dflash：修正 DFlash2 使用 NVFP4 草稿模型時遺漏傳入 attention scale，導致草稿 token 幾乎不被接受的問題

**本節小結**：推測解碼生態系是本月最活躍的主題之一——DeepSeek V4/V3.2、Qwen3-Next、GLM-4.7-Flash/4.5-Air、Nemotron、Muse Glimmer、LFM2、bailingmoe3 陸續補齊 MTP／DFlash／DSpark 支援，並在月底推出全新的 DFlash2 架構；同時修正了多項因草稿模型參數擴散不及所產生的記憶體與正確性問題。

---

## 3. 新模型架構支援與修正

- `a09d8abf` 為 minimax-m3 新增「旋轉（rotated）」KV cache 量化支援
- `157b81fe6` model：新增 Granite-Switch 架構（動態選擇內嵌 LoRA adapter）
- `86c298fb8` llama：修復先前重構遺失的 mmproj 量化功能
- `0377426ce` model-saver：修正 MoE 模型 shared/chunk FFN length 中繼資料鍵值互相覆蓋的問題
- `14e78ddef` model：修正 EXAONE 4.5 因 MTP head 誤判層數導致 SWA 未啟用的問題
- `6e62ba538` mtmd：新增 Pocket-TTS 語音合成模型支援
- `16d222fc` model：新增 MiniMaxText01ForCausalLM／MiniMaxM1ForCausalLM 架構
- `ad1de39e` model：新增 Kimi-K3（2.8T 參數，KDA+MLA 混合架構）文字模型支援，本月規模最大的新模型
- `37333667` model：新增 BailingMoE3（Ling-3.0 系列）完整支援
- `7221e24f` model：新增 GraniteSWAForCausalLM／GraniteMoeSWAForCausalLM 架構
- `5a32f7b6` model：新增 dots3-note 純文字模型架構
- `b2e5e9b2` TP：LFM2／LFM2MOE 系列支援 tensor split
- `54ee5ee6` mtmd：dots3-note 補上視覺＋音訊多模態支援
- `b0539c43` DeepseekV4：修正壓縮式 KV cache 在多序列情境下的 rollback 邏輯
- `c060ca97` model：GLM-4.5-Air 新增 MTP 支援
- `71cc86fa` convert：修正前一日 GLM MTP 支援引入的 `index_tensors` 回歸問題
- `a1305329` mamba2：輸入/輸出投影攤平以派發到 GEMM 而非 GEMV
- `6c84c7d` model：新增 Qwen3.8-Flash-Next（qwen4exp）完整支援，新增約 2900 行
- `d7a2074` models：新增 nanbeige4.2-3B 支援
- `deae5ee` model：簡化 MiniMax-01 計算圖（程式碼精簡，非行為變更）
- `bcb6084` convert：修正 Nemotron-H LoRA GGUF 轉換錯誤

**本節小結**：本月新增/擴充的模型架構數量眾多，其中 **Kimi-K3（2.8T）** 與 **Qwen3.8-Flash-Next（qwen4exp）** 是規模最大的兩筆；EXAONE 4.5、DeepSeek V4、GLM 系列則以正確性修正為主。

---

## 4. 多模態（mtmd）

- `db7d8b24` mtmd：新增 `n_embd_head`
- `de69995` mtmd：新增 minicpmv46 downsample 支援
- `07132750` mtmd：新增 Qwen3-TTS 支援（⚠️ 破壞性變更，影響 `llama-tts` 執行檔相容性）
- `474c92e7` mtmd：修正短輸入產生重複空音訊區塊的問題
- `717dad5c` mtmd：deepseek-ocr 新增多列批次處理支援
- `b06aa774` mtmd：修正 Unlimited-OCR `max_tiles` 設定問題
- `c8e03ce8` mtmd/ggml：新增 `ggml_build_forward_order`，修正音訊圖執行順序造成的 CPU assert 錯誤
- `15586e2d` mtmd：新增 chunk 儲存／讀取功能
- `217df17a` mtmd：修正 Qwen3-TTS 生成時文字串流重複餵入的問題
- `4cb22cd5` mtmd：修正 `longest_edge` 忽略 `min_pixels`/`max_pixels` 的問題
- `9d57ce45` mtmd：修正 Granite4 Vision 影像序列組裝邏輯
- `6fed9f6f` mtmd, common：一系列雜項修正
- `9f0d017e` mtmd：強化 Granite 前處理器防禦性檢查
- `b75ecd19` mtmd：LFM2 非分塊影像略過縮圖生成
- `ed1c3a20` mtmd：輸入雜湊改用 SHA-256
- `533b1825` server：已處理 mtmd chunk 另存為 placeholder
- `7acdbb1f` mtmd：修正 LFM2 影像分塊門檻值判斷錯誤
- `9731ad3f` mtmd：deepseek-ocr SAM `ggml_conv_2d` 的 im2col 保持 F32 精度
- `95c409c1` mtmd：新增 `mtmd_bitmap_set_mergeable`
- `681c29d3` mtmd：新增 `--mmproj-device` 參數
- `d775b896` mtmd：透過 ffmpeg 支援 WebP 圖片格式
- `b21e4de7` mtmd：gemma4v/minimax-m3 等模型改用 `ggml_rope_set_offset`
- `56db501e` mtmd：改用 Pillow 數值一致的縮放演算法，修正各模型 `resize_algo` 選擇錯誤
- `160c6b0b` mtmd：修正影片 `moov atom` 位於檔案尾端時的解析問題
- `f295512` args：新增一系列 `--video-*` CLI 參數

**本節小結**：多模態管線本月持續強化影像/影片前處理精度（Pillow 對齊縮放演算法、SAM F32 精度）、輸入雜湊安全性（SHA-256）與新格式支援（WebP、Qwen3-TTS、Pocket-TTS）；`07132750`（Qwen3-TTS）為需留意相容性的破壞性變更。

---

## 5. Server

- `00054751` server：修正推測解碼需重播 draft token 時「已接受 token 數量」的錯誤邏輯
- `99111b19` server：新增 `get_info` 內建工具
- `4308a4f0` server：修正 Windows 上內建工具輸出 OEM 編碼需轉 UTF-8 的問題
- `0b14b87d` server：加入未來預設 port 將由 8080 改為 9931 的提示
- `a035a888` server：`/metrics` 新增推測解碼相關計數器
- `f9e832c1` server：強化 `file_glob_search` 目錄遍歷安全性與正確性
- `6a32c29a` server：修正 `/cors-proxy` 端點空白回應問題
- `e1470ee6` server(router)：不再逐出忙碌中的模型
- `dff15d4a` server(router)：新增 LRU 排程機制
- `dd2c7c447` server：新增以 Docker 為基礎的工具隔離（isolate）支援
- `7ba604f1c` server：`get_info` 正確回報隔離環境自身的工作目錄
- `1e396e72a` server：Docker 工具測試改為以實際容器執行判斷可用性
- `4ae84dea2` server：新增 SSH 遠端與 Podman rootless 隔離支援
- `5d9e5ac30` server：slot save/restore 支援多模態（媒體）輸入
- `decaf508` server：重構並修正 `/metrics` 統計邏輯
- `981184e4` server：`index.html` 改為 `no-cache` 提供
- `77918caf` server：重構請求佇列，讓 `/metrics`／`/slots` 在 `llama_decode()` 執行期間仍可存取
- `22b8e310` server：重新設計 `yield_to_queue` 執行緒模型
- `ee4c505a` server：新增 `dedup-cache-models` preset 選項
- `ee0ea03a` server：啟用驗證機制時模型清單端點改為需授權
- `947fd9bb` server：重構 sleep 處理，休眠時仍可存取 `/metrics`
- `8a832e4b` server：修正 `--docker-repo` 被誤判為 router 模式
- `9855ad69` server(router)：`startup_models` 改為延遲載入
- `e8eed452` server：新增 `LLAMA_SERVER_SLOTS_N_DIFF` 環境變數
- `1729ed53` server：`--prefill-assistant` 搭配含 `tool_calls` 訊息時明確拋出錯誤，而非靜默移除工具呼叫
- `1844325` server：新增 `--kv-unified-per-slot`，讓每個 slot 擁有獨立 context/KV 配置

**本節小結**：Server 端本月的核心投資是**工具隔離（tool isolation）**能力（Docker/SSH/Podman）與**執行緒模型重構**（讓監控端點在長時間 decode 期間仍可用），另有一則行為變更值得留意：`1729ed53` 讓 prefilled-assistant + tool_calls 的組合由「靜默破壞」改為「明確報錯」。

---

## 6. CUDA 後端

- `15e755f3` cuda：改用 `__byte_perm` 提取 Q2_0 元素
- `69bf64379` CUDA：修正量化 cpy kernel 啟動的 thread/block 數量計算
- `687e77892` CUDA：融合 `rms_norm + mul + rope (+ view + set_rows)`
- `598863317` cuda：新增 warp-per-row WKV7 kernel（單 token 解碼加速）
- `ebb546b7e` CUDA：僅在 `mul_mat_id` 確實需要 stream 同步時才停用 CUDA graphs
- `25ae3a9b` CUDA：DGX Spark 上稠密模型 bs=1 時 MMVQ `nwarps` 調整為 8
- `d9b6be07` ggml-cuda：為 cuBLAS handle 提供靜態 workspace
- `2b562109` CUDA：依硬體與量化型態新增 mvq→MMQ 解碼切換點調校
- `29ea9412` cuda：新增 `POOL_1D` 運算子支援
- `fc35562` cuda：解除 sm_60（Pascal）上 MoE mmq 路徑限制，新增專屬設定檔
- `f1793c1` CUDA：MoE 快速路徑推廣至任意 `n_expert_used`（Qwen3.8-Flash-Next 實測 PP 由 2334→2600 t/s）

---

## 7. Vulkan 後端

- `876a4321` vulkan：新增 `POOL_1D` op
- `eb41d503` vulkan：新增 Windows Intel GPU 驅動版本檢查機制
- `6f3c0a79` vulkan：CI 使用的 Vulkan SDK 升級至 1.4.357.0
- `a7a6d0d` vulkan：`topk_moe` 融合擴充支援 `sqrt(softplus)`
- `f26efa02` vulkan：實作 `GATED_LINEAR_ATTN` 運算
- `22dc605c` ci：修正 Vulkan llvmpipe CI 測試
- `803b7fca` vulkan：修正提交批次門檻時機，新增除錯工具診斷 `DeviceLost`
- `a7cd2f0e9` vulkan：新增 TQ2_0（三元）量化格式支援
- `9b0a2ce8` + `7b38cb71` + `6b4344ec` vulkan：Intel Xe coopmat1 共享記憶體 padding／SLM-A reshape 優化，並修正問題驅動版本判斷
- `98d1e92c` vulkan：0↔2 維度置換 CONT 的分塊轉置
- `b95502ba` vulkan：command pool 清理加入空指標檢查
- `dc72703f` vulkan：coopmat1 路徑 KV cache 的 q8_0 反量化改為僅一次
- `2cfdb5fc` vulkan：為 shader 加入來源分組
- `78ec4c37` vulkan：FA MMQ 的 Q 量化計算改用 fp32
- `3f545bec` vulkan：新增 `PAD_REFLECT_1D` 運算子
- `bf94216` vulkan：新增 `cross_entropy_loss` 與其反向傳播
- `5e6a37c` vulkan：修正 warptile 對 warp size ≤64 的假設，clamp 相容更大 warp
- `cb30059` vulkan：新增 DeepSeek V4 所需的 `LIGHTNING_INDEXER` 支援
- `b387ddf` vulkan：**修正 `ggml_vk_graph_optimize` 中缺失的 view-alias 相依性**，此前會導致有 recurrent state 的模型（如 Qwen3.8）在 AMD/NVIDIA Vulkan 上產生無錯誤訊息的靜默錯誤輸出（greedy decoding 結果錯誤、每次啟動結果不同），屬本月重要正確性修正
- `90c26fc` Vulkan：MoE shader 加入 row ID／專家數量 hoisting 優化
- `77f132c` vulkan：`mul_mat_id` 的 padding 策略由補 N 改為補 K
- `c9ca51c` vulkan：整理重複的 fastdiv 實作

---

## 8. OpenCL 後端（含 Adreno）

- `c745be2a` opencl：修正 `ggml_backend_opencl_init()` ref_count 未遞增
- `0ab9d6fe` opencl：限制 GLU 運算的本地工作群組大小
- `39eab74a` opencl：大型 q6_K lm_head 改走 flat GEMV 路徑
- `689e227db` opencl：FA prefill kernel 對本地記憶體 K tile 做轉置
- `89e0aa6fd` opencl：X1E 平台 FA(c8) cluster width 預設調整為 16
- `cb27fe9c3` opencl：權重超過 `image1d_buffer_t` 上限時改用 flat mv 處理 Q5_K
- `60addddf` opencl：修正通用 FA tile kernel 跨 subgroup 時的 WAR 競爭問題
- `b062ba73` opencl：Mamba-2（d_state 128/256）融合版 `ssm_scan` 移植至 GPU
- `9ee9fc04` opencl：讓 MoE 專家路由 scatter 操作具備確定性
- `6b4fa88a` opencl：修正 `norm` 運算 local size 計算錯誤
- `6503355d` opencl：修正 Adreno A6x/A7x 舊編譯器上 q6_K flat `mul_mat` 相容性問題
- `cd26896c` opencl：Adreno A7X 編譯器問題因應，vocab-scale K-quant lm_head 改留 CPU 執行
- `3af988fa` opencl：gpt-oss MoE per-expert bias 加法融合進 epilogue
- `5854625` opencl：新增 `gemm-moe`（q4_0／mxfp4）bin kernels
- `c841aee` opencl：兩代 Adreno GPU（X2E／A7X）matmul 路徑調整，實測 prefill 提升 9%～25%

---

## 9. Metal 後端

- `9d21b57f` metal：二元運算（bin ops）新增 F16 支援
- `e40bf886` metal：避免 `kernel_lightning_indexer` 中 `threadgroup` 矩陣陣列實例化
- `a194a75b` metal：修正 NORM/RMS_NORM 在列長度留下不完整 simdgroup 時的計算錯誤
- `4a84b0ad` metal：新增 TQ2_0 量化格式支援並優化 `mul_mv` kernel
- `8ef78e64` metal：改用打包型別進行 q8_0 反量化
- `70aff252` metal：Flash Attention 前先將量化 KV 反量化為 F16
- `63b64a50` metal：僅在大 batch 情境對 KV cache 執行反量化
- `a3027337` metal：修正 tensor API mat-mat kernel 在 K 非 32 倍數時的範圍 clamp
- `b615f5b4` metal：原始碼依運算子拆分為約 20 個獨立檔案＋平行編譯（本月最大規模的 Metal 重構之一）
- `f280b269` metal：flash-attn vec 核心新增「依裝置調校」的 (Q, NE) 參數表，新增離線調校工具 `tools/tuning`
- `3737e413` metal：buffer 配置加入 null-check，修正記憶體受限裝置的 OOM 崩潰
- `5d5cb4c3` ggml-meta：新 tensor 正確傳遞 buffer usage 並呼叫初始化
- `11cd988` ggml-metal：新增 chunked SSD MMA，優化 Mamba-2 prefill
- `915dc6d` metal：修正缺少 `@autoreleasepool` 造成的記憶體洩漏
- 多筆 **fa-vec 依裝置調校表**（純數據新增，不影響邏輯）：M4(`f5e85d4`)、M4 Pro(`6d6b697`)、M3 Max/M5/M5 Pro(`8963a9b`)、M1 Max(`5ea1b12`)、M4 Pro 補完(`17252c7`)、M2(`c589f0e`)、M3 Pro(`b8b743c`)、M3 Ultra(`093adb2`)、M2 剩餘 Q4_1/Q5_0/Q5_1(`bd55e6a`)
- `3173a56` metal：加入 shared memory padding 的防禦性 assertion
- `d882575` metal：修正 F16 `mul_mat`/`mul_mat_id` 缺少對應 kernel 時的 null-pipeline 崩潰，改為 fail-closed

**本節小結**：Metal 後端本月經歷了兩次超大規模重構（原始碼依運算子拆分＋平行編譯、flash-attn 裝置調校表，合計異動超過 15000 行），並密集補齊各代 Apple 晶片（M1 Max 至 M5 Pro）的效能調校資料，同時修正多個記憶體/崩潰相關的正確性問題。

---

## 10. SYCL 後端

- `9d9a6d29` SYCL：新增 oneMKL GEMM flash attention（XMX 加速），實測 1.85x~1.97x
- `d5d3e05b` SYCL：補齊 `cpy` 缺漏的資料型態
- `1c5b89ff` sycl：新增裝置間直接記憶體複製支援
- `a2be61dc` SYCL：`mul_mat` 新增 Q2_0 支援
- `15537259` sycl：融合 `RMS_NORM + MUL`
- `272700b3` sycl：修正 iGPU 分類邏輯
- `66fa168a` SYCL：oneDNN SDPA 擴充支援非 FP16 KV cache
- `6c8dcaa7` sycl：平行化非連續記憶體 concat kernel
- `c074cb3f` sycl：`set_rows` 補齊缺漏資料型別
- `eef5f3e3` sycl：修正 Arc 770 上 `FLASH_ATTN_EXT` 錯誤
- `31558dbb` sycl：新增 DSv4 專用算子（LIGHTNING_INDEXER、DSV4_HC 系列）
- `6b5c2efb` sycl：GLU 系列新增扁平化計算路徑
- `fc3f10b3` sycl：修正 UE4M3 解析錯誤
- `f8e30266` sycl：合併 `ssm_conv` 視窗記憶體讀取
- `d415e65a` sycl：`concat` 新增多種量化格式支援
- `8efbf65d` sycl：新增 DMMV ESIMD Q3_K kernel，ESIMD 改為執行期偵測預設啟用
- `1ee1cd9b` sycl：融合 UNARY（silu/sigmoid/softplus）與 MUL
- `154d57af` sycl：GEMM 非 oneDNN 路徑移除多餘 fp32 提升
- `a97123e4` SYCL：支援 Host Pinned Memory 提升傳輸效能
- `3d938853` sycl：融合 gated-delta-net 狀態寫回 cpy
- `65091386` sycl：融合 q4_K dense FFN 的 gate/up matmul + GLU（批次 B=8 提升約 12.4%）
- `37a215c9` SYCL：新增 `OPT_STEP_ADAMW`／`OPT_STEP_SGD` 支援
- `f275595d` sycl：修正量化 cpy kernel thread/block 計算
- `0882c7bc` sycl：支援 `GGML_HINT_SRC0_IS_HADAMARD`
- `6cc504a2` sycl：無裝置時回報零裝置而非中止
- `9e96cf77` sycl：修正 `mlock` 載入模型問題
- `6602dd33` sycl：清理編譯警告
- `1cb3f5eb` sycl：調整 Alchemist GPU OneDNN gate 邏輯
- `9e89a196` sycl：新增 Q5_K ESIMD kernel
- `ff14356e` + `7a0e42fd` sycl：新增 Q2_K reordered MMVQ/ESIMD kernel，合入後很快被 revert
- `86722900` sycl：Q2_K reordered kernel 修正後重新合入（加入額外 gate 參數）
- `814d84bc` sycl：明確標記 TQ2_0 為不支援
- `d077b4c` sycl：BMG（Xe2）量化 KV decode 改用 TILE 演算法
- `be87620` sycl：oneDNN SDPA 路徑的 f16 KV cache 改為原地綁定
- `cc83d7b` sycl：改善 `--fit` 對 VRAM 用量的估算精確度
- `370cb12` sycl：TOP_K 長行拆分給多個 work-group 平行處理

**本節小結**：SYCL 是本月更新頻率最高的後端之一，涵蓋大量算子融合（RMS_NORM+MUL、GLU、gated-delta-net writeback、dense FFN gate/up/GLU）與正確性修正；其中 Q2_K reordered kernel 經歷了「合入→revert→修正後重新合入」的完整週期。

---

## 11. Hexagon（Qualcomm NPU）後端

- `990e3bfe` hexagon：修正 FA 在 HMX 佇列上的執行順序，並打包 rescale D 矩陣
- `192067b` hexagon：新增多 NPU 裝置（IQ9、IQ10）支援，後端改為完全非同步執行，大幅重寫 matmul/get-rows/set-rows
- `e70802a` ggml-hexagon：新增 HTP unary 算子 ABS 與 LOG
- `83d855c` hex-unary：修正 `RMS_NORM_MUL` 分組/廣播情境下權重偏移計算錯誤
- `d7bd3bf` snapdragon：Windows SDK 安裝腳本由 PowerShell 改寫為 Python
- `9e54e68` hexagon：支援裝置探索與按需建立 session
- `9723942` hexagon：修正 CPY 運算子的同步 fence 錯誤

---

## 12. WebGPU 後端

- `8f4646a6` ggml-webgpu：改善長上下文、量化 KV 情境下 flash_attn_vec 的效能與正確性
- `f401bb139` ggml-webgpu：重構 wgsl 檔案，簡化 flash attention
- `84f712946` ggml-webgpu：修正 CI 錯誤，`max_kv_tile==0` 時停用 subgroup matrices，`cpy` 新增 i32 支援
- `5112b973` ggml-webgpu：新增支援 src0/src1 記憶體重疊的 `mulmat`（供 minimax-01 等模型使用）
- `c1c766da` webgpu：修正 flash-attention shader `#include` 順序造成的符號衝突
- `5ea87dda` webgpu：修正 ARGSORT／TOP_K 處理無窮值的錯誤

---

## 13. RPC／分散式後端

- `b114b473` rpc：新增 Apple RDMA 作為傳輸層
- `d0132a6` rpc：實作事件（event）與非同步後端 API
- `2bf0415` rpc：修正不支援 RDMA 的較舊版 macOS 相容性問題
- `742347b` rpc：修正 Apple RDMA transport 關閉連線時洗版式錯誤輸出
- `73f56d1` ggml：新增 `ggml_backend_op_alloc_size_may_expand`，RPC 用以取代硬編碼清單
- `6d1479c` ggml：修正 `ggml_backend_buft_get_alloc_size()` guard 條件錯誤
- `a7cc83b` rpc：修正多伺服器場景下序列化其他伺服器 buffer 造成資料錯亂的問題，新增雙伺服器回歸測試

---

## 14. 其他後端與基礎設施（KleidiAI／OpenVINO／ZenDNN）

- `1e225995` ggml-zendnn：`mul_mat_id` 改用 group matmul 直接 API
- `132753bf4` kleidiai：新增 aarch64 執行期 CPU 特性偵測機制
- `aee56b3a` OpenVINO：大型更新，涵蓋 Qwen3.5（含 GPT-OSS MoE）支援、記憶體優化（RSS 降幅可達 55%）、編譯快取（8B 模型編譯時間降至 2.4 倍速）、多項正確性修正（NEOX RoPE、Qwen3VL decode 崩潰）
- `62b22690` kleidiai：新增 SME2 F32 GEMV kernel 支援
- `d222767c` kleidiai：重構建置系統整合方式
- `0a5ac49b` devops：OpenVINO 改用 `GGML_NATIVE=OFF`
- `511f9c1` OpenVINO：升級至 2026.3.1，新增 whisper.cpp 支援與 Qwen3.5 NPU 靜態 shape 執行

---

## 15. ggml 核心：版本同步與新運算子

- 版本演進：ggml `0.18.1`(`15831f57`) → `0.19.0`(`933f46f3`) → `0.20.0`(`06ae2326`) → `0.20.1`(`cea66f4c`) → `0.20.2`(`da786dc2`) → `0.21.0`(`01ff204f`) → `0.22.0`(`93882361`)；llama.cpp 本體同步升級至 `0.2.0`(`bb4caa75`) → `0.3.0`(`c1d0e7a0`)；每次版本提升均伴隨對應的 `sync: ggml` commit。
- `dbadb68e` ggml：split graph 輸入改為動態配置記憶體
- `f8def7fe1` ggml：要求 CUDA／Metal 的 ROLL 運算子輸入須連續
- `27df9199` fix：讀取 GGUF 陣列前先驗證型別，避免解析不受信任檔案時崩潰
- `680a9ae63` cmake：導入語義化版本控制（semver）
- `13fd0bb55` cmake：`find_package(ggml <version>)` 加入版本比對支援
- `84979813` ggml：修正 backend split scheduler 競爭條件
- `929d47a3` graph：`k_iswa` 的 `build_attn` 中把 V 建立為 K 的 view
- `cd644c39` ggml-cpu：`__fp16` 使用加上 `__ARM_FP16_FORMAT_IEEE` 條件保護
- `fe8156f7` + `749f688f` ggml：新增 `ggml_rope_set_offset`（先於核心＋Metal 落地，隨即擴及 OpenCL/SYCL/WebGPU/Hexagon）
- `873e5d8e` model：既有模型（deepseek2/4、dflash、minicpm3、plm）改用 `ggml_rope_set_offset()`
- `369e1cd6` ggml：`concat` 運算子改用整列 memcpy 取代逐元素
- `0329fcda` gguf-py：`GGUFReader` 加入大小防護檢查
- `6036c635` ggml：修正 `ggml_clamp` 實作錯誤
- `a14dba68` ggml：精簡 CUDA／Metal 虛擬裝置命名字串
- `57291f2` ggml：`graph_optimize` 支援傳入跨後端 alloc 相依資訊
- `0190529` ggml：新增 `SWIGLU_CLAMP` 融合啟動函式運算子，幾乎所有後端補齊實作（本月改動幅度最大的單筆核心變更，涉及 40 檔案、約 768 行）

---

## 16. 量化與模型轉換

- `020760ad` convert：新增 Q1／TQ2 量化的位元序轉換支援
- `eb5667a1` convert：修正 transformers 5.x 下 DeepseekV4 的 RoPE 參數錯誤
- `e700bfb3` convert：接受 `ExaoneMoeForCausalLM` 拼法
- `9de0fcf2` model-conversion：新增 `--model-name` 參數
- `3db4ff87` model-loader：修正量化後 reshape 張量的 stride 計算錯誤
- `1138b851f` model-conversion：causal embedding 驗證改用 `save_output_data`
- `38406d597` imatrix.cpp：finite 檢查改為只檢查實際觸發的 MoE 專家
- `2468576f2` requirements：s390x 改用穩定版 torch
- `6eff59326` convert：處理 Gemma4（transformers 5.15）新版 `per_layer_config`
- `55f453b92` wavtokenizer-dec：`block_count` 加入邊界檢查
- `d8df12eb` vocab：支援整數型態的 tokenizer 分數
- `a3b1effc` convert：修正 Nemotron 3 Ultra 區塊數量計算錯誤
- `dac869b` conversion：修正 Nemotron 3.5 Lightning 層轉換問題（transformers 5.5.1+）
- `50f068f` bench：新增 `--tensor-read-lazy` 參數
- `b19cbe9` convert：避免 `LazyChunkedTensor` 被提前轉換為 ndarray
- `bebc935` common：`--tensor-read-lazy` 改名為 `--lazy-mode`（新增 `-lzm` 簡寫）
- `2578138` llama：改善 `TENSOR_READ_LAZY` 模式下的張量讀取處理
- `fac889f` llama：model_loader 新增 `TENSOR_READ_LAZY` 延遲讀取模式
- `732707d` quantize：限制工作記憶體上限，避免超大張量耗盡記憶體

---

## 17. Common／核心邏輯／取樣器／CLI／Chat

- `ddd4ec1` chat：DS4 在 thinking 區塊內啟用 tool call 解析
- `c629da5` cli：將 `reasoning_content` 持久化至對話歷史
- `f5919bf4` chat：新增 Qwen3 專用解析器（tagged thinking）
- `cd0fa605` grammar：重複次數 ≥2000 自動降級為無上限
- `dd1ea5243` / `030ebb558` llama：新增多輸出（multi-output）後端取樣支援
- `ba360efe1` chat：收緊 Qwen 系列裸函式呼叫解析規則
- `0b1bad14f` chat：修正 Muse Glimmer 在 `<|eom|>` 分隔時工具呼叫誤判問題
- `48d22e295` common/peg：修正不完整跳脫序列處理
- `153d324bc` llama：新增預設載入模式 `auto`，iGPU 環境自動避免 mmap
- `f785fc9ea` spec：重構並簡化 `speculative-simple` 範例
- `8e7f22b67` common：新增系統層級設定檔支援
- `2606220d` chat：修正 LFM2 工具呼叫參數名稱前綴解析歧義
- `a94d563e` common：CPU 相關參數設定一致套用到所有工具
- `0177dcc7` common：`--mmap`/`--mlock`/`--direct-io` 統一遷移為 `--load-mode`
- `5f754ea0` common：`--models-dir` 支援載入 MTP 輔助模型
- `10bf611e` llama：**新增 LoRA 張量檔案邊界檢查**（含整數溢位保護），修正載入損毀/惡意 LoRA 檔案時可能越界讀取的問題（安全性修正）
- `b94041a9` chat：重構 `supports_string_content`/`supports_typed_content` 偵測邏輯
- `dc64a162` common：JSON schema 遇不支援 regex pattern 時優雅回退
- `2e92ecd0` models：移除模型載入流程中重複讀取 metadata
- `9fee29e9` arg：移除 CLI 中的 `-no-cnv` 參數
- `d9f918d2` + `70adb1b4` common：新增 `json.h` 抽象層（跨 45 檔案），並修正其 clang LTO 編譯問題
- `ba8e0edd` common：不需輸出時跳過 device_info 迴圈，避免每裝置浪費約 550MB VRAM
- `0cc5b149` chat：收斂 Qwen3-Coder workaround 的作用範圍
- `eb25b726` grammar：**修正 GBNF 字元集合中 `\-` 轉義解析錯誤**，此問題會直接影響依賴文法約束的工具呼叫（tool-call）結構化輸出
- `c5fc7e3` llama：新增 `--n-cpu-ffn` 選項，指定固定跑在 CPU 上的 FFN 層數
- `62acc89` kv-cells：`for_each_token_in` 掃描提前結束（長 context 下 generation 速度提升，55k context 由 56.3→74.3 t/s）
- `bdf3955` memory：僅在 `k_rot` 已配置 buffer 時才複製 Hadamard 矩陣，修正崩潰

---

## 18. 測試

- `1621a3d3` tests：server 測試套件執行速度提升約 3 倍
- `4801e3c56` tests：跳過 HIP 平台上新增的 multi-output sampling 測試
- `b3df57286` tests：清理 server 測試，CI 改用 `tests.sh`
- `9afff1b74` tests：修正 Windows 平台執行 server 測試的問題
- `79fe7991` tests：Windows/`BUILD_SHARED_LIBS` 情境跳過 `test-unicode`
- `5fff1284` test：將 FA「V 為 K 的 view」情境改為參數化測試案例
- `4a08fa29` test：`tools/parser` 併入 `tests/test-chat-auto-parser.cpp`
- `8d9af256` test：修正多 GPU server 測試失敗問題
- `75844307` tests：WebGPU 停用 `DOTS3NOTE` 架構測試
- `4e97ac8` tests：`test-save-load-state` 擴充為對所有 `.gguf` 模型執行

---

## 19. CI／Release 基礎設施

本月是 CI／Release 流程持續打磨最密集的一個月，重點如下：

- **Release 管線標準化**：`f9779dda` 改用專屬 deploy key、`7c35571e` 讓 `make-release` 可指定任意 commit、`d83f72d4` 修正 dry-run 遺漏檢查、`34af94cd` 明確推送 release tag、`8b864009` 自動產生變更紀錄與 nightly 連結、`0adcc3bb`/`01ac3ad7` 加入發布產物 attestation、`c4b0225d` 新增 `scripts/release.sh`、`17197474`/`e467c2ff`/`353b32d8`/`d7fa69b7`/`a3b9c23e` 一系列 release job 清理與修正。
- **ccache 基礎設施**：`84e908c62`（移除 ccache）、`3a653fea`（`ccache-clear` 新增 older/min/dry-run）、`985b1491`（套用到所有 ccache job）、`790b5713`（試點改存放至 HF bucket）、`da9b5d6`（cache bucket 公開）。
- **ROCm/HIP CI**：`a1f96d4f`（onboard gfx1151）、`9558fa44c`（停用 ubuntu-rocm）、`7a9ff9597`（停用 rocm cache）、`e85caa81`（恢復 ROCm job）、`eab8ee41`（更新至 Ubuntu 24.04）、`6fdd0ac`（打包 HIP 執行期 DLL）、`3217633`（限縮只編譯 ggml-hip）、`0666ad2b2`（目標 ROCm 7.14）。
- **其他建置/CI 修正**：`2e2d99cfd`（Windows ARM64 CUDA 13.4）、`e5275f6f7`（移除 python 版本指定）、`bf0040e1`（Windows 改用 LLVM OpenMP）、`0e1d9185`（改用 shell script 檢查 cmake）、`9d77fa17`（OpenVINO 更新至 2026.3）、`645ca283`（重新啟用 SYCL release 依賴）、`61141f148`（移除 `GGML_HIP_ROCWMMA_FATTN`）、`936918514`（新增 pr-draft-label）、`d8d98872`（缺少建置需求時中止）、`42e98813`（緩解 Windows MSYS2 UCRT64 崩潰）、`4d19b28`（清理 UI 建置流程，改為建置一次多 job 共用）。

---

## 20. 依賴套件更新（vendor）

- BoringSSL：`815a2a5`（0.20260730.0）→ `fe2adf0e`（0.20260803.0）→ `adb55e51`（0.20260813.0）
- cpp-httplib：`94bc47f2`（0.52.0）→ `e23e9440e`（0.53.0）→ `77140d24`（0.53.1）→ `dc7aecf`（0.54.0/0.54.1）
- `61881b1f` vendor：為 `subprocess.h` 套用修補程式
- `4c6766fd7` vendor：同步 `subprocess.h` 最新版並移除本地 patch
- `6657ded4` vendor：再次同步更新 `subprocess.h`

---

## 21. 文件與貢獻指南

- `89482bd` agents：釐清註解風格與 Jinja 樣板知識
- `9303cdd9` security：澄清 AI 生成安全性回報之處理方式
- `6ad4ab0ea` readme：移除開發分支相關連結
- `087f94d8` doc：補充 MCP stdio 伺服器與 CORS 預設值說明
- `a298422d` docs：修正 `ET.md` 拼字錯誤
- `9a286ac9` docs：改善 Windows 建置說明
- `2100e592` / `3aeb9246` readme：更新徽章樣式並修正 server 徽章 alt 文字
- `a278dcef` contrib：建議合併 PR 前等待 CI 完成
- `ccc8fd2b` readme：更新連結，補齊 maintainer 名單

---

## 22. 本月重點摘要

2026 年 8 月共追蹤到約 **535 筆** commit（`5f55650a` → `9723942`），是近月來變更量最大的一個月，主要趨勢：

- **推測解碼生態系全面開花**：DeepSeek V4/V3.2、Qwen3-Next、GLM-4.5-Air/4.7-Flash、Nemotron、Muse Glimmer、LFM2、BailingMoE3 陸續補齊 MTP／DFlash／DSpark 支援，月底更推出全新的 **DFlash2** 架構（本地卷積＋候選選擇器），是本月最集中的單一主題。
- **新模型密集上線**：**Kimi-K3**（2.8T 參數混合 KDA+MLA 架構）與 **Qwen3.8-Flash-Next（qwen4exp）** 是規模最大的兩筆新架構，另有 MiniMax-Text-01/M1、BailingMoE3、GraniteSWA、dots3-note（含視覺+音訊）、Pocket-TTS、nanbeige4.2-3B 等多個模型上線。
- **WebUI 大規模重構**：Store/Services 整併、Settings/MCP 對話框化、對話分頁（Chat Tabs，跨 43 檔案）、內建工具（get_info/get_datetime/read_media/@mentions/斜線指令）持續擴充，同時有兩項供應鏈/資安強化（npm min-release-age、API Key 欄位遮蔽）。
- **Metal 後端兩次超大規模重構**：原始碼依運算子拆分＋平行編譯、flash-attn 依裝置調校表（合計異動超過 15000 行），並密集補齊 M1 Max 至 M5 Pro 全系列調校資料。
- **Server 端聚焦工具隔離與執行緒模型**：新增 Docker/SSH/Podman 三種工具隔離方式，並重構請求佇列讓監控端點在長時間 decode 期間仍可用。
- **值得留意的正確性/安全性修正**：
  - Vulkan `ggml_vk_graph_optimize` 缺失 view-alias 相依性，曾在 AMD/NVIDIA 上對 recurrent-state 模型造成**無錯誤訊息的靜默錯誤輸出**；
  - `ggml_conv_transpose_2d` 在 CPU/Metal 上多批次僅計算第一批的 bug；
  - GBNF 文法 `\-` 轉義解析錯誤，直接影響依賴文法約束的工具呼叫；
  - LoRA 張量檔案邊界檢查（修正惡意/損毀檔案可能導致的越界讀取）；
  - GGUF 陣列型別檢查、GGUFReader 大小防護，強化對不受信任模型檔案的防禦；
  - DFlash2 搭配 NVFP4 草稿模型時遺漏傳入 attention scale，導致草稿 token 幾乎不被接受。
- **CI／Release 基礎設施**是本月投入最密集的支援性工作，涵蓋 release 管線標準化（attestation、可指定 commit、自動變更紀錄）、ccache 機制打磨（HF bucket 試點、清理策略擴充）與 ROCm/Windows 建置持續修正。
- **ggml 核心**本月同步升版七次（0.18.1→0.22.0），新增跨後端一致的 `ggml_rope_set_offset` 與 `SWIGLU_CLAMP` 融合運算子，llama.cpp 本體亦正式進入 semver 版本化（0.2.0→0.3.0）。
- 整體而言，本月變更量與涵蓋面均顯著擴大，除少數需留意的正確性/安全性修正外，未見會影響既有使用方式的重大破壞性變更（Qwen3-TTS 對 `llama-tts` 執行檔相容性的影響除外）。
