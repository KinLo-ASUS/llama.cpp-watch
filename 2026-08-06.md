# llama.cpp 變更報告 (2026-08-06)

- 追蹤倉庫：[ggml-org/llama.cpp](https://github.com/ggml-org/llama.cpp)（`master` 分支）
- 檢查區間：`6ea215d1`（2026-08-05）→ `c8e03ce8`（2026-08-06）
- 本次新增 commit 數：**14**

---

## 分組摘要

### 一、mtmd（多模態）相關修正與強化

涵蓋 deepseek-ocr 多列批次處理、Unlimited-OCR 的 tile 限制設定，以及修正 mtmd 音訊圖執行順序造成的 CPU assert 錯誤。三者皆與多模態（mtmd）子系統的正確性與效能相關。

- `717dad5c` — **mtmd: support multi-row batching for deepseek-ocr** ([#26154](https://github.com/ggml-org/llama.cpp/pull/26154))
  為 deepseek-ocr 模型加入多列（multi-row）批次處理支援，並改為一次性交織（weave）處理各列，取代逐列處理，提升效率。
- `b06aa774` — **mtmd: Unlimited-OCR fix max_tiles, setting in converter** ([#25614](https://github.com/ggml-org/llama.cpp/pull/25614))
  修正 Unlimited-OCR 模式下 `max_tiles` 設定的問題，並在轉換器（converter）中補上對應設定。
- `c8e03ce8` — **mtmd/ggml: add ggml_build_forward_order** ([#26649](https://github.com/ggml-org/llama.cpp/pull/26649))
  新增 `ggml_build_forward_order`：原先用 `ggml_build_forward_expand` 作為排序提示（保持 q/k/v 順序）會連帶標記所有祖先節點為需計算，導致未被選中的分支在輸入尚未上傳時被迫執行，在 mtmd 音訊圖中造成 GEN_WAV 呼叫誤跑 GEN_CODE 分支、觸發 `get_rows` 的邊界斷言錯誤（CPU）。新函式只插入節點但不標記計算旗標，待分支真正被選中時才還原旗標；`clip_graph::build_attn` 中的 q/k/v 提示已切換使用此函式。

### 二、server（伺服器）相關

- `a035a888` — **server: Adding spec-decode counters to /metrics endpoint** ([#26389](https://github.com/ggml-org/llama.cpp/pull/26389))
  在 `/metrics` 端點新增推測解碼（speculative decoding）相關計數器，參數命名已與 vLLM 對齊，方便統一監控。
- `f9e832c1` — **server: harden the file_glob_search directory walk** ([#26626](https://github.com/ggml-org/llama.cpp/pull/26626))
  強化檔案搜尋（`file_glob_search`）目錄遍歷的安全性與正確性：修正 Windows junction 未被視為連結而遭無限遍歷的問題、目錄讀取失敗時不再靜默略過（改設 `truncated` 旗標）、簡化回傳結構（六個輸出參數精簡為一個結果結構）、遍歷時每彈出一個目錄即檢查逾時、路徑正規化避免 `.`/`..` 外洩給用戶端、修正 Windows 上路徑編碼（narrow path 使用作業系統代碼頁造成含重音字元檔名亂碼）等問題；同時修正前端搜尋快取未過期清除的記憶體累積問題。此為本次變更中改動幅度最大的一筆（5 個檔案、889 行新增／613 行刪除）。

### 三、UI（Web 介面）相關

- `e031d956` — **ui: Update vulnerable packages + cleanup Storybook config** ([#26607](https://github.com/ggml-org/llama.cpp/pull/26607))
  升級 Storybook 及一系列有已知漏洞的前端依賴套件（fast-uri、ip-address、js-yaml、immutable、postcss、brace-expansion、sharp、body-parser、vite/esbuild、hono、dompurify、@sveltejs/kit、@modelcontextprotocol/sdk、valibot 等），並移除舊有 setup 檔案、整理程式碼。
- `3e3a7a41` — **ui: show generation statistics by default in chat settings** ([#26624](https://github.com/ggml-org/llama.cpp/pull/26624))
  將聊天設定中的「顯示生成統計資訊」預設改為開啟。

### 四、build / vendor（建置與第三方相依）

- `d52ec04a` — **build: remove GGML_METAL_USE_BF16 from all build scripts** ([#26604](https://github.com/ggml-org/llama.cpp/pull/26604))
  從所有建置腳本中移除 `GGML_METAL_USE_BF16` 選項。
- `61881b1f` — **vendor: apply patches for subprocess.h** ([#26606](https://github.com/ggml-org/llama.cpp/pull/26606))
  為第三方 `subprocess.h` 套件套用修補程式。

### 五、其他個別修正

- `020760ad` — **convert: Add endianness conversion for Q1 and TQ2 quantizations** ([#26618](https://github.com/ggml-org/llama.cpp/pull/26618))
  轉換腳本新增對 Q1 與 TQ2 量化格式的位元序（endianness）轉換支援。
- `cd0fa605` — **grammar: degrade max repetition >= 2000 to unbounded** ([#26613](https://github.com/ggml-org/llama.cpp/pull/26613))
  GBNF 語法規則中，當重複次數上限 `>= 2000` 時，自動降級為無上限處理，避免極端數值造成問題。
- `9a688e51` — **fit: Fix memory allocation for MTP layers** ([#26605](https://github.com/ggml-org/llama.cpp/pull/26605))
  修正 MTP（Multi-Token Prediction）層的記憶體配置錯誤。
- `9303cdd8` — **security: clarify about AI-generated reports** ([#26579](https://github.com/ggml-org/llama.cpp/pull/26579))
  更新安全性政策文件，澄清針對 AI 生成的安全性回報之處理方式。
- `360e1349` — **tests: re-enable MiniMax M3 in test-llama-archs** ([#26633](https://github.com/ggml-org/llama.cpp/pull/26633))
  重新啟用 `test-llama-archs` 測試中對 MiniMax M3 架構的測試。

---

## Commit 逐條列表（依時間先後）

| Commit | 說明 | PR |
|---|---|---|
| `e031d956` | ui: Update vulnerable packages + cleanup Storybook config | [#26607](https://github.com/ggml-org/llama.cpp/pull/26607) |
| `d52ec04a` | build: remove GGML_METAL_USE_BF16 from all build scripts | [#26604](https://github.com/ggml-org/llama.cpp/pull/26604) |
| `3e3a7a41` | ui: show generation statistics by default in chat settings | [#26624](https://github.com/ggml-org/llama.cpp/pull/26624) |
| `61881b1f` | vendor: apply patches for subprocess.h | [#26606](https://github.com/ggml-org/llama.cpp/pull/26606) |
| `020760ad` | convert: Add endianness conversion for Q1 and TQ2 quantizations | [#26618](https://github.com/ggml-org/llama.cpp/pull/26618) |
| `a035a888` | server: Adding spec-decode counters to /metrics endpoint | [#26389](https://github.com/ggml-org/llama.cpp/pull/26389) |
| `9303cdd8` | security: clarify about AI-generated reports | [#26579](https://github.com/ggml-org/llama.cpp/pull/26579) |
| `9a688e51` | fit: Fix memory allocation for MTP layers | [#26605](https://github.com/ggml-org/llama.cpp/pull/26605) |
| `717dad5c` | mtmd: support multi-row batching for deepseek-ocr | [#26154](https://github.com/ggml-org/llama.cpp/pull/26154) |
| `cd0fa605` | grammar: degrade max repetition >= 2000 to unbounded | [#26613](https://github.com/ggml-org/llama.cpp/pull/26613) |
| `b06aa774` | mtmd: Unlimited-OCR fix max_tiles, setting in converter | [#25614](https://github.com/ggml-org/llama.cpp/pull/25614) |
| `360e1349` | tests: re-enable MiniMax M3 in test-llama-archs | [#26633](https://github.com/ggml-org/llama.cpp/pull/26633) |
| `f9e832c1` | server: harden the file_glob_search directory walk | [#26626](https://github.com/ggml-org/llama.cpp/pull/26626) |
| `c8e03ce8` | mtmd/ggml: add ggml_build_forward_order | [#26649](https://github.com/ggml-org/llama.cpp/pull/26649) |

---

*本報告由自動化監控流程產生。*
