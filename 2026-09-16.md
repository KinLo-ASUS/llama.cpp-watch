# llama.cpp 每日變更報告 - 2026-09-16

- 監控倉庫：`ggml-org/llama.cpp`（分支 `master`）
- 檢查範圍：`1bc7a5a`（不含）→ `930e2fa`（含），共 **18** 個新 commit
- 檢查區間對應時間：2026-09-15 ～ 2026-09-16

## 摘要

本次新增的 commit 大致可分為六大類：

1. **CI / 建置流程調整**：修正 Android release 建置、CUDA Windows 版本升級、kleidiai 自架 runner 升級並將警告升級為錯誤、MUSA 建置僅保留單一架構。
2. **CMake 建置系統修正**：改用 `PROJECT_SOURCE_DIR` 取代 `CMAKE_SOURCE_DIR`，修正以 `add_subdirectory` 方式整合 llama.cpp 時路徑解析錯誤的問題。
3. **後端運算子支援與效能優化**：涵蓋 CUDA（DUP i16/i32、SUM_ROWS/MEAN row-contiguous）、Metal（MiniCPM3 所需的 FA kernel）、OpenCL（通用 ssm_scan、MoE 專家矩陣乘依批次大小挑選）、Vulkan（稀疏 Flash Attention）、HIP（ROCm AllReduce）、Hexagon（cpy 運算 DMA 與 contiguous fast-path）等多個後端的新功能與修正。
4. **OpenVINO 後端優化**：針對 stateful decode 與 GPU 上的 MoE 推理進行多項效能與正確性修正。
5. **模型程式碼重構**：調整 `build_arch_graph()` 的定義順序，修正模板特化順序問題。
6. **RPC 後端修正**：修正 RPC hash-cache 機制誤將一般張量傳輸也快取，導致 worker 磁碟空間被大量佔用的問題。
7. **文件更新**：在 CONTRIBUTING.md 中新增「AI 審查時間」的經驗法則說明。

---

## 分組詳情

### 一、CI / 建置流程調整

| Commit | 作者 | 說明 |
| --- | --- | --- |
| `987498f` | Sigbjørn Skjæret | ci：修正 Android release 建置流程的問題。 |
| `0ecb159` | shivamkumard-ctrl | ci：將 CUDA Windows x64 建置使用的 CUDA 版本升級至 13.4.1。 |
| `1af6c65` | Aaron Teo | ci：將 kleidiai 自架 runner 從 Ubuntu 22.04 升級至 24.04，並將 CI 中的警告訊息升級為硬性錯誤（build 失敗）。 |
| `d1d3c33` | Aman Gupta | ci：MUSA 建置流程精簡為只建置單一架構，加快 CI 執行速度。 |

### 二、CMake 建置系統修正

| Commit | 作者 | 說明 |
| --- | --- | --- |
| `69eb250` | Daniel Bevenius | cmake：將函式呼叫中的路徑改用 `PROJECT_SOURCE_DIR` 取代 `CMAKE_SOURCE_DIR`——當專案以 `add_subdirectory` 方式被上層專案整合時，`CMAKE_SOURCE_DIR` 會固定指向呼叫端（上層專案）的根目錄而非 llama.cpp 本身根目錄，導致 `common/common.h` 標頭檔無法正確解析；同時新增 `examples/test-cmake` 測試以涵蓋此情境。 |

### 三、後端運算子支援與效能優化

| Commit | 作者 | 說明 |
| --- | --- | --- |
| `4c9233c` | Aman Karki | cuda：為 `DUP` 運算子新增 i16、i32 型別支援，並同步更新 ops 支援文件。 |
| `1e7bcf3` | Yanzhao Wang | metal：新增 HSK=96、HSV=64（MiniCPM3 所需組合）的 Flash Attention kernel——MiniCPM3 的 `attention.key_length` 為 96，而 `value_length` 預設為 64，先前 Metal 缺少對應的 kernel 實例，導致 `-fa auto` 因找不到 `kernel_flash_attn_ext_vec_f16_dk96_dv64` 而中止；此修正為所有既有 (96, 96) 組合對應的 K/V 型別新增 (96, 64) 的 tile kernel 實例，並在 NE=4 條件下新增對應的 vec kernel。 |
| `6ec1a7e` | lhez | opencl：新增通用（generic）版本的 `ssm_scan` 運算子實作。 |
| `fc82583` | Ruben Ortlam | vulkan：新增對稀疏 Flash Attention 的支援（適用於 DSV4/GLM 架構），並加入對應測試、調校實作與 cm2 f16vec4 decode 向量支援，同時避免非決定性的 `atomicAdd` 使用。 |
| `5431581` | Mohamed Elashri | cuda：為 `SUM_ROWS` 新增 row-contiguous（列連續記憶體）情境的支援，並讓 `MEAN` 運算子共用同一套 kernel 以支援 permute/slice 後的張量，同時補上對應測試。 |
| `9f31776` | Hongqiang Wang | opencl：MoE 專家矩陣乘依批次大小（routing count）挑選預建置 q4_0 GEMM 路徑，並避免對 padding 的 MoE 啟動值槽位寫入多餘的零值，優化投機解碼（speculative decoding）/MTP 情境下的效能。 |
| `38a5b42` | Sandro Steeger | HIP：為 ROCm 啟用 AllReduce 支援。 |
| `72b590d` | Trivikram Reddy | hexagon：`cpy` 運算子在來源與目的地記憶體皆為 contiguous 時改用 DMA 搬移，提升效能。 |
| `930e2fa` | Jhen-Jie Hong | hexagon：修正前次改動遺漏的 contiguous fast-path 與 `hvx_copy_uu`（每次執行皆需要重新設定），修補回歸問題。 |

### 四、OpenVINO 後端優化

| Commit | 作者 | 說明 |
| --- | --- | --- |
| `77d554b` | Zijun Yu | OpenVINO：多項優化與修正，包括排除 GPU/NPU 上失敗的 `POOL_2D` 案例、修正 Gemma-4 每層不同 head size 時 stateful decode 錯誤、修正 MSVC 窄化編譯錯誤、以結構化方式判別交錯式 SWA 模型中的滑動視窗層、新增 `GGML_OPENVINO_REQUANT_KQUANT` 環境變數以選擇 4-bit requant 目標、新增 `GGML_OPENVINO_SPILL_DIR` 讓權重緩衝可溢出（spill）至磁碟、透過 `KVStateSeqAxis` pass 調整 KV cache 記憶體佈局以提升 stateful 效能，並修正超過滑動視窗大小後的 stateful decode 錯誤。 |

### 五、模型程式碼重構

| Commit | 作者 | 說明 |
| --- | --- | --- |
| `9e71716` | Chris Peterson | models：將 `build_arch_graph()` 的函式定義移到 `graph<true>` 與 `graph<false>` 模板特化明確定義之後，修正模板特化順序問題（影響 dflash、eagle3、t5 三個架構檔案）。 |

### 六、RPC 後端修正

| Commit | 作者 | 說明 |
| --- | --- | --- |
| `7609846` | Patrick Hoffmann | rpc：修正 hash-cache 機制誤將一般張量傳輸（activation）也走 hash 快取路徑的問題——`ggml_backend_rpc_buffer_set_tensor` 與 `ggml_backend_rpc_set_tensor_async` 先前只要傳輸大小超過 `HASH_THRESHOLD` 就會雜湊並讓 `rpc-server -c` 快取，但快取原本只該用於權重；在雙節點分割的 Qwen3.8-Flash-Next 情境下，排程器搬移的 activation 也被誤快取，造成 worker 快取目錄一天內累積達 1.4 TB。修正後僅對標記為 `GGML_BACKEND_BUFFER_USAGE_WEIGHTS` 的緩衝區使用 hash 路徑；並進一步調整為僅在 hash 檢查未命中時才寫入快取項目，避免排程器傳送的計算資料持續填滿磁碟。 |

### 七、文件更新

| Commit | 作者 | 說明 |
| --- | --- | --- |
| `6011c34` | Johannes Gäßler | docs：在 `CONTRIBUTING.md` 補充「AI 審查時間」的經驗法則說明（`[no ci]`）。 |

---

## 完整 Commit 清單（依合併時間排序）

1. `69eb250` cmake : use PROJECT_SOURCE_DIR instead of CMAKE_SOURCE_DIR (#28771)
2. `4c9233c` cuda : enable i16 and i32 for DUP (#28897)
3. `987498f` ci : fix android release (#28936)
4. `0ecb159` ci: Bump CUDA Windows x64 builds to 13.4.1 (#28930)
5. `1e7bcf3` metal : add FA kernels for HSK=96, HSV=64 (MiniCPM3) (#28599)
6. `1af6c65` ci: bump kleidiai runners from 22.04 to 24.04 (#28885)
7. `6ec1a7e` opencl: add generic ssm_scan (#28881)
8. `77d554b` OpenVINO: optimize stateful decode and GPU MoE inference (#28638)
9. `fc82583` vulkan: support sparse Flash Attention (#28105)
10. `9e71716` models : move build_arch_graph() after graph() template specialization (#28934)
11. `5431581` cuda: support row-contiguous SUM_ROWS (#26308)
12. `7609846` rpc : hash-cache only weights (#28789)
13. `6011c34` docs: Rule of thumb for AI review time [no ci] (#28945)
14. `d1d3c33` ci: build MUSA for only 1 arch (#28944)
15. `9f31776` opencl: choose the MoE expert matmul by batch size for speculative decoding/MTP (#27637)
16. `38a5b42` HIP: Enable AllReduce for ROCm (#27825)
17. `72b590d` hex-cpy: use dma if src and dst are contiguous (#28906)
18. `930e2fa` hexagon: add back missing contiguous fast-path and hvx_copy_uu for each run (#28886)

---

- 最新已檢查 commit：`930e2fa5995789efbf249a8bf61325bb626e417b`（短碼 `930e2fa`）
