# llama.cpp 更新報告 — 2026-08-23

- **來源倉庫**：[ggml-org/llama.cpp](https://github.com/ggml-org/llama.cpp)，分支 `master`
- **檢查區間**：`d775b896`（2026-08-22 之前的紀錄）→ `70adb1b4c`（本次最新）
- **新增 commit 數**：15 筆

---

## Group 1：推測解碼（Speculative Decoding）/ Draft Model 修正（2 筆）

- [`2c6b141e`](https://github.com/ggml-org/llama.cpp/commit/2c6b141efb3b0868fd39d3cae73f69606e1d654c) common : fix draft-mtp with embeddings (#26352, #27299) (#27400)
- [`2fb989b9`](https://github.com/ggml-org/llama.cpp/commit/2fb989b9e79bf4da8159855e24892c8f4c20300f) fit: also take into account n_streams (#27496)

修正 draft-MTP（多 token 預測）搭配 embeddings 使用時的問題（對應兩個先前已知的 issue）。另一筆則修正非統一（non-unified）KV cache 情境下，draft context 大小未隨 target context 成長而導致的問題：當 slot 填充超過 draft context 上限時，draft batch 會解碼失敗並讓 server 回應 500。修正後 draft context 會跟隨 target context 的大小設定，並讓 `common_fit_params` 可以額外考慮第二個模型（如 draft/MTP 模型）所需的顯存，取代原本以固定 byte 邊界估算記憶體的做法，讓資源分配更精確。

## Group 2：SYCL 後端 — Q2_K kernel 重新加入（1 筆）

- [`86722900`](https://github.com/ggml-org/llama.cpp/commit/86722900390abf479fd9719eda12c299a2b25bbf) sycl : add Q2_K reordered MMVQ and ESIMD kernels (again) (#27490)

延續前次報告提及的事件：Q2_K reordered MMVQ 與 ESIMD kernel 曾在合入後很快被 revert，本次重新提交並加入額外的 gate 參數判斷，修正了先前造成問題的部分後正式合回主線。

## Group 3：模型架構與多模態（mtmd）支援（3 筆）

- [`54ee5ee6`](https://github.com/ggml-org/llama.cpp/commit/54ee5ee643f29abba6852903ddfdb688c2361b5b) mtmd: support dots3-note vision+audio (#27524)
- [`2115b73d`](https://github.com/ggml-org/llama.cpp/commit/2115b73d8ebdbd659075cce66c609506863bc826) model : support DSpark for bailingmoe3 (#27508)
- [`b21e4de7`](https://github.com/ggml-org/llama.cpp/commit/b21e4de74567f5eef213765c9476a843c2e43f0d) mtmd: use ggml_rope_set_offset (#27521)

延續前次新增的 `dots3-note` 純文字模型架構，本次進一步補上其視覺（vision）與音訊（audio）多模態支援，新增對應的 clip 模型實作與 GGUF tensor mapping；`bailingmoe3` 架構新增對 DSpark 變體的支援；另外將 `mtmd`（多模態工具）中 `gemma4v`、`minimax-m3` 等模型的 RoPE 位置偏移邏輯，統一改用 `ggml_rope_set_offset()`，是先前跨後端功能整併工作延伸到多模態模組的收尾。

## Group 4：GGML CPU 效能優化（1 筆）

- [`369e1cd6`](https://github.com/ggml-org/llama.cpp/commit/369e1cd6140b7cbdb552cc3f87613aeb9e122422) ggml: optimize concat op by replacing per-element memcpy with row-level memcpy (#24575)

將 CPU 後端 `concat` 運算子中逐元素（per-element）呼叫 `memcpy` 的方式，改為以整列（row-level）為單位複製，減少函式呼叫次數以提升效能，並補上對應的連續性（contiguity）assert 檢查。

## Group 5：CI 基礎設施（2 筆）

- [`3a653fea`](https://github.com/ggml-org/llama.cpp/commit/3a653fea932e6a026e14cc0fb6507a986679f688) ci : add older, min and dry-run options to ccache-clear (#27504)
- [`e85caa81`](https://github.com/ggml-org/llama.cpp/commit/e85caa81ea2b65797396018c179b87ad61fa38ab) ci : Restore ROCm job for Ubuntu (#27399)

為 `ccache-clear` action 新增 `older`、`min`、`dry-run` 等選項，讓清理快取的行為更具彈性且可先行預覽。另外恢復先前停用的 Ubuntu ROCm CI job，並將 ccache 的 `compiler_check` 改為以內容雜湊（content）而非修改時間（mtime）判斷，避免每次重新安裝 ROCm 工具鏈時因編譯器檔案 mtime 改變而讓整個快取失效，同時將 ccache 上限調高至 1GB 以容納更多架構的建置快取。

## Group 6：CLI 參數清理（1 筆）

- [`9fee29e9`](https://github.com/ggml-org/llama.cpp/commit/9fee29e9435f865ec0b811a783a6471a136d9317) arg: remove -no-cnv from cli [no ci] (#27542)

移除 CLI 中的 `-no-cnv` 參數，屬於介面選項的整理與簡化。

## Group 7：common — JSON 抽象層重構（2 筆）

- [`d9f918d2`](https://github.com/ggml-org/llama.cpp/commit/d9f918d2d06079b4336688e819eee821c8a9cd9e) common: add json.h abstraction (#27511)
- [`70adb1b4`](https://github.com/ggml-org/llama.cpp/commit/70adb1b4cea5ee39f867792c78dc59320921eda7) common: json.h: fix clang lto (#27575)

新增 `common/json.h` 作為 JSON 函式庫的抽象層，並將 server、cli、jinja 樣板引擎、各項測試等大量呼叫端遷移至此抽象介面，是本次規模最大的一筆重構（跨 45 個檔案），目的應是降低對特定 JSON 函式庫實作的直接依賴。緊接著的下一筆則修正該抽象層在 clang LTO（Link Time Optimization）編譯下的問題，屬於重構後的即時修正。

## Group 8：Vulkan 後端新運算子（1 筆）

- [`3f545bec`](https://github.com/ggml-org/llama.cpp/commit/3f545beccee69d9975f466ec7e45fd9aacd8ba90) vulkan : added the PAD_REFLECT_1D operation (#26586)

為 Vulkan 後端新增 `GGML_OP_PAD_REFLECT_1D` 運算子的實作，包含對應的 GLSL compute shader 與 pipeline 註冊，作者並在 Intel Iris Xe（Vulkan 1.4）上驗證正確性與效能。

## Group 9：文件（README 徽章）（2 筆）

- [`2100e592`](https://github.com/ggml-org/llama.cpp/commit/2100e592600e4538496fd5201cbe6a7f8fbeb1e0) readme : update badges (#27531)
- [`3aeb9246`](https://github.com/ggml-org/llama.cpp/commit/3aeb924628c7113b9764b2ec2f3aebc6c51b27a5) readme : fix server badge alt (#27533)

更新 README 中的徽章（badges）樣式，並修正 server 徽章的 alt 文字，屬於文件的小幅調整。

---

## 本次重點摘要

- 本次共 15 筆 commit，數量持續趨緩（前次 26 筆），內容以修正與收尾為主，沒有全新的大型功能。
- **SYCL 的 Q2_K kernel 風波落幕**：前次報告中提到合入後又被 revert 的 Q2_K reordered MMVQ / ESIMD kernel，本次補上 gate 參數修正後重新合入，若先前因此觀察到問題，可留意此版本是否已解決。
- **推測解碼相關修正**是本次較重要的穩定性修正：解決了非統一 KV cache 下 draft context 大小未隨 target 成長導致 server 500 的問題，並讓資源估算（`fit`）機制可考慮 draft/MTP 模型的額外記憶體需求。
- **多模態延伸**：`dots3-note` 由純文字支援擴展到視覺＋音訊；`bailingmoe3` 新增 DSpark 變體；`ggml_rope_set_offset()` 的整併工作也延伸進 mtmd 模組，持續收斂先前跨模組的重構。
- **JSON 抽象層**（`common/json.h`）是本次改動範圍最大的一筆（跨 45 個檔案），將既有 JSON 函式庫呼叫收斂到統一介面，並隔天即修正在 clang LTO 下的編譯問題，建議留意後續是否還有相關 follow-up 修正。
- CI 方面持續打磨快取與建置流程（ccache-clear 選項擴充、恢復 ROCm job），Vulkan 後端新增 `PAD_REFLECT_1D` 運算子支援。
