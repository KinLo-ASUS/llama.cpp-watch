# llama.cpp 變更報告 — 2026-09-06

- 追蹤範圍：`ggml-org/llama.cpp` master 分支
- 檢查區間：`427291b`（2026-09-05 之前的檢查點，不含） → `74a7c89`（本次檢查點）
- 新增 commit 數：4

---

## 分組摘要

### 1. SYCL 後端：除錯工具與測試修復

- **cd8cdf3** — sycl: attribute device allocations by site (GGML_SYCL_MEMTRACE) (#27631)｜Nick Farrell
- **4d91760** — sycl : fix test-backend-ops CI break && restore Kronecker product FWHT support (#28016) (#28254)｜Jingxin (Philip) Li

前者新增兩個環境變數 `GGML_SYCL_MEMTRACE`（需搭配 `-lv 4`）：設為 `1` 時，每當某配置點（call site）的裝置記憶體用量增加超過門檻（預設 64MiB，可用 `GGML_SYCL_MEMTRACE_STEP` 調整）就輸出當前用量；設為 `2` 時則記錄每一次配置與釋放。作者表示此工具對改進 `--fit` 演算法及日後排查記憶體相關問題很有幫助，屬純除錯/可觀測性功能，不影響既有邏輯。後者修復先前因 revert 而中斷的 test-backend-ops CI（新增 `ggml/src/ggml-sycl/fwht.cpp`，172 行），重新套用 Kronecker product 於 384/640/768/1280 等尺寸下的 FWHT（Walsh–Hadamard 轉換）支援，並修正測試中未使用變數與多餘空白等問題，同時將 Kronecker 測試限定於 SYCL 後端執行，避免影響其他後端。

### 2. Metal 後端：修正資源初始化失敗時的記憶體洩漏

- **6a1a922** — metal : fix memory leak in early return (#28399)｜Niklas Wenzel

在 `ggml_metal_init()` 中，當建立 command queue 失敗（`queue == nil`）而提早 return NULL 時，先前配置好的 `res` 結構未被釋放即造成記憶體洩漏；本次修正在 early return 前補上 `free(res)`。屬單行修復，僅影響裝置初始化失敗（極少見）的邊界情況。

### 3. 專案治理：GitHub Issue 範本調整

- **74a7c89** — Github: limit blank issues to maintainers (#28435)｜Johannes Gäßler

將 `.github/ISSUE_TEMPLATE/config.yml` 的 `blank_issues_enabled` 由 `true` 改為 `false`，限制一般貢獻者建立「空白」issue（不使用範本），僅維護者可用，藉此引導使用者改用既有 issue 範本或 discussions，屬專案治理層面的流程調整，非程式碼變更。

---

## 小結

本次檢查區間新增 commit 數量較少（4 筆），內容以維護性修復與工具改善為主：SYCL 後端新增記憶體追蹤除錯工具並修復一項 CI 測試中斷問題，Metal 後端修正一處初始化失敗路徑的記憶體洩漏，另有一項與程式碼無關的 GitHub issue 範本治理調整。本次無新模型支援或重大功能新增。
