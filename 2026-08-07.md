# llama.cpp 變更報告 (2026-08-07)

- 追蹤倉庫：[ggml-org/llama.cpp](https://github.com/ggml-org/llama.cpp)（`master` 分支）
- 檢查區間：`c8e03ce8`（2026-08-06）→ `15586e2d`（2026-08-06）
- 本次新增 commit 數：**8**

---

## 分組摘要

### 一、模型轉換（convert）相關修正

三筆變更皆針對 `convert_hf_to_gguf` 相關轉換腳本，修正特定架構或參數的相容性問題。

- `eb5667a1` — **convert : fix DeepseekV4 rope parameters with transformers 5.x** ([#26673](https://github.com/ggml-org/llama.cpp/pull/26673))
  修正在 transformers 5.x 版本下，DeepseekV4 模型的 RoPE 參數讀取錯誤，避免轉換後模型的位置編碼設定不正確。
- `e700bfb3` — **convert : accept "ExaoneMoeForCausalLM" arch spelling** ([#26660](https://github.com/ggml-org/llama.cpp/pull/26660))
  轉換腳本新增對 `ExaoneMoeForCausalLM`（另一種大小寫拼法）架構名稱的識別，避免因拼寫差異導致轉換失敗。
- `9de0fcf2` — **model-conversion : add --model-name to conversion scripts** ([#26665](https://github.com/ggml-org/llama.cpp/pull/26665))
  在 causal 與 embedding 模型轉換腳本中新增 `--model-name` 參數，可明確指定寫入 GGUF metadata 的 `general.name` 欄位，避免預設沿用模型路徑檔名不符需求的情況。

### 二、GPU 後端與 CI 相關（Vulkan / ROCm）

兩者皆與 GPU 後端穩定性及對應的 CI 驗證有關。

- `803b7fca` — **vulkan: fix submission batching size, add debug tools for diagnosing causes of DeviceLost drivers errors** ([#26371](https://github.com/ggml-org/llama.cpp/pull/26371))
  修正 Vulkan 後端提交（submission）批次門檻套用時機過晚的問題，並新增除錯工具以協助診斷造成 `DeviceLost` 驅動錯誤的原因；同時改用既有的 logging macro 取代直接輸出、以 throw 取代 abort、並清理循環依賴。此為本次改動幅度最大的一筆（單檔 232 行新增／33 行刪除）。
- `a1f96d4f` — **ci : onboard AMD ROCm CI with gfx1151 fixes** ([#26544](https://github.com/ggml-org/llama.cpp/pull/26544))
  為 AMD ROCm（gfx1151 / Strix Halo 內顯）建立 CI 流程，並修正過程中發現的多項相容性問題：CUDA debug assert 允許整合式 GPU 的 host 輸出緩衝區、針對 gfx1151 的記憶體一致性問題暫時啟用 `GGML_CUDA_ENABLE_UNIFIED_MEMORY`、之後改以 `HIP_LAUNCH_BLOCKING=1` 序列化核心啟動以修正非同步執行造成的推論結果錯誤（qwen3 perplexity 由 ~88 修正回 ~9.4）、在 HIP 後端暫時跳過 jamba 架構測試與 top-k/top-p 取樣子測試（ROCm 尚未支援對應運算）。

### 三、其他個別修正

- `3db4ff87` — **model-loader : fix quantized reshaped tensor strides** ([#26672](https://github.com/ggml-org/llama.cpp/pull/26672))
  修正模型載入器在處理量化後、經過 reshape 的張量時，步幅（stride）計算錯誤的問題。
- `6a32c29a` — **server: fix empty response for /cors-proxy** ([#26656](https://github.com/ggml-org/llama.cpp/pull/26656))
  修正 server 的 `/cors-proxy` 端點在特定情況下回傳空白回應的問題，並新增對應單元測試。
- `15586e2d` — **mtmd: add chunk save/load function** ([#26645](https://github.com/ggml-org/llama.cpp/pull/26645))
  為 mtmd（多模態）新增 chunk 的儲存／讀取功能，並補上對應的 C API 測試。

---

## Commit 逐條列表（依時間先後）

| Commit | 說明 | PR |
|---|---|---|
| `803b7fca` | vulkan: fix submission batching size, add debug tools for diagnosing causes of DeviceLost drivers errors | [#26371](https://github.com/ggml-org/llama.cpp/pull/26371) |
| `9de0fcf2` | model-conversion : add --model-name to conversion scripts | [#26665](https://github.com/ggml-org/llama.cpp/pull/26665) |
| `a1f96d4f` | ci : onboard AMD ROCm CI with gfx1151 fixes | [#26544](https://github.com/ggml-org/llama.cpp/pull/26544) |
| `e700bfb3` | convert : accept "ExaoneMoeForCausalLM" arch spelling | [#26660](https://github.com/ggml-org/llama.cpp/pull/26660) |
| `3db4ff87` | model-loader : fix quantized reshaped tensor strides | [#26672](https://github.com/ggml-org/llama.cpp/pull/26672) |
| `eb5667a1` | convert : fix DeepseekV4 rope parameters with transformers 5.x | [#26673](https://github.com/ggml-org/llama.cpp/pull/26673) |
| `6a32c29a` | server: fix empty response for /cors-proxy | [#26656](https://github.com/ggml-org/llama.cpp/pull/26656) |
| `15586e2d` | mtmd: add chunk save/load function | [#26645](https://github.com/ggml-org/llama.cpp/pull/26645) |

---

*本報告由自動化監控流程產生。*
