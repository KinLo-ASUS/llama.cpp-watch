# llama.cpp 更新報告 (2026-08-01)

**檢查範圍**：`5f55650a` (2026-07-30 之前最後檢查點) → `876a4321` (2026-07-31 最新 master)
**新增 commit 數量**：16

---

## 群組一：Vulkan 後端

### 1. `876a4321` — vulkan: add POOL_1D op (#25431)
為 Vulkan 後端新增 `GGML_OP_POOL_1D` 支援，包含新的 push constants 結構、`pool1d.comp` compute shader，並修復了當結束邊界為負值時 shader 會崩潰的問題，同時擴充了相關測試案例。

### 2. `eb41d503` — vulkan: Introduce driver version check for Windows Intel GPU to mitigate crashing (#25192)
針對 Windows 上的 Intel GPU 新增驅動版本檢查機制，取代先前針對 Intel 的崩潰防護（該問題已於驅動 32.0.101.8860 修復），改以解析驅動版本號來決定是否套用因應措施，僅套用於非 Xe2+ 架構的 GPU。

### 3. `6f3c0a79` — vulkan: update vulkan sdk to 1.4.357.0 (#26303)
將 CI 使用的 Vulkan SDK 版本升級至 1.4.357.0（更新 `build-cpu.yml` 與 `release.yml` 兩個 workflow）。

---

## 群組二：SYCL 後端（多項改進與新特性）

### 4. `9d9a6d29` — SYCL: add oneMKL GEMM flash attention for XMX-accelerated prompt processing (#25025)
新增以 oneMKL GEMM 實作的 flash attention 路徑，於 Intel XMX 硬體上加速 prompt 處理階段。此 PR 經過多輪修正，包括修復交錯 dst layout 錯誤、修正量化與 GQA 情境下的 KV stride、限縮 MKL 啟用的合法範圍（gqa≥2、head_dim 64~512 且為 64 倍數、無 sink/alibi/softcap 等），並新增對應環境變數（`GGML_SYCL_ENABLE_MKL_FA` 等）與文件說明。實測 Battlemage GPU 上可達 1.85x~1.97x 加速。

### 5. `d5d3e05b` — [SYCL] support the missed types in cpy (#26005)
補齊 SYCL `cpy`（tensor copy）運算中先前未支援的資料型態轉換。

### 6. `1c5b89ff` — sycl: support dev2dev memcpy by DEV2DEV_MEMCPY_FORWARD (#26234)
新增裝置間（device-to-device）直接記憶體複製支援。

### 7. `a2be61dc` — [SYCL] Support q2 mul_mat (#26231)
為 SYCL 後端的矩陣乘法（mul_mat）新增 Q2_0 量化型態支援。

### 8. `15537259` — sycl: fuse RMS_NORM + MUL (#26015)
將 RMS_NORM 與 MUL 運算融合為單一 kernel，減少 SYCL 後端的運算開銷。

---

## 群組三：其他後端（CUDA / WebGPU / ZenDNN）

### 9. `15e755f3` — cuda: extract Q2_0 elements via __byte_perm (#25603)
改用 `__byte_perm` 指令來提取 Q2_0 量化元素，簡化並優化 CUDA 的 `mmq-load-tiles` 與 `vecdotq` 相關程式碼。

### 10. `8f4646a6` — ggml-webgpu: improve flash_attn_vec for quantized KV at long contexts (#25956)
改善 WebGPU 後端在長上下文、KV cache 量化情境下的 flash attention（vec 版本）效能與正確性，並修復多個相關 bug。

### 11. `1e225995` — ggml-zendnn: group matmul direct API for mul_mat_id (#25918)
為 ZenDNN 後端的 `mul_mat_id`（MoE 專家路由矩陣乘法）改用 group matmul 直接呼叫 API，並依專家數量調整 fallback 門檻。

---

## 群組四：模型核心 / KV Cache / MTP 相關

### 12. `a09d8abf` — Support rotated kv cache quant (#26180)
為 minimax-m3 模型新增對「旋轉（rotated）」KV cache 量化格式的支援。

### 13. `82dbc4f0` — llama: load MTP tensors only if they are really used (#26296)
僅在真正會用到 MTP（Multi-Token Prediction）張量時才載入，避免不必要的記憶體與載入時間浪費，並擴及多個支援 MTP 的模型（cohere2moe、glm-dsa、hy-v3、qwen35、qwen35moe、step35）。

### 14. `69e62fc7` — llama: enforce the same K and V cache types for DeepSeek V4; enable FA if V cache is quantized (#25871)
針對 DeepSeek V4 與 MLA 類模型，強制 K/V cache 使用相同型態，並允許在 V cache 為量化型態時啟用 flash attention。

---

## 群組五：多模態 (mtmd)

### 15. `db7d8b24` — mtmd: add n_embd_head (#26342)
在多模態（mtmd）clip 模型中新增 `n_embd_head` 欄位，補齊 GGUF 常數與 writer 的對應設定。

---

## 群組六：Server

### 16. `00054751` — server: correct accepted tokens when need draft token replay (#26320)
修正 server 在推測解碼（speculative decoding）需要重播 draft token 時，計算「已接受 token 數量」的錯誤邏輯。

---

## 小結

本次更新以 **SYCL 後端** 為重點（5 個 commit），涵蓋 flash attention 加速、資料型態補齊、記憶體複製與運算融合；其餘涉及 Vulkan（POOL_1D、Intel 驅動崩潰因應、SDK 升級）、CUDA、WebGPU、ZenDNN 等多個後端的效能與正確性改進，以及 KV cache／MTP 相關的模型核心邏輯調整。整體以效能優化與硬體相容性修正為主，未見破壞性 API 變更。
