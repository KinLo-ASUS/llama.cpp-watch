# llama.cpp 更新報告 — 2026-08-25

- **來源倉庫**：[ggml-org/llama.cpp](https://github.com/ggml-org/llama.cpp)，分支 `master`
- **檢查區間**：`c060ca97`（2026-08-24 之前的紀錄）→ `f280b269`（本次最新）
- **新增 commit 數**：12 筆

---

## Group 1：Metal 後端大規模重構與效能調校（2 筆）

- [`b615f5b4`](https://github.com/ggml-org/llama.cpp/commit/b615f5b4bd9903206f5e8dcb67bdf2fd5d88efea) metal: per-op source split + parallel compile (#26561)
- [`f280b269`](https://github.com/ggml-org/llama.cpp/commit/f280b26983ad0fdb705a0d9ebf0503e76f2899b0) metal : per-device tuned (Q, NE) for flash-attn vec (#26570)

Metal 後端本次有兩筆規模非常大的改動。第一筆將原本單一巨大的 metallib 原始碼依運算子拆分為約 20 個獨立來源檔（`kernels/*.metal`），並改為平行編譯、依函式名稱自動路由對應的 library，藉此縮短編譯時間並減少單一函式庫的二進位大小；同時補齊多個先前在拆分後新增的核心（如 lightning indexer、DSv4 hyper-connections、TQ2_0、flash-attn KV dequantize、ssm_scan rollback 等）到對應的拆分檔案中，屬於純粹的程式碼搬移、無功能變化。第二筆則為 flash-attention 的向量化（vec）核心新增「依裝置調校」的 (Q, NE) 參數表：新增離線調校工具 `tools/tuning`（含 `fa-vec` 掃描、壓縮與產表流程），擴充 53 組 f16 (Q, NE) 實例，並針對 M1 Pro、M2 Ultra、M5 Max 等實機填入調校結果，目的是讓不同 Apple 裝置在 flash-attn 上有更貼合硬體特性的效能表現。兩者合計新增／異動超過 15000 行，是本次改動幅度最大的部分，建議使用 Metal 後端（Apple Silicon）的使用者留意此次更新後的效能與穩定性。

## Group 2：DeepSeek V4 張量並行（`-sm tensor`）支援（1 筆）

- [`bf0a29cc`](https://github.com/ggml-org/llama.cpp/commit/bf0a29cc16066d313f6a8b52025620287a75cbc4) Deepseek 4: `-sm tensor` (#26490)

為 DeepSeek V4 新增 `-sm tensor`（張量並行切分策略）支援：大幅擴充 `ggml-backend-meta.cpp` 中 meta backend 的邏輯（約新增 200 行），並同步調整 attention head 切分的粒度、修正 dspark 相關問題、支援 dsv4 的模型儲存（model saver）、讓 dflash 可指定回傳到特定裝置，並為共享專家（shared expert）新增延遲 all-reduce 最佳化。此為延續近期 DeepSeek V4 多裝置張量切分工作的後續進展。

## Group 3：GLM 轉換腳本 regression 修正（1 筆）

- [`71cc86fa`](https://github.com/ggml-org/llama.cpp/commit/71cc86fa41ff38c86e6409ba388dc7854e88588d) convert: fix GLM regression in index_tensors (#27655)

修正 `conversion/glm.py` 中 `index_tensors` 的一個回歸問題，此為修正前一天（2026-08-24）新增的 GLM-4.5-Air MTP 支援之後所引入的問題，屬於快速跟進的修補。

## Group 4：CI 與測試整理（2 筆）

- [`985b1491`](https://github.com/ggml-org/llama.cpp/commit/985b14912bc9a3d8f27c1ac569e46f4092123722) ci : apply ccache-clear with older/min/dry-run to all ccache jobs (#27602)
- [`75844307`](https://github.com/ggml-org/llama.cpp/commit/7584430716ee229751771ed0d6bbcb780d105eeb) tests : disable DOTS3NOTE arch test for WebGPU (#27654)

第一筆將既有的 `ccache-clear`（依「過舊 / 最少保留 / 是否僅預演」規則清理 ccache）套用到所有使用 ccache 的 CI job（原本僅涵蓋部分後端），並將清理邏輯抽出成獨立的 `scripts/ccache-clear.sh`，途中修正了 container job 缺少 `gh` CLI、macOS BSD `date` 無法解析 ISO 時間、以及 container 內 `gh cache` 無法自動偵測 repo 等一系列問題。第二筆則在 WebGPU 後端的 `test-llama-archs` 測試中停用 `DOTS3NOTE` 架構測試，避免該測試在 WebGPU 上失敗擋住 CI。

## Group 5：後端修正 — WebGPU / ggml 核心（2 筆）

- [`c1c766da`](https://github.com/ggml-org/llama.cpp/commit/c1c766da59baa97ddc887e49b82cce718325c834) webgpu : reorder includes since V that appears in common_decls.tmpl may be defined as K in flash_attn_decls.tmpl if KV_OVERLAP (#27545)
- [`6036c635`](https://github.com/ggml-org/llama.cpp/commit/6036c635e2d1e9aeca11d01a84188c9db9ea1e2a) ggml : fix ggml_clamp (#27644)

第一筆修正 WebGPU flash-attention shader 的 `#include` 順序問題：在 `KV_OVERLAP` 情境下，`flash_attn_decls.tmpl` 中定義的 `K` 變數可能與 `common_decls.tmpl` 中出現的 `V` 命名衝突，調整引入順序後解決此編譯期符號衝突。第二筆修正核心運算子 `ggml_clamp` 的實作錯誤，並同步更新 `ggml-alloc.c` 以配合修正後的行為，屬於底層數值運算正確性的修補。

## Group 6：其他模型 / 功能相關修正（3 筆）

- [`a1305329`](https://github.com/ggml-org/llama.cpp/commit/a130532ae1c4c54daaae5527795f5b19c184f269) mamba2 : Flatten in/out projections to dispatch GEMM instead of GEMV (#27513)
- [`160c6b0b`](https://github.com/ggml-org/llama.cpp/commit/160c6b0bdd9739d6c8c8539e7067f64e8ea57067) mtmd: video: fix moov atom at the end of file (#27596)
- [`b3c3b96a`](https://github.com/ggml-org/llama.cpp/commit/b3c3b96a139d4ef1bdec926ac17aa040981cfc5d) misc : read repetition_penalty from generation_config.json (#27659)

第一筆將 Mamba2 的輸入／輸出投影運算攤平（flatten），使其在執行時能派發到 GEMM（矩陣乘矩陣）而非效率較差的 GEMV（矩陣乘向量）路徑，可提升 Mamba2 架構模型的推論效能。第二筆修正 `mtmd` 處理影片輸入時，若影片檔的 `moov` atom（MP4 容器中存放中繼資料的區塊）位於檔案尾端時的解析問題，並補上 Windows 平台下處理管線中斷（broken pipe / SIGPIPE）的例外情況。第三筆讓模型中繼資料讀取邏輯支援 Hugging Face 標準鍵名 `repetition_penalty`（先前僅支援 `penalty_repeat`），並對應寫入 `metadata.sampling_penalty_repeat`，提升與 HF `generation_config.json` 格式的相容性。

## Group 7：CUDA / Metal 裝置命名精簡（1 筆）

- [`a14dba68`](https://github.com/ggml-org/llama.cpp/commit/a14dba686aaafba3a2d6b5eb8820b0df5c5d2d92) ggml : shorten virtual device naming in CUDA and Metal (#27608)

精簡 CUDA 與 Metal 後端中「虛擬裝置」（用於多裝置切分情境）的命名字串，並將 Metal 裝置描述字串改為在初始化時就建構完成，屬於顯示訊息與程式碼可讀性的調整，不影響功能行為。

---

## 本次重點摘要

- 本次共 12 筆 commit，數量略少於前次，但質量上偏重：**Metal 後端有兩筆超大規模改動**（原始碼依運算子拆分並平行編譯、flash-attn 依裝置調校 (Q, NE) 參數表），合計新增／異動超過 15000 行，是近期少見的大型後端重構，建議使用 Apple Silicon / Metal 後端的使用者密切留意此次更新的相容性與效能表現。
- **DeepSeek V4 持續擴充張量並行能力**：新增 `-sm tensor` 切分策略，延續近期 DSv4 多裝置支援的開發脈絡。
- **GLM-4.5-Air 快速修補**：前一天新增的 MTP 支援在轉換腳本中引入了 regression，本次已修正，顯示上游對新功能的問題反應相當迅速。
- CI 面持續強化 ccache 清理機制的覆蓋範圍與健壯性（跨 container、macOS、多後端）；WebGPU 後端修正了一個 shader include 順序造成的符號衝突，以及停用一項在 WebGPU 上會失敗的架構測試。
- 其他：`ggml_clamp` 核心運算子修正；Mamba2 效能優化（GEMM 取代 GEMV）；`mtmd` 影片解析修正尾端 `moov atom` 問題；模型中繼資料新增對 HF 標準 `repetition_penalty` 鍵名的相容性讀取；CUDA/Metal 虛擬裝置命名精簡。
