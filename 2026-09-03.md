# llama.cpp 每日變更報告 — 2026-09-03

- 監控範圍：`ggml-org/llama.cpp` `master` 分支
- 檢查區間：`b96806d` (2026-09-02) → `67a17c1` (2026-09-03)
- 新增 commit 數：**23**

---

## 一、Hexagon (Qualcomm NPU) 後端

涵蓋這次更新中份量最大的一批變更，聚焦於 Hexagon HTP 後端的算子融合、效能與除錯能力。

| commit | 說明 |
|---|---|
| `960dffa` | **MUL_MAT / MUL_MAT_ID 融合與修正**：將可落在 HMX 上的 QKV、FFN matmul 融合執行；移除硬編碼的 `ne[1] < 32K` 限制；修正大維度下的 VTCM overhead 估算避免爆量；新增 buffer 排序與簡易 VA defrag 以緩解位址空間碎片化；以 fastdiv 取代部分純量除法。屬於較大規模的效能與穩定性改動。 |
| `f027c4f` | **F16 unary 算子擴充支援 ABS**：在既有 NORM/RMS_NORM/L2_NORM/SCALE/CLAMP/SQR/SQRT 之外新增 ABS 的 F16 路徑，並合併 F32/F16 的 unary 執行函式以減少重複程式碼；已在實機 (QRD8850, Hexagon v81) 驗證通過。 |
| `69320fe` | **補齊 FARF 除錯日誌**：為 cpy/get_rows/set_rows/gdn (gated-delta-net) 等算子補上形狀與 VTCM 相關的除錯日誌，並修正一處 log 印錯 `ne[2]` 的小 bug。 |

**摘要**：本輪針對 Hexagon 後端的投資明顯偏重「算子融合＋效能」與「可觀測性（日誌）」，`960dffa` 是三者中影響面最廣的一筆。

---

## 二、GPU / CPU 後端修正與效能優化

跨 OpenCL、Vulkan、CUDA、Metal、CPU(KleidiAI) 多個後端的修正與微調，彼此獨立但主題相近。

| commit | 後端 | 說明 |
|---|---|---|
| `43d87ff` | OpenCL | 修正 Adreno image kernel 的**越界讀取**：對 q4_K GEMV 的 fetch row 做 padded x-grid clamp，並修正 image KQ/KQV GEMM 的 tiling 契約與 split 判斷邏輯（改由 dispatch 端決定而非依賴 stride）。屬於正確性/安全性修正。 |
| `ba8818c` | Vulkan | IQ3_S mat-vec 在 batch size > 4 時效能優化，n=8 情境下量測有 **5 倍**加速；同步在 test-backend-ops 增補對應測資。 |
| `56dd815` | Vulkan | 僅在裝置實際支援時才要求 `VK_KHR_shader_bfloat16` 擴充，避免在不支援的裝置上初始化失敗。 |
| `8e93a97` | CUDA | 為 DeepSeek-V4 / GLM 系列新增 **sparse flash-attention** 支援，涉及 fattn 系列多個檔案與 ggml 核心 API，屬於較大的功能性改動。 |
| `3d3d7c8` | CUDA | 清理 `mmq-vec-dot.cuh` / `mmq.cuh` 中未使用的變數，純程式碼整理。 |
| `8c0b9cd` | Metal | 修正低記憶體情況下的記憶體查詢邏輯。 |
| `9cc3394` | Metal | 為 M3 晶片新增 flash-attention-vec 的調校參數表。 |
| `b81c99b` | CPU (KleidiAI) | 避免在 dispatch 階段才初始化 KleidiAI buffer type，屬於初始化時機修正。 |

**摘要**：以正確性修正（OpenCL 越界讀取、Vulkan 擴充檢查）與效能調校（Vulkan IQ3_S、Metal M3 調校、CUDA sparse-FA）為主，多為各後端獨立、範圍侷限的變更。

---

## 三、多模態 (mtmd) 與新模型支援

圍繞 DeepSeek-V4 視覺模型上線與既有多模態模型修復的一系列變更。

| commit | 說明 |
|---|---|
| `7798007` | **新增 DeepSeek-V4-Flash-Vision-Exp 支援**：mtmd 新增對應的 clip 模型實作、tensor mapping、CLI token 數量處理等，是本次多模態相關中最大的一筆新增功能。 |
| `9400c89` | 接續上一筆，修正 deepseek4 視覺輸入的正確性（KV cache、hparams 等）。 |
| `0f3a71b` | 修正 **Qwen3-TTS-0.6B**：0.6B checkpoint 因 talker 與 code predictor 共用 hidden size，缺少 `small_to_mtp_projection` 權重，需讓 loader 視其為可選；另修正 code predictor 的 `ffn_down` 需保留 F32 精度，否則會因數值超出 F16 上限（65504）產生 inf/NaN。 |
| `67a17c1` | 修正 **idefics3** 前處理邏輯（本輪最新一筆 commit）。 |
| `7339054` | 新增 `mtmd_tokenize_from_parts()` API，供 mtmd-cli 使用，屬 API 擴充。 |
| `9cffdcc` | server 端 `input_video` / `input_audio` 補上 `data:` URL 支援（原僅 `image_url` 支援），並將驗證錯誤由 500 改回 400。 |

**摘要**：本輪多模態變更的核心是 DeepSeek-V4 視覺能力的上線（新模型），並伴隨多個既有模型（Qwen3-TTS、idefics3）的正確性修復。

---

## 四、Server / 推理功能

| commit | 說明 |
|---|---|
| `e750b88` | `preserve_reasoning` chat-template kwarg **預設改為啟用**（除非使用者明確以 `--reasoning-preserve` / `--no-reasoning-preserve` 指定），server 會記錄其生效狀態；透過 `--chat-template-kwargs` 設定此參數的方式標記為 deprecated。 |

---

## 五、建置 (Build) 與 CI

| commit | 說明 |
|---|---|
| `2637dfe` | 修正 gcc < 15 編譯失敗：CMakeLists 原先無條件加入 SpacemiT IME2 kernel 原始碼，但 gcc 14 僅偵測到 IME1，改為依偵測結果條件式加入對應 kernel。 |
| `d5fec32` | CI：在 server-cuda 上啟用 hf-jobs。 |
| `cff1844` | CI/Release：ROCm 更新至 10.0.0 正式版。 |
| `03dbcc5` | CI：新增檢查缺漏 autoreleasepool 的機制，並將圖形裝置名稱比對規則通用化。 |

---

## 六、訓練 (Finetune)

| commit | 說明 |
|---|---|
| `159b741` | 修正 finetune 在無 KV cache 情況下的錯誤。 |

---

## 小結

今日 (2026-09-02 ~ 2026-09-03) 共 23 筆 commit，重點如下：
1. **Hexagon 後端**持續投入算子融合與效能優化（`960dffa` 影響面最大）。
2. **DeepSeek-V4 視覺（多模態）支援**正式上線，為本輪最重要的新功能。
3. 多個後端（OpenCL、Vulkan、Metal）有**正確性修正**，其中 OpenCL 的越界讀取修正值得留意。
4. CUDA 新增 DSV4/GLM 的 sparse flash-attention，屬效能相關功能擴充。
5. 其餘為建置/CI 穩定性修正與既有多模態模型的 bug fix。

---
*本報告由自動化排程工作產生，比對基準：`b96806d96061049a5b574269b049bf6241d63d46`，本次最新：`67a17c17caa95742186f8b1ecadd1b5abd6d5ebb`。*
