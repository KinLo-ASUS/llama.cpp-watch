# llama.cpp 更新報告 (2026-09-13)

- 檢查範圍：`82d6bb2`（server: refactor subproc handling #28555）→ `790cf51`（chat: improve parsing of complex types in qwen3-coder #28742）
- 新增 commit 數：**18**
- 上游倉庫：[ggml-org/llama.cpp](https://github.com/ggml-org/llama.cpp)，分支 `master`

---

## 分組摘要

### 1. Hexagon (Qualcomm NPU) 多裝置分割支援

| Commit | PR | 摘要 |
|---|---|---|
| `eafe15a` | [#28589](https://github.com/ggml-org/llama.cpp/pull/28589) | hexagon: support for multi-device model split (aka row-split) |

大型變更（超過 60 個子提交合併），為 Hexagon NPU backend 新增**多裝置 row-split**支援，讓模型可以依「row」切分到多個 Hexagon 裝置上執行。內容包含：
- 為 fused kernel（matmul、flash-attention、allreduce 等）加入跨裝置的 work-splitting 邏輯
- 新增 `htp-fence.h` 等機制強化多裝置間的 fence / barrier 同步與錯誤處理
- 大量修正各 op（CPY、ROPE、ARGSORT、PAD、SSM_CONV 等）在 multi-device 模式下的分區與邊界錯誤
- 更新 Hexagon backend 的開發者文件與 `scripts/snapdragon/run.py` 執行腳本
- 新增 `ggml-hexagon-align-macros.py` 巨集對齊輔助工具

影響範圍：僅影響搭載 Qualcomm Hexagon NPU（Snapdragon）裝置的使用者。

---

### 2. OpenCL backend 修正與新增 kernel

| Commit | PR | 摘要 |
|---|---|---|
| `07fc977` | [#28677](https://github.com/ggml-org/llama.cpp/pull/28677) | opencl: add bin kernel `kernel_gemm_noshuffle_q4_k_f32_32b_trans_ila_a8_bin` |
| `8a56aed` | [#27630](https://github.com/ggml-org/llama.cpp/pull/27630) | opencl: fix several bugs where the backend aborts |

新增一個針對 A8/Q4_K（非 MoE）情境的 binary GEMM kernel，並重新整理 binary kernel 選擇邏輯的命名；另一筆修正多個會導致 OpenCL backend 直接 abort 的錯誤，屬於穩定性修正。

---

### 3. GPU backend（WebGPU / CUDA / SYCL）相關修正

| Commit | PR | 摘要 |
|---|---|---|
| `d3146f2` | [#28683](https://github.com/ggml-org/llama.cpp/pull/28683) | ggml-webgpu: 更新至較新版本的 Dawn |
| `3f5e94d` | [#28382](https://github.com/ggml-org/llama.cpp/pull/28382) | webgpu: 將 tensor binding 對齊到該資料型別的 block size |
| `c8edceb` | [#27841](https://github.com/ggml-org/llama.cpp/pull/27841) | ggml-cuda: 為 HIP 新增 AMD GCN 專用的 config table |
| `2a3005c` | [#28681](https://github.com/ggml-org/llama.cpp/pull/28681) | sycl: 對不支援的 tq1_0 量化格式進行優雅失敗處理 |

WebGPU backend 升級底層 Dawn 版本並移除多餘的 module scanning；同時修正 block-quantized tensor 的 binding offset 計算，回退偏移量直到對齊到完整 block，避免 shader 存取到錯誤的 element offset。CUDA/HIP 新增針對 AMD GCN 架構的 mmq config table 以改善其效能設定；SYCL backend 對不支援的 `tq1_0` 量化格式改為回報錯誤而非未定義行為。

---

### 4. Server / router 修正

| Commit | PR | 摘要 |
|---|---|---|
| `c069aa7` | [#28747](https://github.com/ggml-org/llama.cpp/pull/28747) | server: 將 router child 的狀態指令視為完整一行處理 |
| `e192abb` | [#28795](https://github.com/ggml-org/llama.cpp/pull/28795) | server: 補上缺漏的 headers |
| `56381e4` | [#28530](https://github.com/ggml-org/llama.cpp/pull/28530) | server: 修正在 model 數量達上限時仍允許下載模型（fix #26809） |
| `3057bb6` | [#28802](https://github.com/ggml-org/llama.cpp/pull/28802) | ui: 為前端資源加入快取 |

`c069aa7` 修正一個 race：子行程的 log 與狀態指令共用同一個 pipe，logger 輸出的 ANSI 顏色重置碼若沒有接在換行後，會與下一行狀態指令黏在一起，導致 router 誤判為 log 而非指令，使已完成的下載卡在「下載中」狀態；修法是在指令前補上換行以確保行邊界正確。`56381e4` 修正即使已達模型數量上限仍可觸發新模型下載的問題。`3057bb6` 為 Web UI 靜態資源加入快取機制。

---

### 5. Build / vendor / infra

| Commit | PR | 摘要 |
|---|---|---|
| `f3a33df` | [#28492](https://github.com/ggml-org/llama.cpp/pull/28492) | rpc: 修正 `BUILD_SHARED_LIBS=OFF` 時的連結錯誤 |
| `718f7b4` | [#28787](https://github.com/ggml-org/llama.cpp/pull/28787) | vendor: 將 cpp-httplib 更新至 0.56.0 |
| `737e098` | [#28816](https://github.com/ggml-org/llama.cpp/pull/28816) | cmake: 在 clang 上不要把 timestamp 寫入 precompiled header |

`737e098` 修正 clang 系列編譯器（含 clang-cl、Intel LLVM）在使用 ccache 還原的 PCH 快取時，因來源檔案 timestamp 與快取建立時的 checkout 不一致而拒絕使用該 PCH，造成建置失敗的問題。其餘為相依套件更新與靜態連結修正。

---

### 6. Chat template / JSON Schema / Grammar 重構

| Commit | PR | 摘要 |
|---|---|---|
| `acecd56` | [#28736](https://github.com/ggml-org/llama.cpp/pull/28736) | common: 實作 `common_chat_schema` 作為 JSON Schema 的內部表示 |
| `790cf51` | [#28742](https://github.com/ggml-org/llama.cpp/pull/28742) | chat: 改善 qwen3-coder 對複雜型別的解析 |
| `8e33095` | [#28586](https://github.com/ggml-org/llama.cpp/pull/28586) | common: 新增 `LOG_JSON` 巨集以記錄結構化資料 |
| `ae9afff` | [#28817](https://github.com/ggml-org/llama.cpp/pull/28817) | jinja: 支援 dot 語法存取整數字面值的屬性 |

`acecd56` 是本次最大的重構之一：引入 `common_chat_schema` 作為 JSON Schema 的內部表示，並以此重寫 `json-schema-to-grammar`，同時新增獨立的 JSON Schema 優化器與型別/種類（kind）解析邏輯；副作用是移除了舊的 Python 範例腳本（`examples/json_schema_to_grammar.py` 等）並新增 `tests/test-json-schema.cpp` 測試。`790cf51` 在此基礎上改善 qwen3-coder 工具呼叫解析中對複雜巢狀型別的支援。`8e33095` 新增 `LOG_JSON` 方便輸出結構化 log。`ae9afff` 是 jinja 樣板引擎的小修正，支援對整數字面值使用 dot 屬性語法。

---

## 小結

本次更新的重點落在：
1. **Hexagon (Qualcomm NPU) 多裝置切分**是本輪最大的功能性投入，屬於實驗性、大規模的 backend 擴充。
2. **JSON Schema / chat 解析重構**（`common_chat_schema`）是對工具呼叫（tool calling）基礎設施的重要重構，影響所有依賴 grammar-constrained JSON 輸出的功能。
3. 其餘為 OpenCL/CUDA/SYCL/WebGPU 等 backend 的穩定性與相容性修正，以及 server/router 的 bug fix 與建置系統修正，風險與影響範圍相對較小。

未發現需要立即關注的安全性或重大回歸問題。
