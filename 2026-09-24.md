# llama.cpp 更新報告 - 2026-09-24

- 檢查範圍：`e6ab7c1a41054a888ada952eab4c886444c2f5ad` (e6ab7c1) → `bd4f514db14d87fded667787a7a963bfbaa98e89` (bd4f514)
- 新增 commit 數：33
- 上次檢查日期：2026-09-23

## 摘要總覽

本次新增的 33 個 commit 涵蓋範圍相當廣，重點包括：**多個 GPU/加速後端（SYCL、Vulkan、CUDA、Metal、OpenCL）的 kernel 新增與效能/正確性修正**、**server 穩定性與相容性修正**、**Gemma 4 DSpark 推測解碼草稿模型支援**、**jinja 範本引擎的運算子優先權修正**，以及一組**版本號發布維護（ggml 0.25.0 → 0.25.1、llama.cpp 正式邁入 0.5.0）**。整體而言本次是一次跨後端、跨模組的常規開發批次，未發現重大破壞性變更。

---

## Group 1：Server 功能與相容性

| Commit | 標題 | 摘要 |
|---|---|---|
| `bcbc936` | server: Dedup the draft HF model via dedup-cache-models (#27934) | 修 #27846：推測解碼用的草稿（draft）HF 模型改用 dedup-cache-models 機制去重，避免重複下載/快取同一模型；並修正 lambda 補捉 structured binding 造成的編譯問題。 |
| `057494f` | server: accept OpenAI video_url content type and data: video URIs (#27921) | llama-server 先前只接受非標準的 `input_video` 類型，且拒絕影片的 `data:` URI，導致標準 OpenAI 相容用戶端送出 `video_url` 或 base64 影片時失敗。此修正接受 `video_url` 作為別名，並允許 `data:video/*` URI（與圖片行為一致）。 |
| `42916d8` | server: fix token counting API crash on sleep (#29309) | 修正伺服器進入 sleep（休眠）狀態後呼叫 token 計數 API 會崩潰的問題，補上正確的喚醒（wake up）邏輯，並新增回歸測試 `test_sleep.py`。 |
| `bddf826` | common: keep HF cache dir as path, expose UTF-8 only for logs (#29320) | 部分回退 #29125：`fs::path::string()` 在 Windows 上可能造成路徑編碼失真（lossy），因此 `get_cache_directory()` 改回傳 `fs::path`，只在寫 log 時才轉成 UTF-8 字串。 |
| `b9ae43a` | server: allow preset to set log file (#29334) | 讓透過 preset 啟動的模型也能在設定中指定 log file 路徑（先前僅主行程 CLI 支援）。 |

**說明**：這組多為 server 在多模型路由 / 推測解碼 / 休眠等情境下的相容性與正確性修正，`057494f` 對接 OpenAI 標準用戶端的相容性值得留意。

---

## Group 2：GPU / 加速後端 kernel 新增與優化（SYCL / Vulkan / CUDA / Metal / OpenCL）

| Commit | 標題 | 摘要 |
|---|---|---|
| `08b1d2a` | vulkan: hide internal symbols to prevent duplicate-dlopen state destruction (#29139) | 修 #29138：#28732 之後 Vulkan 後端內部符號被意外導出，導致 `ggml_backend_load_all()` dlopen 出的重複副本在 dlclose 時觸發解構子，摧毀了正在使用中的 `vk_instance`，之後裝置查詢就會撞上 `GGML_ASSERT`。修正把導出範圍收回到只有 `GGML_BACKEND_API` 可見，恢復 #28732 之前的行為。 |
| `b1ff4ca` | vulkan: add IQ4_XS MMQ/MMV matmul kernels (#28415) | 新增 Vulkan 後端對 IQ4_XS 量化格式的 MMQ/MMV matmul kernel；review 後移除死碼分支，並在 Intel 裝置上關閉 IQ4_XS 的 MMVQ（A770 上 tg 效能倒退 27.3%）。 |
| `4d7d770` | sycl : support op get_rows_back, only support fp32/fp16 (#25266) | 為 SYCL 後端新增 `get_rows_back` 運算子實作（僅支援 fp32/fp16），更新 `ops.md` 與 SYCL 測試覆蓋矩陣（`docs/ops/SYCL.csv` 為自動產生的測試清單，改動量最大）。 |
| `5e48b31` | sycl: extend MMVQ GLU fusion, add rms_norm+scale and ssm_conv+silu fusions (#28931) | 擴大 SYCL MMVQ 的 GLU 融合支援至混合量化類型組合，並新增 `rms_norm+scale`、`ssm_conv+silu` 兩組運算融合，減少中間結果讀寫以提升效能。 |
| `384a534` | sycl : support new UT case for mul_mat_hadamard fp16 (#29218) | 為 SYCL 的 `mul_mat_hadamard` 新增 fp16 單元測試案例覆蓋。 |
| `e97545d` | sycl : fix compile warnings | 修正 SYCL 後端數個編譯警告。 |
| `1a67982` | cuda: top-k MoE should always fire (#28432) | 修正 CUDA 後端 top-k MoE（Mixture-of-Experts）最佳化路徑在特定條件下未被觸發的問題，確保其總是生效。 |
| `dc9879c` | CUDA: enable sparse-fa for dsv4 prefill (again) (#29298) | 重新啟用 DeepSeek-V4 prefill 情境下的 sparse flash-attention；並對 sparse mask 掃描迴圈做兩輪優化，把迴圈次數改為編譯期模板參數以利展開，49k context 下 batch-1 decode 掃描由 46us 降至 17us，批次 sparse 運算由 586us 降至 244us。 |
| `66fba63` | CUDA: add a reserve to avoid spurious warning on older GCC builds (#29317) | 為避免舊版 GCC 編譯時出現誤報警告，新增一處 `reserve()` 呼叫。 |
| `ee3ecce` | metal : key the fa-vec tuned table by family instead of SKU (#29075) | 將 Metal flash-attention-vec 調校參數表改用 GPU 家族（family）而非個別 SKU 作為索引鍵，未經特別調校的家族則回退至基準參數；調校表大幅精簡（減少近 4000 行）。 |
| `9575389` | metal: add the missing f32 x bf16 mul_mv variants (#28741) | 修正深度可分離卷積（`ggml_conv_1d_dw`）在 bf16 權重下會呼叫到從未實作的 `kernel_mul_mv_f32_bf16` 的問題，補齊 base/_4/_short 三種變體。 |
| `fee39dd` | opencl: add A8 Q6_K non-MoE dp4a binary kernel (#29057) | 新增 OpenCL 平台上 Q6_K 量化、非 MoE 架構的 A8 dp4a binary GEMM kernel，延續近期針對 Adreno 等裝置的效能調校系列。 |
| `9425611` | ggml-meta: resolve multi buffer views (#29266) | 修正 ggml-backend-meta（多裝置/多後端調度層）處理多重 buffer views 的解析邏輯，並加註 TODO 待未來 graph allocator 重構時再檢視。 |

**說明**：本次是各硬體後端 kernel 覆蓋率與效能持續打磨的常規批次，涵蓋 SYCL、Vulkan、CUDA、Metal、OpenCL 五種後端。其中 `08b1d2a` 為修正因先前 PR 造成的 dlopen 解構子競爭問題（實際使用中會觸發 assert 崩潰），屬正確性修正，若專案有透過 `ggml_backend_load_all()` 動態載入多後端，建議優先更新。`dc9879c` 標題含「(again)」，代表 sparse-fa 曾被停用後又重新啟用，建議留意是否有相關效能迴歸的後續回報。

---

## Group 3：模型轉換與推測解碼（Speculative Decoding）支援

| Commit | 標題 | 摘要 |
|---|---|---|
| `633733d` | model : support Gemma4 DSpark draft backbone (#29226) | 新增 Gemma 4 DSpark 推測解碼草稿模型的 GGUF 轉換與執行期支援，涵蓋 full-attention 與 SWA（sliding window attention）兩種變體、tied output weights 與 boolean backbone metadata；dflash 執行期改為自動由 metadata 推斷 Gemma 草稿模型特徵。 |
| `bd4f514` | convert : allow vision target for DFlash/Dspark (#29339) | 修正轉換流程改用 `get_model_architecture` 解析目標架構，讓 vision 目標模型（如 `Lfm2VlForConditionalGeneration`）能正確對應到其文字模型以取得 vocab；同時修正 LFM2/LFM2.5 DSpark 草稿模型的 rope 重排被套用兩次的問題。 |

**說明**：DFlash/DSpark 推測解碼系列持續針對更多模型架構（Gemma 4、LFM2 系列）補齊相容性。

---

## Group 4：Jinja 範本引擎

| Commit | 標題 | 摘要 |
|---|---|---|
| `4e416ee` | jinja : parse unary +/- before variables (#29244) | 修正運算子優先權：詞法分析器（lexer）已能產生 `unary_operator`，但語法解析器未在乘法優先權層級解析它，導致像 `items[:-n]` 或 GigaChat 範本中 `indent[:-indent_factor]` 這類切片運算式解析錯誤；同時修正 unary +/- 應只綁定最近運算元（`-n|abs` 應解析為 `(-n)|abs` 而非 `-(n|abs)`），並補上對應迴歸測試。 |

**說明**：屬 chat template 相容性修正，可能影響使用切片語法或特定 unary 運算式的自訂聊天範本（如 GigaChat）。

---

## Group 5：CI / 建置設定

| Commit | 標題 | 摘要 |
|---|---|---|
| `86b2daa` | ci : run python (jinja) test (#29302) | 在 build-cpu CI workflow 補上執行 Python (jinja) 測試的步驟。 |
| `26758d3` | ci : fix build-cmake runner target (#29299) | 修正 `build-cmake-pkg` workflow 中指定的 CI runner 目標設定錯誤。 |
| `6e60f35` | ci : use hf-jobs-cpu-xl runner in server sanitize workflow (#29297) | `server-sanitize` workflow 改用規格較大的 `hf-jobs-cpu-xl` CI runner。 |

---

## Group 6：測試/開發工具

| Commit | 標題 | 摘要 |
|---|---|---|
| `18f9f7b` | model-conversion : add causal-compare-logits recipe (#29305) | 在 model-conversion 範例的 Makefile 新增一個可針對「既有」模型輸出（logits）做驗證比對的 recipe：先跑一次原始模型存下 token/logits（大型模型很耗時，且原始模型鮮少變動），之後手動跑轉換後模型，再用此 recipe 比對兩者結果，避免每次驗證都重跑原始模型。 |
| `d2e5458` | tests: add `-b/--backend` option to test-llama-archs for testing a specific backend (#27372) | 為 `test-llama-archs` 測試工具新增 `-b/--backend` 參數，可指定只針對特定後端執行架構測試，而非固定跑遍全部後端。 |

---

## Group 7：版本號與發布維護

| Commit | 標題 | 摘要 |
|---|---|---|
| `503549c` | ggml : bump version to 0.25.0 (ggml/1635) | ggml 子專案版本號提升至 0.25.0，並更新 make-release 摘要腳本。 |
| `45062d4` | sync : ggml | 將 ggml 上游同步狀態記錄更新。 |
| `183d2a0` | make-release : update summary prompt | 更新發布摘要（release summary）產生用的提示文字。 |
| `e4e2f62` | ggml : bump version to 0.25.1 (ggml/1637) | ggml 子專案版本號再提升至 0.25.1。 |
| `177cd8c` | sync : ggml | 再次同步 ggml 上游狀態記錄。 |
| `7fe450e` | llama.cpp : bump version to 0.5.0 (#29333) | llama.cpp 主專案版本號提升至 0.5.0。 |

**說明**：純發布維護性質的版本號調整與上游同步紀錄，本次 llama.cpp 正式邁入 **0.5.0**，值得記錄作為版本里程碑。

---

## 其他核心變更

| Commit | 標題 | 摘要 |
|---|---|---|
| `441df11` | sampler: reduce the size of the probe (#29285) | 縮小 sampler 內部一個 probe（探測用暫存結構）的大小，屬記憶體/效能微調。 |

---

## 觀察與建議

1. **`08b1d2a`（Vulkan dlopen 解構子競爭修正）**：修正因 #28732 導致的內部符號被意外導出問題，實際使用中可能觸發 `GGML_ASSERT` 崩潰。若專案透過 `ggml_backend_load_all()` 動態載入多個後端，建議優先更新。
2. **`dc9879c`（CUDA sparse-fa for dsv4 prefill "again"）**：標題顯示此功能曾被停用後又重新啟用，若使用 DeepSeek-V4 系列模型並依賴 sparse flash-attention，建議留意後續是否有效能或正確性回報。
3. **llama.cpp 0.5.0 / ggml 0.25.1 版本里程碑**：本次批次包含正式版本號提升（Group 7），可作為升級規劃的參考點。
4. **`4e416ee`（jinja unary 運算子優先權修正）**：若專案使用了包含切片語法（如 `items[:-n]`）的自訂聊天範本，建議確認升級後範本渲染行為是否符合預期（此為修正回正確行為，非破壞性變更）。
5. 本次沒有發現明顯的破壞性變更（breaking change）或安全性問題。
