# llama.cpp 每日追蹤報告 - 2026-09-01

## 檢查範圍

- 專案：[ggml-org/llama.cpp](https://github.com/ggml-org/llama.cpp)（分支：`master`）
- 上次檢查 commit：`9723942`（2026-08-31）
- 本次檢查最新 commit：`458681e`（2026-09-01）
- 新增 commit 數量：**18**

## 整體概覽

這批 commit 的重心明顯集中在**推論效能調校**：三個不同後端（Vulkan、ROCm、Metal）幾乎同步導入了 radix 排序版本的 Top-K 運算子，Metal 也新增了針對 Apple M1 / M1 Ultra 的 Flash-Attention 向量化調校參數。此外還有 CUDA 上的 MoE 融合範圍擴大、KV cache 還原效能優化（單一案例量測到還原時間從 25~63 秒降到 221~424 毫秒），以及數個跨後端的記憶體安全性修正。整體屬於穩定性 + 效能優化的迭代，沒有破壞性的介面變更。

## 分組摘要

### 群組 1：多後端 Top-K Radix Select 實作

三個後端各自導入了以 radix select 演算法加速 Top-K 運算的實作，主要用於大 k 值（例如 MoE router 或長序列）情境下取代原本較慢的排序方式。

| Commit | 後端 | 說明 |
|---|---|---|
| `daef7b6` | Vulkan | 新增 radix select shader，專門處理 k >= 1024 的情境（針對 Qwen 3.8 Flash Next 模型優化），並新增對應的 top-k/QSA 融合與測試案例 |
| `f8dbcd6` | ROCm (CUDA path) | 為長 row 情境新增 radix TOP_K 實作，取代原本效率較差的方式 |
| `2a74817` | Metal | 新增 top-k radix 實作，擴充 `argsort.metal` kernel |

### 群組 2：Metal Flash-Attention 向量化調校參數

針對蘋果自家晶片新增手動調校的 Flash-Attention（fa-vec）參數表，屬於純調校資料、不影響邏輯正確性。

| Commit | 說明 |
|---|---|
| `5d4a3be` | 新增 M1 的 fa-vec 調校參數 |
| `458681e` | 新增 M1 Ultra 的 fa-vec 調校參數，並將其排列在 M1 Max 區塊之後 |

### 群組 3：CUDA MoE 與 Flash-Attention 效能優化

| Commit | 說明 |
|---|---|
| `41ef91f` | 將 MoE 融合（glu fusion / topk-router fusion）從原本僅支援單一 token 擴展到支援推測解碼（speculative decoding）等多 token 情境，並補上 `SWIGLU_CLAMP` 案例 |
| `e4b9af0` | 為 Flash-Attention 的 K/V shared memory fp16 tile 導入 XOR swizzle，降低 bank conflict；同時修正 DGX Spark 上的一個 shared memory race condition，並支援非二的次方（non-pow2）形狀 |

### 群組 4：跨後端記憶體管理與穩定性修正

| Commit | 後端 | 說明 |
|---|---|---|
| `557614e` | WebGPU | 將 `MUL_MAT` 加入「可能需要額外記憶體」的運算列表，避免配置不足 |
| `8e53fce` | WebGPU | 修正 `ggml_backend_tensor_get()` 在 offset 非 4 的倍數時會 crash 的問題 |
| `a32af33` | SYCL | 改用 Level Zero / SYCL API 取得 Intel GPU 可用記憶體資訊，取代舊有方式 |
| `2cdae80` | Vulkan | 針對 AMD Strix Halo（RDNA3）調整批次推論時 mat-vec 的 row 參數為靜態值，實測較預設值快 |

### 群組 5：CPU / OpenCL 量化運算效能優化

| Commit | 說明 |
|---|---|
| `85c5522` | 新增 AVX2 上針對 IQ 系列量化模型的大 batch size prompt processing 加速路徑（新增 `ggml-cpu/iqp.cpp`，含 NUMA fallback 與新測試） |
| `010be96` | 針對 Intel Xe-LP GPU 調整 OpenCL 上 Q4_K/Q5_K 的 mul_mv / mul_mm 參數（N_DST、tile 大小），改善 token generation 與 prompt processing 效能 |

### 群組 6：KV Cache 與推測解碼（Speculative Decoding）核心優化

| Commit | 說明 |
|---|---|
| `2d8d612` | 優化非連續 KV cell 的狀態還原：將原本逐個 cell 複製改為依連續區塊批次複製，實測某生產環境案例從 136 萬次複製 / 25~63 秒，降至 224 次複製 / 221~424 毫秒；同時修正裝置端讀取器在讀寫分塊不一致時的 fallback 邏輯，並補上對應測試 |
| `662a0b0` | 將 DFlash 的 encoder 融合進 KV cache 注入的 decode 流程，省去原本需要的一次裝置↔主機資料往返與額外的 graph build |

### 群組 7：Metal 量化張量支援擴充

| Commit | 說明 |
|---|---|
| `ab0b3bd` | Metal 後端新增對量化類型的 `concat` 運算支援 |

### 群組 8：CI 與 WebUI 雜項

| Commit | 說明 |
|---|---|
| `580e88d` | CI 腳本新增對 `unzip` 指令是否存在的檢查 |
| `774ee0e` | WebUI 修正 agentic（多輪工具呼叫）回應合併顯示時，複製按鈕會複製到空字串的問題，改為正確組合所有文字段落後複製 |

## 備註

本次 18 個 commit 皆為 2026-08-31 至 2026-09-01 之間合併，皆為 `ggml-org/llama.cpp` `master` 分支的正常演進，未發現任何需要特別留意的破壞性變更或安全性議題。
