# llama.cpp 變更報告 — 2026-09-08

- 追蹤範圍：`ggml-org/llama.cpp` master 分支
- 檢查區間：`465e49b`（2026-09-07 之前的檢查點，不含） → `050dde5`（本次檢查點）
- 新增 commit 數：22

---

## 分組摘要

### 1. GPU 後端：新功能、算子擴充與效能／正確性修復（Vulkan / CUDA / SYCL / HIP / Hexagon / WebGPU）

- **8fe90e1** — vulkan: add TQ1_0 support (mm, mat-vec, mat-vec-id, dequant, get_rows) (#27765)｜Anjielon（10 files, +233/-5）
- **0cae430** — vulkan: support type-aligned GET_ROWS (#28253)｜Jeff Bolz（5 files, +89/-25）
- **9ac8c40** — vulkan: rms_norm fusion opportunities (#28024)｜Jeff Bolz（5 files, +445/-97）
- **7a333e7** — vulkan: add DeepSeek-V4 hyper-connection fused ops (DSV4_HC_COMB/PRE/POST) (#26578)｜Kevin Hopper（6 files, +462）
- **c0b1871** — webgpu: format the GET_ROWS case block (#28542)｜Pascal（1 file, +15/-13）
- **dbeb375** — sycl: add a batched L2_NORM kernel (#28222)｜Titaniumtown（4 files, +226）
- **4735997** — ggml: add gfx90c HIP support (#26454)｜AuroraRAS（4 files, +14/-10）
- **050dde5** — hexagon: add RELU and LEAKY_RELU ops (#28585)｜Todor Boinovski（7 files, +150/-1）
- **73ab759** — CUDA: branchless Q4_K/Q5_K unpack to speed up mmvq, L2 prefetch on DGX Spark (#26705)｜Pranesh Gonegandla（3 files, +100/-20）
- **b74f590** — ggml-cuda: fix divergent barrier in f16 flash attention (#27870)｜Siavash Norouzi（1 file, +48/-48）
- **0c96345** — CUDA: size routed MoE MMQ N-tiles from typical expert width on RDNA3 (#24546)｜ravel7524（10 files, +36/-12）
- **e71b805** — Revert "CUDA: size routed MoE MMQ N-tiles from typical expert width on RDNA3 (#24546)" (#28551)｜Piotr Wilkin（10 files, -24 淨變更）

本次區間仍以各硬體後端的算子擴充與效能調校為主軸：Vulkan 後端新增 TQ1_0 三元量化格式的完整支援（矩陣乘法／向量矩陣乘法／MoE 專用向量矩陣乘法／解量化／GET_ROWS）、為 GET_ROWS 加入型別對齊支援、並針對 rms_norm 尋找可融合的算子機會以減少 kernel 呼叫次數，另外也為 DeepSeek-V4 的 hyper-connection 機制新增三個專用融合算子（combine/pre/post）。WebGPU 則只是對既有 GET_ROWS 分支程式碼做排版整理，屬純風格調整。SYCL 新增批次化的 L2_NORM kernel，HIP 為 AMD gfx90c（如 Radeon Vega 系列 iGPU）補上支援。Hexagon（Qualcomm NPU）後端新增 RELU 與 LEAKY_RELU 兩個啟用函式算子。

CUDA 方面除效能優化（Q4_K/Q5_K 反量化改為無分支寫法以加速 mmvq，並在 DGX Spark 上加入 L2 快取預取）外，也修正 f16 flash attention 中一處分支發散導致的 barrier 使用錯誤（可能造成部分執行環境下的未定義行為）。值得留意的是，`ravel7524` 針對 RDNA3（AMD）依典型專家寬度調整 MoE MMQ N-tile 大小的效能優化（#24546）在合入後很快就被作者 Piotr Wilkin 以 `e71b805` 整筆回退，該回退提交由 Claude Fable 5.1 協助產生，回退說明並未附上根因，僅標註「revert commit 0c96345」，推測上線後發現有回歸問題，需待後續重新提交修正版本。

### 2. Server／核心運算圖：正確性修復

- **160bd03** — server: fix LRU hang on multiple requests same model (#28539)｜Xuan-Son Nguyen（3 files, +89/-94）
- **992cb50** — ggml: allow backend inputs to not create another split (#28387)｜Aman Gupta（1 file, +2/-13）
- **5202104** — caps : recheck typed content if template checks for string (#28511)｜Sigbjørn Skjæret（2 files, +40/-2）

`server` 修正一個 LRU（最近最少使用）模型置換機制的死結（hang）問題：當同一個模型收到多個請求、且某個等待者在模型仍在載入時放棄等待，該模型會處於「已載入但無請求使用」的閒置狀態，但可用 slot 數未被重新計算，導致排在後面的第二個請求永遠卡在佇列中無法被服務（`tick()` 過去只在 join / claim / 請求結束時被觸發）。修法是讓佇列項目在成功 claim 後仍保留，避免正在啟動的模型在其等待者尚未使用前就被選為淘汰對象，並在每次狀態變化與等待者離開佇列時重新計算可用 slot，模型一旦啟動完成且無人等待即立刻被淘汰。`ggml` 核心運算圖新增選項，允許後端輸入（backend inputs）不必因跨後端而強制建立新的運算分割（split），減少不必要的圖分割開銷。`caps`（能力偵測，用於聊天模板相容性判斷）修正邏輯：當模板明確要求字串型別內容時，需重新檢查已標記為「有型別（typed）」的內容是否真的符合，避免誤判。

### 3. 模型轉換工具（convert.py / gguf-py）

- **d23c47f** — convert : refactor Hy4-preview conversion - move HC tensor mapping to the global map (#28451)｜fairydreaming（2 files, +61/-90）
- **9a75705** — convert : write explicit recurrent_layers for Qwen3-Next / Qwen3.5 (#28208)｜DevVexus（3 files, +11）

前者將 Hy4-preview 模型轉換中原本獨立處理的 HC（hyper-connection）張量對應規則，整併進全域張量映射表，屬程式碼精簡與維護性重構，行為上不改變輸出。後者修正一個實際會造成模型錯誤運算的問題：載入端會優先讀取 `<arch>.attention.recurrent_layers` 陣列，若不存在才 fallback 使用 `full_attention_interval` 等距推算，但轉換器過去只會寫入等距的 interval 值，對於非均勻分布的 `layer_types`（即哪些層是完整注意力、哪些是循環/線性注意力）會被錯誤還原——例如一個 12 層、週期為 4/3/5 混合排列的模型，實測會有第 6、7 層型別互換的錯誤，換算到 52 層的非均勻堆疊則會有 15 層被誤判。由於目前公開的 Qwen3.5 checkpoint 都是均勻分布，此問題尚未被實際觸發。修法是在 `constants.py` 新增 `RECURRENT_LAYERS` 鍵、在 `gguf_writer.py` 新增對應寫入函式，並讓 `Qwen3NextModel`（涵蓋 Qwen3-Next、Qwen3.5、Qwen3.5-MoE）直接依 `layer_types` 寫出明確陣列，同時保留舊版 interval 以維持向下相容；若 `layer_types` 長度與 `num_hidden_layers` 不符，轉換階段會直接報錯而非產出錯誤的 GGUF。作者已在 62 層非均勻的 Qwen3.8-27B 上驗證修正有效，但 MTP（多token預測）區塊的補零（padding）邏輯尚未在真實 MTP 模型上測試。

### 4. 測試／CI／建置維護性變更

- **f114f91** — tests : initialize the L2_NORM batch array (#28553)｜Pascal（1 file, +1/-1）
- **67672dc** — ci : bump ty to 0.0.78 (#28548)｜Sigbjørn Skjæret（7 files, +11/-10）
- **ccc3646** — nix : update deprecated expressions (#28145)｜Zhaolun Yin（2 files, +8/-8）
- **1173700** — examples : print ggml_version and ggml_commit in test-cmake [no ci] (#28538)｜Daniel Bevenius（1 file, +2/-1）
- **2092353** — ci : add container image checking and tagging (wip) (#28394)｜Daniel Bevenius（2 files, +69）

本組均屬維護性變更：修正 L2_NORM 測試中未初始化的批次陣列（避免測資含未定義值影響測試穩定性）；升級 CI 使用的 Python 型別檢查工具 `ty` 版本並同步調整相關程式碼；更新 Nix 建置腳本中已標記為過時（deprecated）的表達式寫法；`test-cmake` 範例程式新增輸出 ggml 版本號與 commit hash，方便除錯建置產物版本；並新增（尚在開發中，WIP）容器映像檢查與標籤（tagging）的 CI 流程。

---

## 小結

本次檢查區間新增 22 筆 commit，重心明顯集中在多硬體後端（Vulkan、CUDA、SYCL、HIP、Hexagon、WebGPU）的算子擴充與效能調校，包含 TQ1_0 量化支援、GET_ROWS 型別對齊、rms_norm 融合機會、DeepSeek-V4 專用融合算子、SYCL/Hexagon 新算子等；其中一項 RDNA3 MoE 效能優化在合入後很快被回退，需留意後續是否有修正版重新提交。正確性修復方面，`server` 解決了同模型多請求下的 LRU 死結問題，CUDA 修正 flash attention 的分支發散 barrier 錯誤，轉換工具則修正了非均勻循環層排列在 Qwen3-Next/3.5 系列上會被錯誤還原的問題（目前公開模型尚未受影響）。另有若干 CI／測試／建置面的例行維護性提交。整體未見影響現有功能的破壞性變更，僅需關注 RDNA3 MoE 優化回退後的後續進度。
