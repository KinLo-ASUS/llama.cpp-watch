# llama.cpp 每日追蹤報告 — 2026-08-27

- 檢查範圍：`ggml-org/llama.cpp` master 分支
- 上次檢查點：`5d5cb4c3a` (2026-08-26)
- 本次最新 commit：`11cd98842874cc1b87ac274bd2d5cceb38102bb2`
- 新增 commit 數：11

---

## 分類摘要

### 1. GPU 後端運算核優化與修正（Metal / CUDA / Vulkan）

多個後端在算子效能與正確性上有更新，涵蓋 Mamba-2 狀態空間掃描、MoE 量化矩陣乘、交叉熵損失運算，以及較新 GPU 上 warp 大小假設造成的錯誤。

- **`11cd988` ggml-metal: add chunked SSD MMA for Mamba-2 prefill optimization (#26647)**
  為 Metal 後端的 SSM_SCAN（Mamba-2 狀態空間掃描）加入分塊（chunked）MMA 矩陣乘實作，取代舊有的純量路徑，用於多 token prefill 情境的效能優化，並保留序列化尾端 kernel 作為 fallback。

- **`fc35562` cuda: unblock mmq for MoE on sm_60 (#26264)**
  解除 CUDA sm_60（Pascal 架構）上 MoE 模型 mmq（量化矩陣乘）路徑的限制，並針對不支援 dp4a 的 Pascal 卡新增專屬設定檔，同時降低 Q2_K/Q4_K/Q5_K/Q6_K 在非 dp4a Pascal 上的 occupancy 以避免資源不足。

- **`bf94216` Implemented vulkan cross_entropy_loss and cross_entropy_loss_back (#27216)**
  為 Vulkan 後端新增 `cross_entropy_loss` 與其反向傳播 `cross_entropy_loss_back` 運算子（含對應 shader），補齊訓練/微調相關的算子支援。

- **`5e6a37c` vulkan: warptiles currently assume warp sizes <= 64, clamp to work around larger warps (#27726)**
  修正 Vulkan warptile 實作原先假設 warp 大小不超過 64 的問題，加入 clamp 以相容 warp 大小更大的裝置，避免計算錯誤。

### 2. 核心推理引擎（llama core）

- **`925e117` llama: add token ID tracking to KV cell (#27762)**
  在 KV cache 的 cell 結構中加入 token ID 追蹤，並提供 `get_prev_tokens` 介面，讓上層能取得目前 KV cache 中對應的原始 token，為後續功能（例如上下文重建、除錯）鋪路。

### 3. 分散式運算 / RPC 後端

- **`d0132a6` rpc : implement event and async backend APIs (#18626)**
  為 RPC 後端（分散式/跨機推理）實作事件（event）與非同步後端 API，並快取 `RPC_CMD_GET_ALLOC_SIZE` 的回應結果，提升多節點通訊效率。

### 4. 模型轉換（conversion）

- **`dac869b` conversion : fix Nemotron 3.5 Lightning layers (#27729)**
  修正在 transformers 5.5.1 之後版本轉換 NVIDIA Nemotron 3.5 Lightning 模型時，因設定檔以 `full_attention` 取代 `attention` 表示層類型、導致轉換腳本比對失敗（重複鍵警告、head_count_kv 全部為 0）的問題。已對應 issue #27718。

### 5. CI / 建置基礎設施

- **`da9b5d6` ci : make cache bucket public (#27728)**
  將 CUDA/Ubuntu 建置流程使用的 ccache 儲存桶設為公開，並加入 HF token 檢查，簡化快取存取設定。

- **`4d19b28` ci: Clean up UI builds from releases (#27706)**
  重整 WebUI 建置流程：將 UI 版本解析改為內嵌於 `ui-build.yml`，改成「建置一次、多個 release job 共用產物」的模式（改用 `llama-ui.zip` artifact），伺服器相關 job 不再各自用 npm 重新建置 UI；同時將 `LLAMA_BUILD_UI` 預設關閉，UI 預設改由本地 `tools/ui/dist` 或預建下載（`LLAMA_USE_PREBUILT_UI=ON`）內嵌，並釐清 Windows 各後端（CUDA/Vulkan/SYCL）打包時如何注入含 UI 的 llama-server。

### 6. Web UI

- **`0379a19` ui: Update Dialog component styling (#27743)**
  調整基礎 Dialog 元件樣式，使其支援響應式佈局與 sticky header，並將關閉按鈕移至 sticky header 上。

- **`539f245` ui: Move Settings and MCP Servers routes to dialog-based views (#27744)**
  將原本以獨立路由（`/mcp-servers`、`/settings`）呈現的「MCP Servers」與「Settings」頁面改為對話框（Dialog）形式呈現：新增 `DialogMcpServers`、`DialogMcpResourcesBrowser`、`DialogSettingsChat` 等元件，移除對應的路由與側邊欄項目，並讓 MCP 伺服器卡片上的 Resources 徽章可點擊開啟資源瀏覽對話框。

---

## 小結

本次追蹤共 11 筆新 commit，涵蓋範圍包括：Metal/CUDA/Vulkan 三個 GPU 後端的效能優化與相容性修正、KV cache token 追蹤新功能、RPC 分散式後端的事件與非同步 API、Nemotron 模型轉換修正，以及 CI 建置流程重整與 WebUI 對話框化改版。未發現有安全性相關或破壞性 API 變更需要特別留意。
