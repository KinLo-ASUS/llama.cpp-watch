# llama.cpp Master 分支變更報告 - 2026-07-22

> 涵蓋範圍：`76f46ad` → `66e4bf7`（共 11 筆 commits，時間約為 2026-07-21 ~ 2026-07-22）

## 分組摘要

### 1. Server 錯誤處理修正
針對 server 在例外情境下的錯誤處理進行修正，避免因未預期狀態造成崩潰或錯誤的 HTTP 狀態碼。

- `40b740a` server : properly handle null llama_context (#25868) — 修正 server 在 `llama_context` 為 null 時未妥善處理的問題。
- `fd41bf6` server: return 400 instead of 500 on validation error with X-Conversation-Id (#25760) — 修正帶有 `X-Conversation-Id` header 的請求在參數驗證失敗時，因串流回呼未設定而觸發 `std::bad_function_call`，錯誤地回傳 500 而非 400；同時修正 WebUI 送出 `backend_sampling` 佔位字串導致設定驗證失敗的問題。

### 2. CUDA 後端優化與新功能
CUDA 後端的效能優化與 DeepSeek V4 相關運算子支援。

- `305ba51` CUDA: vectorize same-type get_rows with int4 copy (#25929) — 將 `k_get_rows_float` 中每個元素重複計算的列不變資料抽出迴圈外，並新增以 int4（16 bytes）為單位的向量化拷貝路徑，在符合對齊與型態條件時大幅提升 `get_rows` 效能（Strix Halo 上量測到約 -27% 耗時），並以佔用率門檻避免小規模拷貝時的效能回退。
- `846e991` cuda: add sqrt_softplus in topk-moe for dsv4 (#25896) — 於 topk-moe 運算中新增 `sqrt_softplus` 支援，供 DeepSeek V4 模型使用。

### 3. 推測解碼 / 模型下載參數
- `60f6a17` common: resolve draft repo to its requested sidecar (#25955) — 修正使用 `-hfd` 指定草稿模型 repo 時，若該 repo 附帶推測解碼 sidecar 檔案，草稿模型會被錯誤解析成主模型而非對應的 sidecar；現在會依照主模型的回退邏輯正確採用已探索到的 sidecar，使 `--spec-type draft-dflash` 搭配 `-hf`/`-hfd` 正常運作。

### 4. Vulkan 後端
- `f048010` vulkan: Refactor vk_queue to use per-instance mutexes and unique handles (#23570) — 大規模重構 `vk_queue`，整合 `VK_KHR_internally_synchronized_queues` 擴充功能，將 queue 提交邏輯抽象為多型介面；若驅動支援硬體端同步，可完全略過 host 端的 mutex 鎖定，並改用每個 instance 各自的 mutex 與 unique handle 管理生命週期，以提升多執行緒下的 queue 使用效率與正確性。

### 5. Hexagon 後端
- `71102a7` hexagon: check tensor type when reusing descriptors (#25968) — 重用 tensor descriptor 前檢查張量型態是否一致，避免型態不符時誤用快取的 descriptor。

### 6. CPU 後端（KleidiAI）
- `fb0e6b6` kleidiai : warn once when a weight type has no KleidiAI kernel (#25701) — 當某權重型態沒有對應的 KleidiAI kernel 時，僅警告一次，避免大量重複的日誌輸出。

### 7. 多模態（mtmd）
- `b4aa7dd` mtmd : use align_corners for qwen3vl vision position embedding interpolation (#25781) — 修正 Qwen3-VL 視覺位置編碼插值方式，改用 `align_corners=True`（對齊 transformers 官方實作），避免非正方形或較大圖片的定位座標隨圖片尺寸偏移。

### 8. 模型轉換（conversion）
- `66e4bf7` convert: fix handle HunyuanVL XD-RoPE config (#25514) — 修正轉換 HunyuanVL 模型時對 XD-RoPE 設定的處理問題。

### 9. 其他後端支援
- `5735e10` ggml-openvino: Add GGML_BACKEND_DL_IMPL invocation for OpenVINO backend (#25795) — 補上 OpenVINO 後端缺少的 `GGML_BACKEND_DL_IMPL()` 巨集呼叫，使其與其他後端一致。

## 統計
- 新增/變更 commits 總數：11
- 涉及子系統：Server、CUDA、共用參數處理（推測解碼）、Vulkan、Hexagon、CPU（KleidiAI）、多模態（mtmd）、模型轉換、OpenVINO

---
*本報告由自動化監控系統產生。下次檢查將從 commit `66e4bf7e592a98dfefcb15202fc5926967dc734e` 之後開始。*
