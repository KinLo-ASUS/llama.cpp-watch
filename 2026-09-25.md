# llama.cpp 更新報告 - 2026-09-25

- 檢查範圍：`bd4f514db14d87fded667787a7a963bfbaa98e89` (bd4f514) → `84e76d8a23162eca70490da131945ebec1f09bf4` (84e76d8)
- 新增 commit 數：22
- 上次檢查日期：2026-09-24

## 摘要總覽

本次新增的 22 個 commit 中，最值得注意的是新增了公開 API **`llama_batch_ext`**（歷時已久的 PR #24669），以及 **Ling 3.0 VL** 多模態模型支援。其餘多為各硬體後端（CUDA、Vulkan、Metal、Hexagon）的 kernel 新增、效能優化與正確性修正，加上測試工具與 CI 的維護性改動。整體屬常規開發批次，`llama_batch_ext` 屬於較大的 API 面變更，值得追蹤是否有後續相依調整。

---

## Group 1：新公開 API — llama_batch_ext

| Commit | 標題 | 摘要 |
|---|---|---|
| `fc343a8` | llama: add llama_batch_ext (#24669) | 新增 `llama_batch_ext` 這一組新的 batch API，取代/補充舊有的 `llama_batch`，支援 token、embedding 與 state 三種輸入同時存在於一個 batch 中（`llama_batch_ext_add_embd`、`llama_batch_ext_set_embd_state` 等）。此 PR 開發時間跨度長，經過多輪設計調整（如移除 `pos_max`、API 改名為 `embd_token` 等），並補上與舊 API 相容性的測試。 |

**說明**：這是一個歷時較久、經過多次設計反覆的公開 API 新增，屬本次批次中影響面最大的變更。如果專案有直接呼叫 `llama_batch` 系列 C API 進行推論整合，建議關注此 API 是否適用或未來是否會取代現有介面。

---

## Group 2：模型支援 — 多模態

| Commit | 標題 | 摘要 |
|---|---|---|
| `f830688` | model : add Ling 3.0 VL support (#29151) | 新增 Ling 3.0 VL（視覺語言模型）支援，將其架構併入既有的 BailingMoeV3 架構中實作；並修正在 bailingmoe3 依 mrope sections 判斷是否啟用時，共享的 NORM rope 列表被錯誤清空的問題。 |

---

## Group 3：GPU / 加速後端（CUDA / Vulkan / Metal / Hexagon）

| Commit | 標題 | 摘要 |
|---|---|---|
| `53ed051` | cuda : add conv3d with implicit GEMM (#29137) | 新增 CUDA 後端 conv3d（3D 卷積）運算子，採用 implicit GEMM 方式實作，並處理空 kernel 的邊界情況。 |
| `a72e04a` | cuda : add F16 kernel support for CONV_2D_DW (#29064) | 為深度可分離 2D 卷積（CONV_2D_DW）新增 F16 精度的 CUDA kernel 支援。 |
| `3423f94` | vulkan: tune KHR cooperative matrix support for Adreno GPUs (#29328) | 為 Vulkan 後端在 Qualcomm Adreno GPU 上啟用並調校 KHR cooperative matrix 支援，修正 mul_mat_s 相關問題。 |
| `6b790a9` | vulkan: handle misalignment in conv_2d and conv_3d (#29365) | 修正 Vulkan 後端 conv_2d、conv_3d 運算子在記憶體未對齊情況下的處理，並修正 test-backend-ops 的輸出格式。 |
| `70c4e15` | vulkan: int8 coopmat1 matmul implementation for AMD RDNA3 and RDNA4 (#27952) | 大型 PR，為 AMD RDNA3/RDNA4 架構新增使用 int8 coopmat1（cooperative matrix v1）的量化 matmul shader，涵蓋 Q8_0、Q4_1、Q5_0/1、Q3_K～Q6_K、IQ4_NL、IQ4_XS、NVFP4/MXFP4 等多種量化格式，並包含大量效能調校（workgroup 排程、subgroup size、vgpr 使用優化等）與 `mul_mat_id` 支援。 |
| `cdc0642` | metal : optimize sparse FA + clean-up (#29377) | 優化 Metal 後端 sparse flash-attention：將 sparse 索引快取進 shared memory、簡化 shared memory 大小計算，並展開（unroll）sparse 索引載入迴圈以提升效能。 |
| `84e76d8` | metal : fix graph capture and handle empty graphs (#29390) | 修正 Metal graph capture 邏輯：空 graph（無節點）時提早返回；移除多餘的 capture_compute 重置（原計數器遞減邏輯已確保只會 capture 一次）；在錯誤訊息中提示需設定 `METAL_CAPTURE_ENABLED=1`；並修正傳給 `ggml_metal_op_init` 的 `use_capture` 參數改用布林轉換值而非原始計數器，避免 GPU debug-group 標記在非 capture 情況下被誤發。 |
| `9710a32` | hexagon: reject MUL_MAT_ID when src1 precision is F32 (#29348) | Hexagon 後端在 `MUL_MAT_ID` 運算的 src1 為 F32 精度時明確拒絕（不支援），避免產生錯誤結果。 |
| `97a418b` | hexagon: support I32 CPY and CONT (#29379) | 為 Hexagon 後端新增 I32 型別的 CPY、CONT 運算子支援。 |
| `07fc586` | hexagon: dynamic quantizer improvements (#29395) | 一系列 Hexagon 動態量化器改進：修正 Q8_0、N=1 情境下 MUL_MAT 的精度問題、修正暫存器溢位（register spill）、所有動態量化路徑統一改用 DMA 傳輸、移除過時的 `run_quant_task`、統一活化資料（activation data）命名（改用 `act_` 取代易混淆的 `src1_`）等清理與正確性修正。 |
| `a02c7f5` | hexagon: handle multi-sequence in concat_2d (#29344) | 修正 Hexagon 後端 `concat_2d` 運算子在多序列（multi-sequence）情境下的處理問題。 |

**說明**：本次後端相關改動仍以 CUDA/Vulkan/Metal/Hexagon 的功能覆蓋率與效能打磨為主。其中 `70c4e15`（AMD RDNA3/4 int8 coopmat1）是本批次中規模最大、涵蓋量化格式最廣的效能優化 PR；`84e76d8` 的 Metal graph capture 修正屬於除錯工具正確性修正，若專案使用 `METAL_CAPTURE_ENABLED` 進行除錯建議留意。

---

## Group 4：測試工具與正確性修正

| Commit | 標題 | 摘要 |
|---|---|---|
| `2b70583` | server,common : fix the GCC 12 stringop-overread false positive (again) (#29325) | 再次修正 GCC 12 對 server/common 程式碼誤判的 stringop-overread 警告（標題含「(again)」，代表此類 GCC 誤報修正已非首次）。 |
| `4c5957c` | test-save-load-state : print a per-model results table in --models mode (#29316) | 改善 `test-save-load-state` 在 `--models` 模式下的輸出：原本每個模型都會印出大量 token dump、逐測試標頭與 PASS 訊息，改為僅印一份每模型一列、每測試一欄的彩色結果表格（PASS/FAIL/SKIP），並在 baseline 測試失敗時正確跳過（SKIP）後續測試；同時補上 `-h` 說明頁的使用範例。 |
| `8212c78` | test: flush status (#28352) | 修正測試工具輸出狀態訊息時未即時 flush 的問題。 |
| `5cf3a35` | llama-grammar: fix numeric truncation for token_id parsing (#29382) | 修正 `llama-grammar` 解析 token_id 時的數值截斷（numeric truncation）問題，避免大於特定範圍的 token id 被錯誤截斷。 |

---

## Group 5：UI

| Commit | 標題 | 摘要 |
|---|---|---|
| `945064f` | ui : fix missing svg use and animation elements in preview and download (#28962) | 修正 web UI 的 SVG 消毒器（sanitizer）過度嚴格，導致預覽與下載時遺失 `<use>` 與動畫元素；同時針對 SVG 動畫的 `href` 重新指向（retargeting）做安全處理，避免消毒放寬後引入新的安全風險。 |

---

## Group 6：CI / 建置與發布維護

| Commit | 標題 | 摘要 |
|---|---|---|
| `013b31c` | scripts : make-release-desc - link previous release in changelog title (#29336) | `make-release-desc.sh` 產生的變更日誌標題改為附上指向前一版本 release 頁面的連結；repo URL 從 origin remote 推導，若無法解析（如本地無 origin 的情況）則退回純文字標題。 |
| `70596c4` | ci : use hf-jobs-cpu-performance, disable pytest workers (#29369) | server-sanitize CI workflow 改用 `hf-jobs-cpu-performance` runner，並歷經多輪調整（先停用 pytest workers、又改回自動、最終改為固定 4 個 worker）。 |
| `308883b` | server : change default pytest workers to 4 (#29376) | 承接上一個 PR 的討論結果，將 server 測試預設的 pytest worker 數量固定改為 4（而非 auto）。 |
| `bced459` | sync : ggml (#29396) | 同步上游 ggml：版本號提升至 0.25.2、再到 0.25.3；並修正 `ggml_graph_nbytes` 的 UBSan（undefined behavior sanitizer）錯誤。 |

---

## 觀察與建議

1. **`fc343a8`（新增 `llama_batch_ext` API）**：這是本次批次中影響面最大的變更，新增一組可同時處理 token/embedding/state 的新 batch API。若專案直接使用 `llama_batch` C API 進行推論整合，建議關注此 API 後續是否成為推薦的整合介面，以及是否有相容性文件更新。
2. **`70c4e15`（Vulkan AMD RDNA3/4 int8 coopmat1）**：大型效能優化 PR，涵蓋多種量化格式；若在 AMD RDNA3/RDNA4 GPU 上使用 Vulkan 後端，此更新後應有明顯的量化 matmul 效能提升，建議關注後續是否有效能回報或需要調校的問題。
3. **`84e76d8`（Metal graph capture 修正）**：修正 Metal 除錯用 graph capture 機制的邏輯錯誤，屬正確性修正，若有使用 `METAL_CAPTURE_ENABLED=1` 進行除錯的工作流程建議更新。
4. **`5cf3a35`（llama-grammar token_id 數值截斷修正）**：屬正確性修正，若使用 grammar 限制解碼且詞彙表較大（token id 數值較大），建議更新以避免潛在的錯誤行為。
5. **`bced459`（sync ggml，含 UBSan 修正）**：ggml 子模組同步並修正一個 undefined behavior，建議留意是否影響 sanitizer 建置流程。
6. 本次沒有發現明顯的破壞性變更（breaking change）或安全性問題；`945064f` 的 SVG sanitizer 調整已同時處理放寬規則後的安全考量。
